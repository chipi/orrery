name: docker-e2e

# Stands up the local docker-compose stack inside the GH Actions runner
# and runs Playwright against it. This is the VPS-readiness gate per
# ADR-066: it asserts the nginx-serving contract (cache headers, SPA
# fallback, mime types, the live /data overlay) that the standard
# e2e.yml workflow doesn't exercise — that one targets `vite preview`.
#
# Path-gated on PRs so a Svelte-only change doesn't pay the docker tax;
# always runs on push to main so trunk health is known.
#
# On green main push, the `publish` job builds + pushes the
# `orrery-pipeline-runner` image to GHCR as multi-arch (amd64 + arm64).
# Pattern adapted from podcast_scraper's stack-test.yml (RFC-081 §Layer 1).

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'Dockerfile'
      - 'docker-compose.yml'
      - '.dockerignore'
      - 'ops/docker/**'
      - 'package.json'
      - 'package-lock.json'
      - 'playwright.config.ts'
      - '.github/workflows/docker-e2e.yml'
      - 'scripts/check-no-secrets-in-image.sh'
  workflow_dispatch:

concurrency:
  group: docker-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker-e2e:
    # Matrix: desktop-chromium + mobile-chromium run as parallel jobs.
    # Each leg gets its own runner, so wall-clock is ~the same as
    # desktop-only used to be. CI minutes 2x; that's the trade for
    # closing the desktop/mobile asymmetry without blowing through
    # the workflow timeout. Sequential mobile+desktop on workers=1
    # hit 30+ min and timed out (May 2026 mobile-extension first run).
    strategy:
      fail-fast: false
      matrix:
        project: [desktop-chromium, mobile-chromium]
    name: docker-e2e (${{ matrix.project }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # Produce build/ on the runner. The web container bind-mounts it,
      # so this step is the precondition for `docker compose up -d web`
      # to serve anything.
      - run: npm run build

      - name: Bring up web container
        run: docker compose up -d web

      - name: Wait for web to be healthy
        run: |
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8080/ >/dev/null; then
              echo "Web container responding on :8080"
              exit 0
            fi
            sleep 2
          done
          echo "Web container failed to respond within 120s — dumping logs:"
          docker compose logs web
          exit 1

      - name: Smoke-test critical paths
        run: |
          set -e
          # Production-shape nginx must serve: SPA root, a sub-route,
          # the data manifest with no-cache, and an immutable hashed
          # asset with the year-long cache header.
          curl -sf http://localhost:8080/ >/dev/null
          curl -sf http://localhost:8080/missions/launches >/dev/null
          curl -sfI http://localhost:8080/data/launches.json | grep -i 'cache-control: no-cache'
          curl -sfI http://localhost:8080/data/launches.json | grep -i 'content-type: application/json'

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run Playwright against docker web (${{ matrix.project }})
        env:
          PLAYWRIGHT_BASE_URL: http://localhost:8080
        # Both viewports run as separate matrix legs; this leg runs
        # only its own --project. fail-fast: false so a mobile failure
        # doesn't suppress useful desktop diagnostics + vice versa.
        run: npx playwright test --project=${{ matrix.project }}

      - name: Capture web logs on failure
        if: failure()
        run: docker compose logs web

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: docker-e2e-playwright-report-${{ matrix.project }}
          path: playwright-report/
          retention-days: 14

      - name: Tear down stack
        if: always()
        run: docker compose down

  # ════════════════════════════════════════════════════════════════════
  # publish — pushes the pipeline-runner image to GHCR (multi-arch).
  #
  # Pattern adapted from podcast_scraper's stack-test.yml (RFC-081 §Layer 1).
  # Two-stage buildx per image:
  #   1. amd64-only `--load` so check-no-secrets-in-image.sh can run
  #      against a real local image. BuildKit multi-arch output can't
  #      be --load'd (single-arch daemon).
  #   2. amd64+arm64 `--push` directly to GHCR with cache reused from
  #      step 1 (amd64 layer is near-instant; arm64 is the new cost).
  #
  # NOT published:
  #   - `web` container — it's stock nginx:alpine + bind-mounted ./build
  #     per ADR-063. The deploy unit is the static bundle, not an image.
  #   - `grafana-agent` — vendor image (grafana/agent:v0.43.4).
  #
  # Tags pushed:
  #   :main         — track-latest-green-main tag
  #   :sha-<short>  — pin-and-rollback tag for incident response
  # ════════════════════════════════════════════════════════════════════
  publish:
    needs: docker-e2e
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Strict gate: only on main pushes from canonical repo, and only
    # when docker-e2e succeeded. Forks + PRs + branch pushes skip.
    if: |
      github.repository == 'chipi/orrery'
      && github.ref == 'refs/heads/main'
      && github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    env:
      REGISTRY: ghcr.io
      OWNER: chipi
    steps:
      - uses: actions/checkout@v6

      # arm64 builds via QEMU emulation on this amd64 runner. setup-qemu
      # must come before setup-buildx so the builder picks up the
      # registered binfmt_misc handlers.
      - name: Set up QEMU (arm64 emulation)
        uses: docker/setup-qemu-action@v4
        with:
          platforms: linux/arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          set -euo pipefail
          SHORT="${GITHUB_SHA:0:7}"
          echo "short_sha=${SHORT}" >> "$GITHUB_OUTPUT"

      # Step 1: amd64-only --load so the no-secrets gate can docker run
      # against a real local image. Step 2: multi-arch --push reusing
      # the cache from step 1 for amd64.
      - name: Build + assert + push (pipeline-runner)
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ env.OWNER }}/orrery-pipeline-runner
          SHORT_SHA: ${{ steps.tags.outputs.short_sha }}
        run: |
          set -euo pipefail
          docker buildx build \
            --platform linux/amd64 \
            --tag "${IMAGE}:build-${SHORT_SHA}" \
            --load \
            --cache-from "type=gha,scope=publish-pipeline-runner" \
            --cache-to "type=gha,scope=publish-pipeline-runner,mode=max" \
            -f Dockerfile .
          scripts/check-no-secrets-in-image.sh "${IMAGE}:build-${SHORT_SHA}"
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --tag "${IMAGE}:main" \
            --tag "${IMAGE}:sha-${SHORT_SHA}" \
            --push \
            --cache-from "type=gha,scope=publish-pipeline-runner" \
            --cache-to "type=gha,scope=publish-pipeline-runner,mode=max" \
            -f Dockerfile .

      # #712-style acceptance gate (per podcast_scraper RFC-081): assert
      # the pushed manifest actually carries both linux/amd64 + linux/arm64.
      # Belt-and-suspenders against silent platform regressions if someone
      # later removes --platform.
      - name: Verify multi-arch manifest
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ env.OWNER }}/orrery-pipeline-runner
          SHORT_SHA: ${{ steps.tags.outputs.short_sha }}
        run: |
          set -euo pipefail
          echo "==> Inspecting ${IMAGE}:sha-${SHORT_SHA}"
          MANIFEST=$(docker buildx imagetools inspect "${IMAGE}:sha-${SHORT_SHA}" --raw)
          for ARCH in amd64 arm64; do
            if ! echo "$MANIFEST" | grep -q "\"architecture\": \"${ARCH}\""; then
              echo "::error::${IMAGE}:sha-${SHORT_SHA} missing linux/${ARCH} in manifest"
              echo "$MANIFEST"
              exit 1
            fi
          done
          echo "  ${IMAGE}:sha-${SHORT_SHA} OK (amd64 + arm64)"

      - name: Summary
        run: |
          set -euo pipefail
          SHORT="${{ steps.tags.outputs.short_sha }}"
          {
            echo "## GHCR publish complete (multi-arch)"
            echo ""
            echo "Pushed the pipeline-runner image with tags \`:main\` + \`:sha-${SHORT}\`,"
            echo "as a multi-arch manifest covering \`linux/amd64\` + \`linux/arm64\`."
            echo ""
            echo "* \`ghcr.io/${{ env.OWNER }}/orrery-pipeline-runner:main\`"
            echo "* \`ghcr.io/${{ env.OWNER }}/orrery-pipeline-runner:sha-${SHORT}\`"
            echo ""
            echo "The \`web\` container is intentionally NOT published — it's"
            echo "stock nginx:alpine + bind-mounted \`./build\` per ADR-063."
            echo "The static bundle is the deploy artefact, not an image."
          } >> "$GITHUB_STEP_SUMMARY"
