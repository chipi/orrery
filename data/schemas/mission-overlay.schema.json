{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mission-overlay.schema.json",
  "title": "MissionOverlay",
  "description": "Editorial overlay per mission per locale (ADR-017, ADR-020). One file per mission per locale at i18n-src/[locale]/missions/[dest]/[id].json. Merged with the base record at fetch time.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "type", "first", "description"],
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "type": { "type": "string", "minLength": 1 },
    "first": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 },
    "dispatch": {
      "type": "string",
      "minLength": 1,
      "description": "Editorial lead paragraph (PRD-029 / programs-editorial-voice.md §6) — the why-it-matters, in the programs voice, shown at the top of OVERVIEW. Authored when a program's roster links this mission. Optional; distinct from `description` (the facts)."
    },
    "events": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["met", "label", "note", "type"],
        "properties": {
          "met": { "type": "number", "minimum": 0 },
          "label": { "type": "string", "minLength": 1 },
          "note": { "type": "string", "minLength": 1 },
          "type": { "enum": ["nominal", "info", "warning"] }
        }
      }
    }
  }
}
