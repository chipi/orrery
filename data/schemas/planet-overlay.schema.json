{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "planet-overlay.schema.json",
  "title": "PlanetOverlay",
  "description": "Editorial overlay per planet per locale. One file at data/i18n/[locale]/planets/[id].json where id is the lowercase planet name. Merged with base record from planets.json at fetch time (ADR-017). Surfaced by the /explore panel.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "type",
    "fact",
    "bio"
  ],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Display name (matches base entry but available in user's locale)."
    },
    "type": {
      "type": "string",
      "minLength": 1,
      "description": "One-line classification (e.g., \"Terrestrial planet\", \"Gas giant · Rings\")."
    },
    "fact": {
      "type": "string",
      "minLength": 1,
      "description": "One-paragraph editorial summary shown in the OVERVIEW tab."
    },
    "bio": {
      "type": "string",
      "minLength": 1,
      "description": "Editorial paragraph contextualising the orbit/axial-tilt/rotation data shown in the TECHNICAL tab."
    },
    "missionable": {
      "type": "boolean",
      "description": "When true, the panel surfaces the \"PLAN A MISSION\" CTA."
    },
    "links": {
      "type": "array",
      "description": "Tiered learn-more links for the LEARN tab (v0.1.10). Same shape as mission.links — intro / core / deep tiers.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "l",
          "u",
          "t"
        ],
        "properties": {
          "l": {
            "type": "string",
            "minLength": 1
          },
          "u": {
            "type": "string",
            "format": "uri"
          },
          "t": {
            "enum": [
              "intro",
              "core",
              "deep"
            ]
          }
        }
      }
    },
    "science_sections": {
      "type": "array",
      "description": "Per-body curated science-card selection rendered in the OVERVIEW tab. Each entry points to /science/<tab>/<section> with an optional one-line per-body why prefix translated per locale.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "tab",
          "section"
        ],
        "properties": {
          "tab": {
            "type": "string",
            "minLength": 1
          },
          "section": {
            "type": "string",
            "minLength": 1
          },
          "why": {
            "type": "string"
          }
        }
      }
    }
  }
}
