import{_ as t,o as a,c as r,a2 as o}from"./chunks/framework.CKCxdkvf.js";const u=JSON.parse('{"title":"ADR-010 — Keplerian half-ellipses for transfer arc","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-010.md","filePath":"adr/ADR-010.md"}'),n={name:"adr/ADR-010.md"};function i(s,e,l,c,d,h){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="adr-010-—-keplerian-half-ellipses-for-transfer-arc" tabindex="-1">ADR-010 — Keplerian half-ellipses for transfer arc <a class="header-anchor" href="#adr-010-—-keplerian-half-ellipses-for-transfer-arc" aria-label="Permalink to &quot;ADR-010 — Keplerian half-ellipses for transfer arc&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-04-15 Last revised · 2026-04-29 (Slice 4 implementation note) TA anchor · §components/fly-screen · §constraints</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The fly screen needs to draw the spacecraft&#39;s transfer trajectory between Earth and Mars. An approximation is needed — a full n-body propagation is out of scope.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The transfer arc is computed as a Keplerian half-ellipse using the actual semi-major axis and eccentricity of the Earth-Mars Hohmann transfer ellipse. The outbound arc sweeps true anomaly ν: 0→π. The return arc sweeps ν: π→2π on a second ellipse oriented so its perihelion aligns with Earth&#39;s actual position at the return date.</p><h2 id="rationale" tabindex="-1">Rationale <a class="header-anchor" href="#rationale" aria-label="Permalink to &quot;Rationale&quot;">​</a></h2><p>Bezier curves were the original implementation. They produced arcs that passed through the Sun, required manual tuning of control points, and broke whenever the Earth-Mars geometry changed. A Keplerian ellipse is physically correct, parameterised by the actual orbital mechanics, and never intersects the Sun. The perihelion-of-return-orbit targeting Earth&#39;s actual return position ensures the spacecraft ends at Earth&#39;s real location on the return date — not where Earth was at departure.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Bezier curves — fast to implement; geometrically wrong; passes through the Sun in some configurations; rejected.</li><li>Full Lambert propagation for the arc — would produce a more accurate trajectory shape; requires computing the full transfer ellipse from the Lambert solution. Acceptable future refinement; current Keplerian half-ellipse is correct to first order.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>Positive: arc is physically correct; never intersects the Sun; parametric — automatically adapts to different launch windows. Negative: the return arc is an approximation (uses same transfer ellipse as outbound, not a separate Lambert solution for the return leg). Good enough for educational purposes.</p><h2 id="implementation-note-slice-4-2026-04-29" tabindex="-1">Implementation note (Slice 4, 2026-04-29) <a class="header-anchor" href="#implementation-note-slice-4-2026-04-29" aria-label="Permalink to &quot;Implementation note (Slice 4, 2026-04-29)&quot;">​</a></h2><p>The implementation in <code>src/lib/mission-arc.ts</code> realises this ADR with two distinct geometries, only one of which is a true Keplerian ellipse:</p><ul><li><strong>Outbound arc</strong> (<code>outboundArc</code>) — exact Keplerian half-ellipse: <code>r = a(1−e²)/(1+e·cos(ν))</code> swept ν: 0→π, with the perihelion direction rotated to match Earth&#39;s launch angle. ✓ matches the ADR.</li><li><strong>Return arc</strong> (<code>returnArc</code>) — <strong>parametric cosine radius profile</strong> (<code>r = a + Δr·cos(πt)</code>) sweeping the long-way (~321°) CCW path from Mars flyby to Earth return position. <strong>Not</strong> a strict second Keplerian ellipse with shifted perihelion as the original ADR text implied. The cosine profile gives a visually correct free-return loop that ends at Earth&#39;s actual return position, but doesn&#39;t satisfy <code>r = a(1−e²)/(1+e·cos(ν))</code> for any single <code>(a, e)</code> pair.</li></ul><p>Why the deviation: a strict second-ellipse return arc requires solving a second Lambert problem (Mars flyby state + Earth return position + return TOF → second transfer ellipse). The infrastructure exists (<code>solveLambert</code> ships in <code>src/lib/lambert.ts</code> since Slice 3) but adopting it would require event timing precision the simulation doesn&#39;t currently surface. The cosine profile is the prototype P03&#39;s solution and matches what the educational view needs.</p><p>If a future slice gains a need for true Keplerian fidelity on the return leg (e.g., to surface real ∆v at flyby), the path forward is: feed <code>solveLambert(marsFlybyPos, earthReturnPos, returnTOF, μ_sun)</code> and render the resulting ellipse via <code>outboundArc</code>-style ν-sweep on the returned <code>a</code>. Tracked as a v2 candidate.</p><p><strong>Tests:</strong> <code>src/lib/mission-arc.test.ts</code> covers the outbound arc as a true Keplerian ellipse (perihelion / aphelion / midpoint radius checks) and the return arc as a &quot;long CCW path with cosine midpoint radius A_TRANSFER&quot; — the cosine-profile spec, not the Keplerian one.</p>',18)])])}const f=t(n,[["render",i]]);export{u as __pageData,f as default};
