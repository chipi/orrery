import{c as i,Q as t,j as s,m as a}from"./chunks/framework.CdiUUa3G.js";const u=JSON.parse('{"title":"ADR-058 — Cislunar view: Earth-centered second camera + per-mission Moon trajectory profiles","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-058.md","filePath":"adr/ADR-058.md"}'),o={name:"adr/ADR-058.md"};function n(r,e,l,c,h,d){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="adr-058-—-cislunar-view-earth-centered-second-camera-per-mission-moon-trajectory-profiles" tabindex="-1">ADR-058 — Cislunar view: Earth-centered second camera + per-mission Moon trajectory profiles <a class="header-anchor" href="#adr-058-—-cislunar-view-earth-centered-second-camera-per-mission-moon-trajectory-profiles" aria-label="Permalink to &quot;ADR-058 — Cislunar view: Earth-centered second camera + per-mission Moon trajectory profiles&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-05-13 Related ADRs · ADR-001 (Three.js), ADR-009 (free-return scenario), ADR-010 (Keplerian half-ellipses), ADR-019 (schema validation), ADR-020 (canonical mission schema), ADR-024 (mission URL contract), ADR-027 (flight params + timeline), ADR-030 (/fly trajectory math: pure-function isolation) Closes · The longstanding &quot;Moon missions render as two straight chords&quot; gap surfaced by the maintainer reviewing the live Artemis II rendering against the published NASA trajectory</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>/fly</code> renders every Moon mission&#39;s trajectory via <code>moonHelioArc()</code> (<code>src/routes/fly/+page.svelte:183</code>), which linearly blends an Earth-relative offset over the mission duration. Because the entire <code>/fly</code> scene is heliocentric (Sun at origin, Earth orbit at 80 scene units, Moon-Earth distance exaggerated ×150 via <code>MOON_FLY_RADIUS_AU = 0.4</code>), the rendered cislunar trajectory degenerates visually to <strong>two straight chords</strong> — outbound from Earth to a fixed offset, return from offset to Earth.</p><p>This is not just a fidelity problem; it is a <em>structural</em> one. The real Earth-Moon system spans ~0.0026 AU (~0.21 scene units at current SCALE_3D). At Sun-out zoom every real cislunar trajectory — Apollo&#39;s free-return figure-8, Artemis II&#39;s hybrid free-return swinging 9,200 km past the lunar far side, Chandrayaan-3&#39;s multi-burn Earth-bound spiral, Chang&#39;e 5&#39;s lunar-orbit rendezvous — occupies a fraction of a pixel. Even a perfect <code>moonHelioArc()</code> replacement would render the same two-chord-on-a-pixel shape at this zoom.</p><p>Sixteen Moon missions in <code>static/data/missions/moon/</code> share the same broken visualisation today. The mission JSONs themselves (ADR-027 flight schema) already encode the <em>physical</em> differences — <code>arrival.periapsis_km</code>, <code>orbit_insertion_dv_km_s</code>, <code>totals.tli_or_tmi_dv_km_s</code> — but nothing in the rendering path consumes the differences. Two missions with radically different trajectory architectures (Apollo 11 direct ascent + LOI vs Chandrayaan-3 5-burn spiral up) currently render as visually identical straight-line pairs.</p><p>The project&#39;s stated ambition (<code>AGENTS.md</code>, the maintainer&#39;s user role memory, and ADR-009&#39;s framing of free-return as &quot;the mission that inspired Orrery&quot;) explicitly includes showing each mission&#39;s <em>actual</em> trajectory architecture. Apollo&#39;s figure-8, Artemis II&#39;s hybrid free-return, and the variety of post-Apollo profiles are themselves a teaching artefact — <em>why mission design changed</em> is one of the stories the app exists to tell.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Add a second view mode to <code>/fly</code> — &quot;cislunar&quot; — with its own Earth-centered scene + camera, rendered alongside the existing heliocentric scene. Auto-switch to it when a Moon mission loads. Drive its per-mission geometry from a new optional <code>flight.cislunar_profile</code> block in the mission schema.</strong></p><h3 id="view-architecture" tabindex="-1">View architecture <a class="header-anchor" href="#view-architecture" aria-label="Permalink to &quot;View architecture&quot;">​</a></h3><blockquote><p><strong>Note (2026-05-14 amendment):</strong> the user-facing Solar/Cislunar toggle and the picture-in-picture inset described in this section were de-scoped during smoke-test polish. The current implementation derives <code>viewMode</code> from <code>isMoonMission</code> directly and renders only the active scene. See the <strong>Amendment — 2026-05-14: Solar/Cislunar toggle de-scoped</strong> section at the bottom of this ADR for the rationale. Everything else in this section still describes the shipped architecture.</p></blockquote><ul><li>A second <code>THREE.Scene</code> (<code>cislunarScene</code>) + <code>THREE.PerspectiveCamera</code> (<code>cislunarCamera</code>) live alongside the existing heliocentric scene/camera in <code>src/routes/fly/+page.svelte</code>. No restructuring of the existing scene graph.</li><li><code>cislunarScene</code> is in <strong>Earth-Centered Inertial (ECI) coordinates measured in km</strong>, scaled to scene units by a new constant <code>SCALE_CISLUNAR</code> such that Earth-Moon distance (384,400 km) maps to ~38 scene units — same visual span as Earth→Mars in the heliocentric scene.</li><li>The render loop branches once per frame on a new <code>viewMode: &#39;heliocentric&#39; | &#39;cislunar&#39;</code> state, rendering whichever scene is active. Mouse controls are gated likewise.</li><li><s>An <code>~800 ms</code> camera tween on toggle, using an inline rAF lerp in the existing <code>animate()</code> loop. <strong>No new tween dependency.</strong></s> <em>(superseded — no toggle)</em></li><li><s>A <strong>picture-in-picture inset</strong> (~220×220 px, bottom-right) renders the <em>other</em> scene each frame via <code>renderer.setScissor</code> + <code>setScissorTest</code>. Single <code>WebGLRenderer</code>, two render passes. Click the inset to swap which view is main.</s> <em>(superseded — inset descoped)</em></li></ul><h3 id="default-behavior" tabindex="-1">Default behavior <a class="header-anchor" href="#default-behavior" aria-label="Permalink to &quot;Default behavior&quot;">​</a></h3><ul><li>On mission load (<code>applyMissionAsLoaded</code>), <code>if (m.dest === &#39;MOON&#39;) viewMode = &#39;cislunar&#39;</code>; else <code>heliocentric</code>.</li><li>A toggle button is shown in the HUD whenever <code>isMoonMission === true</code>, mirroring the existing 2D/3D toggle pattern (<code>+page.svelte:660</code> + button at line 2496).</li><li>View mode is <strong>session-only state</strong> (no URL persistence, no cookie — consistent with ADR-024 and ADR-057). The 2D/3D toggle is the same; this matches.</li></ul><h3 id="schema-extension-—-flight-cislunar-profile" tabindex="-1">Schema extension — <code>flight.cislunar_profile</code> <a class="header-anchor" href="#schema-extension-—-flight-cislunar-profile" aria-label="Permalink to &quot;Schema extension — \`flight.cislunar_profile\`&quot;">​</a></h3><p>Optional sub-object under <code>flight</code>, additive (no existing-field changes):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cislunar_profile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;source_tier&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tier_1_analytic&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;tier_2_published&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;parking_orbit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;altitude_km&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> &quot;inclination_deg&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> &quot;revs&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tli&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;dv_kms&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> &quot;c3_km2_s2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;translunar&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;direct&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;free_return&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;hybrid_free_return&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;spiral&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;lunar_arrival&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;impact&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;orbit&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;flyby&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;lor_orbit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;altitude_km&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> &quot;inclination_deg&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">,</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> &quot;periselene_km&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;return&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;none&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;tei_direct&quot;</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;tei_lor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;dv_kms&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;waypoints_km&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [[</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">met_days</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">x_km</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">y_km</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">z_km</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Tier 2 only</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>flight.events[].type</code> enum extended with: <code>parking_orbit_exit</code>, <code>loi</code> (lunar orbit insertion), <code>tei</code> (trans-Earth injection), <code>descent_start</code>, <code>ascent</code>. Optional <code>dv_km_s</code> field on events so the HUD can annotate burns.</p><h3 id="two-tier-data-strategy" tabindex="-1">Two-tier data strategy <a class="header-anchor" href="#two-tier-data-strategy" aria-label="Permalink to &quot;Two-tier data strategy&quot;">​</a></h3><ul><li><strong>Tier 1 — parametric (all 16 Moon missions).</strong> Trajectory shape generated procedurally from the small parameter set above. Real Keplerian arcs with Earth at focus (translunar/trans-Earth) and Moon at focus (lunar orbit/flyby). Looks physically correct, uses real numbers, but does not match published state vectors to the kilometre.</li><li><strong>Tier 2 — published waypoints (4 marquee missions).</strong> Apollo 11, 13, 17, Artemis II carry <code>cislunar_profile.waypoints_km</code> arrays of ECI state-vector samples reconstructed from NASA publications (TND reports for Apollo, post-flight reconstruction for Artemis II). Renderer prefers waypoints when present, falls back to parametric.</li><li><strong>No Tier 3 (live SPICE / Horizons ephemerides).</strong> SPICE kernel parsing in-browser is heavy; Apollo records pre-date NAIF IDs; deferred.</li></ul><h3 id="trajectory-math-isolation" tabindex="-1">Trajectory math isolation <a class="header-anchor" href="#trajectory-math-isolation" aria-label="Permalink to &quot;Trajectory math isolation&quot;">​</a></h3><p>Per ADR-030, the cislunar trajectory math lives in a pure-function module <code>src/lib/cislunar-geometry.ts</code>, parallel to <code>src/lib/mission-arc.ts</code>. Unit-testable in isolation; <code>/fly</code> consumes it and renders into Three.js. Phase generators are pure functions returning <code>Vec3Km[]</code> arrays; <code>buildCislunarTrajectory(mission): CislunarTrajectory</code> composes them from the mission&#39;s <code>cislunar_profile</code> + <code>flight.events[]</code>.</p><h3 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h3><p><code>scripts/validate-data.ts</code> extends to enforce, for Tier 2 missions: <code>waypoints_km</code> is sorted by <code>met_days</code>, <code>waypoints_km[0].met_days === 0</code>, <code>last.met_days &lt;= transit_days * 2</code>. Tier 1 fields validate automatically through AJV against the schema. <code>npm run preflight</code> continues to gate all changes.</p><h3 id="i18n" tabindex="-1">i18n <a class="header-anchor" href="#i18n" aria-label="Permalink to &quot;i18n&quot;">​</a></h3><p>New Paraglide UI strings in <code>src/lib/i18n/en-US.json</code> for the toggle (<code>fly_label_view_cislunar</code>, <code>fly_label_view_solar</code>, etc.) and phase names (<code>fly_phase_parking_orbit</code>, <code>fly_phase_translunar_coast</code>, …). Other 13 locales receive translations through the established wave23 toolchain (<code>project_science_translation_pipeline.md</code>). Mission-overlay schema gains an optional <code>cislunar_notes</code> field for editorial annotations per phase.</p><h3 id="implementation-rollout-sequential-stages-separate-prs" tabindex="-1">Implementation rollout (sequential stages, separate PRs) <a class="header-anchor" href="#implementation-rollout-sequential-stages-separate-prs" aria-label="Permalink to &quot;Implementation rollout (sequential stages, separate PRs)&quot;">​</a></h3><ul><li><strong>Stage 0 — ADR + spike</strong> (3–4 days). This ADR + schema fields + <code>cislunar-geometry.ts</code> core phases + dual-camera wiring + render Artemis II only. Maintainer checkpoint before Stage 1.</li><li><strong>Stage 1 — Tier 1</strong> (~8 days). Remaining phase generators (lunar_orbit, descent, ascent, spirals); <code>cislunar_profile</code> for all 16 Moon missions; picture-in-picture inset; Paraglide strings + wave23 translation roll.</li><li><strong>Stage 2 — Tier 2 marquee</strong> (~6 days). Apollo 11/13/17 + Artemis II waypoints. (Apollo 13 mission JSON does not exist yet; created alongside its waypoints.)</li><li><strong>Stage 3 — polish</strong> (~3 days). Science-lens phase-boundary annotations, HUD strings for cislunar phase ticker, pre-rendered cislunar mission-card thumbnails, perf + a11y passes.</li></ul><h2 id="rationale" tabindex="-1">Rationale <a class="header-anchor" href="#rationale" aria-label="Permalink to &quot;Rationale&quot;">​</a></h2><p>The fundamental problem is a coordinate-system mismatch, not a math bug. Replacing <code>moonHelioArc()</code> with a true Keplerian translunar ellipse in the existing heliocentric scene would produce a more accurate chord but still no visible cislunar geometry — the chord would still occupy ~1 scene unit at Sun-out zoom. <strong>The only path that surfaces real cislunar architecture is a second camera at the right scale.</strong></p><p>Two-tier data avoids the false dichotomy of &quot;ship parametric vagueness&quot; vs &quot;build a SPICE pipeline.&quot; Parametric Tier 1 gets all 16 missions to a state where each renders its <em>correct architecture</em> (figure-8 / spiral / LOR / direct) at the right scale; Tier 2 then upgrades the four marquee stories to match published reality. Each tier is independently shippable — Stage 0 alone produces a feature; Stage 2 deepens it without re-architecture.</p><p>The Earth-centered scene is in km, not AU, because cislunar dynamics are naturally km-scale. Translating between the two coordinate systems happens only at scene-graph entry (apply <code>SCALE_CISLUNAR</code>), not inside the trajectory math, which keeps the math module pure and unit-test-friendly per ADR-030.</p><p>The picture-in-picture inset is included rather than deferred because it removes the cognitive cost of mode-switching: the maintainer&#39;s pivot review of Artemis II (which prompted this work) was specifically about how <em>disconnected</em> the heliocentric and cislunar contexts feel without it. Two render passes per frame is the standard Three.js inset pattern; perf impact is bounded.</p><p>Session-only view-mode state matches ADR-024 (URL = mission) and ADR-057 (URL is canonical except for one narrow locale-cookie exception). Adding a <code>?view=</code> param would either duplicate the mission-derived default or invite future &quot;remember my view&quot; requests — both rejected.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Fix <code>moonHelioArc()</code> in place (curved, no second scene).</strong> Cheap (~1 day). Trajectory becomes a proper Keplerian curve instead of a straight line, but still renders at heliocentric zoom where the cislunar system is one pixel wide. Solves the math, not the visualisation. Rejected as too narrow given the project&#39;s intent.</li><li><strong>Inset-only (no main-view swap).</strong> Render the cislunar view permanently as a 250 px corner inset; heliocentric stays main. Cheaper UX, no toggle. Rejected because cislunar <em>is</em> the natural main view for Moon missions; making it permanently secondary undersells the geometry.</li><li><strong>Replace heliocentric for Moon missions entirely (no toggle).</strong> No second mode, just swap the scene wholesale when a Moon mission loads. Simpler. Rejected because losing solar context for crewed lunar missions is exactly the kind of disorientation the maintainer flagged in the review — the toggle + inset preserves &quot;I am at Earth, on a journey to the Moon, while Earth orbits the Sun&quot; as a single coherent story.</li><li><strong>JPL Horizons / SPICE ephemeris (Tier 3) from day one.</strong> Most accurate possible. Rejected: SPICE in-browser is a 2–3 week pipeline by itself; Apollo records pre-date NAIF IDs; Tier 1 + Tier 2 already cover &gt;95% of the educational value at ~15% of the engineering cost. Revisit if Artemis II&#39;s full DSN telemetry is ever released and a single mission warrants the effort.</li><li><strong>Earth-Moon rotating (synodic) frame instead of ECI inertial.</strong> Mathematically elegant — free-return becomes a literal figure-8 — but pedagogically misleading: the Moon &quot;doesn&#39;t move,&quot; which hides the fact that real cislunar missions chase a moving target. Rejected as default; could be added as a sub-toggle inside cislunar view later if needed.</li><li><strong>Defer to v2.x (don&#39;t ship in current release).</strong> Rejected: this is the feature the project was conceived around (per <code>AGENTS.md</code> framing and user memory). The current straight-chord rendering is the single biggest fidelity gap in <code>/fly</code>.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p><strong>Positive:</strong></p><ul><li>Moon missions visibly differ from each other for the first time — Apollo&#39;s free-return, Artemis II&#39;s hybrid, Chandrayaan-3&#39;s spiral, Chang&#39;e 5&#39;s LOR all render their actual architecture.</li><li>The maintainer&#39;s &quot;what I had in mind at the start&quot; outcome is met.</li><li>The schema extension is additive — every existing mission JSON remains valid.</li><li>Pure-function trajectory math (<code>cislunar-geometry.ts</code>) is unit-testable in isolation per ADR-030.</li><li>Tier 2 path leaves room to ratchet specific missions to published reality without re-architecture.</li></ul><p><strong>Negative:</strong></p><ul><li>Adds a second scene graph + camera + render pass — roughly doubles the <code>/fly</code> Three.js surface area. Mitigated by the flat-scene insertion strategy and the existing rAF loop.</li><li>Adds ~10 mission JSON updates across <code>static/data/missions/moon/</code> (Tier 1) plus 4 Tier 2 waypoint sets — sustained data-entry effort.</li><li>The wave23 translation toolchain has to roll for 12 non-en-US locales for the new UI strings (Stage 1).</li><li>Tier 1 trajectories are <em>plausible</em>, not literally accurate; this must be honestly communicated. The existing <code>flight_data_quality</code> flag provides the precedent; cislunar fields adopt the same honesty convention (<code>source_tier</code> field).</li><li>Picture-in-picture adds GPU work per frame; needs a perf gate on low-end devices (Stage 3).</li></ul><h2 id="decision-status" tabindex="-1">Decision status <a class="header-anchor" href="#decision-status" aria-label="Permalink to &quot;Decision status&quot;">​</a></h2><p><strong>Accepted on 2026-05-13.</strong> Implementation begins immediately with Stage 0 (this ADR + schema fields + spike rendering Artemis II in cislunar view). Maintainer checkpoint at end of Stage 0 before committing to Stage 1.</p><h2 id="amendment-—-2026-05-14-solar-cislunar-toggle-de-scoped" tabindex="-1">Amendment — 2026-05-14: Solar/Cislunar toggle de-scoped <a class="header-anchor" href="#amendment-—-2026-05-14-solar-cislunar-toggle-de-scoped" aria-label="Permalink to &quot;Amendment — 2026-05-14: Solar/Cislunar toggle de-scoped&quot;">​</a></h2><p><strong>Stages 0–3 shipped. The user-facing Solar/Cislunar toggle and the picture-in-picture inset were removed during smoke testing.</strong></p><p>The toggle was meant to let Moon missions also render in the heliocentric Solar scene. In practice the Solar view of a Moon mission was either:</p><ul><li><strong>Misleading</strong> — showing the exaggerated 0.4 AU <code>moonHelioArc</code> chord, the same straight-line rendering this ADR was conceived to fix; or</li><li><strong>Redundant</strong> — once the Earth-Moon pair was exaggerated to fit at solar zoom, the view collapsed into a fuzzy duplicate of the cislunar view.</li></ul><p>The smoke-tester&#39;s question <em>&quot;what is the difference between solar and cislunar?&quot;</em> was the honest signal: the two views are not differentiable when the cislunar trip is physically sub-pixel at solar zoom.</p><p><strong>Resolution.</strong> Each mission now has the one view its scale fits:</p><ul><li><code>dest === &#39;MOON&#39;</code> → cislunar scene only.</li><li>Everything else → heliocentric scene only.</li></ul><p><code>viewMode</code> becomes a derived state from <code>isMoonMission</code> (no user-toggle, no auto-switch animation). The picture-in-picture inset, the 800 ms cross-fade, the inset click hitbox, the four toggle Paraglide keys (<code>fly_label_view_cislunar</code> / <code>_solar</code> / <code>_*_aria</code>), and the ten phase-name Paraglide keys (<code>fly_phase_*</code>, never wired into the HUD) are removed across all 14 locales.</p><p>The cislunar scene infrastructure (camera, mouse controls, phase-coloured lines, ∆v annotation sprites, Earth/Moon meshes, spacecraft sprite) is unchanged. Both Three.js cameras still exist (they back different coordinate spaces), but only one is ever active per frame.</p><h2 id="amendment-—-2026-05-14-later-geometry-ux-smoke-test-pass" tabindex="-1">Amendment — 2026-05-14 (later): Geometry + UX smoke-test pass <a class="header-anchor" href="#amendment-—-2026-05-14-later-geometry-ux-smoke-test-pass" aria-label="Permalink to &quot;Amendment — 2026-05-14 (later): Geometry + UX smoke-test pass&quot;">​</a></h2><p>Smoke testing on the cislunar view (Apollo 11, Apollo 13, Artemis II, SLIM, Chandrayaan-3, Chang&#39;e 5) surfaced five geometry corrections and two UX polish items. All fixed:</p><p><strong>Geometry fixes:</strong></p><ul><li>Return-leg straight-line bug (Artemis II): tei_coast re-entry offset 150° prograde from apogee azimuth to break the collinear-through-Earth degeneracy.</li><li>Phase-pinning gaps closed: <code>lunarOrbit</code>, <code>spiralLunar</code>, <code>spiralEarth</code> accept <code>startPoint</code>/<code>endAzimuth</code> so phases connect at exact endpoints (all 17 missions now 0.0 km gap).</li><li>Orbiter vs lander disambiguated: <code>lunar_arrival.type</code> enum split into <code>orbit</code> (orbiter only — LRO, Clementine, Chandrayaan-1) and <code>orbit_and_land</code> (lander after orbit — Chandrayaan-3, Chang&#39;e 4, Luna 17, SLIM, Blue Moon Mk1). The 3 orbiters no longer fake-descend.</li><li>Inclined orbits: <code>lunarOrbit</code> rotates the orbit plane to contain both moonPos and startPoint when startPoint is provided. <code>inclination_deg</code> is ignored in this Tier 1 mode (recoverable in Stage 2 via waypoints). Closes ~5 km y-axis gaps across Apollo 17, Artemis III, Luna 17/24, Chang&#39;e 5/6, Blue Moon Mk1.</li><li>Time-consistent lunar-phase exit: <code>lastPoint</code> is shifted by <code>(moonAtExit - moonAtFlyby)</code> after the lunar phase so tei_coast departs from the spacecraft&#39;s true co-moving position with the Moon, not from the frozen flyby point.</li></ul><p><strong>Moon-frame group:</strong> lunar-phase lines (lunar_orbit, spiral_lunar, descent, ascent) and the spacecraft sprite ride inside a <code>THREE.Group</code> whose position tracks <code>(currentMoon - moonAtFlyby) × SCALE_CISLUNAR</code>, so orbit + descent visibly move with the Moon mesh instead of staying anchored where the Moon was at flyby_day. Same offset is applied in 2D.</p><p><strong>Auto-zoom (3D + 2D):</strong> when the active phase is lunar (<code>lunar_orbit</code> / <code>spiral_lunar</code> / <code>lunar_flyby</code> / <code>descent</code> / <code>ascent</code>), the camera lerps to a close-up Moon framing (~3.5 unit distance in 3D; ×8 scale + Moon-centred in 2D). Otherwise wide Earth-Moon view. ~1.25 s lerp; mouse wheel in 3D cancels for the current phase.</p><p><strong>UX:</strong></p><ul><li>Spacecraft glyph: rotation-dependent chevron → solid red filled circle (<code>#ff3a4c</code>) with halo + dark outline. Always readable, distinct from every phase colour.</li><li>Moon mesh exaggeration 5× → 3× so the lunar orbit reads as a ring around the mesh.</li><li>Lunar phase MET allocation 0.15 → 0.4 of <code>transit_days</code> (round-trip) — closer to real Apollo (~30 % of flight in lunar operations) and gives the auto-zoom enough on-screen time.</li><li>Moon-mission default simSpeed 1× → 0.4× so the camera transitions feel deliberate.</li><li>Press Play after arrival rewinds to dep_day (replays mission).</li></ul><p><strong>2D Moon-mission view</strong> now renders the cislunar phase model (Earth at canvas centre, Moon orbit ring, phase-coloured lines with moon-frame offset, red spacecraft sprite, auto-zoom on lunar phases). Replaces the older heliocentric drawSplit chord. Non-Moon missions unchanged.</p><p>Tier 2 NASA waypoints (Apollo 11/13/17 + Artemis II) remain tracked at issue #107.</p>`,62)])])}const m=i(o,[["render",n]]);export{u as __pageData,m as default};
