import{_ as t,o as a,c as o,a2 as r}from"./chunks/framework.CpZKh-tG.js";const u=JSON.parse('{"title":"ADR-062 — Sphere → flat ground patch transition at deep zoom","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-062.md","filePath":"adr/ADR-062.md"}'),s={name:"adr/ADR-062.md"};function i(n,e,l,c,h,d){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="adr-062-—-sphere-→-flat-ground-patch-transition-at-deep-zoom" tabindex="-1">ADR-062 — Sphere → flat ground patch transition at deep zoom <a class="header-anchor" href="#adr-062-—-sphere-→-flat-ground-patch-transition-at-deep-zoom" aria-label="Permalink to &quot;ADR-062 — Sphere → flat ground patch transition at deep zoom&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-05-30 Closes into · <code>src/lib/surface-scene/SurfaceFlatPatch.svelte</code> (new component) + <code>src/lib/surface-scene/SurfaceScene.svelte</code> (transition trigger) Related · ADR-038 (per-body 2D projection), ADR-061 (regions via <code>region_bounds</code>), ADR-072 (shared SurfaceScene + drift consolidation), Issue <a href="https://github.com/chipi/orrery/issues/283" target="_blank" rel="noreferrer">#283</a> (Surface hotspots v2 epic — Slice 4) Triggered by · Honest-data redesign for surface hotspots: the sphere-bound circular/rectangular Tier-2 patches still misrepresent HiRISE / LROC-NAC footprints by 100× (a stylized callout, not true ground extent). True-scale rectangles need a flat-map view to be legible at any zoom</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The surface-hotspots v0.7 work (RFC-017, PRD-014) put high-resolution orbital imagery on the planet sphere as <code>CircleGeometry</code> Tier-2 patches sized at 1 unit² (≈ 100 km × 100 km in scene scale). Real HiRISE swaths are ~5 km wide; real LROC-NAC ROIs ~1 km. The patch was <strong>always</strong> a stylized &quot;you are HERE&quot; callout, never a true footprint.</p><p>ADR-061 / Slice 1 added <code>region_bounds</code> to surface sites and Slice 3a swapped the geometry from <code>CircleGeometry</code> to a stylized rectangle whose aspect matches the region&#39;s lon-extent vs lat-extent ratio. That&#39;s honest about <em>shape</em>; it&#39;s still dishonest about <em>scale</em>.</p><p>The user research baseline (12 mockup frames, ADR-072) shows that the redesign requires <strong>true-scale</strong> rectangles at deep zoom — and at true scale, HiRISE is sub-pixel inside CTX on a sphere-projected view. The only honest projection at that zoom is <strong>flat</strong>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>At a per-planet zoom threshold, fade the spherical scene out and materialize a flat 2D ground-patch view of the selected region.</strong> Implemented as a new <code>src/lib/surface-scene/SurfaceFlatPatch.svelte</code> component owned by <code>SurfaceScene</code>. Camera state maps both directions so the back gesture lands smoothly back on the sphere.</p><h3 id="trigger-conditions" tabindex="-1">Trigger conditions <a class="header-anchor" href="#trigger-conditions" aria-label="Permalink to &quot;Trigger conditions&quot;">​</a></h3><p>The flat patch becomes the active view when <strong>all three</strong> are true:</p><ol><li>A site has been selected (<code>selected !== null</code>).</li><li>The selected site has <code>region_bounds</code> (Slice 1 backfilled all 35 surface sites).</li><li>The camera distance <code>camR</code> has dropped below a per-planet threshold: <ul><li>Moon: <code>camR &lt; 30.5</code> (just inside the Tier-2 detail-fade end at 30.5).</li><li>Mars: <code>camR &lt; 30.5</code> (same — Mars&#39;s Tier-2 ramp also ends at 30.5 post-consolidation).</li></ul></li></ol><p>The threshold is also gated by <strong>selection direction</strong>: zooming in past the threshold while a region is selected → flat patch appears. Manually deselecting (Esc, close-panel) or clicking the &quot;Back to planet&quot; button → flat patch hides, sphere returns. Wheel/pinch zoom-out past the threshold while in flat-patch mode does NOT auto-return — the user explicitly leaves via the back button to avoid jarring zoom-to-orbit mid-pan.</p><h3 id="animation-timing" tabindex="-1">Animation timing <a class="header-anchor" href="#animation-timing" aria-label="Permalink to &quot;Animation timing&quot;">​</a></h3><p><strong>600 ms shared cubic-bezier(0.4, 0, 0.2, 1)</strong> (Material Design&#39;s &quot;standard&quot; ease-in-out) applied symmetrically to both layers. Same duration as Slice 2&#39;s fly-in tween (Drift 14) for consistency. During the transition:</p><ul><li>Sphere opacity transitions 1 → 0 over the full 600 ms.</li><li>Flat-patch opacity transitions 0 → 1 over the full 600 ms.</li><li>Both run on the same easing curve simultaneously (single shared transition, no stagger / offset).</li><li>After the entering transition completes, the Three.js per-frame animation loop short-circuits its body when <code>flatPatchPhase === &#39;visible&#39;</code> — RAF still queues (so it can resume cleanly on leaving) but no scene work runs.</li><li>Reverse-fade on leaving: same curve, opacities swap targets, sphere resumes per-frame work as soon as the leaving phase starts so it&#39;s rendered behind the fading-out flat patch.</li></ul><blockquote><p><strong>Amendment from original spec:</strong> the first draft of this ADR called for an overlapping 60 % / 40 % fade with the sphere going first. In practice that read as a sequential hand-off (sphere disappears, then patch arrives) rather than a clean cross-fade. A single shared cubic-bezier without offset matches the intent better — both layers easing simultaneously creates the &quot;you&#39;re in two views at once for a moment&quot; feel ADR-072&#39;s design language calls for. Code reflects the shared-curve version; this paragraph documents the divergence.</p></blockquote><h3 id="camera-state-mapping" tabindex="-1">Camera state mapping <a class="header-anchor" href="#camera-state-mapping" aria-label="Permalink to &quot;Camera state mapping&quot;">​</a></h3><ul><li><strong>Sphere → patch:</strong> the selected site&#39;s <code>region_bounds</code> centroid is the patch&#39;s centred lat/lon. The initial flat-patch zoom is set so the region fills ~60 % of the viewport diagonal (leaving room for HUD elements).</li><li><strong>Patch → sphere:</strong> restore the pre-transition <code>camR</code>, <code>camP</code>, <code>camT</code>. Auto-spin resumes if it was on. Selection state stays put — the panel remains open with the same site.</li></ul><h3 id="layer-composition-rectangular-honest" tabindex="-1">Layer composition (rectangular &amp; honest) <a class="header-anchor" href="#layer-composition-rectangular-honest" aria-label="Permalink to &quot;Layer composition (rectangular &amp; honest)&quot;">​</a></h3><p>The flat patch renders four canvas 2D layers, drawn back-to-front:</p><ol><li><strong>Base</strong> — solid dark background (matches Orrery&#39;s <code>--color-bg</code>).</li><li><strong>Regional</strong> — Murray Lab CTX V01 mosaic (Mars) or LROC NAC ROI mosaic (Moon) at its <em>true ground extent</em>, sized to <code>region_bounds</code> projected into screen pixels via <code>latLonToEquirect</code>. Toggleable via a layer chip.</li><li><strong>Detail</strong> — HiRISE (Mars) or LROC NAC closeup (Moon) at <em>true ground extent</em> — typically a small rectangle inside the regional layer. Toggleable.</li><li><strong>Vector overlays</strong> — traverse polyline (Mars rovers), waypoint markers (start / curated sample stops / current rover position), site label, north arrow. Markers stay at real-world scale per ADR-072 §&quot;scale-aware marker rule&quot; (Slice 5).</li></ol><h3 id="hud" tabindex="-1">HUD <a class="header-anchor" href="#hud" aria-label="Permalink to &quot;HUD&quot;">​</a></h3><ul><li><strong>Scale bar</strong> bottom-left — km or mi based on locale (Orrery follows the locale&#39;s measurement system).</li><li><strong>Lat/lon readout</strong> bottom-right — live, updated on pan, formatted to 4 decimals (≈ 11 m precision at planetary scales).</li><li><strong>Back to planet</strong> button top-left — explicit affordance, Esc keyboard shortcut also fires it.</li><li><strong>Layer toggles</strong> top-right — Regional / Detail / Traverse on/off chips, similar shape to the sphere&#39;s <code>LayerChipRow</code>.</li></ul><h3 id="mobile" tabindex="-1">Mobile <a class="header-anchor" href="#mobile" aria-label="Permalink to &quot;Mobile&quot;">​</a></h3><p>Same patch component, same layer composition. Pinch-zoom + single-finger pan replace mouse wheel + drag. The back gesture is the top-left button (no swipe-to-dismiss in v1 — that requires touch-state tracking the back gesture would interfere with the pan handler). Mobile-chromium e2e coverage in Slice 6.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Spherical quads following planet curvature</strong> — keep everything on the sphere but render HiRISE/CTX as curved quads at true scale. Rejected because at the scales involved (HiRISE swath ~5 km across a 3389 km Mars radius), the curved quad is visually indistinguishable from a planar one, AND the surrounding sphere texture is so much lower resolution that the HiRISE swath becomes a sub-pixel speck. The user can never get close enough to read the high-res imagery without dropping the sphere context anyway — so the flat patch is the honest move.</li><li><strong>Inline flat patch as a Three.js orthographic camera over the sphere</strong> — keep one canvas, swap projections. Simpler integration but loses the ability to use canvas 2D drawing primitives (scale bar, lat/lon graticule, marker dots are all way cheaper as Canvas 2D than as Three.js objects). Rejected.</li><li><strong>Full-screen overlay div with image tiles</strong> (Leaflet / OpenLayers / MapLibre) — would give us continuous zoom + tile streaming for free. Rejected because (a) we don&#39;t actually need tile streaming at v0.7 scope (only ~30 marquee regions to render), (b) the library footprint (~200 KB even minified) is large for a feature used at deep zoom on ~30 sites, (c) adding an external map library to the dependency graph crosses the bar set by AGENTS.md &quot;no new dependencies without ADR.&quot; If #284 (base sphere texture LOD) ever justifies it, we can revisit.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>SurfaceScene</code> gains state for <code>flatPatchActive</code> + transition progress + saved-sphere-camera. The animation loop checks <code>flatPatchActive</code> and skips per-frame sphere rendering when the flat patch owns the view.</li><li>New <code>SurfaceFlatPatch.svelte</code> lives next to <code>SurfaceScene.svelte</code> in <code>src/lib/surface-scene/</code>. Slim component — most logic is canvas 2D drawing.</li><li>Slice 5&#39;s &quot;scale-aware marker primitive&quot; lands inside SurfaceFlatPatch (markers shrink with zoom, never grow).</li><li>Slice 6 adds mobile e2e + i18n + full Playwright coverage.</li><li>The legacy disc-only rendering on the sphere (when <code>region_bounds</code> is absent on a hypothetical future site) is preserved as the fallback path — the flat-patch view is opt-in via <code>region_bounds</code>.</li><li>Future Earth-launchpad route (#285) inherits this flat-patch view for free once it adopts SurfaceScene — launchpads are inherently small regions where true scale matters.</li><li>Issue #284 (base sphere texture LOD) becomes lower priority — the soft mid-zoom sphere texture matters much less when the user can drop into the flat patch for real detail.</li></ul>',29)])])}const m=t(s,[["render",i]]);export{u as __pageData,m as default};
