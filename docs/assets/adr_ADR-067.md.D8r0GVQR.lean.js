import{c as s,Q as i,j as a,m as t}from"./chunks/framework.CdiUUa3G.js";const k=JSON.parse('{"title":"ADR-067 — Sentry as client-side error-tracking layer (errors only, env-var-gated, no committed secrets)","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-067.md","filePath":"adr/ADR-067.md"}'),n={name:"adr/ADR-067.md"};function r(o,e,l,h,d,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="adr-067-—-sentry-as-client-side-error-tracking-layer-errors-only-env-var-gated-no-committed-secrets" tabindex="-1">ADR-067 — Sentry as client-side error-tracking layer (errors only, env-var-gated, no committed secrets) <a class="header-anchor" href="#adr-067-—-sentry-as-client-side-error-tracking-layer-errors-only-env-var-gated-no-committed-secrets" aria-label="Permalink to &quot;ADR-067 — Sentry as client-side error-tracking layer (errors only, env-var-gated, no committed secrets)&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-05-22 Closes into · RFC-025 §4, §6 Related ADRs · ADR-016 (no client storage), ADR-029 (PWA + service worker), ADR-057 (locale cookie exception), ADR-051 (CC-BY ship-gate / citation discipline) Reuses · podcast_scraper RFC-081 §Layer-2 Sentry pattern (component-aware DSN, <code>sendDefaultPii: false</code>, <code>beforeSend</code> scrub)</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Orrery ships ~50 KLOC of TypeScript / Svelte 5 to every browser. A bug that hydrates fine on Chromium-darwin but throws on Firefox-Android (or in a Paraglide-compiled locale we don&#39;t manually test) is currently invisible — the user closes the tab and we never know. The existing Umami analytics layer counts page views + a handful of custom events; it does not capture exceptions, console errors, or unhandled promise rejections. The hosted live site (<code>chipi.github.io</code>) and the eventual VPS-served build (per RFC-024) both need a way to surface client-side errors so the operator can triage them without waiting for a user to file an issue.</p><p>The decision is <strong>which SDK, with what config, configured how</strong>, given Orrery&#39;s load-bearing constraints:</p><ul><li><strong>Privacy promise.</strong> The README&#39;s <code>## Privacy</code> section commits to &quot;no PII&quot;, &quot;aggregate only&quot;, &quot;zero analytics by default on forks.&quot; Whatever ships has to satisfy all three.</li><li><strong>Static site, no server.</strong> <code>adapter-static</code> produces a pre-rendered SPA. There is no server-side initialisation surface; only the client SDK can run.</li><li><strong>No committed secrets.</strong> Per the operator&#39;s explicit direction during RFC-025 scoping: every credential — DSN included — lives behind an env var. The repo never contains a working DSN.</li><li><strong>Fork-friendliness.</strong> Self-hosters who clone Orrery and build for their own domain must get <em>zero</em> Sentry traffic by default. The Umami pattern is hostname-gated; the operator chose env-var-gated for Sentry (per RFC-025 §3 design question) so that forks that DO want their own Sentry can populate the env var without editing source.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Install <code>@sentry/sveltekit</code>. Initialise client-side only, via <code>src/hooks.client.ts</code> calling a thin <code>src/lib/observability/sentry.ts</code> module. The init reads <code>PUBLIC_SENTRY_DSN</code> from the build-time env (SvelteKit&#39;s <code>$env/dynamic/public</code> surface); when empty, the SDK is not invoked at all and no network request fires. When set, the SDK is initialised with <code>tracesSampleRate: 0</code> (errors only), <code>sendDefaultPii: false</code>, and a <code>beforeSend</code> hook that strips URL query parameters, nulls <code>request.headers</code> + <code>request.cookies</code>, and sets <code>user.ip_address</code> to <code>0.0.0.0</code> (Sentry&#39;s discard sentinel). No <code>hooks.server.ts</code> integration (adapter-static has no server runtime). No source-map upload (deferred to RFC-025 §11). No session replay, no Web Vitals, no performance tracing.</strong></p><h3 id="what-the-sdk-actually-captures" tabindex="-1">What the SDK actually captures <a class="header-anchor" href="#what-the-sdk-actually-captures" aria-label="Permalink to &quot;What the SDK actually captures&quot;">​</a></h3><ul><li><strong>Unhandled JavaScript exceptions</strong> during route navigation, load functions, and client-side <code>+page.svelte</code> lifecycle.</li><li><strong>Unhandled promise rejections</strong> (Sentry&#39;s default <code>globalHandlers</code> integration catches <code>unhandledrejection</code>).</li><li><strong>Errors thrown inside Svelte effects + event handlers</strong> (caught by SvelteKit&#39;s <code>handleError</code> hook, wrapped by <code>handleErrorWithSentry</code>).</li><li><strong>NOT performance traces, NOT route timings, NOT Web Vitals, NOT session replay.</strong> Each of those is explicitly disabled or simply not configured.</li></ul><h3 id="pii-scrubbing-—-what-beforesend-does-to-each-event" tabindex="-1">PII scrubbing — what <code>beforeSend</code> does to each event <a class="header-anchor" href="#pii-scrubbing-—-what-beforesend-does-to-each-event" aria-label="Permalink to &quot;PII scrubbing — what \`beforeSend\` does to each event&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">beforeSend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 1. URL — preserve path, strip query string + hash.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (event.request?.url) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> u</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event.request.url);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      event.request.url </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> u.origin </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> u.pathname;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* leave as-is, malformed URL */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 2. Headers + cookies — null. The SDK sometimes attaches referer + a cookie</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // dump even with sendDefaultPii: false; this is the belt to that suspenders.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  event.request </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event.request</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event.request, headers: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, cookies: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    :</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 3. IP — set to 0.0.0.0 which Sentry&#39;s server interprets as &quot;discard&quot;.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  event.user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event.user, ip_address: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;0.0.0.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">beforeBreadcrumb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(crumb) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ui.input breadcrumbs carry the typed value verbatim — drop entirely.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (crumb.category </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;ui.input&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Navigation breadcrumbs carry full URLs; strip query.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (crumb.category </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;navigation&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> crumb.data?.to </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;string&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> to</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> crumb.data.to;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> u</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(to, location.origin); crumb.data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">crumb.data, to: u.pathname }; } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> crumb;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="env-var-gate-the-fork-silent-contract" tabindex="-1">Env-var gate (the fork-silent contract) <a class="header-anchor" href="#env-var-gate-the-fork-silent-contract" aria-label="Permalink to &quot;Env-var gate (the fork-silent contract)&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { env } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;$env/dynamic/public&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { dev } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;$app/environment&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> initSentry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dsn</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> env.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PUBLIC_SENTRY_DSN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dsn) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Fork-silent default — no DSN, no init, no network.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (dev) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Vite dev never reports.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Sentry.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ dsn, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* …config… */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The <code>PUBLIC_</code> prefix is SvelteKit&#39;s signal that the var is exposed to the client bundle at build time. The Sentry DSN is public-by-design (it identifies the project, doesn&#39;t authenticate); inlining it into the bundle is correct and explicit.</p><h3 id="build-wiring" tabindex="-1">Build wiring <a class="header-anchor" href="#build-wiring" aria-label="Permalink to &quot;Build wiring&quot;">​</a></h3><p><code>.github/workflows/preview.yml</code> (the GH Pages deploy workflow) gets one new env block on the build step:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">env</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  PUBLIC_SENTRY_DSN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\${‌{ secrets.SENTRY_DSN_WEB }}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  PUBLIC_SENTRY_ENVIRONMENT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">production</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  PUBLIC_SENTRY_RELEASE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">gha-\${‌{ github.sha }}</span></span></code></pre></div><p>The secret is set once in repo settings → Secrets → Actions by the operator. Forks that don&#39;t have the secret will have an empty env var → silent SDK → zero outbound traffic.</p><h2 id="rationale" tabindex="-1">Rationale <a class="header-anchor" href="#rationale" aria-label="Permalink to &quot;Rationale&quot;">​</a></h2><p><code>@sentry/sveltekit</code> rather than <code>@sentry/browser</code>: the framework-specific SDK provides the <code>handleError</code> hook integration, automatic source-map awareness, and the SvelteKit-compatible env-var loading. Adding <code>@sentry/browser</code> would require manual error boundary plumbing that the framework SDK already provides.</p><p>Errors-only (not perf): the user picked it during RFC-025 scoping. The reasoning: error volume is bounded by <em>unique bugs in the bundle</em>, not by visitor count, so it scales gracefully with site popularity. Performance tracing volume scales linearly with visitors and is much more likely to exhaust the Sentry free tier under viral load. We can add perf tracing later if a real-user performance regression motivates it.</p><p><code>sendDefaultPii: false</code> + <code>beforeSend</code> is the standard belt-and-suspenders Sentry pattern. Sentry&#39;s SDK respects <code>sendDefaultPii</code> for the IP address + headers, but the <code>beforeSend</code> hook is the right place to enforce additional project-specific scrubbing (URL query stripping in our case, since Orrery&#39;s URL contract carries mission IDs + locale codes that aren&#39;t PII themselves but could become so if a future feature accepts user-typed search text in a query param).</p><p>Env-var-gated, not hostname-gated: the existing <code>src/lib/analytics.ts</code> uses a <code>HOSTNAME_ALLOWLIST</code> to ensure Umami only loads on <code>chipi.github.io</code>. We considered the same pattern for Sentry but chose env-var-gating instead. Reasoning: forks that DO want their own Sentry project should be able to populate <code>PUBLIC_SENTRY_DSN</code> in their own CI secrets without editing source code; hostname-gating would force them to modify <code>analytics.ts</code>-style allowlist files. Env-var-gating makes Sentry &quot;BYO project&quot; by construction. The trade-off is that a misconfigured CI could accidentally point a fork at the operator&#39;s project; mitigated by Sentry&#39;s project-side rate limits + the public-by-design DSN model.</p><p>No source-map upload in Slice 1: source-map upload requires <code>SENTRY_AUTH_TOKEN</code> (the genuinely secret one, not the public DSN), the <code>@sentry/vite-plugin</code>, and a release-tagging convention. Useful for production debugging but adds a moving piece. Land it as a follow-up RFC once the base SDK + scrubbing have bedded in and the operator has triaged at least one real error and felt the pain of minified stack traces.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Hostname-gated like Umami.</strong> <code>analytics.ts:HOSTNAME_ALLOWLIST</code> lists exactly one production hostname. Same pattern would work for Sentry. Rejected because env-var-gated makes BYO-Sentry forks possible without source edits.</li><li><strong><code>@sentry/browser</code> + manual hooks.</strong> Lower-level SDK, finer-grained control. Rejected — <code>@sentry/sveltekit</code> is the supported framework integration and its <code>handleError</code> wrapping is non-trivial to replicate by hand.</li><li><strong>Errors + 10% performance tracing.</strong> Adds Web Vitals + route-transition timing. Rejected during RFC-025 scoping; defer until a real-user perf regression motivates it. Adding it later is a one-line config change.</li><li><strong>Session replay (<code>@sentry/replay</code>).</strong> Records user UI for error reproduction. Rejected explicitly — the Replay SDK captures DOM mutations + input events; the privacy posture would have to change materially to accommodate it. Out of bounds for Orrery.</li><li><strong>No client error tracking; rely on user reports.</strong> Status quo. Rejected because users don&#39;t report errors — they close the tab. We&#39;ve already seen this with the i18n cookie test that passed against vite preview but failed against nginx (caught in CI, not by a user). Production errors that ONLY fire on specific browser/locale combinations need an observation surface.</li><li><strong>A self-hosted error sink (e.g. GlitchTip on the VPS).</strong> Self-hosted Sentry-compatible service. Rejected — operator already has a Sentry org for podcast_scraper; doubling that infrastructure for Orrery alone doesn&#39;t pay back. Revisit if Sentry-Cloud free-tier limits become a problem.</li><li><strong>Hostname-allowlist + env-var-gate combined.</strong> Belt + suspenders. Rejected as over-engineered — one of the two gates is enough.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p><strong>Positive:</strong></p><ul><li>Production JS errors on <code>chipi.github.io</code> (and the future VPS) become visible without users reporting them.</li><li>Strict privacy: <code>sendDefaultPii: false</code> + <code>beforeSend</code> scrub + <code>tracesSampleRate: 0</code> means no IP, no headers, no query params, no UI input — only error type, stack trace (minified pre-source-map-upload), and a path-only URL.</li><li>Fork-silent by default — the <code>PUBLIC_SENTRY_DSN</code> env var is empty in forks&#39; CI, SDK no-ops, zero outbound traffic.</li><li>BYO-Sentry forks possible — set <code>PUBLIC_SENTRY_DSN</code> in your own CI, your fork reports to your project.</li><li>Reuses the operator&#39;s existing Sentry org (podcast_scraper RFC-081) — one set of credentials, one billing surface.</li></ul><p><strong>Negative:</strong></p><ul><li>Adds ~30 KB to the client bundle (<code>@sentry/sveltekit</code> minified+gzipped). Acceptable; the bundle is ~700 KB total already (per the chunkSizeWarningLimit in <code>vite.config.ts</code>).</li><li>Production minified stack traces are ugly until source-map upload lands (deferred). Errors are still triageable from the file:line + message, but resolving back to the original Svelte component requires manual sourcemap consultation.</li><li>One more thing to maintain. ~150 LOC of new TypeScript + the GH Actions env block. Routine.</li><li>A future contributor could write <code>Sentry.captureMessage(&#39;user typed: &#39; + input)</code> and bypass the scrubber for the message argument. Documented in the operator guide as a &quot;don&#39;t do this&quot; and caught by code review.</li></ul><h2 id="decision-status" tabindex="-1">Decision status <a class="header-anchor" href="#decision-status" aria-label="Permalink to &quot;Decision status&quot;">​</a></h2><p><strong>Accepted on 2026-05-22.</strong> Shipped in commit <code>cc9bc3423</code> (Slice 1 of RFC-025). The base contract held — <code>@sentry/sveltekit</code> v10.53.1 SDK, env-var-gated init, errors only, <code>sendDefaultPii: false</code>, <code>beforeSend</code> scrub. One adjustment landed in implementation: <code>@sentry/cli</code> is a transitive dep under Sentry&#39;s own <code>FSL-1.1-MIT</code> source-available license (MIT-compatible after a 2-year competitive-use embargo), so the license allowlist in <code>scripts/build-tech-bom.ts</code> was extended to include <code>FSL-1.1-MIT</code> with reasoning in a comment.</p><p>Validated end-to-end:</p><ul><li><code>PUBLIC_SENTRY_DSN= npm run build</code> succeeds, fork-silent path (SDK no-op) confirmed by absence of DSN in bundle.</li><li><code>PUBLIC_SENTRY_DSN=&lt;test-DSN&gt; npm run build</code> succeeds, DSN inlined into <code>build/_app/env.js</code>, SDK code bundled into <code>build/_app/immutable/entry/app.*.js</code> (158 KB chunk).</li><li><code>npm run typecheck</code>: 0 errors / 0 warnings. <code>npm run lint</code>: clean. <code>npm run test</code>: 1141/1141 passing. <code>npm run check-no-secrets:all</code>: 8801 files clean.</li></ul><p>No anticipated amendments at this scope; the source-map upload follow-up (RFC-025 §11) is its own RFC.</p>`,38)])])}const u=s(n,[["render",r]]);export{k as __pageData,u as default};
