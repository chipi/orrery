import{c as s,Q as t,j as a,m as i}from"./chunks/framework.CyZn50Me.js";const k=JSON.parse('{"title":"ADR-073 — Surface texture LOD via per-region insets (Layer A) + 2K → 4K base swap (Layer B); no tile streaming","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-073.md","filePath":"adr/ADR-073.md"}'),n={name:"adr/ADR-073.md"};function r(o,e,l,h,d,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="adr-073-—-surface-texture-lod-via-per-region-insets-layer-a-2k-→-4k-base-swap-layer-b-no-tile-streaming" tabindex="-1">ADR-073 — Surface texture LOD via per-region insets (Layer A) + 2K → 4K base swap (Layer B); no tile streaming <a class="header-anchor" href="#adr-073-—-surface-texture-lod-via-per-region-insets-layer-a-2k-→-4k-base-swap-layer-b-no-tile-streaming" aria-label="Permalink to &quot;ADR-073 — Surface texture LOD via per-region insets (Layer A) + 2K → 4K base swap (Layer B); no tile streaming&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-05-30 Closes into · PRD-021 (product framing), #284 (issue) Related ADRs · ADR-001 (Three.js r128 pin — constrains LOD mechanism), ADR-046 / ADR-047 (provenance for new textures), ADR-061 (<code>region_bounds</code> on <code>SurfaceSite</code> — Layer A line-of-sight gate), ADR-072 (shared <code>SurfaceScene.svelte</code> — Layer A + Layer B integration point) Related issues · #276 (W6 mobile-tier precache — couples), #283 (surface hotspots v2 — hard prereq), #285 (launch sites on <code>/earth</code> — gates Layer A on Earth)</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Surface routes <code>/mars</code>, <code>/moon</code>, <code>/earth</code>, plus the cislunar / station scenes that share the same body textures, currently render every base sphere with a single 2048×1024 equirectangular texture. At wide zoom this is fine. As the camera approaches the surface — <em>before</em> the flat ground-patch threshold trips (#283 Slice 6, Mars + Moon only) — texels become larger than screen pixels and the sphere reads as visibly soft. NASA&#39;s interactive surface maps swap higher-detail tiles continuously; we don&#39;t, and the softness is the first thing reviewers notice when comparing the two.</p><p>PRD-021 frames the user-facing problem. This ADR locks the <em>technical</em> shape of the fix.</p><p>The decision space:</p><ul><li><strong>(A) Per-resolution base variants</strong> — 2K / 4K / 8K of each body, swap based on camera distance.</li><li><strong>(B) Tiled cubemap or quadtree streaming</strong> — NASA-like continuous tile load + LOD selection.</li><li><strong>(C) Per-region high-detail insets</strong> — generate higher-res sphere-projected textures only for the SurfaceSite regions users zoom toward.</li></ul><p>The trade-offs:</p><table tabindex="0"><thead><tr><th>Option</th><th>Asset-pipeline cost</th><th>Runtime cost in r128</th><th>Mobile impact</th><th>Where it shines</th></tr></thead><tbody><tr><td>A</td><td>Low — one fetch per resolution per body, manifest entry per file</td><td>Trivial — <code>material.map = newTexture</code> on threshold</td><td><strong>Heavy at 8K</strong> — ~250 MB GPU + ~80 MB on-disk per body; even 4K is ~4 MB on-disk per body × 3 bodies + a precache hit</td><td>Desktop, when the body fills the viewport everywhere. Cheap to ship.</td></tr><tr><td>B</td><td>High — tile generation pipeline (<code>gdal_translate</code> or equivalent), per-zoom manifests, per-tile provenance, per-tile license if mixing sources</td><td>High — Three.js r128 has no built-in quadtree, so we&#39;d write a custom loader, LOD selector keyed to camera distance, eviction policy, network / cache discipline</td><td>Best at the cost of large infra investment</td><td>Continuous-zoom interactive map — the right <em>eventual</em> answer if Orrery becomes a full geospatial browser</td></tr><tr><td>C</td><td>Medium — only ~10-20 regions per body need insets, stored as separate sphere-projected overlays. Same provenance shape as base textures</td><td>Medium — overlay-material swap on approach corridor (camera distance + line-of-sight via <code>region_bounds</code>); UV math to clip the inset to its lat/lon extent. r128-native (no new API)</td><td>Bounded by region count × inset size. Mostly cacheable per region. Mobile tier loads on-demand</td><td>Aligns with #283&#39;s region work. You pay the cost only where users zoom <em>toward</em>, not everywhere</td></tr></tbody></table><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Layer A (option C — per-region insets) is the canonical LOD strategy. Layer B (option A cut-down to a single 2K → 4K base swap, NOT 8K) is the backstop for bodies-of-no-interest zoom.</strong></p><h3 id="layer-a-—-per-region-high-detail-insets" tabindex="-1">Layer A — per-region high-detail insets <a class="header-anchor" href="#layer-a-—-per-region-high-detail-insets" aria-label="Permalink to &quot;Layer A — per-region high-detail insets&quot;">​</a></h3><p>Each <code>SurfaceSite</code> that has user-attention gains two new optional fields on the schema:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;apollo-11&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;region_bounds&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},                                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ADR-061 (existing)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;inset_texture_path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;textures/insets/moon/apollo-11/2k.jpg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ADR-073 (new)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;inset_zoom_threshold_ratio&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.5</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                              // ADR-073 (new, optional)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The runtime check in <code>SurfaceScene.svelte</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ratio</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> camR </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> BODY_RADIUS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ratio </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> site.inset_zoom_threshold_ratio</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    &amp;&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cameraLineOfSightIntersects</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(site.region_bounds)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    &amp;&amp;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">insetMounted) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  mountInsetOverlay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(site);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ((ratio </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> site.inset_zoom_threshold_ratio </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">insetMounted) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> insetMounted) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  unmountInsetOverlay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(site);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The inset is a sphere-projected texture overlay in a bounded UV rectangle around the region&#39;s lat/lon extent. Alpha-feathered seam so the inset edge doesn&#39;t read as a hard line against the base sphere.</p><h3 id="layer-b-—-base-2k-→-4k-swap" tabindex="-1">Layer B — base 2K → 4K swap <a class="header-anchor" href="#layer-b-—-base-2k-→-4k-swap" aria-label="Permalink to &quot;Layer B — base 2K → 4K swap&quot;">​</a></h3><p>For zoom levels where no SurfaceSite is in line-of-sight, the base sphere material swaps the <code>map</code> reference between <code>2k_&lt;body&gt;_daymap.jpg</code> and <code>4k_&lt;body&gt;_daymap.jpg</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Hysteresis: swap to 4K when camR &lt;= 4K_IN, swap back to 2K when camR &gt;= 2K_OUT.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Deadband prevents per-frame thrashing if camR sits right on the boundary.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> EARTH_LOD_4K_IN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 22</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> EARTH_LOD_2K_OUT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 28</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (camR </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> EARTH_LOD_4K_IN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lod </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;4k&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">swapTo4K</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (camR </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> EARTH_LOD_2K_OUT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lod </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;2k&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">swapTo2K</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>Per-body thresholds are tuned to where the 2K texels visibly exceed screen-pixel size, measured against the body&#39;s typical camera-distance range. Lazy-load the 4K texture on first threshold cross; reuse on subsequent crosses; dispose on route teardown alongside the active 2K.</p><h3 id="compose" tabindex="-1">Compose <a class="header-anchor" href="#compose" aria-label="Permalink to &quot;Compose&quot;">​</a></h3><p>A user zooming toward Apollo 11 on <code>/moon</code>:</p><ol><li>Wide view, base 2K sphere</li><li>Closing approach → Layer B threshold → swap base to 4K</li><li>Line-of-sight hits Apollo 11 <code>region_bounds</code> → Layer A → mount Apollo 11 inset overlay on top of the 4K base</li><li>Pull back: Layer A unmounts first (line-of-sight check fails), then Layer B reverts to 2K (camR &gt; 2K_OUT)</li></ol><p>The flat ground patch (#283 Slice 6) takes over below the Layer A threshold ratio; this ADR&#39;s mechanism doesn&#39;t fire inside the ground-patch view at all.</p><h2 id="rationale" tabindex="-1">Rationale <a class="header-anchor" href="#rationale" aria-label="Permalink to &quot;Rationale&quot;">​</a></h2><h3 id="why-not-b-tile-streaming" tabindex="-1">Why not (B) tile streaming <a class="header-anchor" href="#why-not-b-tile-streaming" aria-label="Permalink to &quot;Why not (B) tile streaming&quot;">​</a></h3><ul><li><strong>r128 has no built-in quadtree.</strong> We&#39;d implement the tile loader, LOD selector, eviction policy ourselves. That&#39;s a real subsystem, ~500-1000 LOC + tests + provenance per tile.</li><li><strong>The zoom band where tiling shines is the same band the flat ground patch (#283 Slice 6) is taking over.</strong> Past the inset threshold, the flat patch is the better UX. Above the inset threshold, the 4K base + inset combo is enough. The &quot;continuous tile streaming&quot; use case lives in a narrow band that doesn&#39;t justify the infrastructure cost yet.</li><li><strong>Asset-pipeline scope</strong>: tile-based assets need their own gdal-based generation pipeline + per-tile manifest + per-tile provenance. Significantly more than insets, which fit the existing per-region asset pattern.</li><li><strong>Provenance</strong> at the tile level becomes complex when mixing sources (e.g. CTX global mosaic + HiRISE inset patches). Per ADR-046 / ADR-047, every distributed asset needs a clean attribution chain. Tile mosaics blur that.</li></ul><p>Re-evaluation trigger: if user signal demonstrates that the mid-zoom band on <code>/mars</code> or <code>/moon</code> remains visually weak <em>after</em> this ADR ships and the flat ground patch ships, tile streaming becomes the obvious next step. ADR-073 is not a permanent rejection; it&#39;s a deferral until the easier mechanism is proven insufficient.</p><h3 id="why-not-a-alone-8k-base-for-everyone" tabindex="-1">Why not (A) alone (8K base for everyone) <a class="header-anchor" href="#why-not-a-alone-8k-base-for-everyone" aria-label="Permalink to &quot;Why not (A) alone (8K base for everyone)&quot;">​</a></h3><ul><li><strong>GPU cost</strong>: 8K texture allocation is ~256 MB per body. Three bodies = ~768 MB. Some mobile GPUs can&#39;t allocate that.</li><li><strong>Bandwidth cost</strong>: 8K base for three bodies × ~50 MB each = ~150 MB additional precache or on-demand load. Pushes mobile install size past comfortable.</li><li><strong>Coverage waste</strong>: an 8K base spends pixels evenly across the sphere, including ocean / mare / regolith where the user has no specific reason to zoom. Insets pay only for regions of interest.</li></ul><p>The 2K → 4K base swap (Layer B) is a <em>sub-case</em> of option A — keeping just the smallest step up. It buys most of the visible quality improvement for the bodies-of-no-interest case without paying the full 8K cost.</p><h3 id="why-r128-material-map-works" tabindex="-1">Why r128 + <code>material.map = ...</code> works <a class="header-anchor" href="#why-r128-material-map-works" aria-label="Permalink to &quot;Why r128 + \`material.map = ...\` works&quot;">​</a></h3><p>Three.js r128 <code>MeshPhongMaterial</code> and <code>MeshStandardMaterial</code> both support runtime <code>.map</code> reassignment + <code>.needsUpdate = true</code>. Validated locally on <code>/earth</code> in the spike (commit forthcoming). No r130+ API is taken on — ADR-001 pin stays clean.</p><p>The lazy-load pattern uses <code>THREE.TextureLoader.load(url, onLoad, onProgress, onError)</code> — the legacy callback shape that&#39;s r128-native. Async/await pattern is not used because <code>loadAsync</code> only landed cleanly later. The error callback resets a <code>loadStarted</code> flag so a transient network failure doesn&#39;t permanently lock the route into 2K.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Procedural / shader-side LOD via mipmap bias on a single 8K base.</strong> Rejected. 8K base eats the GPU + bandwidth cost everywhere; mipmap bias doesn&#39;t help if the source texture itself is 2K (no extra detail to reveal). Was a half-step toward (A) without the runtime benefit.</li><li><strong>CSS / canvas hybrid for the inset rather than a Three.js sphere overlay.</strong> Rejected. The inset has to sit on the sphere correctly under the camera&#39;s projection; doing that in a 2D layer is harder than just adding the overlay material to the existing scene graph.</li><li><strong>One-size-fits-all inset zoom threshold ratio per body.</strong> Rejected. Regions vary in size — Apollo 11&#39;s 1.5 km × 1 km ellipse hits the threshold at a different camera distance than a 10 km × 50 km surface-feature region. Per-region <code>inset_zoom_threshold_ratio</code> (optional, defaults to body-wide value) gives the SurfaceSite authors a knob they can tune per-region without code changes.</li><li><strong>Pre-load 4K base on mount instead of lazy-load.</strong> Rejected. Adds ~4 MB to the initial route paint cost across three bodies × routes that load Earth (cislunar, ISS, Tiangong). Lazy-load gates the cost on actual approach.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p><strong>Positive:</strong></p><ul><li><strong>Visual fidelity matches user intent.</strong> Zooming in on a region you care about gets you a higher-detail texture there; orbiting empty terrain still gets a sharp-enough base from the 4K swap.</li><li><strong>Mobile-tier discipline preserved.</strong> No new infrastructure on mobile beyond what W6 (#276) was already planning. Layer A insets load on-demand on mobile; Layer B can run if/when the mobile budget allows the 4K base.</li><li><strong>Asset pipeline doesn&#39;t grow new shapes.</strong> Insets are per-region textures with provenance entries — same pattern as fleet hero images, mission patches, etc.</li><li><strong>r128 pin stays untouched.</strong> No new API surface taken on.</li><li><strong>Reversibility.</strong> If tile streaming becomes the right answer later, both layers come out cleanly — Layer A&#39;s <code>inset_texture_path</code> field stays optional, Layer B&#39;s swap becomes one branch in a richer LOD selector.</li></ul><p><strong>Negative:</strong></p><ul><li><strong>Two layers means two places to tune thresholds.</strong> Layer A&#39;s per-region threshold + Layer B&#39;s per-body threshold. Documented and defaulted, but the tunable surface is bigger than (A) alone.</li><li><strong>Per-region inset asset generation adds work to the SurfaceSite authoring workflow.</strong> Mitigated by tooling — a script in <code>scripts/</code> to crop / scale a high-res source map to the inset extent and write the path back to the SurfaceSite JSON. Authoring slice in the implementation plan.</li><li><strong>The 4K base textures need a real source.</strong> Solar System Scope&#39;s <code>download/</code> URL started returning HTML some time after <code>scripts/fetch-assets.ts</code> was last run (verified 2026-05-30). The fetch script needs a re-point, OR a different source picked. Tracked as part of PRD-021 Slice 2.</li><li><strong>One more failure mode to test in e2e.</strong> The LOD swap must not flash a lower-res texture mid-swap or leave the route in a half-swapped state on rapid camera input. Covered by the success-criteria gate in PRD-021.</li></ul><h2 id="decision-status" tabindex="-1">Decision status <a class="header-anchor" href="#decision-status" aria-label="Permalink to &quot;Decision status&quot;">​</a></h2><p><strong>Accepted on 2026-05-30.</strong> Implementation gated on #283 Slice 6 per PRD-021. Earth Layer B spike committed alongside this ADR (<code>/earth</code> + <code>/fly</code> cislunar texture-swap mechanism in r128) to derisk the runtime pattern before the gate opens.</p><p><strong>Layer B implementation status (2026-05-31, drift-cleanup pass):</strong></p><table tabindex="0"><thead><tr><th>Consumer</th><th>Status</th><th>Wired in</th></tr></thead><tbody><tr><td><code>/earth</code> orbital (EarthOrbitalScene)</td><td>✅ shipped</td><td>#284 (<code>34e7591a7</code>)</td></tr><tr><td><code>/explore</code> (per-planet)</td><td>✅ shipped</td><td>#287 (<code>b88a77573</code>)</td></tr><tr><td><code>/earth?mode=surface</code> (SurfaceScene &#39;earth&#39;)</td><td>✅ shipped</td><td>This pass</td></tr><tr><td><code>/moon</code> (SurfaceScene)</td><td>✅ shipped</td><td>This pass</td></tr><tr><td><code>/mars</code> (SurfaceScene)</td><td>✅ shipped</td><td>This pass</td></tr><tr><td><code>/fly</code> cislunar Earth + Moon</td><td>✅ shipped</td><td>This pass</td></tr><tr><td><code>/iss</code> Earth backdrop</td><td>✅ shipped</td><td>This pass</td></tr><tr><td><code>/tiangong</code> Earth backdrop</td><td>✅ shipped</td><td>This pass</td></tr></tbody></table><p>The <code>textureUrl4k?: string</code> optional field on <code>SurfaceSceneConfig</code> carries the Layer B contract for SurfaceScene consumers. Cislunar (<code>buildCislunarScene</code>) carries <code>earthTextureUrl4k?: string</code> + <code>moonTextureUrl4k?: string</code>. <code>/iss</code> and <code>/tiangong</code> carry the swap inline because their Earth backdrop isn&#39;t routed through a shared scene builder — same pattern, route-local. All consumers dispose the 4K texture explicitly on teardown (it lives in a closure, not the scene graph, so <code>disposeScene</code> alone misses it).</p><p><strong>Layer A status: not yet shipped.</strong> The per-region inset mechanism + the <code>inset_texture_path</code> schema field remain unimplemented as of this writing — Layer B alone gives the bulk of the perceived sharpness win; Layer A&#39;s per-region payoff lands when the showcase site insets (Apollo 11, Curiosity Bradbury Landing, LC-39A) are sourced and authored.</p>`,48)])])}const g=s(n,[["render",r]]);export{k as __pageData,g as default};
