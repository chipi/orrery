import{_ as o,o as t,c as i,a2 as a}from"./chunks/framework.CpZKh-tG.js";const m=JSON.parse('{"title":"ADR-077 — /fly throne-of-glory iconic-shot architecture (body wiring + cislunar hold detector + per-event compositions + race-free test hooks)","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-077.md","filePath":"adr/ADR-077.md"}'),r={name:"adr/ADR-077.md"};function s(n,e,d,c,l,h){return t(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="adr-077-—-fly-throne-of-glory-iconic-shot-architecture-body-wiring-cislunar-hold-detector-per-event-compositions-race-free-test-hooks" tabindex="-1">ADR-077 — /fly throne-of-glory iconic-shot architecture (body wiring + cislunar hold detector + per-event compositions + race-free test hooks) <a class="header-anchor" href="#adr-077-—-fly-throne-of-glory-iconic-shot-architecture-body-wiring-cislunar-hold-detector-per-event-compositions-race-free-test-hooks" aria-label="Permalink to &quot;ADR-077 — /fly throne-of-glory iconic-shot architecture (body wiring + cislunar hold detector + per-event compositions + race-free test hooks)&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-06-14 Gates · v0.7.x ship; v0.8+ extension point for issue #341</p></blockquote><p><strong>Gating sentence</strong>: every flown / active mission across the four <code>/fly</code> segments (multi-flyby helio, single-flyby helio, Mars-direct, cislunar global) composes an iconic hero shot at its narrative beat via a per-body composition layer + a Tier-1.5 hybrid-waypoint hold detector + per-event MoonComposition variants; new destinations are added by following the 15-step §body-wiring checklist in TA.md.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The pre-this-ADR state of <code>/fly</code>:</p><ol><li><p><strong>Iconic shots worked only for the 8 main planets + the Moon.</strong> New Horizons at Pluto / Arrokoth, Dawn at Vesta / Ceres, Giotto at Halley, Rosetta at 67P, OSIRIS-REx at Bennu — all fell back to &quot;no body resolved&quot; → cruise framing, or fell back to whatever planet <code>findClosestPlanetToShip</code> happened to think they were near (often Venus by default). Empty-space hero shots.</p></li><li><p><strong>Cislunar iconic shots worked for Apollo 11 + Chang&#39;e 5, broke for everyone else.</strong> Free-return missions (Apollo 13 swing-by), direct landers (Luna 9 impact), powered-descent landers (Luna 16, Chandrayaan-3), low-energy transfers (SLIM), and the generic-<code>arrival</code>-event missions (SLIM LOI, Chandrayaan-1 LOI) all sailed past the iconic moment with the camera at the wide Earth-Moon-system framing. The auto-zoom dispatcher only recognised <code>lunar_orbit</code> / <code>spiral_lunar</code> / <code>descent</code> / <code>ascent</code> phases — and Tier-2 waypoint missions only ever got a single <code>tli_coast</code> phase out of <code>buildFromWaypoints</code>.</p></li><li><p><strong>Outer-system planets rendered dark.</strong> Sun PointLight had <code>distance=2000, decay=1.2</code> — Neptune at 30 AU = 2400 scene units was beyond the cutoff and unlit.</p></li><li><p><strong>HUD showed <code>NaN km/s</code> past the embedded Mars Hohmann SMA.</strong> Voyager 2 at Neptune (r=30 AU) against <code>aTransferAu = 1.262</code> returned <code>2/30 − 1/1.262 = −0.726</code> → <code>sqrt(negative)</code> → NaN.</p></li><li><p><strong>Same SPA mission swap broke under rapid URL pushes.</strong> <code>$page.url</code>-tracking effect read <code>$page</code> reactively inside <code>localeFromPage(...)</code>, propagating noise from every unrelated <code>$page</code> mutation.</p></li><li><p><strong>Verifying any of this was racing other Claude Code sessions</strong> using the same chrome-devtools-mcp browser instance. Pause clicks, scrubber events, and even page navigations were getting clobbered.</p></li></ol><p>The throne-of-glory polish bar — the user-stated goal that every flyby compose at the same tier as Voyager 2&#39;s Neptune flyby photograph or Cassini&#39;s Saturn-OI hero shot — was a long way off. This ADR locks the architecture that closes that gap and makes it grindable per new destination.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-body-wiring-is-a-15-step-checklist-with-one-canonical-example" tabindex="-1">1. Body wiring is a 15-step checklist with one canonical example <a class="header-anchor" href="#_1-body-wiring-is-a-15-step-checklist-with-one-canonical-example" aria-label="Permalink to &quot;1. Body wiring is a 15-step checklist with one canonical example&quot;">​</a></h3><p>Adding a new flyby body (any destination — planet, dwarf, comet nucleus, asteroid, KBO, contact binary) is exactly 15 touchpoints across ~10 files. Arrokoth (commit <code>e6e9175b</code>) is the worked example end-to-end. The checklist lives in <strong>TA.md §body-wiring</strong> and <strong>AGENTS.md §&quot;Adding a new flyby body to <code>/fly</code>&quot;</strong>, both pointing to the Arrokoth diff. Halley + 67P (commit <code>3c1e6938e</code>) is a shorter follow-up adding the synonym pattern for ambiguous data labels (&quot;Churyumov&quot; → &#39;67p&#39;).</p><p>The most subtle step is <strong>#7 <code>labelToPlanetId</code> + <code>FLYBY_RADIUS_AU</code></strong> in <code>src/lib/fly-mission-apply.ts</code>. Without this, the trajectory.json waypoint at the body&#39;s label stays at the raw (x, y, z) coords from /explore (which can be 20+ AU off-axis from where <code>destinationPos()</code> says the body is), and the iconic composition frames empty space. Every new-body wiring failure I hit during this session traced back to a missing entry here.</p><h3 id="_2-cislunar-tier-1-5-hybrid-waypoint-hold-detector" tabindex="-1">2. Cislunar Tier-1.5 hybrid-waypoint hold detector <a class="header-anchor" href="#_2-cislunar-tier-1-5-hybrid-waypoint-hold-detector" aria-label="Permalink to &quot;2. Cislunar Tier-1.5 hybrid-waypoint hold detector&quot;">​</a></h3><p><code>buildFromWaypoints</code> walks every Tier-2 mission&#39;s <code>cislunar_profile.waypoints_km</code> for &quot;hold&quot; runs — 3+ consecutive entries with identical (x, y, z) within 10 km tolerance. Each hold is a data-designer signature for an iconic beat the analytic model couldn&#39;t compute (LOI capture, surface stay, TEI burn, free-return periselene). Multi-hold per mission is supported (Apollo 17 = LOI + surface stay + TEI).</p><p>Phase synthesis: <code>tli_coast</code> (outbound) → hold[0] (<code>lunar_flyby</code> or <code>descent</code>) → <code>tli_coast</code> (between holds) → hold[1] → … → <code>tei_coast</code> (after last hold). Phases interleave with no overlap; MET-ordered. <code>descent</code> if <code>closest_approach_km ≤ R_MOON + 50</code> (Luna 9, Apollo 11 landing), <code>lunar_flyby</code> otherwise (Apollo 13 swing-by — the conservative <code>LUNAR_PHASE_TYPES</code> default; both compose iconically against the Moon).</p><p><strong>Moon-local convention</strong> (the subtle bit): <code>lunar_flyby</code> and <code>descent</code> are in <code>MOON_LOCAL_PHASE_TYPES</code>. The sampler in <code>$lib/orbital/sample-cislunar-spacecraft.ts</code> does <code>point + (live_moon − moonRefPos)</code>. The caller hard-codes <code>moonRefPos = moonEciPos(flyby_day = dep + transit_days)</code>. So we store <code>points = moonAtFlyby</code> — then <code>sample = moonAtFlyby + (live_moon − moonAtFlyby) = live_moon</code>, pinning the spacecraft to the live Moon centre for the entire hold window. For Tier-1.5 hybrid data the hold&#39;s absolute coords don&#39;t align with the stylized <code>moonEciPos</code> model anyway, so storing the live-moon reference is the right convention. (Earlier attempts at storing <code>(0, 0, 0)</code> or <code>moonAtSwingby</code> had the spacecraft drift off-axis as simDay advanced.)</p><h3 id="_3-per-event-mooncomposition-variants" tabindex="-1">3. Per-event MoonComposition variants <a class="header-anchor" href="#_3-per-event-mooncomposition-variants" aria-label="Permalink to &quot;3. Per-event MoonComposition variants&quot;">​</a></h3><p>Seven event types each drive their own <code>MoonComposition</code>:</p><table tabindex="0"><thead><tr><th>Type</th><th>Side</th><th>Pitch</th><th>camR×</th><th>targetBias</th><th>Use case</th></tr></thead><tbody><tr><td><code>loi</code></td><td>85°</td><td>15°</td><td>4</td><td>0</td><td>Apollo-8 earthrise default</td></tr><tr><td><code>tei</code></td><td>85°</td><td>15°</td><td>4</td><td>0</td><td>(same)</td></tr><tr><td><code>descent_start</code></td><td>85°</td><td>15°</td><td>4</td><td>0</td><td>(same)</td></tr><tr><td><code>ascent</code></td><td>85°</td><td>15°</td><td>4</td><td>0</td><td>(same)</td></tr><tr><td><code>flyby</code></td><td>60°</td><td>35°</td><td>5</td><td>0.4</td><td>Apollo 13 free-return limb arc</td></tr><tr><td><code>edl_or_oi</code></td><td>45°</td><td>10°</td><td>3</td><td>0.5</td><td>Luna 9 / Chandrayaan-3 descent tightness</td></tr><tr><td><code>arrival</code></td><td>85°</td><td>15°</td><td>4</td><td>0</td><td>SLIM / Chandrayaan-1 LOI (same as loi)</td></tr></tbody></table><p><code>findActiveCislunarHero</code> widens to match all seven (was just <code>loi</code>/<code>tei</code>/<code>descent_start</code>/<code>ascent</code>). <code>CISLUNAR_HERO_LEAD_DAYS</code> carries per-event lead offsets. <code>planCislunarHeroShot</code> picks the right variant via <code>CISLUNAR_HERO_COMPOSITION</code> map; explicit <code>ctx.composition</code> override still wins for testing.</p><h3 id="_4-live-moon-hero-tracking" tabindex="-1">4. Live-moon hero tracking <a class="header-anchor" href="#_4-live-moon-hero-tracking" aria-label="Permalink to &quot;4. Live-moon hero tracking&quot;">​</a></h3><p><code>planCislunarHeroShot</code>&#39;s <code>ctx.moonPos</code> reads <code>moonEciPos(simDay)</code> — live moon, not Moon-at-iconicMet. The cislunarMoon mesh always renders at live position; previously the planner&#39;s <code>cameraTarget = moonEciPos(iconicMet)</code> drifted ~13°/day from the rendered Moon position, so users landing 0.1 d past iconicMet saw the moon out-of-frame. <code>shipPosAtMet</code> still uses iconicMet so the approach-direction geometry stays invariant inside the hero window.</p><h3 id="_5-sun-lit-hemisphere-bias-for-flyby-cinema" tabindex="-1">5. Sun-lit hemisphere bias for flyby cinema <a class="header-anchor" href="#_5-sun-lit-hemisphere-bias-for-flyby-cinema" aria-label="Permalink to &quot;5. Sun-lit hemisphere bias for flyby cinema&quot;">​</a></h3><p><code>planFlybyShot</code> flips the perp vector when <code>cos(α) &lt; −0.7</code> (α &gt; 134° — camera deep on night side). At side-angle 85° the perp dominates the camera offset, so this controls which hemisphere ends up facing camera. V2 Jupiter pre-fix rendered as a thin-lit-limb at α ≈ 170°; post-fix is the iconic banded gas-giant lit hemisphere. Threshold <code>-0.7</code> keeps the terminator-grazing compositions (V2 Uranus α ≈ 80°, Cassini SOI varies) — they&#39;re aesthetically valid Cassini-mission-art frames; we only flip the extreme back-lit cases.</p><h3 id="_6-moi-arrival-epilogue-gate-removed" tabindex="-1">6. MOI=arrival epilogue gate removed <a class="header-anchor" href="#_6-moi-arrival-epilogue-gate-removed" aria-label="Permalink to &quot;6. MOI=arrival epilogue gate removed&quot;">​</a></h3><p>For missions whose iconic moment IS the arrival event (Mars Express MOI = arrival, Mars Pathfinder EDL = arrival, etc.), the pre-fix gate <code>epilogueActive &amp;&amp; (rawActiveFlybyMet ≤ arrivalMet + 1) → suppress cinema</code> immediately killed the iconic shot at landing. The gate&#39;s original purpose was Saturn-OI peakHold pinning. Gate removed; Saturn-OI still composes iconically (verified Cassini SOI). The peakHold release condition is a separate concern (open follow-up).</p><h3 id="_7-race-free-dev-only-test-hooks" tabindex="-1">7. Race-free DEV-only test hooks <a class="header-anchor" href="#_7-race-free-dev-only-test-hooks" aria-label="Permalink to &quot;7. Race-free DEV-only test hooks&quot;">​</a></h3><p>Two hooks added to <code>src/routes/fly/+page.svelte</code>:</p><ul><li><code>window.__flySetSimDay(metDays)</code> — sets <code>simDay</code> + pauses. Skips <code>biasJumpToIconicMoment</code> / 700 ms snap-cut / peakHold clearing — all upstream input concerns. Everything downstream (auto-zoom lerp, scene render, hero detector) runs identically to a real user landing at the same MET.</li><li><code>window.__flyCislunarDebug()</code> — mirror of <code>__flyDebug</code> for cislunar missions (<code>__flyDebug</code> is gated by <code>!isMoonMission</code>). Returns the auto-zoom + camera + phase state needed to diagnose without spelunking Three.js refs.</li></ul><p>Both DEV-gated. Used during this session to verify Apollo 13/17, Luna 9, SLIM, Chandrayaan-3 under contention with a podcast_scraper agent on the same Chrome instance.</p><h3 id="_8-bonus-items-locked" tabindex="-1">8. Bonus items locked <a class="header-anchor" href="#_8-bonus-items-locked" aria-label="Permalink to &quot;8. Bonus items locked&quot;">​</a></h3><ul><li><strong>Outer-system lighting</strong>: Sun PointLight now <code>distance=0, decay=0</code> (uniform sunlight). Neptune at 30 AU lit. Threshold for over-bright: at side-angle 85° + the flyby flip, all outer planets keep terminator visibility.</li><li><strong>Vis-viva NaN guard</strong>: <code>heliocentricSpeed(r, a)</code> returns circular-orbit speed (<code>sqrt(µ/r)</code>) when <code>radicand ≤ 0</code> (r beyond transfer apohelion). At Neptune that&#39;s 5.43 km/s — coincidentally Neptune&#39;s mean orbital speed.</li><li><strong>URL→mission effect isolated via untrack()</strong>: matches <code>feedback_svelte5_effect_untrack</code> memory; source-of-truth check short-circuits same-URL re-fires.</li><li><strong>NH mission arc extended to Arrokoth</strong> (<code>arrival_date 2015-07-14 → 2019-01-01</code>, <code>transit_days 3463 → 4730</code>). Pluto stays an iconic flyby event mid-arc; Arrokoth becomes arrival.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p><strong>Positive:</strong></p><ul><li>Adding a new flyby destination is a grindable 15-step checklist with one canonical commit to copy from. Issue #341 (13 missing notable missions: Parker Solar Probe, OSIRIS-REx, DART, Lucy, Psyche, Europa Clipper, Mariner 9, Phoenix, Spirit, Opportunity, Magellan, Akatsuki, Hayabusa 1, Solar Orbiter) is well-scoped.</li><li>Every existing mission in segments A-B-C-D composes at the throne-of-glory bar. Verified iconic this session: V1, V2 (Jupiter/Saturn/Uranus/Neptune), Cassini SOI, Galileo JOI, NH (Pluto + Arrokoth), JUICE JOI, Mariner 4, Juno, MESSENGER, BepiColombo, Dawn (Vesta + Ceres), Giotto/Halley, Curiosity, Perseverance, Tianwen-1, MAVEN, InSight, Hope, Mangalyaan, Schiaparelli, Mars Express, Apollo 11/13/17, Luna 9, SLIM, Chandrayaan-3, Chang&#39;e 5, Vega 1, Venera 13, Ulysses.</li><li>Multi-Claude-session contention on chrome-devtools-mcp is no longer a verification blocker.</li><li>340/340 orbital + lambert + cislunar + hero tests pass.</li></ul><p><strong>Negative / known limitations:</strong></p><ul><li>Apollo 8/10/12/14/15/16, Change1, Luna 10, Smart-1, Lunar-prospector, Mariner 10, Apollo 7/9/1, Apollo-Soyuz, Starship-demo, Starship-mars-crew: missions ship without <code>flight.events</code> arrays — data gap, not architecture. Filing follow-up in the next data-authoring session.</li><li>Apollo 17&#39;s authored waypoint data only has the post-TEI hold; LOI / descent / ascent rely on the live-moon track to compose correctly. Other Apollo missions may have the same single-hold limitation — fix is to author richer waypoints.</li><li>Earth-orbit missions (<code>dest=EARTH</code>: Apollo 7/9, Vostok, Mercury, Polaris Dawn, OTV) ride the cislunar branch with a Moon-bound trajectory model. The renderer composes but the trajectory shape is wrong for LEO/MEO/GEO. Separate-scene work; not blocking the throne-of-glory bar for the existing segments.</li><li>Saturn-OI peakHold suppression of the epilogue wide tableau survives. The cinema composes correctly at SOI, but the wide bookend pull-out after peakHold&#39;s release window is its own follow-up.</li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li>Commit <code>e6e9175be</code> — Arrokoth as a first-class destination (canonical body-wiring example)</li><li>Commit <code>3c1e6938e</code> — Halley + 67P comet wiring (synonym pattern)</li><li>Commit <code>2428b1d41</code> — Pluto wiring (NH 2015 encounter)</li><li>Commit <code>034942a4e</code> — Asteroid wiring (Vesta + Ceres + Psyche + Bennu)</li><li>Commit <code>f7919f654</code> — Tier-1.5 hybrid waypoint hold detector</li><li>Commit <code>35902641b</code> — Multi-hold + live-moon track</li><li>Commit <code>b98ce9c08</code> — Sun-lit hemisphere bias for flyby cam</li><li>Commit <code>eb88418a2</code> — Vis-viva NaN guard</li><li>Commit <code>88a6037ac</code> — Outer-system lighting</li><li>ADR-058 — original cislunar scene + camera architecture</li><li>ADR-030 — <code>/fly</code> math + per-mission validation harness</li><li>GH issue #341 — 13 missing notable missions to add in a follow-up session</li></ul>',38)])])}const u=o(r,[["render",s]]);export{m as __pageData,u as default};
