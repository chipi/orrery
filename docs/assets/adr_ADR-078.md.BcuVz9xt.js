import{_ as t,o,c as r,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const p=JSON.parse('{"title":"ADR-078 — Mobile wrapper reshaped: iOS-first + stream-heavy bundle (reverses RFC-018 Android-first + §8.2)","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-078.md","filePath":"adr/ADR-078.md"}'),i={name:"adr/ADR-078.md"};function s(n,e,d,l,c,h){return o(),r("div",null,[...e[0]||(e[0]=[a('<h1 id="adr-078-—-mobile-wrapper-reshaped-ios-first-stream-heavy-bundle-reverses-rfc-018-android-first-§8-2" tabindex="-1">ADR-078 — Mobile wrapper reshaped: iOS-first + stream-heavy bundle (reverses RFC-018 Android-first + §8.2) <a class="header-anchor" href="#adr-078-—-mobile-wrapper-reshaped-ios-first-stream-heavy-bundle-reverses-rfc-018-android-first-§8-2" aria-label="Permalink to &quot;ADR-078 — Mobile wrapper reshaped: iOS-first + stream-heavy bundle (reverses RFC-018 Android-first + §8.2)&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-07-06 · Addendum 2026-07-07 (Capacitor 8 + iOS safe-area shim) Gates · v0.8 Capacitor mobile wrapper (epic #149); supersedes two locked decisions in PRD-015 / RFC-018 (2026-05-16)</p></blockquote><blockquote><p><strong>Addendum 2026-07-07.</strong> Two implementation decisions from the first on-simulator run:</p><ul><li><strong>Capacitor 7 → 8.</strong> RFC-018 §2&#39;s &quot;Capacitor 7&quot; was version-at-writing (May); upgraded to Capacitor 8.4.1 via <code>cap migrate</code> while the scaffold was still fresh (deployment target → iOS 15, Android needs JDK 21). No reason to pin 7.</li><li><strong>iOS safe-area via a native shim, not CSS <code>env()</code>.</strong> Capacitor&#39;s iOS WKWebView returns <code>env(safe-area-inset-*)</code> = 0 (measured), so the nav/footer render under the status bar / Dynamic Island. A <code>CAPBridgeViewController</code> subclass (<code>ios/App/App/AppDelegate.swift</code> → <code>SafeAreaViewController</code>, wired via <code>Main.storyboard</code>) reads the native <code>view.safeAreaInsets</code> and injects them as CSS vars (<code>--safe-area-inset-*</code>); CSS reads <code>var(--safe-area-inset-top, env(...))</code>. The <code>@capacitor-community/safe-area</code> plugin was added (it&#39;s mainly an Android edge-to-edge tool) but does not fix iOS <code>env()</code>. Locale flags dropped (flag emoji are unreliable cross-platform — iOS Simulator can&#39;t render them; the <code>EN/ES/FR</code> shortTag remains).</li></ul></blockquote><p><strong>Gating sentence</strong>: the v0.8 Capacitor mobile wrapper ships <strong>iOS-first</strong> (App Store before Google Play, reversing the RFC-018 Android-first lock) and slims the on-device bundle by <strong>streaming all gallery imagery and narration audio from <code>chipi.github.io</code></strong> (RFC-018 §8.1 network-aware service worker, selected over the deferred §8.2 disable-SW plan) because the real 2026-07 build is ~2 GB — not the 355 MB RFC-018 modelled — and non-executable JSON/image/audio streaming is App Store §2.5.2-clean; the trade is that PRD-015 M5 &quot;fully offline&quot; relaxes to &quot;core experience offline from install, galleries + audio offline after first view&quot;.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>RFC-018 and PRD-015 froze the mobile plan on <strong>2026-05-16</strong> as Draft v0.3. Seven weeks of <code>main</code> (image pipeline v2, /fly cinematic + tour narration, 14-locale expansion) invalidated the two load-bearing assumptions the plan rested on. A re-assessment on 2026-07-06 measured current <code>main</code>:</p><h3 id="_1-the-bundle-is-6×-larger-than-modelled-and-the-mix-changed" tabindex="-1">1. The bundle is ~6× larger than modelled, and the mix changed <a class="header-anchor" href="#_1-the-bundle-is-6×-larger-than-modelled-and-the-mix-changed" aria-label="Permalink to &quot;1. The bundle is ~6× larger than modelled, and the mix changed&quot;">​</a></h3><p>RFC-018 §4 modelled a <strong>355 MB</strong> naive build and proposed three slimming tactics to reach ~85 MB. Measured today (<code>static/</code> copies wholesale into <code>build/</code> via adapter-static, <code>svelte.config.js</code>):</p><table tabindex="0"><thead><tr><th>Bucket</th><th>RFC-018 (May)</th><th>Measured (2026-07-06)</th></tr></thead><tbody><tr><td><code>static/images/</code> (galleries, heroes, surfaces)</td><td>~120 MB</td><td><strong>1.6 GB</strong></td></tr><tr><td><code>static/audio/</code> (/fly narration, Curator Tour)</td><td><em>not modelled</em></td><td><strong>97 MB</strong></td></tr><tr><td><code>static/data/</code> (mission/site/fleet JSON, overlays)</td><td>~150 MB @ 12 locales</td><td><strong>67 MB</strong> @ 14 locales</td></tr><tr><td><code>static/textures/</code> (planet surfaces)</td><td>~30 MB, &quot;8K&quot;</td><td><strong>64 MB</strong>, max 4K (tiered 2k/4k LOD)</td></tr><tr><td><strong><code>static/</code> total</strong></td><td>~355 MB</td><td><strong>~1.9 GB</strong></td></tr></tbody></table><p>A naive <code>cap sync</code> therefore ships a <strong>~2 GB app</strong> — 10× the iOS 200 MB OTA install cap and a guaranteed poor-experience rejection long before the 4 GB binary limit.</p><h3 id="_2-two-of-rfc-018-s-three-slim-tactics-are-moot-or-already-done" tabindex="-1">2. Two of RFC-018&#39;s three slim tactics are moot or already done <a class="header-anchor" href="#_2-two-of-rfc-018-s-three-slim-tactics-are-moot-or-already-done" aria-label="Permalink to &quot;2. Two of RFC-018&#39;s three slim tactics are moot or already done&quot;">​</a></h3><ul><li><strong>§4.1 (swap 8K→4K textures, &quot;saves 22 MB&quot;)</strong> — there are <strong>no 8K textures</strong> on disk; max is 4K, and a 2k-default / 4k-on-approach LOD already shipped (<code>src/lib/three/fly-cislunar-scene.ts</code> <code>updateTextureLod()</code>). Tactic is dead.</li><li><strong>§4.3 (256-px fleet thumbnails, hero streaming)</strong> — never built. <code>pickHero()</code> (<code>src/lib/image-hero.ts:125</code>) resolves 100% local paths. This tactic is the <em>only</em> one still worth landing, and the growth of <code>static/images/</code> to 1.6 GB makes it central rather than incremental.</li><li><strong>§4.2 (lazy locales)</strong> — still valid; 14 locales now, not 12.</li></ul><h3 id="_3-marko-reverses-the-android-first-lock" tabindex="-1">3. Marko reverses the Android-first lock <a class="header-anchor" href="#_3-marko-reverses-the-android-first-lock" aria-label="Permalink to &quot;3. Marko reverses the Android-first lock&quot;">​</a></h3><p>PRD-015 §Goal and RFC-018 §9 locked <strong>Android-first</strong> (&quot;Google Play ships before App Store&quot;, 2026-05-16) on the reasoning that Android Internal Testing stands up faster and cheaper than TestFlight. Marko&#39;s 2026-07-06 direction is <strong>iOS-first, Android next</strong>. The trade-off is understood and accepted: iOS front-loads the two hardest gates —</p><ul><li>the <strong>200 MB OTA cap</strong> (stricter than Play&#39;s ~150 MB soft flag), forcing the bundle fix to be <em>complete</em> before the first TestFlight rather than deferrable, and</li><li><strong>WKWebView aggressive WebGL context-loss</strong> on background (RFC-018 §11.2, iOS-only correctness gate).</li></ul><p>Front-loading the hard 20% is the point: &quot;MVP&quot; becomes &quot;the hard problems solved first&quot;, with no false sense of progress from cheap plugin wins.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="d1-—-ios-first-sequencing" tabindex="-1">D1 — iOS-first sequencing <a class="header-anchor" href="#d1-—-ios-first-sequencing" aria-label="Permalink to &quot;D1 — iOS-first sequencing&quot;">​</a></h3><p>The v0.8 wrapper targets <strong>iOS / App Store first</strong>, Android / Google Play second. This reverses PRD-015 §Goal (&quot;Android and iOS in parallel&quot; / &quot;Android Internal Testing comes online faster… iteration loop runs Android-first&quot;) and RFC-018 §9–§10 (&quot;first to ship&quot;). The Capacitor core, <code>capacitor.config.ts</code>, plugin set, and web-side slimming are platform-neutral and unaffected; only the <strong>build/submit/test ordering</strong> flips. Child issues #200/#201 (iOS) move ahead of #198/#199 (Android) in the epic.</p><h3 id="d2-—-stream-heavy-bundle-select-rfc-018-§8-1-over-§8-2" tabindex="-1">D2 — Stream-heavy bundle (select RFC-018 §8.1 over §8.2) <a class="header-anchor" href="#d2-—-stream-heavy-bundle-select-rfc-018-§8-1-over-§8-2" aria-label="Permalink to &quot;D2 — Stream-heavy bundle (select RFC-018 §8.1 over §8.2)&quot;">​</a></h3><p>Adopt the <strong>network-aware service-worker</strong> strategy that RFC-018 §8 <em>recommended against</em> for v1.0. The on-device bundle carries only:</p><ul><li>app shell + Three.js + Svelte chunks (code),</li><li>planet textures (2k default, 4k LOD),</li><li>core mission/site/fleet <strong>JSON</strong> (the data model that drives the 3D scenes),</li><li>the default locale bundle (others lazy per §4.2).</li></ul><p><strong>Streamed from <code>chipi.github.io</code>, SW-cached on first view:</strong></p><ul><li>all <code>static/images/</code> galleries + non-critical heroes (the 1.6 GB),</li><li>all <code>static/audio/</code> narration + Curator Tour (the 97 MB).</li></ul><p>This is App Store <strong>§2.5.2-clean</strong>: nothing streamed is executable — images, audio, and JSON are inert content. The frozen-at-install code path (Three.js, Svelte) never refreshes at runtime. RFC-018 §8.2&#39;s original reason for deferring §8.1 (App Store review caution on the <em>first</em> ship) is outweighed now that §8.1 is the <em>only</em> path under the OTA cap.</p><h3 id="d3-—-m5-offline-requirement-relaxed" tabindex="-1">D3 — M5 offline requirement relaxed <a class="header-anchor" href="#d3-—-m5-offline-requirement-relaxed" aria-label="Permalink to &quot;D3 — M5 offline requirement relaxed&quot;">​</a></h3><p>PRD-015 M5 (&quot;App functions fully offline… all 14 routes with no network&quot;) is relaxed to:</p><blockquote><p><strong>Core experience offline from install</strong> (all 3D scenes, physics, navigation, science text, core data render with no network). <strong>Galleries + narration audio are offline after first view</strong> (SW-cached on tap while online).</p></blockquote><p>The airplane-mode success criterion (PRD-015 §Success) is amended to test the core experience, with a documented note that gallery imagery + audio require one online view to cache. This is the explicit cost of D2 and is accepted.</p><h3 id="d4-—-docs-cascade" tabindex="-1">D4 — Docs cascade <a class="header-anchor" href="#d4-—-docs-cascade" aria-label="Permalink to &quot;D4 — Docs cascade&quot;">​</a></h3><p>PRD-015 → <strong>v0.4</strong>, RFC-018 → <strong>v0.4</strong>, both citing this ADR. RFC-018 §4 is rewritten with measured numbers and the streaming architecture; §8 resolution flips to §8.1; §9–§11 re-sequence iOS-first. A tracking issue under epic #149 records the re-plan.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p><strong>Positive</strong></p><ul><li>The bundle target becomes achievable (est. ~150–250 MB on-device vs an impossible ~2 GB), unblocking <em>any</em> iOS submission.</li><li>iOS-first surfaces the WebGL context-loss and OTA-cap risks while they are cheapest to fix.</li><li>The streaming manifest (<code>build-info.json</code>, RFC-018 §8.1) becomes the future content-update channel for free — reopening the deferred RFC-018 O.Q. 5 as in-scope.</li></ul><p><strong>Negative / risks</strong></p><ul><li><strong>Offline regression</strong> vs the shipped PWA: first gallery/audio view now requires network. Mitigated by SW-caching after first view and by keeping the <em>core</em> experience fully offline. Must be called out in the store listing (&quot;some media loads on first view&quot;).</li><li><strong>App Store review still novel</strong>: §8.1 has not been reviewed by Apple for this app. If Apple objects to the runtime cache despite §2.5.2 compliance, fallback is a route-subset local bundle (the re-assessment&#39;s rejected &quot;local-slim&quot; option). Rollback path exists.</li><li><strong>Streaming infra dependency</strong>: the app now hard-depends on <code>chipi.github.io</code> availability for media. Acceptable — it is the same origin the browser build already serves from, on GitHub&#39;s SLA.</li></ul><p><strong>Rollback</strong>: D2 is a build-time asset-partition + SW-config change; reverting to a local bundle is a config flip (at the cost of exceeding the OTA cap). D1 is pure ordering. No data migration, no irreversible step.</p><h2 id="status-of-superseded-decisions" tabindex="-1">Status of superseded decisions <a class="header-anchor" href="#status-of-superseded-decisions" aria-label="Permalink to &quot;Status of superseded decisions&quot;">​</a></h2><ul><li>PRD-015 Resolved-decision framing &quot;Android Google Play ships first regardless&quot; (§iOS-signing, Resolved #4) — <strong>superseded</strong> by D1 for <em>sequencing</em>; the signing-ownership fact stands.</li><li>RFC-018 §15 Resolved #1 (&quot;8.2 disabled SW under Capacitor for v1.0&quot;) — <strong>superseded</strong> by D2 (now §8.1).</li><li>RFC-018 §15 Resolved #2 (~85 MB via §4 three-tactic plan) — <strong>superseded</strong> by D2; new target ~150–250 MB via streaming, since §4.1 is moot and the image bucket grew 13×.</li></ul><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li>Epic #149 · <a href="./../prd/PRD-015">PRD-015</a> · <a href="./../rfc/RFC-018">RFC-018</a></li><li>ADR-029 (PWA service worker), ADR-057 (locale cookie), ADR-058 (cislunar camera), ADR-046 (texture downsample pipeline)</li><li>Re-assessment session: 2026-07-06 (branch <code>mobile</code>)</li></ul><hr><p><em>Orrery · ADR-078 · Mobile wrapper reshaped — iOS-first + stream-heavy bundle · July 2026</em></p>',43)])])}const f=t(i,[["render",s]]);export{p as __pageData,f as default};
