import{_ as t,o,c as s,a2 as r}from"./chunks/framework.Cdp8-jB6.js";const h=JSON.parse('{"title":"ADR-080 — Responsive image size ladder + srcset delivery (mobile · web · Google TV)","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-080.md","filePath":"adr/ADR-080.md"}'),a={name:"adr/ADR-080.md"};function i(n,e,d,l,c,g){return o(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="adr-080-—-responsive-image-size-ladder-srcset-delivery-mobile-·-web-·-google-tv" tabindex="-1">ADR-080 — Responsive image size ladder + <code>srcset</code> delivery (mobile · web · Google TV) <a class="header-anchor" href="#adr-080-—-responsive-image-size-ladder-srcset-delivery-mobile-·-web-·-google-tv" aria-label="Permalink to &quot;ADR-080 — Responsive image size ladder + `srcset` delivery (mobile · web · Google TV)&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-07-08 Gates · #383 (tracking) · #379 (image weight) · builds on ADR-079 (assetUrl spine + source/derived + WebP) · mobile epic #149 · Google TV target Design · RFC-030</p></blockquote><p><strong>Gating sentence</strong>: display images ship <strong>WebP-only</strong> as a size ladder — an unsuffixed base <code>NN.webp</code> (the canonical, capped <strong>≤ 3072 px q80</strong>) plus smaller width-suffixed <code>NN-&lt;w&gt;.webp</code> rungs — that DOM <code>&lt;img&gt;</code> selects via <code>srcset</code>/<code>sizes</code> (viewport × DPR — phone, desktop, and 4K TV each fetch what they render) and 3D textures select by quality-tier; the full-resolution original is kept permanently in a <strong>GitHub-LFS <code>masters/</code> store</strong> (so any rung/format regenerates losslessly). <strong><code>hotspots/</code> (zoom tiers) and <code>posters/</code> (downloadable art) stay full-res JPEG</strong>; the Google-TV target is a <strong>Capacitor Android</strong> build that streams like mobile. (The auto-derive <code>validate-data</code> invariant is Slice 4, pending.)</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Orrery is becoming a <strong>three-screen product</strong> — Capacitor mobile, VPS web, and a <strong>Google TV</strong> app targeting 40–50″ (4K, 3840×2160) — a ~3× spread in display size. Meanwhile the served image bulk is ~1.9 GB, and the full-size tail is broken (gallery photos at 5000–6720 px / 7.5 MB shown in a <strong>fit-to-viewport, no-zoom</strong> lightbox).</p><p>Two measurements shape the decision (RFC-030 §Survey):</p><ol><li><strong>Weight is quality, not pixels</strong> — capping at native 4K (3840 px) + q85 saves −312 MB with only 40 files resized; the fat is bloated JPEG quality, so <strong>TV resolution is not in tension with size</strong>.</li><li><strong>A single cap can&#39;t serve three screens</strong> — cap high for TV and mobile over-fetches on cellular; cap low for mobile and TV goes soft.</li></ol><p>Images also <strong>churn continuously</strong> (better sources found, bad ones deleted, missions/fleet growing), so any fix must be <strong>self-maintaining in the pipeline</strong>, not a one-off.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Pre-generated WebP ladder (1280/2048/3072) + <code>srcset</code>, not an on-the-fly image server.</strong> The app is static-origin (VPS nginx + Capacitor streaming from a static CDN); <code>srcset</code> is browser-native, viewport-driven, and composes with the <code>assetUrl</code> spine + SW caching. The browser asks for the right rung per client — mobile (and the Capacitor Android-TV build) stream the size their viewport needs automatically. WebP encoding (ADR-079 D3) is <strong>folded into the ladder derivation</strong>, not a later slice. (imgproxy / Cloudflare Images reconsidered only if the ladder becomes unwieldy.)</p></li><li><p><strong>Served rungs are WebP capped at ≤ 3072 px q80</strong> (the largest is the unsuffixed base <code>NN.webp</code>); <strong>the full-res original is kept permanently and never mutated</strong> in a non-served <code>masters/</code> store (ADR-079 D2). The cap is on <em>derivatives</em> only, so every change — a sharper rung, WebP, AVIF, a re-crop — regenerates losslessly from masters. This RFC/ADR <strong>de-facto executes ADR-079&#39;s deferred Slice C</strong>, and <strong>git-LFS is the hard precondition</strong>: masters (~1–2 GB) live in LFS, out of main git, and nothing destructive lands until it exists.</p></li><li><p><strong>Client selects by viewport, never by build-flag.</strong> <code>srcset</code>/<code>sizes</code> on DOM <code>&lt;img&gt;</code>; the browser chooses by viewport × DPR. A build flag (<code>__MOBILE__</code>) is too coarse — it can&#39;t tell a phone-on-web from a 4K monitor, and TV-as-Capacitor-Android would collide with mobile&#39;s flag.</p></li><li><p><strong>3D / programmatic textures select by quality-tier</strong> (the existing <code>4k</code>/<code>2k</code> gate) — <code>srcset</code> can&#39;t reach <code>TextureLoader</code>.</p></li><li><p><strong>Exclude <code>hotspots/</code> and <code>posters/</code></strong> — the zoomable 3D surface tiers keep full resolution + their tier system; downloadable art posters stay full-res JPEG (universally openable + print-friendly; the <code>/posters</code> page links them for download). Both are excluded from the ladder, the cap, and the provenance re-key.</p></li><li><p><strong>Auto-derive on ingest + a <code>validate-data</code> invariant</strong> (the churn guarantee) — <strong>the design intent; Slice 4, not yet shipped.</strong> The ladder is derived by the pipeline (<code>build-display-ladder.mjs</code>, cache-keyed) from masters; the target end-state adds a <code>validate-data</code> invariant that fails preflight if any shipped image exceeds the cap or is missing a fresh ladder rung (fixable with an <code>images:derive</code> script), so a swapped/manually-dropped image can&#39;t ship un-derived. Until Slice 4 lands, re-derivation is a manual <code>node scripts/vision/build-display-ladder.mjs</code> step.</p></li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>On-the-fly image server</strong> (imgproxy / thumbor / Cloudflare Images, <code>?w=</code>) — rejected for v1: adds a service + CPU + cache layer and doesn&#39;t fit Capacitor&#39;s stream-from-static-origin model. Not foreclosed.</li><li><strong>Per-platform build flag</strong> (mobile→small, web/TV→large) — rejected: too coarse; ignores the phone-on-web and 4K-monitor gradations that viewport handles for free.</li><li><strong>Single global cap</strong> (one size for all) — rejected: any single value compromises at least one of the three screens (RFC-030 §Survey measurement 2).</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>+</strong> Each client fetches an appropriately-sized image; mobile cellular payload drops sharply; TV renders sharp.</li><li><strong>+</strong> <code>build/images</code> ~1.9 GB → <strong>~897 MB</strong> (shipped, WebP-only, posters excluded); deploy ~2.2 GB → ~1.15 GB.</li><li><strong>+</strong> Lossless / reversible — originals live in <code>masters/</code>.</li><li><strong>−</strong> The ladder adds ~3 derived files per master (~9 k files). VPS-only (GH Pages retired) makes file-count far less critical; ladder kept short to bound it.</li><li><strong>−</strong> A <code>sizes</code>-per-component contract to maintain (RFC-030 §contract), guarded by a lint/test.</li><li><strong>Dependency</strong>: gated on the <strong>GitHub-LFS <code>masters/</code> store</strong> (ADR-079 D2 / Slice C) built first — the permanent-masters invariant (decision 2) can&#39;t hold otherwise. Cost: masters ~1 GB sit inside GitHub LFS&#39;s 10 GiB free tier → <strong>~$0/mo</strong>, <em>conditional</em> on CI/agents skipping the master smudge (<code>lfs.fetchexclude</code>) so bandwidth stays within free (RFC-030 §cost).</li></ul><h2 id="rollback" tabindex="-1">Rollback <a class="header-anchor" href="#rollback" aria-label="Permalink to &quot;Rollback&quot;">​</a></h2><p>Every step is reversible from <code>masters/</code>: the cap/ladder are regenerable derivatives; reverting <code>srcset</code> is a component-markup change; the <code>validate-data</code> invariant is additive. No irreversible step once masters exist.</p><hr><p><em>Orrery · ADR-080 · Responsive image ladder + srcset delivery · July 2026 — design in RFC-030, builds on ADR-079.</em></p>',18)])])}const m=t(a,[["render",i]]);export{h as __pageData,m as default};
