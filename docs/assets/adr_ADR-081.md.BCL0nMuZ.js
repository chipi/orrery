import{_ as t,o,c as a,a2 as n}from"./chunks/framework.Cdp8-jB6.js";const m=JSON.parse('{"title":"ADR-081 — Self-hosted Umami analytics (env-var-gated, no committed IDs)","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-081.md","filePath":"adr/ADR-081.md"}'),s={name:"adr/ADR-081.md"};function r(i,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="adr-081-—-self-hosted-umami-analytics-env-var-gated-no-committed-ids" tabindex="-1">ADR-081 — Self-hosted Umami analytics (env-var-gated, no committed IDs) <a class="header-anchor" href="#adr-081-—-self-hosted-umami-analytics-env-var-gated-no-committed-ids" aria-label="Permalink to &quot;ADR-081 — Self-hosted Umami analytics (env-var-gated, no committed IDs)&quot;">​</a></h1><blockquote><p>Status · Accepted Date · 2026-07-22 Related ADRs · ADR-067 (Sentry self-hosted error tracking — the sibling pattern), ADR-016 (no client storage), ADR-057 / ADR-075 (functional-cookie exceptions) Reuses · the GlitchTip telemetry-vhost shape (browser → Cloudflare → origin-locked, tracking-only edge → homelab), applied to Umami</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Orrery has always run <strong>Umami</strong> for privacy-respecting, cookieless UX analytics (<code>src/lib/analytics.ts</code> — a typed event registry + custom-event helpers wired across ~15 components). Until now it pointed at the <strong><code>cloud.umami.is</code> SaaS</strong> and was gated by a <strong>hostname allowlist</strong> pinned to the old GitHub Pages mirror (<code>chipi.github.io/orrery</code>).</p><p>Two things forced a change when the site moved to <code>orrerylearn.com</code> (VPS + CF):</p><ol><li><strong>The hostname gate meant analytics never fired on the real production domain.</strong> It only counted the GitHub Pages mirror; <code>orrerylearn.com</code> traffic was invisible.</li><li><strong>The estate went self-hosted.</strong> Logs moved to VictoriaLogs and errors to self-hosted GlitchTip (ADR-067). Analytics was the last SaaS dependency in the observability stack; keeping it on <code>cloud.umami.is</code> split the estate and left third-party data egress in place.</li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Move to self-hosted Umami and switch enablement from hostname-gating to env-var-gating, mirroring the Sentry pattern in ADR-067.</strong></p><ul><li><strong>Edge (owned by the infra repo, not Orrery):</strong> <code>analytics.orrerylearn.com</code> — browser → Cloudflare (valid cert, origin-locked to CF IPs on :443) → a tracking-only edge exposing just <code>/script.js</code> + <code>/api/send</code> → homelab Umami. Identical secure shape to the GlitchTip <code>telemetry.orrerylearn.com</code> vhost. The admin dashboard (<code>/login</code>, <code>/api/auth</code>, …) is <strong>not</strong> publicly exposed.</li><li><strong>App wiring (this repo):</strong> <code>src/lib/analytics.ts</code> loads the tracker and fires events only when <strong>both</strong> <code>PUBLIC_UMAMI_HOST</code> and <code>PUBLIC_UMAMI_WEBSITE_ID</code> are baked into the build (and not in <code>vite dev</code>). The deploy wiring mirrors Sentry (ADR-067) exactly: the fixed edge host is <strong>hardcoded in the workflow</strong> (like <code>PUBLIC_SENTRY_ENVIRONMENT: prod</code>), and the single operator-set value — the website id — lives in <strong>one secret</strong> <code>PUBLIC_UMAMI_WEBSITE_ID</code>, like <code>PUBLIC_SENTRY_DSN</code>. The id is public (it ships in the bundle); the secret only keeps it out of the repo. So the operator sets exactly one value, same as Sentry.</li><li><strong>nginx CSP</strong> (<code>ops/docker/nginx.conf</code>): <code>script-src</code> + <code>connect-src</code> allow <code>https://analytics.orrerylearn.com</code> (replacing <code>cloud.umami.is</code>).</li></ul><h2 id="why-env-var-gated-not-hostname-gated" tabindex="-1">Why env-var-gated (not hostname-gated) <a class="header-anchor" href="#why-env-var-gated-not-hostname-gated" aria-label="Permalink to &quot;Why env-var-gated (not hostname-gated)&quot;">​</a></h2><p>ADR-067 §&quot;Env-var-gated, not hostname-gated&quot; already argued this for Sentry and called out that Umami&#39;s hostname allowlist was the weaker pattern. This ADR finishes that alignment:</p><ul><li><strong>Fires on the actual production domain</strong> without an allowlist to keep in sync.</li><li><strong>Fork-friendly by construction:</strong> a fork populates its <em>own</em> <code>PUBLIC_UMAMI_*</code> in its CI and gets its own dashboard without editing source.</li><li><strong>Silent by default everywhere else:</strong> empty vars → no script injected, every <code>track()</code> a no-op. Local dev / vite preview / CI / screenshots stay silent; <code>dev</code> is additionally hard-blocked.</li><li><strong>No IDs committed:</strong> the website ID lives in a secret, not in the tree.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>One value must be set before analytics goes live: register the Orrery website in the self-hosted Umami admin (over the tailnet) → set the <code>PUBLIC_UMAMI_WEBSITE_ID</code> <strong>secret</strong> (the host is already hardcoded in the deploy) → next deploy bakes it in. Until then the build ships analytics-silent (safe).</li><li>Historical <code>cloud.umami.is</code> data is <strong>not</strong> migrated — it was thin (the gating bug meant near-zero <code>orrerylearn.com</code> data anyway); the self-hosted instance starts fresh.</li><li>The privacy copy was updated to match the self-hosted reality: README §Privacy (Umami + Sentry paragraphs → self-hosted <code>analytics.</code> / <code>telemetry.</code> edges, env-gating, <code>orrerylearn.com</code>) and the <code>/credits</code> <code>credits_storage_intro</code> string (reworded across all 14 locales — &quot;no analytics&quot; → &quot;anonymous, cookieless usage analytics and crash reports, self-hosted and never used to identify you&quot;). The honest posture is now <em>stronger</em> than before (fully self-hosted, no third-party SaaS), not weaker.</li></ul><h2 id="not-covered-out-of-scope" tabindex="-1">Not covered / out of scope <a class="header-anchor" href="#not-covered-out-of-scope" aria-label="Permalink to &quot;Not covered / out of scope&quot;">​</a></h2><ul><li>The public-vhost hardening that hides the Umami dashboard (<code>/login</code>) is the infra repo&#39;s responsibility, not this ADR.</li><li>No change to the event registry, the privacy posture (cookieless, PII-free, length-capped search text), or the typed-helper API — only the transport target and the enablement gate moved.</li></ul>',16)])])}const p=t(s,[["render",r]]);export{m as __pageData,p as default};
