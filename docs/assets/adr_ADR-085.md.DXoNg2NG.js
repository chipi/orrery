import{_ as o,o as t,c as n,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"ADR-085 — Geocentric Earth→Moon porkchop on /plan","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-085.md","filePath":"adr/ADR-085.md"}'),r={name:"adr/ADR-085.md"};function i(s,e,c,d,l,h){return t(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="adr-085-—-geocentric-earth→moon-porkchop-on-plan" tabindex="-1">ADR-085 — Geocentric Earth→Moon porkchop on <code>/plan</code> <a class="header-anchor" href="#adr-085-—-geocentric-earth→moon-porkchop-on-plan" aria-label="Permalink to &quot;ADR-085 — Geocentric Earth→Moon porkchop on `/plan`&quot;">​</a></h1><blockquote><p>Status · Accepted (implemented + Fable-5-reviewed 2026-08-28 — see §Implementation &amp; review notes for deviations from the proposal) Date · 2026-08-28 Gates · #308 (geocentric Lambert porkchop — Moon) Builds on · ADR-026 / ADR-028 (multi-destination heliocentric porkchop), ADR-022 / RFC-003 (Lambert worker protocol), ADR-006/017 (data client + locale overlay)</p></blockquote><p><strong>Gating sentence</strong>: <code>/plan</code> gains the <strong>Moon</strong> as a destination via a <strong>second, geocentric Lambert path</strong> that reuses the solver <code>solveLambert(r1, r2, tof, mu)</code> with <strong>μ_Earth</strong> (in km/s), plus one small solver change — an <strong><code>aMax</code> parameter</strong> (default AU-scale, so the 9 heliocentric grids stay byte-identical) — and the app&#39;s existing <strong><code>astronomy/moon.ts#geocentricMoon</code></strong> ephemeris (AU→km); destinations become a <strong>discriminated union on <code>kind: &#39;helio&#39; | &#39;geo&#39;</code></strong> so the grid + precompute pick the right central body; each cell is shaded by <strong>total TLI + LOI ∆v</strong>, where <strong>TLI</strong> is a geocentric departure burn from a circular LEO and <strong>LOI</strong> is a <strong>patched-conic</strong> insertion at the Moon (via v∞ and <strong>μ_Moon</strong>) into a circular low-lunar orbit — the two impulses broken out in the readout; the TOF axis is <strong>[2.5, 5.5 d]</strong> (the short-way solver&#39;s feasible band), amending #308&#39;s original [3,14 d]; the existing 9 heliocentric destinations are <strong>untouched</strong>. Planetocentric moons of the giants (Europa/Titan/…) are <strong>explicitly deferred</strong> to a follow-up (same tag, three-body sequencing).</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>/plan</code>&#39;s porkchop is heliocentric-only: every destination plugs into <code>lambert-grid.constants.ts#DESTINATIONS</code> with a heliocentric semi-major axis, and <code>computeDv</code> (in <code>lambert-grid.ts</code>) solves a Sun-centered Lambert (<code>MU_SUN</code>, AU/yr units) between Earth&#39;s circular-orbit position and the destination&#39;s. The Moon has no heliocentric orbit — it orbits Earth — so &quot;plan a trip to the Moon,&quot; one of the most natural <code>/plan</code> requests, silently isn&#39;t offered. <code>static/data/porkchop/earth-to-moon.json</code> does not exist.</p><p>Two facts make a second path tractable (verified in code):</p><ol><li><strong>The Lambert solver is μ-parameterized</strong> — <code>solveLambert(r1, r2, tof, mu)</code> takes <code>mu</code> as a parameter (<code>src/lib/lambert.ts:51</code>). A geocentric sibling <code>computeDvGeo</code> reuses it. <strong>Caveat (found in Fable-5 ADR review):</strong> the solver is μ-agnostic but <strong>not scale-agnostic</strong> — its bisection ceiling <code>aHi = 200.0</code> (lambert.ts:67) is an AU-scale bound; an Earth–Moon transfer needs <code>a ≈ 1.8–3.5 × 10⁵ km</code>, so without a larger ceiling <em>every</em> geocentric cell returns null. Hence the <code>aMax</code> parameter (D5). All other epsilons are relatively smaller in km and stay valid.</li><li><strong>The Moon ephemeris already exists</strong> — <code>astronomy/moon.ts#geocentricMoon(jd): MoonPos</code> (Schlyter/Brown analytic, ~1–2 arcmin, J2000 ecliptic, AU), the same model <code>/explore</code> uses. No new ephemeris data.</li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="d1-—-tagged-destination-kind-one-code-path-not-a-parallel-module" tabindex="-1">D1 — Tagged destination <code>kind</code>, one code path (not a parallel module) <a class="header-anchor" href="#d1-—-tagged-destination-kind-one-code-path-not-a-parallel-module" aria-label="Permalink to &quot;D1 — Tagged destination `kind`, one code path (not a parallel module)&quot;">​</a></h3><p>Make <code>DestinationConstants</code> a <strong>discriminated union on <code>kind</code></strong> (per the Fable-5 review — avoids a Moon entry carrying meaningless heliocentric <code>a</code>/<code>a0</code>/<code>meanMotionRadPerDay</code> fields): <code>{ kind: &#39;helio&#39;; a; a0; meanMotionRadPerDay; e? } | { kind: &#39;geo&#39; }</code> — the 9 existing entries become explicit <code>kind: &#39;helio&#39;</code> (their shape unchanged); the geo constants (μ_Earth, μ_Moon, R_LEO, R_LLO, epoch, the <code>geocentricMoon</code> adapter) live in a new <code>lambert-geocentric-grid.constants.ts</code>. The grid driver branches on <code>kind</code>: <code>&#39;helio&#39;</code> → today&#39;s <code>computeDv</code> (μ_Sun, AU/yr); <code>&#39;geo&#39;</code> → new <code>computeDvGeo</code> (μ_Earth, km/s). Rejected: a fully parallel duplicated module (drifts; the deferred planetocentric-moons work would then need a <em>third</em> copy — the tag generalizes, a fork does not).</p><h3 id="d2-—-the-geocentric-transfer-model-2d-ecliptic-patched-conic" tabindex="-1">D2 — The geocentric transfer model (2D-ecliptic patched conic) <a class="header-anchor" href="#d2-—-the-geocentric-transfer-model-2d-ecliptic-patched-conic" aria-label="Permalink to &quot;D2 — The geocentric transfer model (2D-ecliptic patched conic)&quot;">​</a></h3><p>Consistent with the heliocentric porkchop, which is itself a 2D ecliptic-plane circular-orbit approximation. <code>solveLambert</code> returns scalar transfer <strong>speeds</strong> <code>v1, v2</code> (km/s) at the two endpoints (lambert.ts:18-25). For each <code>(departure_day, tof)</code> cell:</p><ul><li><strong>r2</strong> = <code>geocentricMoon(jd_arrival)</code> position (AU) <strong>× AU_TO_KM = 1.495978707×10⁸</strong>, projected to the 2D ecliptic plane (x, y in km). <code>jd_arrival</code> maps from <code>depDay + tof</code> against the <strong>same epoch the heliocentric grid&#39;s L0 uses</strong> (D5).</li><li><strong>r1</strong> = a point at <strong>LEO radius</strong> <code>R_LEO = 6578 km</code> (200 km circular parking orbit), fixed <strong>antipodal to the arrival direction</strong> (<code>−r̂2 · R_LEO</code>) — the perigee-opposite-apogee geometry of a trans-lunar coast. The Fable-5 review confirmed the ∆v is second-order-insensitive to r1&#39;s direction (<code>R_LEO ≪ |r2|</code>; a 90° swing moves v1 &lt; 1%), and antipodal makes r1 the <em>exact</em> perigee at the min-∆v cell, where the scalar-difference ∆v below is exact.</li><li>Solve <code>solveLambert(r1, r2, tof_seconds, MU_EARTH, aMax_geo)</code> with <strong>TOF in seconds</strong> (<code>tofDay × 86400</code>), <code>MU_EARTH = 398600.4418 km³/s²</code>, <code>aMax_geo = 2×10⁶ km</code> (D5).</li><li><strong>TLI</strong> = <code>v1 − v_LEO_circ</code>, both <strong>geocentric</strong> (<code>v_LEO_circ = √(μ_Earth/R_LEO) = 7.784 km/s</code>). Departure burn from the circular parking orbit.</li><li><strong>LOI (patched-conic, corrected per Fable-5 B1).</strong> <code>v1</code>/<code>v2</code> are <em>geocentric</em> speeds; a low-lunar orbit is <em>selenocentric</em>, so LOI must cross the lunar SOI, not subtract mismatched frames: <ul><li><code>v∞ = | v_Moon − v2 |</code> — the hyperbolic-excess speed relative to the Moon, where <code>v_Moon</code> is the Moon&#39;s geocentric orbital speed at arrival (finite-difference <code>geocentricMoon</code> at <code>jd ± Δ</code>). ≈ 0.83–0.88 km/s.</li><li><code>LOI = √(v∞² + 2·μ_Moon/R_LLO) − √(μ_Moon/R_LLO)</code>, with <strong><code>μ_Moon = 4902.8 km³/s²</code></strong> and <code>R_LLO = 1837 km</code> (≈ 100 km altitude). ≈ 0.82–0.84 km/s.</li></ul></li><li><strong>Cell ∆v = TLI + LOI</strong> (shaded with the geo path&#39;s own min/max range ~3.9–4.3 km/s — <em>not</em> the heliocentric <code>max(3.2, …)</code> clamp; D5); the readout shows the TLI/LOI split.</li></ul><p><strong>TOF axis = [2.5, 5.5 d]</strong> (amends #308&#39;s [3,14 d]). With the antipodal-r1 (≈180°) geometry the <strong>short-way</strong> solver&#39;s feasible band is the parabolic floor (~2.1 d) to the Hohmann half-period ceiling (~5.0 d at mean distance); cells above ~5.5 d have no short-way solution and would render as false &quot;unreachable.&quot; Covering slow/phasing-loop transfers would need the solver&#39;s high branch (α→2π−α) — deferred as solver surgery with little pedagogical payoff. Departure axis <code>[0, 365 d]</code> at ≥150 columns to resolve the ~27.55-d perigee-distance stripe the <code>geocentricMoon</code> eccentricity produces (a real, teachable effect).</p><p><strong>Honesty bar (validation).</strong> The model is a teaching approximation, not a trajectory tool — it ignores the Moon&#39;s 5° inclination (real missions launch <em>into</em> the transfer plane, so there&#39;s no plane-change burn to model — the 2D projection is honest, ≤0.4% on |r2|) and lunar-SOI fine structure. It is <em>honest</em> iff it lands on the real numbers: <strong>TLI ≈ 3.05–3.15 km/s, LOI ≈ 0.8–0.9 km/s, total ≈ 3.9–4.05</strong> (Apollo LEO→LLO). Unit tests assert the minimum-∆v cell falls in those bands (Fable-5 back-of-envelope: TLI ≈ 3.13, LOI ≈ 0.82–0.84 ✓); if it doesn&#39;t, the model — not the test — is wrong (PA §&quot;fail honestly&quot;, §&quot;real physics not approximations that mislead&quot;). The <code>/plan</code> caption states it&#39;s a representative two-body estimate; phasing/launch-window geometry is not modeled.</p><h3 id="d5-—-solver-units-changes-minimal-back-compat" tabindex="-1">D5 — Solver + units changes (minimal, back-compat) <a class="header-anchor" href="#d5-—-solver-units-changes-minimal-back-compat" aria-label="Permalink to &quot;D5 — Solver + units changes (minimal, back-compat)&quot;">​</a></h3><ul><li><strong><code>solveLambert</code> gains an optional <code>aMax</code> parameter</strong>, default <code>200.0</code> (AU-scale) — the 9 heliocentric grids call it unchanged and stay byte-identical; the geo path passes <code>2×10⁶</code> (km). This is the <em>only</em> change to the shared solver.</li><li><strong>Units are isolated inside each <code>computeDv*</code></strong>: heliocentric stays AU + years + μ(AU³/yr²); geocentric uses km + <strong>seconds</strong> + μ(km³/s²). Both emit km/s into the grid. The days→seconds conversion (<code>×86400</code>) is explicit at the geo call site — the one seam a silent days-vs-seconds bug would hide.</li><li><strong>Epoch</strong>: <code>depDay</code>/<code>arrDay</code> map to JD against the same epoch anchoring the heliocentric <code>L0</code>; pinned in <code>lambert-geocentric-grid.constants.ts</code> so the two grids share a clock.</li><li><strong><code>geocentricMoon</code> adapter</strong> converts its AU output to km (<code>× AU_TO_KM</code>) in the geo constants module.</li></ul><h3 id="d3-—-ephemeris-source-reuse-geocentricmoon-not-a-new-jpl-sample" tabindex="-1">D3 — Ephemeris source: reuse <code>geocentricMoon</code> (not a new JPL sample) <a class="header-anchor" href="#d3-—-ephemeris-source-reuse-geocentricmoon-not-a-new-jpl-sample" aria-label="Permalink to &quot;D3 — Ephemeris source: reuse `geocentricMoon` (not a new JPL sample)&quot;">​</a></h3><p>The precompute samples the app&#39;s existing <code>astronomy/moon.ts#geocentricMoon</code>. Rejected: sampling JPL DE-440 into a new data file — higher fidelity than a 2D circular-orbit porkchop can use, and it introduces a second lunar-ephemeris source to keep in sync with <code>/explore</code>.</p><h3 id="d4-—-precompute-delivery-unchanged-pipeline-shape" tabindex="-1">D4 — Precompute + delivery, unchanged pipeline shape <a class="header-anchor" href="#d4-—-precompute-delivery-unchanged-pipeline-shape" aria-label="Permalink to &quot;D4 — Precompute + delivery, unchanged pipeline shape&quot;">​</a></h3><p><code>scripts/precompute-porkchops.ts</code> gains an <code>earth-to-moon</code> spec (its own axis ranges) and emits <code>static/data/porkchop/earth-to-moon.json</code> — the same per-destination JSON contract <code>/plan</code> already loads. No new runtime path; the grid is precomputed at build like the other 9.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p><strong>Positive</strong></p><ul><li>The single most-requested <code>/plan</code> destination is offered, honestly modeled and validated against Apollo.</li><li>The <code>kind</code> tag is the seam the deferred planetocentric-moons work (Europa/Titan) extends — no rework.</li><li>Zero change to the 9 heliocentric destinations; the solver, worker protocol, and JSON contract are reused.</li></ul><p><strong>Negative / accepted</strong></p><ul><li>A second ∆v semantics (TLI+LOI vs heliocentric total) means <code>/plan</code> must label which model a destination uses, and the geo grid needs its own colour range (~3.9–4.3) — small UI/caption complexity.</li><li><strong>The shared <code>solveLambert</code> gains one parameter (<code>aMax</code>)</strong> — a minimal, default-preserving touch to a load-bearing function; the 9 heliocentric grids stay byte-identical (asserted by their existing tests).</li><li><strong>TOF axis narrowed to [2.5, 5.5 d]</strong> (from #308&#39;s [3,14 d]) — the short-way solver&#39;s honest feasible band; slow/phasing transfers are out of scope until the solver&#39;s high branch lands. Better a small honest window than a mostly-red false-&quot;unreachable&quot; one.</li><li>The 2D-ecliptic circular approximation understates lunar-SOI fine structure; mitigated by the honest caption + the validation bands (we ship the number only if it matches Apollo reality).</li></ul><p><strong>Neutral</strong></p><ul><li>Units differ per path (AU/yr heliocentric, km/s geocentric); isolated inside each <code>computeDv*</code>, both output km/s for the grid.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Parallel duplicated module</strong> (D1 alt) — rejected: drift + a third copy for moons.</li><li><strong>Single-impulse (LEO-departure ∆v only), no LOI</strong> — rejected: dishonest for the Apollo/Artemis context the issue names; LOI is half the story.</li><li><strong>Full 3D inclined patched-conic through the lunar SOI</strong> — rejected for v1: correct but far beyond a teaching porkchop&#39;s fidelity budget and the 2D heliocentric model it sits beside; revisit only if <code>/fly</code> needs it.</li><li><strong>JPL DE-440 sampled ephemeris</strong> (D3 alt) — rejected: over-fidelity + a second lunar source.</li></ul><h2 id="implementation-review-notes-2026-08-28" tabindex="-1">Implementation &amp; review notes (2026-08-28) <a class="header-anchor" href="#implementation-review-notes-2026-08-28" aria-label="Permalink to &quot;Implementation &amp; review notes (2026-08-28)&quot;">​</a></h2><p>Built + browser-verified; a post-implementation Fable-5 review caught one physics blocker and two integration bugs (all fixed). Deviations from the proposal above, recorded per the anti-drift rule:</p><ul><li><strong>D1 — no discriminated union.</strong> <code>/plan</code> loads porkchops as static JSON (<code>getPorkchopGrid</code> → <code>earth-to-{id}.json</code>); the live Lambert worker is unused by any route. So the Moon needs no shared-worker <code>kind</code> dispatch — it is a <strong><code>PlanDestinationId = DestinationId | &#39;moon&#39;</code></strong> string union (the Moon is deliberately absent from the heliocentric <code>DESTINATIONS</code>, matching the issue&#39;s premise), and the grid is built entirely in <code>precompute-porkchops.ts</code> via <code>geoTransferDv</code>. Simpler than the proposed union; the heliocentric path is untouched.</li><li><strong>D2 — LOI uses a VECTOR v∞, not scalar (review fix A1).</strong> The proposal&#39;s scalar <code>|v2 − vMoon|</code> (mirroring the heliocentric arrival ∆v) is wrong here: it <strong>inverts the TOF gradient</strong> (fast arrivals carry a large <em>radial</em> v∞ the scalar ignores, so it made fast transfers look cheapest — teaching &quot;faster is cheaper&quot;, which is false). The 180° antipodal geometry gives an exact semi-latus-rectum <code>p = 2·r1·r2/(r1+r2)</code>; decompose the arrival velocity into tangential <code>vt2 = √(µ_Earth·p)/r2</code> + radial <code>vr2 = √(v2² − vt2²)</code>, then <code>v∞ = hypot(vr2, vt2 − vMoon)</code>. Result: fast = expensive, near-Hohmann = cheap; <strong>min-total ≈ 3.94 km/s</strong> (Apollo band). TLI stays scalar (the transfer is ≥99.9% tangential at LEO). The honesty test now anchors on the <strong>min-total</strong> cell (the one the UI shades cheapest) + a gradient regression guard.</li><li><strong>Colour range</strong> is <strong>[3.84, 4.38]</strong> km/s (was estimated ~3.9–4.3), recomputed from the corrected grid.</li><li><strong>/plan↔/fly consistency (review fixes A2/A3).</strong> Moon <code>dep</code>/<code>tof</code> are URL-encoded <strong>fractional</strong> (grid rows are ~0.03 d apart; integer rounding snapped to the wrong transfer). The <code>/fly</code> handoff (<code>applyPlanSelectionMoon</code>) sets <strong><code>simSpeed = 0.4</code></strong> (a 2.5–5.5-d cislunar flight would otherwise play in &lt;1 s at the 7 d/s default) and builds the cislunar trajectory with <strong><code>lunar_arrival: &#39;orbit&#39;</code></strong> so a one-way LANDING arrives <em>at</em> the Moon rather than a mid-mission flyby.</li><li><strong>Caption:</strong> the readout shows <code>from LEO</code> (14 locales); the fuller &quot;representative two-body estimate&quot; provenance lives in the grid&#39;s <code>credit</code> field.</li></ul>',33)])])}const m=o(r,[["render",i]]);export{g as __pageData,m as default};
