import{_ as o,o as t,c as n,a2 as r}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"ADR-086 — Earth→(planetary moon) multi-leg porkchops on /plan","description":"","frontmatter":{},"headers":[],"relativePath":"adr/ADR-086.md","filePath":"adr/ADR-086.md"}'),a={name:"adr/ADR-086.md"};function s(i,e,l,c,d,h){return t(),n("div",null,[...e[0]||(e[0]=[r('<h1 id="adr-086-—-earth→-planetary-moon-multi-leg-porkchops-on-plan" tabindex="-1">ADR-086 — Earth→(planetary moon) multi-leg porkchops on <code>/plan</code> <a class="header-anchor" href="#adr-086-—-earth→-planetary-moon-multi-leg-porkchops-on-plan" aria-label="Permalink to &quot;ADR-086 — Earth→(planetary moon) multi-leg porkchops on `/plan`&quot;">​</a></h1><blockquote><p>Status · Accepted · 2026-08-28 · Closes #308&#39;s deferred &quot;planetocentric moons of the giants&quot; scope · builds on ADR-085 (geocentric Earth→Moon) · ADR-026/028 (the heliocentric porkchop set)</p></blockquote><p><strong>Gating sentence.</strong> <code>/plan</code> gains <strong>nine planetary moons</strong> as destinations — Jupiter&#39;s Galileans (Io, Europa, Ganymede, Callisto), Saturn&#39;s Titan + Enceladus, Neptune&#39;s Triton, and Mars&#39; Phobos + Deimos — each shaded by the <strong>total ∆v of a multi-leg patched-conic mission</strong>: the heliocentric Earth→host-planet transfer&#39;s departure burn <strong>plus</strong> a moon-orbit insertion, where the host arrival v∞ is carried down the host&#39;s gravity well to the moon and captured (µ_host then µ_moon). The nine grids ride their <strong>host planet&#39;s</strong> departure/TOF axes; the heliocentric leg reuses <code>solveLambert</code> (µ_Sun) unchanged, so every existing planet/dwarf grid stays byte-identical.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR-085 modelled the Moon as a single geocentric Lambert arc (Earth→Moon, µ_Earth) because the Moon orbits Earth and you launch from Earth. A moon of a giant planet is different: you reach it via its <strong>host planet</strong>, so the honest model is a chain, not one arc. #308 named &quot;Moon + moons&quot;; the moons were deferred in ADR-085 as &quot;three-body sequencing.&quot; This closes that.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><strong>Two-leg patched-conic</strong> (<code>src/lib/moon-transfer.ts</code>, <code>interplanetaryMoonDv</code>):</p><ol><li><strong>Heliocentric Earth→host</strong> — the existing porkchop leg (<code>solveLambert</code>, µ_Sun). Departure ∆v = <code>|v1 − v_Earth|</code>. The spacecraft is <strong>not</strong> captured at the host; its hyperbolic arrival v∞ = <code>|v_host − v2|</code> is carried into leg 2 (no separate host-capture burn — that would double-count the energy that reaches the moon).</li><li><strong>Moon-orbit insertion</strong> — fall down the host well from v∞ to the moon&#39;s orbital radius (<code>v_at_moon = √(v∞² + 2µ_host/r_moon)</code>), take the excess relative to the moon (<code>v∞_moon = |v_at_moon − v_moon_circ|</code>, tangential approximation), capture into a low circular moon orbit (<code>MOI = √(v∞_moon² + 2µ_moon/r_lo) − √(µ_moon/r_lo)</code>).</li></ol><p><strong>Cell ∆v = departure + MOI.</strong> The <code>/plan</code> readout breaks it out as <code>DEP x + MOI y</code>, mirroring the Moon&#39;s <code>TLI + LOI</code>.</p><p><strong>Data.</strong> Moon orbital radius is <strong>derived from its sidereal period</strong> (the value <code>explore-scene.ts</code> already ships for the 3D renderer) via Kepler&#39;s third law with µ_host — one source of truth, so this can&#39;t drift from the renderer. µ_host / µ_moon / mean radii are JPL/NASA reference constants (<code>src/lib/moon-transfer.constants.ts</code>). Low-orbit radius = mean radius + 100 km.</p><p><strong>Type system.</strong> A new <code>MoonMissionDestId</code> (the 9 ids) joins <code>PlanDestinationId</code> alongside <code>DestinationId</code> and the geo <code>&#39;moon&#39;</code>; <code>isMoonMissionDest()</code> gates the split readout. The moons are <strong>not</strong> heliocentric <code>DestinationId</code>s (no single Lambert arc) and <strong>not</strong> geocentric (not Earth-orbiting) — a third category.</p><h2 id="honesty-bar-validation" tabindex="-1">Honesty bar (validation) <a class="header-anchor" href="#honesty-bar-validation" aria-label="Permalink to &quot;Honesty bar (validation)&quot;">​</a></h2><p>A 2D two-body teaching estimate, the giant-planet analogue of ADR-085 — it ships only if the numbers match reality:</p><ul><li><strong>Kepler-derived orbital radii</strong> match JPL to <strong>≤0.1%</strong> for all nine (Europa 670 965 vs 671 100 km).</li><li><strong>Arrival v∞</strong> at Jupiter ~5.6 km/s and <strong>v∞ at Europa</strong> ~6.5 km/s match the Galileo / Europa-Clipper flyby band.</li><li><strong>Direct Europa EOI ~5 km/s</strong> matches the &quot;direct moon capture is brutal at the giants&quot; literature — this is the honest <strong>upper bound</strong>; real missions use gravity-assist tours to slash the moon leg, and the caption + credit say so.</li><li>The Galilean gradient (Io &gt; Europa &gt; Ganymede &gt; Callisto — deeper well = costlier) and the Mars-moon cheapness (Phobos/Deimos ~4.8 km/s, shallow well) fall out correctly.</li></ul><p>Unit tests (<code>moon-transfer.test.ts</code>) assert the Kepler match, the Europa/Jupiter bands, the Galilean gradient, and a feasible positive departure+MOI split for every moon.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Positive</strong> — closes #308 fully; the nine moons reuse the existing porkchop UI (heatmap, magnifier, sandbox) with only a split-readout addition; no change to any heliocentric grid.</li><li><strong>Neutral / accepted</strong> — the moon grids <strong>inherit their host planet&#39;s feasibility profile</strong> exactly (the heliocentric leg is low-branch only, like the existing planet grids), so outer-moon porkchops (Triton 9%, matching Neptune) are mostly the fast side of the TOF band. This is consistent + honest, not a regression; a fuller outer-planet feasibility pass would be a change to all heliocentric grids, out of scope here.</li><li><strong>Accepted</strong> — direct-capture ∆v ignores gravity-assist tours; the caption frames it as the representative upper bound.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Full 3-leg with an explicit host-orbit-insertion (JOI) then a Hohmann to the moon</strong> — rejected for v1: more realistic for a real orbiter but adds a mission-design choice (capture-orbit apoapsis) with little pedagogical gain over the direct-capture upper bound.</li><li><strong>Planetocentric-only leg (host→moon, ignoring the interplanetary journey)</strong> — rejected: it hides that the Earth→host transfer is the dominant cost, which is the whole point.</li><li><strong>Hard-coded moon orbital radii</strong> — rejected: derive from the shipped period so the table can&#39;t silently drift from the 3D scene.</li></ul>',19)])])}const u=o(a,[["render",s]]);export{g as __pageData,u as default};
