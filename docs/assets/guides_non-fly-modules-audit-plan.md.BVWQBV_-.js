import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.7nMnW4XS.js";const k=JSON.parse('{"title":"Non-/fly modules — refactor & test-coverage plan","description":"","frontmatter":{},"headers":[],"relativePath":"guides/non-fly-modules-audit-plan.md","filePath":"guides/non-fly-modules-audit-plan.md"}'),o={name:"guides/non-fly-modules-audit-plan.md"};function n(l,e,r,d,c,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="non-fly-modules-—-refactor-test-coverage-plan" tabindex="-1">Non-<code>/fly</code> modules — refactor &amp; test-coverage plan <a class="header-anchor" href="#non-fly-modules-—-refactor-test-coverage-plan" aria-label="Permalink to &quot;Non-\`/fly\` modules — refactor &amp; test-coverage plan&quot;">​</a></h1><blockquote><p>Status · Plan Date · 2026-06-11 Tracking issue · TBD (filed alongside this doc) Companion to · the /fly audit closed in #324 + the typed <code>fly-cinematic-beats</code> module pattern (commit <code>5caf1c697</code>)</p></blockquote><blockquote><p><strong>Why this plan exists.</strong> After polish-waves 1-3 on <code>/fly</code> revealed structural debt — 13-let state spaghetti, scattered reset logic, magic-constant scattering, animate-loop coupling — we landed surgical refactors that bottomed out the debt for that route. A subsequent audit of the other major routes + supporting library modules found that <strong>no route has /fly-scale debt</strong>, but several have smaller, concrete patterns worth fixing before the codebase scales to ~80 missions. This document is the action plan.</p></blockquote><p>The audit verdict in one paragraph: routes <code>/plan</code>, <code>/missions</code>, <code>/fleet</code>, <code>/library</code>, <code>/mars</code>, <code>/moon</code>, <code>/earth</code> are healthy and need no work. Routes <code>/iss</code>, <code>/tiangong</code>, <code>/explore</code> have moderate debt (shared boilerplate, state proliferation). Library <code>data.ts</code> is a god-object that should be split. Several lib modules lack unit tests. Test-coverage gaps exist for <code>/tiangong</code>, <code>/plan</code>, <code>/moon</code> at the e2e level.</p><p>The full audit transcript lives in this conversation&#39;s session log; this doc is the execution plan derived from it.</p><hr><h2 id="how-this-plan-is-organised" tabindex="-1">How this plan is organised <a class="header-anchor" href="#how-this-plan-is-organised" aria-label="Permalink to &quot;How this plan is organised&quot;">​</a></h2><p>Ten actions, grouped into five batches. Each batch is independently committable; later batches don&#39;t depend on earlier ones except where noted. Batches are ordered by impact-per-effort, not by dependency.</p><ul><li><strong>Batch 1</strong> — shared state extraction (Actions 1, 5). Biggest cleanup win; touches /iss + /tiangong + a new lib module.</li><li><strong>Batch 2</strong> — e2e coverage gaps (Actions 2, 3). Three small specs.</li><li><strong>Batch 3</strong> — <code>data.ts</code> split (Action 4). Largest single refactor; isolatable but risky.</li><li><strong>Batch 4</strong> — <code>/explore</code> state consolidation (Actions 7, 9). Smaller scope, lower risk.</li><li><strong>Batch 5</strong> — unit-test + cleanup (Actions 6, 8, 10).</li></ul><p>Suggested execution order: Batch 1 → Batch 2 → Batch 4 → Batch 5 → Batch 3. Reasoning: Batches 1, 2, 4, 5 are small-to-medium with low blast radius. Batch 3 (data.ts split) touches dozens of importers and is best done with a clear cache (no pending unrelated refactors to merge against).</p><hr><h2 id="batch-1-—-shared-station-assembly-state" tabindex="-1">Batch 1 — Shared station-assembly state <a class="header-anchor" href="#batch-1-—-shared-station-assembly-state" aria-label="Permalink to &quot;Batch 1 — Shared station-assembly state&quot;">​</a></h2><h3 id="action-1-—-extract-src-lib-station-assembly-state-ts" tabindex="-1">Action 1 — Extract <code>src/lib/station-assembly-state.ts</code> <a class="header-anchor" href="#action-1-—-extract-src-lib-station-assembly-state-ts" aria-label="Permalink to &quot;Action 1 — Extract \`src/lib/station-assembly-state.ts\`&quot;">​</a></h3><p><strong>Problem.</strong> <code>/iss</code> and <code>/tiangong</code> declare identical assembly-state machinery:</p><ul><li>Both have <code>assemblyOpen</code>, <code>assemblyPlaying</code>, <code>assemblyProgress</code> <code>$state</code> variables (<a href="./../../src/routes/iss/+page.svelte"><code>iss/+page.svelte:46-50</code></a> and the matching block in <code>tiangong/+page.svelte</code>)</li><li>Both have an <code>assemblyRef = { active, playing, progress }</code> object used by the animate loop</li><li>Both have three <code>$effect</code> syncs writing the reactive state into the ref each frame</li><li>Both have a <code>requestMaterialRefresh</code> + <code>resetCamera</code> closure pattern in the same shape</li></ul><p>Approximately 150 lines of identical boilerplate per route.</p><p><strong>Spec.</strong></p><p>Create <code>src/lib/station-assembly-state.ts</code>:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Shared state container for station-assembly animations (/iss + /tiangong).</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Used by both routes to drive the per-frame assembly playback inside their</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * animate() loops.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Factory function returns a tuple of:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *   - reactive Svelte $state bindings (the values the UI sees + toggles)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *   - a plain mutable ref object the animate loop reads each frame</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *   - the $effect sync wiring that keeps them coherent</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * The factory is called from the component&#39;s &lt;script&gt; tag; callers</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * destructure the returned shape and bind to template directly.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> StationAssemblyRefs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Reactive — bound by the template / button handlers. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  state</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    assemblyOpen</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    assemblyPlaying</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    assemblyProgress</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  };</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Plain object the animate() loop reads each frame. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  ref</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">active</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">playing</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">progress</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createStationAssemblyState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> StationAssemblyRefs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Add unit tests in <code>src/lib/station-assembly-state.test.ts</code>:</p><ol><li>Factory returns fresh state (assemblyOpen=false, assemblyPlaying=false, assemblyProgress=0).</li><li>Mutating state.assemblyOpen propagates to ref.active (via effect, microtask-deferred).</li><li>Reset path: setting all three back to defaults clears ref to defaults.</li></ol><p><strong>Files to edit.</strong></p><ul><li>New: <code>src/lib/station-assembly-state.ts</code></li><li>New: <code>src/lib/station-assembly-state.test.ts</code></li><li>Modify: <code>src/routes/iss/+page.svelte</code> (~150 lines removed)</li><li>Modify: <code>src/routes/tiangong/+page.svelte</code> (~150 lines removed)</li></ul><p><strong>Acceptance.</strong></p><ul><li>[ ] Module + tests land; <code>npx vitest run</code> passes (current 2527 + ~5 new = ~2532)</li><li>[ ] <code>/iss</code> route file is ~150 lines shorter</li><li>[ ] <code>/tiangong</code> route file is ~150 lines shorter</li><li>[ ] Both routes&#39; assembly animations still work in-browser (verify the assembly slider in each route)</li><li>[ ] <code>npx svelte-check</code> 0 errors</li><li>[ ] Existing <code>tests/e2e/iss.spec.ts</code> still passes</li></ul><p><strong>Risk.</strong> Low. Svelte <code>$state</code> + <code>$effect</code> semantics across module/script boundaries need a brief check — the factory must run in a <code>&lt;script&gt;</code> context where rune APIs are available. Pattern is already used in <code>audio-state.svelte.ts</code> (search the codebase for <code>.svelte.ts</code> files).</p><p><strong>Estimated diff.</strong> <code>+150 / -300</code> net (<code>-150</code> lines).</p><h3 id="action-5-—-move-dock-events-truss-phases-to-src-lib-iss-assembly-phases-ts" tabindex="-1">Action 5 — Move <code>DOCK_EVENTS</code> + <code>TRUSS_PHASES</code> to <code>src/lib/iss-assembly-phases.ts</code> <a class="header-anchor" href="#action-5-—-move-dock-events-truss-phases-to-src-lib-iss-assembly-phases-ts" aria-label="Permalink to &quot;Action 5 — Move \`DOCK_EVENTS\` + \`TRUSS_PHASES\` to \`src/lib/iss-assembly-phases.ts\`&quot;">​</a></h3><p><strong>Problem.</strong> <code>/iss/+page.svelte:82-250</code> declares 7 dock events + 11 truss phases + 3 iROSA arrays inline. The route file owns DATA that should live in a library module — same separation-of-concerns principle the audit applied to <code>/fly</code> (moving constants into <code>fly-cinematic-beats.ts</code>).</p><p><strong>Spec.</strong></p><p>Create <code>src/lib/iss-assembly-phases.ts</code>:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DockEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  date</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  vehicle</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  module</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** ... fields from current declaration */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ISS_DOCK_EVENTS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DockEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ISS_TRUSS_PHASES</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TrussPhase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ISS_IROSA_PHASES</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IrosaPhase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span></code></pre></div><p>Tests in <code>src/lib/iss-assembly-phases.test.ts</code>:</p><ol><li>Arrays are non-empty.</li><li>All entries have required fields.</li><li>Dates parse as ISO 8601.</li><li>No duplicate IDs.</li><li>(Optional) Ordering invariants (chronological).</li></ol><p><strong>Files to edit.</strong></p><ul><li>New: <code>src/lib/iss-assembly-phases.ts</code></li><li>New: <code>src/lib/iss-assembly-phases.test.ts</code></li><li>Modify: <code>src/routes/iss/+page.svelte</code> (remove declarations; add import)</li></ul><p><strong>Acceptance.</strong></p><ul><li>[ ] Data round-trips identically (no value changes)</li><li>[ ] Tests assert structural invariants</li><li>[ ] Route imports from new module</li><li>[ ] <code>npm run preflight</code> exit 0</li><li>[ ] Existing iss e2e still passes</li></ul><p><strong>Risk.</strong> Low. Pure data move.</p><p><strong>Estimated diff.</strong> <code>+200 / -200</code> net (zero — just relocates).</p><hr><h2 id="batch-2-—-e2e-coverage-gaps" tabindex="-1">Batch 2 — e2e coverage gaps <a class="header-anchor" href="#batch-2-—-e2e-coverage-gaps" aria-label="Permalink to &quot;Batch 2 — e2e coverage gaps&quot;">​</a></h2><p>The audit found three routes with no e2e spec at all. These need basic smoke coverage before the codebase scales.</p><h3 id="action-2-—-add-tests-e2e-tiangong-spec-ts" tabindex="-1">Action 2 — Add <code>tests/e2e/tiangong.spec.ts</code> <a class="header-anchor" href="#action-2-—-add-tests-e2e-tiangong-spec-ts" aria-label="Permalink to &quot;Action 2 — Add \`tests/e2e/tiangong.spec.ts\`&quot;">​</a></h3><p><strong>Spec.</strong> Mirror <code>tests/e2e/iss.spec.ts</code> (whatever specs it asserts) for /tiangong. At minimum:</p><ol><li>Route loads without console errors</li><li>Canvas mounts within 10s</li><li>Module-list panel toggles</li><li>Clicking a module updates the URL (<code>?selected=X</code>)</li><li>Assembly slider scrubs (asserts on <code>assemblyProgress</code> data attribute or visual state)</li></ol><p><strong>Acceptance.</strong></p><ul><li>[ ] Spec file exists, runs on <code>desktop-chromium</code> + <code>mobile-chromium</code> projects</li><li>[ ] All assertions pass</li><li>[ ] No console errors (use the <code>errors</code> collector pattern from <code>fly-no-cislunar.spec.ts</code>)</li></ul><p><strong>Risk.</strong> Low. Read iss.spec.ts and adapt.</p><p><strong>Estimated diff.</strong> <code>+150</code> lines.</p><h3 id="action-3-—-add-tests-e2e-plan-spec-ts-and-tests-e2e-moon-spec-ts" tabindex="-1">Action 3 — Add <code>tests/e2e/plan.spec.ts</code> and <code>tests/e2e/moon.spec.ts</code> <a class="header-anchor" href="#action-3-—-add-tests-e2e-plan-spec-ts-and-tests-e2e-moon-spec-ts" aria-label="Permalink to &quot;Action 3 — Add \`tests/e2e/plan.spec.ts\` and \`tests/e2e/moon.spec.ts\`&quot;">​</a></h3><p><strong>Spec for <code>plan.spec.ts</code>:</strong></p><ol><li>Route loads with default Mars selection</li><li>Porkchop grid SVG renders</li><li>Selecting a different destination updates URL + grid</li><li>Clicking a grid cell opens the rocket-suggestion panel</li><li>(Optional) Departure / arrival slider scrubbing updates DOM state</li></ol><p><strong>Spec for <code>moon.spec.ts</code>:</strong></p><ol><li>Route loads, canvas mounts</li><li>Site picker shows a non-zero count</li><li>Clicking a site opens panel</li><li>(Optional) <code>?site=apollo11</code> deep-link loads correct site selected</li></ol><p><strong>Acceptance.</strong></p><ul><li>[ ] Both specs land, all assertions pass</li><li>[ ] Spec count: existing fly + station + earth + mars tests + 3 new = consistent count after add</li></ul><p><strong>Risk.</strong> Low. Use existing <code>mars.spec.ts</code> as the moon-spec template.</p><p><strong>Estimated diff.</strong> <code>+200</code> lines across two files.</p><hr><h2 id="batch-3-—-data-ts-split-largest-item" tabindex="-1">Batch 3 — <code>data.ts</code> split (largest item) <a class="header-anchor" href="#batch-3-—-data-ts-split-largest-item" aria-label="Permalink to &quot;Batch 3 — \`data.ts\` split (largest item)&quot;">​</a></h2><h3 id="action-4-—-split-src-lib-data-ts-1565-lines-83-exports-into-domain-modules" tabindex="-1">Action 4 — Split <code>src/lib/data.ts</code> (1565 lines, 83 exports) into domain modules <a class="header-anchor" href="#action-4-—-split-src-lib-data-ts-1565-lines-83-exports-into-domain-modules" aria-label="Permalink to &quot;Action 4 — Split \`src/lib/data.ts\` (1565 lines, 83 exports) into domain modules&quot;">​</a></h3><p><strong>Problem.</strong> <code>data.ts</code> is a god-object spanning missions, fleet, science, surface sites, station modules, porkchop grids, plus core cache + fetch + locale-overlay machinery. Single-responsibility violated. Tree-shaking is also compromised — anyone importing <code>getMission()</code> pulls in the entire fleet + science + station code path.</p><p><strong>Target modules.</strong></p><table tabindex="0"><thead><tr><th>New file</th><th>Owns</th><th>Approx LOC</th></tr></thead><tbody><tr><td><code>src/lib/data-core.ts</code></td><td>cache + fetch + locale overlay primitives</td><td>~250</td></tr><tr><td><code>src/lib/missions-data.ts</code></td><td><code>getMission*</code>, <code>filterMissions</code></td><td>~250</td></tr><tr><td><code>src/lib/fleet-data.ts</code></td><td><code>getFleet*</code>, <code>getFleetGallery</code></td><td>~200</td></tr><tr><td><code>src/lib/science-data.ts</code></td><td><code>getScienceTabs</code>, <code>getScienceSection</code>, …</td><td>~250</td></tr><tr><td><code>src/lib/surface-site-data.ts</code></td><td><code>getMarsSites</code>, <code>getMoonSites</code>, <code>getEarthSites</code>, <code>getTraverse</code></td><td>~250</td></tr><tr><td><code>src/lib/station-data.ts</code></td><td><code>getIssModules</code>, <code>getTiangongModules</code>, <code>getIssVisitors</code>, <code>getTiangongVisitors</code>, gallery getters</td><td>~200</td></tr><tr><td><code>src/lib/scenarios-data.ts</code></td><td><code>getScenarios</code>, <code>getScenario</code></td><td>~80</td></tr><tr><td><code>src/lib/rockets-data.ts</code></td><td><code>getRockets</code></td><td>~80</td></tr></tbody></table><p><code>data.ts</code> itself becomes a barrel re-export for backward compat (so existing <code>from &#39;$lib/data&#39;</code> imports keep working through the transition).</p><p><strong>Migration sequence.</strong></p><ol><li><strong>Create <code>data-core.ts</code></strong> — extract <code>cache</code>, <code>fetchJson</code>, <code>getLocaleOverlay</code>, and any internal helpers. Update <code>data.ts</code> to import from it. Verify all tests still pass.</li><li><strong>Per-domain extraction</strong> — one at a time: <ul><li>Move exports from <code>data.ts</code> to the new domain module</li><li>Re-export from <code>data.ts</code> for backward compat</li><li>Move matching tests from <code>data.test.ts</code> to <code>&lt;domain&gt;-data.test.ts</code></li><li>Run vitest after each domain to catch breakage early</li></ul></li><li><strong>Sweep callers</strong> — once all domains are extracted, replace <code>from &#39;$lib/data&#39;</code> imports across the codebase with the specific domain module. Use grep + Edit.</li><li><strong>Remove the barrel</strong> — once no callers reference <code>$lib/data</code> directly, delete it (or leave it with <code>// @deprecated</code> comments).</li></ol><p><strong>Acceptance.</strong></p><ul><li>[ ] All 8 new domain modules exist with the documented split</li><li>[ ] <code>data.ts</code> is either a thin barrel OR deleted</li><li>[ ] All existing <code>data.test.ts</code> assertions still pass (split into domain test files)</li><li>[ ] <code>npx svelte-check</code> 0 errors</li><li>[ ] <code>npm run preflight</code> exit 0</li><li>[ ] All existing e2e tests still pass</li></ul><p><strong>Risk.</strong> Medium-high. Touches ~30+ importer files. Cache singleton pattern must be preserved (don&#39;t introduce module-load-order races). Best done with no other in-flight refactors on the same branch.</p><p><strong>Estimated diff.</strong> <code>+1500 / -1565</code> across ~40 files. Net ~<code>-100</code> lines (test files balance out the new module headers).</p><hr><h2 id="batch-4-—-explore-state-consolidation" tabindex="-1">Batch 4 — <code>/explore</code> state consolidation <a class="header-anchor" href="#batch-4-—-explore-state-consolidation" aria-label="Permalink to &quot;Batch 4 — \`/explore\` state consolidation&quot;">​</a></h2><h3 id="action-7-—-collapse-explore-state-bools-into-typed-bags" tabindex="-1">Action 7 — Collapse <code>/explore</code> state bools into typed bags <a class="header-anchor" href="#action-7-—-collapse-explore-state-bools-into-typed-bags" aria-label="Permalink to &quot;Action 7 — Collapse \`/explore\` state bools into typed bags&quot;">​</a></h3><p><strong>Problem.</strong> <code>src/routes/explore/+page.svelte:699-788</code> declares 18 independent <code>$state()</code> bools/objects: <code>panelOpen</code>, <code>sunPanelOpen</code>, <code>sizesOpen</code>, <code>smallBodyPanelOpen</code>, <code>satellitePanelOpen</code>, <code>beltPanelOpen</code>, <code>lensOn</code>, <code>hoverLayerOn</code>, <code>focusedOnPlanet</code>, <code>statsOverlayOn</code>, <code>pathsLegendOpen</code>, …</p><p>Each is read + written from multiple sites. Cohesion is poor. No reset funnel.</p><p><strong>Spec.</strong> Group into four typed <code>$state</code> objects:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> panelState </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  planet: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  sun: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  sizes: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  smallBody: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  satellite: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  belt: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  pathsLegend: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> layerState </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  lens: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  hover: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  statsOverlay: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cameraState </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  focusedOnPlanet: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> selectionState </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Whatever per-frame selected items live here</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Provide <code>resetExplorePanelState()</code> for use on route param changes / canvas-clear.</p><p><strong>Acceptance.</strong></p><ul><li>[ ] All 18 <code>$state()</code> declarations consolidated into 4 typed objects</li><li>[ ] Template references updated (<code>panelOpen</code> → <code>panelState.planet != null</code>, etc.)</li><li>[ ] Reset funnel exists + is called where state should clear</li><li>[ ] <code>npx svelte-check</code> 0 errors</li><li>[ ] In-browser smoke: every panel still opens/closes; lens toggle works; small-body selection works</li></ul><p><strong>Risk.</strong> Medium. Lots of references — easy to miss one. Recommend <code>grep</code>-driven systematic conversion (similar to /fly cine migration).</p><p><strong>Estimated diff.</strong> <code>+50 / -150</code> net (<code>-100</code> lines from removing duplicate declarations + reset calls).</p><h3 id="action-9-—-add-key-declarations-to-each-blocks-at-explore-4560-4574" tabindex="-1">Action 9 — Add <code>(key)</code> declarations to <code>{#each}</code> blocks at <code>explore:4560, 4574</code> <a class="header-anchor" href="#action-9-—-add-key-declarations-to-each-blocks-at-explore-4560-4574" aria-label="Permalink to &quot;Action 9 — Add \`(key)\` declarations to \`{#each}\` blocks at \`explore:4560, 4574\`&quot;">​</a></h3><p><strong>Problem.</strong> Two <code>{#each}</code> blocks have no explicit key. Svelte 5 prefers explicit keys for stable DOM under reordering. Low priority but cheap to fix.</p><p><strong>Spec.</strong></p><ul><li><code>explore:4560</code> — change <code>{#each PATHS_LEGEND as entry}</code> → <code>{#each PATHS_LEGEND as entry (entry.mission_id)}</code></li><li><code>explore:4574</code> — change <code>{#each agencyToLogoPaths(entry.agency) as logoPath}</code> → <code>{#each agencyToLogoPaths(entry.agency) as logoPath (logoPath)}</code></li></ul><p><strong>Acceptance.</strong></p><ul><li>[ ] Both blocks have explicit keys</li><li>[ ] <code>npx svelte-check</code> 0 errors</li></ul><p><strong>Risk.</strong> Trivial.</p><p><strong>Estimated diff.</strong> <code>+0 / -0</code> (line count unchanged; tokens added).</p><hr><h2 id="batch-5-—-unit-tests-cleanups" tabindex="-1">Batch 5 — Unit tests + cleanups <a class="header-anchor" href="#batch-5-—-unit-tests-cleanups" aria-label="Permalink to &quot;Batch 5 — Unit tests + cleanups&quot;">​</a></h2><h3 id="action-6-—-add-unit-tests-for-moon-lander-models-ts-mars-lander-models-ts" tabindex="-1">Action 6 — Add unit tests for <code>moon-lander-models.ts</code> + <code>mars-lander-models.ts</code> <a class="header-anchor" href="#action-6-—-add-unit-tests-for-moon-lander-models-ts-mars-lander-models-ts" aria-label="Permalink to &quot;Action 6 — Add unit tests for \`moon-lander-models.ts\` + \`mars-lander-models.ts\`&quot;">​</a></h3><p><strong>Spec.</strong> For each module, add tests covering:</p><ol><li>Builder factory returns a <code>THREE.Group</code> (or whatever the public return type is)</li><li>Group contains the expected number of meshes per lander variant</li><li>Material colors match expected agency-color palette</li><li>Position offsets are reasonable (non-zero, finite)</li><li>Lander variants list is comprehensive (test count matches the agency-mission roster)</li></ol><p><strong>Acceptance.</strong></p><ul><li>[ ] <code>src/lib/moon-lander-models.test.ts</code> exists with 5+ tests</li><li>[ ] <code>src/lib/mars-lander-models.test.ts</code> exists with 5+ tests</li><li>[ ] <code>npx vitest run</code> passes (test count ~2540+)</li></ul><p><strong>Risk.</strong> Low. Three.js can be mocked in jsdom; use existing test patterns (search for other <code>*-models.test.ts</code> files).</p><p><strong>Estimated diff.</strong> <code>+200</code> lines across two files.</p><h3 id="action-8-—-increase-iss-proxy-model-test-ts-coverage-from-0-18x-to-0-30x-ratio" tabindex="-1">Action 8 — Increase <code>iss-proxy-model.test.ts</code> coverage from 0.18x to 0.30x ratio <a class="header-anchor" href="#action-8-—-increase-iss-proxy-model-test-ts-coverage-from-0-18x-to-0-30x-ratio" aria-label="Permalink to &quot;Action 8 — Increase \`iss-proxy-model.test.ts\` coverage from 0.18x to 0.30x ratio&quot;">​</a></h3><p><strong>Problem.</strong> 364 test lines for 2008 source lines = 0.18x ratio. Below the codebase&#39;s general level (cislunar-geometry is 0.39x, data is 0.63x).</p><p><strong>Spec.</strong> Add tests for:</p><ol><li><code>MODULE_BOXES</code> table is complete (one entry per <code>ISS_MODULE_IDS</code>)</li><li>Each module&#39;s bounding box is finite + reasonable</li><li><code>buildIssProxyStation</code> returns a Group with the expected mesh count</li><li>Decal-center calculations land within module bounds</li><li>Truss-section geometry maintains expected ratios</li></ol><p>Target: add ~200 lines of tests to bring ratio to ~0.30x.</p><p><strong>Acceptance.</strong></p><ul><li>[ ] <code>iss-proxy-model.test.ts</code> test count grows by ~10</li><li>[ ] <code>npx vitest run</code> passes</li><li>[ ] Test:source ratio ≥ 0.30 (per <code>wc -l</code> count)</li></ul><p><strong>Risk.</strong> Low. Tests are read-only on the geometry.</p><p><strong>Estimated diff.</strong> <code>+200</code> test lines.</p><h3 id="action-10-—-remove-dead-flyby-only-gravity-assist-caveat-destinations-constants-from-plan" tabindex="-1">Action 10 — Remove dead <code>FLYBY_ONLY</code> + <code>GRAVITY_ASSIST_CAVEAT_DESTINATIONS</code> constants from <code>/plan</code> <a class="header-anchor" href="#action-10-—-remove-dead-flyby-only-gravity-assist-caveat-destinations-constants-from-plan" aria-label="Permalink to &quot;Action 10 — Remove dead \`FLYBY_ONLY\` + \`GRAVITY_ASSIST_CAVEAT_DESTINATIONS\` constants from \`/plan\`&quot;">​</a></h3><p><strong>Problem.</strong> <code>src/routes/plan/+page.svelte:26-34</code> declares two constants commented for v0.8. They are never read in the current script. Per audit recommendation, dead code shouldn&#39;t sit in active routes.</p><p><strong>Spec.</strong> Per the comment in RFC-026 (v0.8 multi-destination expansion), check whether these constants are actually needed for v0.8 work-in-flight:</p><ul><li>If yes — move to RFC-026&#39;s tracking issue + delete from /plan</li><li>If no — delete outright</li></ul><p><strong>Acceptance.</strong></p><ul><li>[ ] Constants removed from /plan</li><li>[ ] <code>npx svelte-check</code> 0 errors</li><li>[ ] If needed for v0.8: notes added to RFC-026 about where they&#39;ll re-enter</li></ul><p><strong>Risk.</strong> Trivial.</p><p><strong>Estimated diff.</strong> <code>-10</code> lines.</p><hr><h2 id="cross-batch-verification" tabindex="-1">Cross-batch verification <a class="header-anchor" href="#cross-batch-verification" aria-label="Permalink to &quot;Cross-batch verification&quot;">​</a></h2><p>After each batch, the next agent should:</p><ol><li><code>npx svelte-check --tsconfig ./tsconfig.json</code> — must be 0 errors</li><li><code>npx vitest run</code> — total count grows with batch 1, 5; stays same elsewhere</li><li><code>npm run preflight</code> — exit 0</li><li><strong>Browser-verify the touched routes</strong> — don&#39;t trust typecheck alone (this rule landed during /fly Task A/B): <ul><li>Batch 1 → /iss + /tiangong assembly slider + module selection</li><li>Batch 2 → just run the new specs</li><li>Batch 3 → load <code>/fly</code>, <code>/missions</code>, <code>/fleet</code>, <code>/science</code>, <code>/library</code>, <code>/explore</code> and confirm each loads cleanly</li><li>Batch 4 → /explore — every panel, lens toggle, small-body selection, paths-legend</li><li>Batch 5 → no UI surface; tests-only</li></ul></li></ol><h2 id="commit-conventions" tabindex="-1">Commit conventions <a class="header-anchor" href="#commit-conventions" aria-label="Permalink to &quot;Commit conventions&quot;">​</a></h2><p>One commit per batch (or per action within a batch if commits get unwieldy). Message format mirrors the /fly refactor commits:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>refactor(&lt;area&gt;): &lt;short summary&gt; + &lt;secondary outcome&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>&lt;3-5 paragraph body explaining: problem, solution, verification, deferred items&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Closes #&lt;tracking-issue&gt; (or &quot;Closes part of #&lt;tracking-issue&gt;&quot; for partial batches)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Co-Authored-By: &lt;agent name&gt;</span></span></code></pre></div><p>Example: <code>refactor(stations): extract shared assembly state shared by /iss and /tiangong</code>.</p><h2 id="out-of-scope-for-this-plan" tabindex="-1">Out of scope for this plan <a class="header-anchor" href="#out-of-scope-for-this-plan" aria-label="Permalink to &quot;Out of scope for this plan&quot;">​</a></h2><ul><li><code>/fly</code> further refactor (animate-loop full extraction): tracked in #324 (closed) — leave at the current Task A+B level</li><li>raf+Three.js test harness: tracked in #325</li><li>Any new features: this plan is debt cleanup only</li><li>Mission-data onboarding (Voyager, Galileo, Juno, etc.): a separate work track Marko owns</li></ul><hr><h2 id="estimated-total" tabindex="-1">Estimated total <a class="header-anchor" href="#estimated-total" aria-label="Permalink to &quot;Estimated total&quot;">​</a></h2><ul><li>~10 action items</li><li>~5 batches</li><li>2500-3000 line diff across the codebase (mostly relocations + tests)</li><li>~20-30 new tests</li><li>Net <code>~-200 lines</code> of route boilerplate</li><li>4-6 commits</li></ul><p>Suggested cadence: one batch per session.</p><hr><h2 id="post-execution-review-·-2026-06-11" tabindex="-1">Post-execution review · 2026-06-11 <a class="header-anchor" href="#post-execution-review-·-2026-06-11" aria-label="Permalink to &quot;Post-execution review · 2026-06-11&quot;">​</a></h2><p>Execution of #326 surfaced gaps between this plan-as-written and the actual codebase state at the time of execution. Recorded here so future readers don&#39;t follow stale guidance.</p><h3 id="what-didn-t-hold" tabindex="-1">What didn&#39;t hold <a class="header-anchor" href="#what-didn-t-hold" aria-label="Permalink to &quot;What didn&#39;t hold&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Plan claim</th><th>Actual state</th></tr></thead><tbody><tr><td>§Action 2: &quot;Add <code>tests/e2e/tiangong.spec.ts</code>&quot;</td><td>Already existed since commit <code>fb882461b</code> (119 lines, 6 tests). What was actually missing: an assembly-slider scrub assertion.</td></tr><tr><td>§Action 3: &quot;Add <code>tests/e2e/plan.spec.ts</code> and <code>tests/e2e/moon.spec.ts</code>&quot;</td><td>Both already existed. Plan.spec.ts had 13 tests across porkchop loading + cell click + pin/compare + vehicle ∆v + FLY + Jupiter + LANDING/FLYBY pills + dest switch + Neptune + Ceres + mobile magnifier. Moon.spec.ts had 6 tests across 3D load + 2D toggle + Apollo 11 click + console errors + GALLERY + LEARN tabs. What was actually missing: a <code>?site=apollo11</code> deep-link assertion in moon.spec.ts.</td></tr><tr><td>§Action 3: &quot;Departure / arrival slider scrubbing updates DOM state&quot; for plan.spec.ts</td><td>/plan has no temporal slider. The UI is the porkchop grid, already covered by cell-click + dest-switch. No test added; noted as not-applicable.</td></tr><tr><td>§Action 1: &quot;/iss + /tiangong share ~150 lines of identical assembly-state boilerplate&quot;</td><td>The pure assembly-state container (3 <code>$state</code> declarations + <code>assemblyRef</code> POJO + <code>$effect</code> sync) is ~12 lines per route, not 150. The 150-line figure was likely double-counting §Action 5 territory (DOCK_EVENTS / TRUSS_PHASES) under §Action 1.</td></tr><tr><td>§Action 1: factory returns <code>{state, ref, $effect-wiring}</code></td><td>Svelte 5 <code>$effect</code> must run at <code>&lt;script&gt;</code> top level, so it can&#39;t live inside a plain-<code>.ts</code> factory. The shipped API is <code>createAssemblyRef()</code> + <code>syncAssemblyRef(ref, snap)</code>, with the component owning the <code>$effect</code>. Closer to <code>fly-cinematic-beats.ts</code> pure-data + pure-fn style than the <code>audio-state.svelte.ts</code> class.</td></tr><tr><td>§Action 7: &quot;/explore has 18 independent <code>$state()</code> bools at explore:699-788&quot;</td><td>The actual count is 11 boolean toggles (panelOpen / sunPanelOpen / sizesOpen / smallBodyPanelOpen / satellitePanelOpen / beltPanelOpen / pathsLegendOpen / lensOn / hoverLayerOn / focusedOnPlanet / statsOverlayOn). The remaining &quot;$state declarations&quot; in the line range are typed selection IDs (string | null) and complex bags (<code>layers</code>, <code>hoverData</code>, etc.), not bools.</td></tr><tr><td>§Action 7: example bag includes <code>panelState.planet: string | null</code> merging <code>panelOpen + selectedId</code></td><td>Implementation kept bools as bools and left <code>selectedId</code> separate to preserve ordering invariants across the panel-mutex code (selectSun, selectSmallBody, …). The semantic consolidation can be revisited but isn&#39;t required for the audit&#39;s purpose.</td></tr><tr><td>§Action 7: &quot;4 typed bags&quot; — <code>panelState</code> / <code>layerState</code> / <code>cameraState</code> / <code>selectionState</code></td><td>Shipped 3 bags. The fourth (<code>selectionState</code>) was placeholder in the plan (&quot;Whatever per-frame selected items live here&quot;) — the selection IDs (<code>selectedId</code>, <code>selectedSmallBodyId</code>, <code>selectedSatelliteKey</code>, <code>selectedBeltId</code>) stayed as separate lets.</td></tr><tr><td>§Action 9: line references <code>explore:4560, 4574</code></td><td>Lines shifted to <code>explore:4590, 4604</code> by the time §Action 7 was applied (no surprise — the bag declarations added ~30 lines). Pattern identified correctly; line numbers stale.</td></tr><tr><td>§Action 6: &quot;moon-lander-models.ts has no unit tests&quot; + acceptance &quot;<code>src/lib/moon-lander-models.test.ts</code> exists with 5+ tests&quot;</td><td>The file already existed on disk with 9 well-targeted tests (J-mission LRV inclusion, Chang&#39;e sample-return silhouette equivalence, Lunokhod sharing, generic-fallback mesh counts). Plan was written pre-checkin. Only <code>mars-lander-models.test.ts</code> was genuinely missing.</td></tr><tr><td>§Action 10: &quot;FLYBY_ONLY + GRAVITY_ASSIST_CAVEAT_DESTINATIONS are dead code&quot;</td><td>Not dead. Both are intentional scaffolding per the inline comment + RFC-026 line 66 (&quot;<code>FLYBY_ONLY</code> and <code>GRAVITY_ASSIST_CAVEAT_DESTINATIONS</code> are already in place as dead code (kept by ADR-076 for exactly this purpose)&quot;). Deleting them would force a re-write at v0.8 multi-destination expansion, which is exactly what the comment + RFC argue against. No change made.</td></tr></tbody></table><h3 id="what-did-hold" tabindex="-1">What did hold <a class="header-anchor" href="#what-did-hold" aria-label="Permalink to &quot;What did hold&quot;">​</a></h3><p>The two structural refactors landed close to the plan&#39;s intent — extracting the assembly-state helpers (<code>station-assembly-state.ts</code>), relocating <code>DOCK_EVENTS / TRUSS_PHASES</code> to <code>iss-assembly-phases.ts</code>, consolidating /explore state into typed bags + a reset funnel, and bumping <code>iss-proxy-model.test.ts</code> from 0.181× to 0.304× test:source ratio.</p><p>Six commits landed on <code>fixes</code> covering Batches 1, 2, 4, 5 (Actions 1, 2, 3, 5, 6, 7, 8, 9 — see #326 for per-action commit hashes). Action 4 (Batch 3, data.ts split) was deferred to #327 with a separate architecture-review phase because the plan undercounted scope (10-11 modules, not 8) and missed architectural choices that affect what the right split looks like.</p><h3 id="lesson-for-future-plans" tabindex="-1">Lesson for future plans <a class="header-anchor" href="#lesson-for-future-plans" aria-label="Permalink to &quot;Lesson for future plans&quot;">​</a></h3><p>When the plan author writes &quot;this file doesn&#39;t exist&quot; or &quot;this constant is dead,&quot; verify against the current branch before committing the plan. Several of the staler claims here were artifacts of the audit being done on a slightly older branch state, then drifting against subsequent unrelated commits before the plan was written down. A <code>git ls-files | grep</code> pass at plan-write time would have caught most of them.</p>`,142)])])}const g=s(o,[["render",n]]);export{k as __pageData,g as default};
