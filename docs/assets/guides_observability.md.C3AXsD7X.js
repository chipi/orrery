import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"Observability — Sentry (client errors) + Grafana Cloud (docker logs)","description":"","frontmatter":{},"headers":[],"relativePath":"guides/observability.md","filePath":"guides/observability.md"}'),n={name:"guides/observability.md"};function o(r,e,l,p,d,h){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="observability-—-sentry-client-errors-grafana-cloud-docker-logs" tabindex="-1">Observability — Sentry (client errors) + Grafana Cloud (docker logs) <a class="header-anchor" href="#observability-—-sentry-client-errors-grafana-cloud-docker-logs" aria-label="Permalink to &quot;Observability — Sentry (client errors) + Grafana Cloud (docker logs)&quot;">​</a></h1><p>Operator guide for the observability stack defined in <strong><a href="./../rfc/RFC-025">RFC-025</a></strong> with <strong><a href="./../adr/ADR-067">ADR-067</a></strong> (Sentry) and <strong><a href="./../adr/ADR-068">ADR-068</a></strong> (Grafana Agent). Reuses the operator&#39;s existing Sentry org + Grafana Cloud stack from <code>podcast_scraper</code> RFC-081.</p><p>The integration shape is <strong>load-bearing by env var, silent by default.</strong> Empty environment variables mean both integrations short-circuit — Sentry&#39;s SDK returns before init, the Grafana Agent&#39;s entrypoint picks a no-clients config. Fork-friendly. No committed secrets anywhere in the repo.</p><blockquote><p><strong>In-browser sibling:</strong> for live, per-route inspection (FPS, current locale, page-specific debug views), use the in-app DebugPanel — append <code>?debug=1</code> to any route. Sentry catches production errors after the fact; the DebugPanel is the during-development surface. See AGENTS.md §&quot;Debugging — <code>?debug=1</code> is the in-app inspector&quot; for tabs, registrar pattern, and when to expand stubs.</p></blockquote><hr><h2 id="sentry-client-side-js-errors" tabindex="-1">Sentry (client-side JS errors) <a class="header-anchor" href="#sentry-client-side-js-errors" aria-label="Permalink to &quot;Sentry (client-side JS errors)&quot;">​</a></h2><h3 id="what-it-captures" tabindex="-1">What it captures <a class="header-anchor" href="#what-it-captures" aria-label="Permalink to &quot;What it captures&quot;">​</a></h3><ul><li><strong>Unhandled exceptions</strong> during route navigation, load functions, and Svelte component lifecycle (wired via <code>handleErrorWithSentry()</code> in <code>src/hooks.client.ts</code>).</li><li><strong>Unhandled promise rejections</strong> (Sentry&#39;s default <code>globalHandlers</code> integration catches <code>window.onerror</code> + <code>unhandledrejection</code>).</li><li><strong>NOTHING else.</strong> No performance tracing (<code>tracesSampleRate: 0</code>). No Web Vitals. No session replay. No user identifiers. The <code>beforeSend</code> hook strips URL query + hash, nulls headers + cookies, and sets <code>ip_address: &#39;0.0.0.0&#39;</code> (Sentry&#39;s discard sentinel). The <code>beforeBreadcrumb</code> hook drops <code>ui.input</code> breadcrumbs entirely.</li></ul><p>See <strong><a href="../../src/lib/observability/sentry.ts"><code>src/lib/observability/sentry.ts</code></a></strong> for the full scrubber implementation.</p><h3 id="one-time-setup-sentry-org" tabindex="-1">One-time setup (Sentry org) <a class="header-anchor" href="#one-time-setup-sentry-org" aria-label="Permalink to &quot;One-time setup (Sentry org)&quot;">​</a></h3><ol><li>Sign in to the operator&#39;s Sentry org (same one as podcast_scraper).</li><li>Create a new project: <strong>Projects → New</strong> → platform <strong>JavaScript / SvelteKit</strong> → name <strong><code>orrery-web</code></strong>.</li><li>From the project&#39;s <strong>Settings → Client Keys (DSN)</strong>, copy the DSN string. It looks like <code>https://&lt;32-char-public-key&gt;@&lt;numeric-org-id&gt;.ingest.us.sentry.io/&lt;numeric-project-id&gt;</code>. This is the value of <code>PUBLIC_SENTRY_DSN</code>.</li><li>(Recommended) Set <strong>Data Scrubbing</strong> under <strong>Project → Settings → Security &amp; Privacy</strong> to &quot;Scrub IP Addresses&quot; and add <code>password</code>, <code>token</code>, <code>secret</code>, <code>api_key</code>, <code>apikey</code> to the sensitive-fields list. Belt-and-suspenders alongside the SDK&#39;s <code>beforeSend</code> hook.</li></ol><h3 id="one-time-setup-gh-actions-secret" tabindex="-1">One-time setup (GH Actions secret) <a class="header-anchor" href="#one-time-setup-gh-actions-secret" aria-label="Permalink to &quot;One-time setup (GH Actions secret)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Repo → Settings → Secrets and variables → Actions → New repository secret</span></span>
<span class="line"><span>  Name:  SENTRY_DSN_WEB</span></span>
<span class="line"><span>  Value: &lt;the DSN from step 3&gt;</span></span></code></pre></div><p>That&#39;s the only secret the production build needs. <code>.github/workflows/preview.yml</code> threads it into the build&#39;s <code>env:</code> block as <code>PUBLIC_SENTRY_DSN</code>; SvelteKit inlines it into the static bundle at build time. Forks that don&#39;t have this secret in their own repo will build with an empty value and the SDK no-ops.</p><h3 id="local-dev-posture" tabindex="-1">Local-dev posture <a class="header-anchor" href="#local-dev-posture" aria-label="Permalink to &quot;Local-dev posture&quot;">​</a></h3><ul><li><code>npm run dev</code> and <code>npm run preview</code> both default to empty DSN → SDK does not initialise → no network requests. Verified in browser devtools network tab (search &quot;ingest.sentry.io&quot; → no hits).</li><li>If you want to test against a real Sentry project from your laptop: create a separate <strong><code>orrery-dev</code></strong> project in Sentry, copy its DSN into your local <code>.env</code> as <code>PUBLIC_SENTRY_DSN=…</code>, and <strong>don&#39;t commit it</strong>. The <code>.env</code> is gitignored; the <code>scripts/check-no-secrets.ts</code> preflight gate scans the staged diff and fails the commit if a DSN slips through.</li></ul><h3 id="verifying-it-works" tabindex="-1">Verifying it works <a class="header-anchor" href="#verifying-it-works" aria-label="Permalink to &quot;Verifying it works&quot;">​</a></h3><p>Once <code>SENTRY_DSN_WEB</code> is set in GH Actions secrets and a deploy has run:</p><ol><li><p>Visit a production route on <code>chipi.github.io</code>, open browser devtools, and force-throw an error from the console:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setTimeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Sentry smoke test from chipi.github.io&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); }, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div></li><li><p>Within ~30 seconds, the error lands in Sentry&#39;s <strong>Issues</strong> list under the <code>orrery-web</code> project, with <code>request.url</code> showing the route path only (no query), <code>request.headers</code> undefined, <code>user.ip_address</code> shown as <code>0.0.0.0</code>.</p></li><li><p>If it doesn&#39;t appear: check the <strong>Stats</strong> view for rate-limited or filtered events; confirm <code>PUBLIC_SENTRY_DSN</code> is set in the deploy workflow&#39;s env (visible in the deploy workflow&#39;s logs as a redacted secret reference).</p></li></ol><h3 id="don-t-do-this-—-pii-leak-vectors" tabindex="-1">Don&#39;t do this — PII leak vectors <a class="header-anchor" href="#don-t-do-this-—-pii-leak-vectors" aria-label="Permalink to &quot;Don&#39;t do this — PII leak vectors&quot;">​</a></h3><p><code>sendDefaultPii: false</code> + the <code>beforeSend</code> scrubber covers the SDK&#39;s default surfaces, but it cannot catch user data passed explicitly to Sentry calls. Avoid:</p><ul><li><code>Sentry.captureMessage(&#39;user typed: &#39; + input)</code> — the message string isn&#39;t scrubbed.</li><li><code>Sentry.setUser({ email: &#39;...&#39; })</code> — Sentry attaches the entire object to subsequent events.</li><li><code>Sentry.addBreadcrumb({ message: input })</code> — the message bypasses <code>beforeBreadcrumb</code>&#39;s category filter.</li><li>Reading <code>localStorage</code>/<code>sessionStorage</code> into Sentry context (Orrery doesn&#39;t use either, per ADR-016; just don&#39;t add it).</li></ul><p>If you need to capture context for debugging, redact at the call site: <code>Sentry.setContext(&#39;mission&#39;, { id: missionId })</code> is fine (mission IDs are non-PII identifiers and live in the URL anyway).</p><hr><h2 id="grafana-cloud-agent-docker-stack-logs" tabindex="-1">Grafana Cloud Agent (docker-stack logs) <a class="header-anchor" href="#grafana-cloud-agent-docker-stack-logs" aria-label="Permalink to &quot;Grafana Cloud Agent (docker-stack logs)&quot;">​</a></h2><h3 id="what-it-ships" tabindex="-1">What it ships <a class="header-anchor" href="#what-it-ships" aria-label="Permalink to &quot;What it ships&quot;">​</a></h3><ul><li><strong><code>orrery-web</code> container stdout/stderr.</strong> nginx access logs + error log.</li><li><strong><code>orrery-pipeline-runner-*</code> container stdout/stderr.</strong> Pipeline invocation output (every <code>docker compose run --rm pipeline-runner …</code> creates a uniquely-named container; the agent&#39;s <code>docker_sd_configs</code> regex picks them up automatically).</li></ul><p>NOT shipped: other docker containers running on the host (the <code>keep</code> action on <code>__meta_docker_container_name</code> matching <code>/(orrery-web|orrery-pipeline-runner-.*)</code> filters them out — important on a laptop running multiple compose projects).</p><h3 id="one-time-setup-grafana-cloud-stack" tabindex="-1">One-time setup (Grafana Cloud stack) <a class="header-anchor" href="#one-time-setup-grafana-cloud-stack" aria-label="Permalink to &quot;One-time setup (Grafana Cloud stack)&quot;">​</a></h3><ol><li><p>Sign in to the operator&#39;s Grafana Cloud org (same one as podcast_scraper).</p></li><li><p>From the operator&#39;s stack home page, click <strong>Connect data → Loki</strong>.</p></li><li><p>Note the <strong>Loki ingest URL</strong> (the <code>host</code> field — e.g. <code>https://logs-prod-&lt;NN&gt;.grafana.net</code>). The full push URL is <code>&lt;host&gt;/loki/api/v1/push</code>. Note the numeric <strong>instance ID</strong> displayed alongside.</p></li><li><p>Click <strong>Access Policies → Create access policy</strong>. Name <code>orrery-logs-write</code>. Scope: <strong>logs:write</strong> only. Click <strong>Add token</strong>, name <code>orrery-agent</code>. Copy the <code>glc_…</code> token immediately (Grafana shows it once).</p></li><li><p>Set these in <code>.env</code> for local testing or in your production environment file:</p><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_CLOUD_LOKI_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=https://logs-prod-&lt;NN&gt;.grafana.net/loki/api/v1/push</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_CLOUD_LOKI_USER</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=&lt;numeric-instance-id-from-step-3&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_CLOUD_API_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=glc_&lt;token-from-step-4&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_AGENT_ENV</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=local-dev    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># or &#39;staging&#39; / &#39;production-vps&#39;</span></span></code></pre></div></li></ol><h3 id="bringing-up-the-stack-with-observability" tabindex="-1">Bringing up the stack with observability <a class="header-anchor" href="#bringing-up-the-stack-with-observability" aria-label="Permalink to &quot;Bringing up the stack with observability&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Web only (no agent — default for normal local-dev)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> web</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Web + agent (silent until env vars populated)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> observability</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>When <code>GRAFANA_CLOUD_*</code> env vars are empty (the local-dev default), <code>ops/observability/agent-entrypoint.sh</code> picks the no-clients <code>grafana-agent.silent.yaml</code> config — the agent starts, opens its HTTP server on <code>:12345</code>, and does nothing else. Zero outbound TCP traffic.</p><p>When the env vars are all populated, the entrypoint picks <code>grafana-agent.yaml</code> — full Promtail mode shipping to Grafana Cloud Loki.</p><h3 id="verifying-the-silent-default" tabindex="-1">Verifying the silent default <a class="header-anchor" href="#verifying-the-silent-default" aria-label="Permalink to &quot;Verifying the silent default&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> observability</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> grafana-agent</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> orrery-grafana-agent</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → exactly one line:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   [agent-entrypoint] Grafana Cloud creds NOT present → starting in silent mode (no shipping)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Host-side outbound check from the agent container&#39;s PID:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lsof</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> inspect</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{‌{.State.Pid}}&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> orrery-grafana-agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> TCP</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → no ESTABLISHED outbound connections, only the local listener on :12345</span></span></code></pre></div><h3 id="verifying-the-shipping-default" tabindex="-1">Verifying the shipping default <a class="header-anchor" href="#verifying-the-shipping-default" aria-label="Permalink to &quot;Verifying the shipping default&quot;">​</a></h3><p>With creds populated in <code>.env</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> observability</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> grafana-agent</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> orrery-grafana-agent</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → [agent-entrypoint] Grafana Cloud creds present → starting with shipping config</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → (then Grafana Agent&#39;s normal Promtail/Loki client startup log)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Generate some web traffic</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:8080/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/dev/null</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Wait ~30 seconds, then check Grafana Cloud Loki:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># In the Grafana UI under Explore → Loki:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   {app=&quot;orrery&quot;, env=&quot;local-dev&quot;, container=&quot;orrery-web&quot;}</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → log lines from the curl above should appear.</span></span></code></pre></div><h3 id="importing-the-dashboards" tabindex="-1">Importing the dashboards <a class="header-anchor" href="#importing-the-dashboards" aria-label="Permalink to &quot;Importing the dashboards&quot;">​</a></h3><p>Two dashboards live in <code>ops/observability/dashboards/</code>:</p><ul><li><code>orrery-web-access.json</code> — web container log volume + stderr-incidence + recent lines.</li><li><code>orrery-pipelines.json</code> — pipeline invocations + error-line detection + recent logs.</li></ul><p>Import them once per environment via the operator&#39;s Grafana Cloud Grafana instance:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GRAFANA_HTTP_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">your-stack</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.grafana.net</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GRAFANA_API_TOKEN=glsa_&lt;service-account-token-with-editor-role&gt; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  ./ops/observability/dashboards/import.sh</span></span></code></pre></div><p>The script POSTs each dashboard JSON to <code>/api/dashboards/db</code> with <code>overwrite: true</code>. Re-running it updates the dashboards in-place (matched by <code>uid</code>). The Grafana API token is <strong>not</strong> the same as the Loki write token — it&#39;s a service account token with <code>Editor</code> role, created under <strong>Administration → Service accounts → Add token</strong>.</p><h3 id="don-t-ship-sensitive-content-through-logs" tabindex="-1">Don&#39;t ship sensitive content through logs <a class="header-anchor" href="#don-t-ship-sensitive-content-through-logs" aria-label="Permalink to &quot;Don&#39;t ship sensitive content through logs&quot;">​</a></h3><p>The Loki ship pipeline mirrors whatever the web + pipeline-runner containers print to stdout/stderr. Don&#39;t print:</p><ul><li>API keys, DSNs, OAuth tokens.</li><li>User-typed search strings (Orrery doesn&#39;t accept any today, but be aware if you add a feature that does).</li><li>Full request URLs from pipeline scripts when those URLs may contain query-string secrets (e.g. signed S3 URLs).</li></ul><p>For most existing scripts this is fine — they print mission IDs, agency names, status codes. Use structured logging (<code>JSON.stringify({ at: &#39;fetch-launches&#39;, stage: &#39;merge&#39;, count })</code>) when you want better Loki querying; unstructured stdout still works.</p><hr><h2 id="architecture-summary" tabindex="-1">Architecture summary <a class="header-anchor" href="#architecture-summary" aria-label="Permalink to &quot;Architecture summary&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌────────────────── browser (chipi.github.io) ───────────────────┐</span></span>
<span class="line"><span>│  SvelteKit app                                                 │</span></span>
<span class="line"><span>│    → hooks.client.ts                                           │</span></span>
<span class="line"><span>│      → initSentry()                                            │</span></span>
<span class="line"><span>│        if PUBLIC_SENTRY_DSN empty → return (no-op)             │</span></span>
<span class="line"><span>│        else → Sentry.init() with beforeSend scrubber           │</span></span>
<span class="line"><span>│                ↓                                               │</span></span>
<span class="line"><span>│                Sentry Cloud (operator&#39;s org / orrery-web)      │</span></span>
<span class="line"><span>└────────────────────────────────────────────────────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>┌─────────────── docker-compose stack (local + future VPS) ──────┐</span></span>
<span class="line"><span>│  web (nginx)                pipeline-runner (on-demand)        │</span></span>
<span class="line"><span>│    stdout/stderr               stdout/stderr                   │</span></span>
<span class="line"><span>│         ↓                          ↓                           │</span></span>
<span class="line"><span>│  ┌─────────────────────────────────────────────────────────┐   │</span></span>
<span class="line"><span>│  │  grafana-agent (profile-gated)                          │   │</span></span>
<span class="line"><span>│  │    agent-entrypoint.sh picks:                           │   │</span></span>
<span class="line"><span>│  │      - grafana-agent.silent.yaml (no creds → silent)    │   │</span></span>
<span class="line"><span>│  │      - grafana-agent.yaml (creds present → shipping)    │   │</span></span>
<span class="line"><span>│  │                                                         │   │</span></span>
<span class="line"><span>│  │  Shipping mode → Grafana Cloud Loki (operator&#39;s stack)  │   │</span></span>
<span class="line"><span>│  └─────────────────────────────────────────────────────────┘   │</span></span>
<span class="line"><span>└────────────────────────────────────────────────────────────────┘</span></span></code></pre></div><p>Sentry observes the browser; Grafana Agent observes the docker stack. Both are silent by default. Neither has hardcoded credentials in the repo. The <code>scripts/check-no-secrets.ts</code> preflight gate scans every commit for DSN + API-key patterns.</p><hr><h2 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h2><ul><li><strong>RFC-025</strong> · <a href="./../rfc/RFC-025"><code>docs/rfc/RFC-025.md</code></a> — full architecture rationale + scope + risks.</li><li><strong>ADR-067</strong> · <a href="./../adr/ADR-067"><code>docs/adr/ADR-067.md</code></a> — Sentry config decisions.</li><li><strong>ADR-068</strong> · <a href="./../adr/ADR-068"><code>docs/adr/ADR-068.md</code></a> — Grafana Agent compose pattern.</li><li><strong>README §Privacy</strong> — user-facing summary of what Sentry collects + doesn&#39;t.</li><li><strong><code>src/lib/observability/sentry.ts</code></strong> — the scrubber.</li><li><strong><code>ops/observability/</code></strong> — agent config + dashboards + entrypoint.</li><li><strong>podcast_scraper RFC-081 §Layer-2</strong> — the original integration pattern this RFC adapts.</li></ul><hr><p><em>Orrery · docs/guides/observability.md · 2026-05-22 — Slice 3 of RFC-025 implementation</em></p>`,58)])])}const k=s(n,[["render",o]]);export{g as __pageData,k as default};
