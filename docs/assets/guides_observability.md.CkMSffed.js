import{_ as o,o as t,c as s,a2 as r}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"Observability — GlitchTip (client errors) + Umami (usage) + docker logs","description":"","frontmatter":{},"headers":[],"relativePath":"guides/observability.md","filePath":"guides/observability.md"}'),a={name:"guides/observability.md"};function i(n,e,d,l,c,h){return t(),s("div",null,[...e[0]||(e[0]=[r(`<h1 id="observability-—-glitchtip-client-errors-umami-usage-docker-logs" tabindex="-1">Observability — GlitchTip (client errors) + Umami (usage) + docker logs <a class="header-anchor" href="#observability-—-glitchtip-client-errors-umami-usage-docker-logs" aria-label="Permalink to &quot;Observability — GlitchTip (client errors) + Umami (usage) + docker logs&quot;">​</a></h1><p>Operator guide for browser telemetry. <strong>Source of truth: <a href="./../adr/ADR-082">ADR-082</a></strong> (the telemetry environment ladder), with <strong><a href="./../adr/ADR-067">ADR-067</a></strong> (Sentry SDK → GlitchTip) and <strong><a href="./../adr/ADR-081">ADR-081</a></strong> (Umami) as the per-integration decisions. RFC-025 is a historical snapshot — do not wire from its diagram.</p><blockquote><p><strong>The error sink is self-hosted <a href="https://glitchtip.com/" target="_blank" rel="noreferrer">GlitchTip</a>, not Sentry SaaS.</strong> We use the <code>@sentry/sveltekit</code> SDK, but it points at our self-hosted GlitchTip at <code>telemetry.orrerylearn.com</code>. Anywhere below that still says &quot;Sentry org / ingest.sentry.io&quot;, read &quot;GlitchTip project&quot;.</p></blockquote><p><strong>The env ladder (ADR-082).</strong> Both browser integrations report on three isolated rungs — <strong>prod</strong> (<code>orrerylearn.com</code>), <strong>staging</strong> (<code>chipi.github.io/orrery</code>), and the maintainer&#39;s <strong>local <code>vite dev</code></strong> — each with its own GlitchTip project + Umami site, tagged by environment:</p><table tabindex="0"><thead><tr><th>Rung</th><th>GlitchTip project</th><th>Umami site</th><th>env tag</th><th>selected by</th></tr></thead><tbody><tr><td>prod</td><td>4</td><td>prod</td><td><code>production</code></td><td><code>deploy-prod.yml</code> (<code>prod</code> GH env)</td></tr><tr><td>staging</td><td>6</td><td>staging</td><td><code>staging</code></td><td><code>staging.yml</code> (<code>staging</code> GH env)</td></tr><tr><td>dev</td><td>7</td><td>dev</td><td><code>dev</code></td><td><code>sentry.ts</code> / <code>analytics.ts</code> fallback, over the Tailscale <code>homelab</code> host</td></tr></tbody></table><p>The integration shape is <strong>env-gated, fork-silent by construction.</strong> Deploy rungs bake <code>PUBLIC_*</code> from GH environment secrets a fork doesn&#39;t have; the dev rung&#39;s endpoints resolve only on our tailnet (<code>homelab</code>), so a stranger&#39;s <code>vite dev</code> transport-fails silently. <code>vite preview</code> / CI / screenshots are not <code>dev</code> and carry no baked vars → silent. No committed secrets — only the public DSN/site-id browser ids, which ship in the bundle by design.</p><blockquote><p><strong>In-browser sibling:</strong> for live, per-route inspection (FPS, current locale, page-specific debug views), use the in-app DebugPanel — append <code>?debug=1</code> to any route. Sentry catches production errors after the fact; the DebugPanel is the during-development surface. See AGENTS.md §&quot;Debugging — <code>?debug=1</code> is the in-app inspector&quot; for tabs, registrar pattern, and when to expand stubs.</p></blockquote><hr><h2 id="sentry-client-side-js-errors" tabindex="-1">Sentry (client-side JS errors) <a class="header-anchor" href="#sentry-client-side-js-errors" aria-label="Permalink to &quot;Sentry (client-side JS errors)&quot;">​</a></h2><h3 id="what-it-captures" tabindex="-1">What it captures <a class="header-anchor" href="#what-it-captures" aria-label="Permalink to &quot;What it captures&quot;">​</a></h3><ul><li><strong>Unhandled exceptions</strong> during route navigation, load functions, and Svelte component lifecycle (wired via <code>handleErrorWithSentry()</code> in <code>src/hooks.client.ts</code>).</li><li><strong>Unhandled promise rejections</strong> (Sentry&#39;s default <code>globalHandlers</code> integration catches <code>window.onerror</code> + <code>unhandledrejection</code>).</li><li><strong>NOTHING else.</strong> No performance tracing (<code>tracesSampleRate: 0</code>). No Web Vitals. No session replay. No user identifiers. The <code>beforeSend</code> hook strips URL query + hash, nulls headers + cookies, and sets <code>ip_address: &#39;0.0.0.0&#39;</code> (Sentry&#39;s discard sentinel). The <code>beforeBreadcrumb</code> hook drops <code>ui.input</code> breadcrumbs entirely.</li></ul><p>See <strong><a href="../../src/lib/observability/sentry.ts"><code>src/lib/observability/sentry.ts</code></a></strong> for the full scrubber implementation.</p><h3 id="one-time-setup-glitchtip-projects-—-one-per-rung" tabindex="-1">One-time setup (GlitchTip projects — one per rung) <a class="header-anchor" href="#one-time-setup-glitchtip-projects-—-one-per-rung" aria-label="Permalink to &quot;One-time setup (GlitchTip projects — one per rung)&quot;">​</a></h3><p>Done once on the self-hosted GlitchTip (over the tailnet). Already provisioned: <strong>project 4</strong> (prod), <strong>project 6</strong> (staging), <strong>project 7</strong> (dev). Each project&#39;s <strong>Settings → Client Keys (DSN)</strong> gives a public DSN of the form <code>http(s)://&lt;32-hex-key&gt;@&lt;host&gt;/&lt;project-id&gt;</code> — the project id at the end is what routes events to the right rung.</p><ul><li><strong>prod / staging</strong> DSNs are set as the <code>PUBLIC_SENTRY_DSN</code> secret inside the <strong><code>prod</code></strong> and <strong><code>staging</code></strong> GH environments respectively (Repo → Settings → Environments → pick env → Secrets). Never at the repo level — the environment gate is the point.</li><li><strong>dev</strong> DSN is the <code>DEV_SENTRY_DSN</code> constant in <code>src/lib/observability/sentry.ts</code> (project 7, <code>homelab:8090</code>). It&#39;s committed on purpose: a DSN is a public browser id, and the <code>homelab</code> host is tailnet-only, so it&#39;s inert off the tailnet.</li></ul><h3 id="ci-wiring-already-in-the-workflows" tabindex="-1">CI wiring (already in the workflows) <a class="header-anchor" href="#ci-wiring-already-in-the-workflows" aria-label="Permalink to &quot;CI wiring (already in the workflows)&quot;">​</a></h3><ul><li><code>deploy-prod.yml</code> (<code>environment: prod</code>) and <code>staging.yml</code> (<code>environment: staging</code>) each thread their environment&#39;s <code>PUBLIC_SENTRY_DSN</code> + <code>PUBLIC_UMAMI_*</code> into the build <code>env:</code> block, with <code>PUBLIC_SENTRY_ENVIRONMENT</code> = <code>production</code> / <code>staging</code>. SvelteKit inlines them at build time. A fork lacks both environments → empty → SDK no-ops.</li><li>There is <strong>no repo-level <code>SENTRY_DSN_WEB</code> secret</strong> anymore (that was the old single-env design); the value lives per-GH-environment.</li></ul><h3 id="local-dev-posture-the-dev-rung" tabindex="-1">Local-dev posture (the dev rung) <a class="header-anchor" href="#local-dev-posture-the-dev-rung" aria-label="Permalink to &quot;Local-dev posture (the dev rung)&quot;">​</a></h3><ul><li><code>vite dev</code> <strong>now reports</strong> — with no <code>PUBLIC_SENTRY_*</code> override it falls back to the dev DSN (GlitchTip project 7) via <code>homelab:8090</code>. On the maintainer&#39;s tailnet this is live; off the tailnet the host doesn&#39;t resolve, the transport fails, and nothing leaves the browser (fork-silent by construction). Dev events carry a <code>worktree</code> tag (git branch) so parallel local sessions are separable.</li><li><code>vite preview</code>, the screenshot pipeline, and CI are <strong>not</strong> <code>dev</code> and carry no baked vars → silent. To point local dev at a <em>different</em> project, set <code>PUBLIC_SENTRY_DSN=…</code> in your gitignored <code>.env</code> (the <code>scripts/check-no-secrets.ts</code> gate still scans the staged diff).</li></ul><h3 id="verifying-it-works" tabindex="-1">Verifying it works <a class="header-anchor" href="#verifying-it-works" aria-label="Permalink to &quot;Verifying it works&quot;">​</a></h3><p>Pick the rung. For <strong>dev</strong>: run <code>vite dev</code> on the tailnet, force-throw from the console, and confirm the event lands in GlitchTip <strong>project 7</strong> tagged <code>environment=dev</code> + your <code>worktree</code>. For a <strong>deploy rung</strong>: visit the route (<code>orrerylearn.com</code> for prod, <code>chipi.github.io/orrery</code> for staging) and do the same — it lands in project <strong>4</strong> / <strong>6</strong> tagged <code>production</code> / <code>staging</code>:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setTimeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;GlitchTip smoke test&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); }, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>The event shows <code>request.url</code> as the route path only (no query), <code>request.headers</code> undefined, <code>user.ip_address</code> as <code>0.0.0.0</code>. If it doesn&#39;t appear: confirm the DSN&#39;s project id matches the rung, and that <code>PUBLIC_SENTRY_DSN</code> is set in that GH environment (redacted in the deploy logs).</p><h3 id="umami-usage-analytics-—-same-ladder" tabindex="-1">Umami (usage analytics) — same ladder <a class="header-anchor" href="#umami-usage-analytics-—-same-ladder" aria-label="Permalink to &quot;Umami (usage analytics) — same ladder&quot;">​</a></h3><p>Umami mirrors the above (ADR-081): prod/staging bake <code>PUBLIC_UMAMI_HOST</code> + <code>PUBLIC_UMAMI_WEBSITE_ID</code> (their own site ids) from the matching GH environment; <code>vite dev</code> falls back to the dev site via <code>homelab:3001</code>. Only the website id changes per rung — the host is the shared <code>analytics.orrerylearn.com</code> edge for deploys, <code>homelab</code> for dev. Fork-silence + the &quot;not <code>dev</code> → silent&quot; rule are identical. Event registry + privacy scrubbing live in <code>src/lib/analytics.ts</code>.</p><h3 id="don-t-do-this-—-pii-leak-vectors" tabindex="-1">Don&#39;t do this — PII leak vectors <a class="header-anchor" href="#don-t-do-this-—-pii-leak-vectors" aria-label="Permalink to &quot;Don&#39;t do this — PII leak vectors&quot;">​</a></h3><p><code>sendDefaultPii: false</code> + the <code>beforeSend</code> scrubber covers the SDK&#39;s default surfaces, but it cannot catch user data passed explicitly to Sentry calls. Avoid:</p><ul><li><code>Sentry.captureMessage(&#39;user typed: &#39; + input)</code> — the message string isn&#39;t scrubbed.</li><li><code>Sentry.setUser({ email: &#39;...&#39; })</code> — Sentry attaches the entire object to subsequent events.</li><li><code>Sentry.addBreadcrumb({ message: input })</code> — the message bypasses <code>beforeBreadcrumb</code>&#39;s category filter.</li><li>Reading <code>localStorage</code>/<code>sessionStorage</code> into Sentry context (Orrery doesn&#39;t use either, per ADR-016; just don&#39;t add it).</li></ul><p>If you need to capture context for debugging, redact at the call site: <code>Sentry.setContext(&#39;mission&#39;, { id: missionId })</code> is fine (mission IDs are non-PII identifiers and live in the URL anyway).</p><hr><h2 id="container-refresh-logs-—-shared-node-alloy-→-victorialogs" tabindex="-1">Container + refresh logs — shared node Alloy → VictoriaLogs <a class="header-anchor" href="#container-refresh-logs-—-shared-node-alloy-→-victorialogs" aria-label="Permalink to &quot;Container + refresh logs — shared node Alloy → VictoriaLogs&quot;">​</a></h2><p>Server-side log shipping, self-hosted. <strong>The per-app <code>orrery-grafana-agent</code> (grafana/agent v0.43) is retired</strong> — it was EOL and its <code>docker_sd</code> keep-filter leaked, mislabelling podcast-infra logs as <code>app=orrery</code>. It&#39;s replaced by a <strong>shared node <a href="https://grafana.com/docs/alloy/" target="_blank" rel="noreferrer">Grafana Alloy</a></strong> that runs once on the box for all apps; Orrery contributes a config fragment. Anything below referencing &quot;Grafana Cloud&quot;, &quot;grafana-agent&quot;, <code>GRAFANA_CLOUD_*</code>, or a <code>--profile observability</code> service is historical.</p><ul><li><strong>Shipper:</strong> the shared node Alloy (owned by the infra repo, ADR-121 / podcast infra #1268), config in <code>/etc/alloy/config.d/</code>.</li><li><strong>Sink:</strong> self-hosted <strong>VictoriaLogs</strong> (via the shared <code>loki.write &quot;logs_sink&quot;</code> component in <code>base.alloy</code>), queried with <strong>LogsQL</strong>.</li><li><strong>Dashboards / query UI:</strong> self-hosted <strong>Grafana</strong> on the tailnet (<code>http://homelab:3000</code>) — a viewer over VictoriaLogs, <em>not</em> Grafana Cloud.</li></ul><h3 id="orrery-s-config-fragment-—-ops-observability-orrery-alloy" tabindex="-1">Orrery&#39;s config fragment — <code>ops/observability/orrery.alloy</code> <a class="header-anchor" href="#orrery-s-config-fragment-—-ops-observability-orrery-alloy" aria-label="Permalink to &quot;Orrery&#39;s config fragment — \`ops/observability/orrery.alloy\`&quot;">​</a></h3><p>This repo owns exactly one file: <code>ops/observability/orrery.alloy</code>, dropped into the box&#39;s <code>/etc/alloy/config.d/</code> on deploy and hot-reloaded with <code>docker kill -s HUP alloy</code> (never restart the shared Alloy). It references shared components from <code>base.alloy</code> (<code>discovery.docker &quot;app&quot;</code>, <code>loki.write &quot;logs_sink&quot;</code>) — <strong>do not touch <code>base.alloy</code> or the podcast sources.</strong></p><h3 id="what-it-ships" tabindex="-1">What it ships <a class="header-anchor" href="#what-it-ships" aria-label="Permalink to &quot;What it ships&quot;">​</a></h3><ul><li><strong><code>orrery-web</code></strong> (nginx) stdout/stderr → labelled <code>surface=web</code>.</li><li><strong><code>orrery-pipeline-runner-*</code></strong> (each on-demand <code>docker compose run --rm pipeline-runner …</code> container) → <code>surface=pipeline</code>.</li><li><strong>The on-VPS launch-data refresh log</strong> — <code>/srv/orrery/data-refresh.log</code> (RFC-035, the 6-hourly cron; Alloy sees the host root at <code>/rootfs</code>), labelled <code>job=orrery-data-refresh</code>. Its <code>[refresh-prod-data] … ok — … bytes</code> / <code>FETCH FAILED</code> lines drive the <strong><code>orrery-launch-data-stale</code></strong> alert.</li></ul><p>Every Orrery stream carries <code>app=orrery</code> (set in <code>orrery.alloy</code>, since <code>logs_sink</code> no longer sets <code>app</code> globally). A <code>discovery.relabel</code> <code>keep</code> on <code>/(orrery-web|orrery-pipeline-runner-.*)</code> scopes it tightly — the too-broad keep was the old agent&#39;s leak. Other containers on the host are not shipped under <code>app=orrery</code>.</p><h3 id="deploying-the-config-no-per-app-service-no-cloud-creds" tabindex="-1">Deploying the config (no per-app service, no cloud creds) <a class="header-anchor" href="#deploying-the-config-no-per-app-service-no-cloud-creds" aria-label="Permalink to &quot;Deploying the config (no per-app service, no cloud creds)&quot;">​</a></h3><p>There is nothing to bring up in Orrery&#39;s compose stack for logs — the shipper is the node-level Alloy, and Orrery only provides a config fragment. Deploy is: copy <code>ops/observability/orrery.alloy</code> into the box&#39;s <code>/etc/alloy/config.d/</code>, then hot-reload:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># on the box (over the tailnet), after updating the fragment:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> HUP</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> alloy</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # reload config.d/ — do NOT restart the shared Alloy</span></span></code></pre></div><p>No <code>GRAFANA_CLOUD_*</code> env vars, no Loki write token, no <code>--profile observability</code> service. The VictoriaLogs sink + Docker-socket discovery live in <code>base.alloy</code> (infra repo). A checkout without tailnet access to the box simply doesn&#39;t ship — there&#39;s no Orrery-side credential to leak.</p><h3 id="verifying-logs-land" tabindex="-1">Verifying logs land <a class="header-anchor" href="#verifying-logs-land" aria-label="Permalink to &quot;Verifying logs land&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># generate some web traffic against the running stack</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:8080/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/dev/null</span></span></code></pre></div><p>Then query VictoriaLogs from the self-hosted Grafana (<code>http://homelab:3000</code>, Explore → VictoriaLogs datasource, LogsQL):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app:orrery surface:web           # nginx access/error lines from the curl above</span></span>
<span class="line"><span>app:orrery surface:pipeline      # pipeline-runner invocations</span></span>
<span class="line"><span>app:orrery job:orrery-data-refresh   # the 6-hourly launch-data refresh cron</span></span></code></pre></div><p>If nothing appears: confirm <code>orrery.alloy</code> is in <code>/etc/alloy/config.d/</code> and was hot-reloaded (<code>docker kill -s HUP alloy</code>), and that you&#39;re on the tailnet.</p><h3 id="importing-the-dashboards" tabindex="-1">Importing the dashboards <a class="header-anchor" href="#importing-the-dashboards" aria-label="Permalink to &quot;Importing the dashboards&quot;">​</a></h3><p>Three dashboards live in <code>ops/observability/dashboards/</code> (all panels query VictoriaLogs via LogsQL):</p><ul><li><code>orrery-overview.json</code> — cross-surface health at a glance.</li><li><code>orrery-web.json</code> — web container log volume + stderr incidence + recent lines.</li><li><code>orrery-pipelines.json</code> — pipeline invocations + error-line detection + recent logs.</li></ul><p>Import them into the self-hosted Grafana (tailnet) — idempotent, matched by <code>uid</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GRAFANA_HTTP_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">http://homelab:3000</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GRAFANA_API_TOKEN=glsa_&lt;service-account-token-with-editor-role&gt; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  ./ops/observability/dashboards/import.sh</span></span></code></pre></div><p>The script POSTs each dashboard to <code>/api/dashboards/db</code> with <code>overwrite: true</code> into the <code>orrery</code> folder (<code>GRAFANA_FOLDER_UID</code>, default <code>orrery</code>). The token is a Grafana <strong>service-account token with <code>Editor</code> role</strong> (Administration → Service accounts → Add token) — unrelated to any log-ingest credential.</p><h3 id="don-t-ship-sensitive-content-through-logs" tabindex="-1">Don&#39;t ship sensitive content through logs <a class="header-anchor" href="#don-t-ship-sensitive-content-through-logs" aria-label="Permalink to &quot;Don&#39;t ship sensitive content through logs&quot;">​</a></h3><p>The pipeline mirrors whatever the web + pipeline-runner containers print to stdout/stderr. Don&#39;t print:</p><ul><li>API keys, DSNs, OAuth tokens.</li><li>User-typed search strings (Orrery doesn&#39;t accept any today, but be aware if you add a feature that does).</li><li>Full request URLs from pipeline scripts when those URLs may contain query-string secrets (e.g. signed S3 URLs).</li></ul><p>For most existing scripts this is fine — they print mission IDs, agency names, status codes. Use structured logging (<code>JSON.stringify({ at: &#39;fetch-launches&#39;, stage: &#39;merge&#39;, count })</code>) when you want sharper VictoriaLogs (LogsQL) querying; unstructured stdout still works.</p><hr><h2 id="architecture-summary" tabindex="-1">Architecture summary <a class="header-anchor" href="#architecture-summary" aria-label="Permalink to &quot;Architecture summary&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────── browser (prod orrerylearn.com · staging chipi.github.io) ───────┐</span></span>
<span class="line"><span>│  SvelteKit app                                                          │</span></span>
<span class="line"><span>│    → hooks.client.ts → initSentry()   +   +layout → initAnalytics()     │</span></span>
<span class="line"><span>│        DSN/site id resolves for the rung? (ADR-082)                      │</span></span>
<span class="line"><span>│          prod  → GlitchTip 4  + Umami prod   (env=production)            │</span></span>
<span class="line"><span>│          staging → GlitchTip 6 + Umami staging (env=staging)             │</span></span>
<span class="line"><span>│          vite dev → GlitchTip 7 + Umami dev  (env=dev, via homelab)      │</span></span>
<span class="line"><span>│          else (fork / preview / CI) → no-op                             │</span></span>
<span class="line"><span>│                ↓ (self-hosted, Cloudflare-fronted)                      │</span></span>
<span class="line"><span>│      telemetry.orrerylearn.com (GlitchTip) · analytics.orrerylearn.com  │</span></span>
<span class="line"><span>└─────────────────────────────────────────────────────────────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>┌─────────────── the box (VPS + local docker stack) ──────────────────────┐</span></span>
<span class="line"><span>│  orrery-web (nginx)        orrery-pipeline-runner-*   data-refresh.log   │</span></span>
<span class="line"><span>│    stdout/stderr             stdout/stderr             (RFC-035 cron)    │</span></span>
<span class="line"><span>│         ↓                        ↓                        ↓              │</span></span>
<span class="line"><span>│  ┌───────────────────────────────────────────────────────────────────┐  │</span></span>
<span class="line"><span>│  │  shared node Grafana Alloy  (infra repo · ADR-121)                 │  │</span></span>
<span class="line"><span>│  │    /etc/alloy/config.d/orrery.alloy  (this repo&#39;s fragment)        │  │</span></span>
<span class="line"><span>│  │    labels: app=orrery, surface=web|pipeline, job=orrery-data-...   │  │</span></span>
<span class="line"><span>│  │                     ↓  loki.write &quot;logs_sink&quot; (base.alloy)         │  │</span></span>
<span class="line"><span>│  │              self-hosted VictoriaLogs → Grafana (homelab:3000)     │  │</span></span>
<span class="line"><span>│  └───────────────────────────────────────────────────────────────────┘  │</span></span>
<span class="line"><span>└──────────────────────────────────────────────────────────────────────────┘</span></span></code></pre></div><p>GlitchTip + Umami observe the browser (three isolated rungs, ADR-082); the shared node Alloy ships the box&#39;s logs to VictoriaLogs. The whole estate is self-hosted — no third-party SaaS. Orrery holds no ingest credential in the repo: browser DSN/site-ids are public, deploy secrets live in GH environments, and the log path is tailnet-only. The <code>scripts/check-no-secrets.ts</code> preflight gate scans every commit for DSN + API-key patterns.</p><hr><h2 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h2><ul><li><strong>ADR-082</strong> · <a href="./../adr/ADR-082"><code>docs/adr/ADR-082.md</code></a> — <strong>the telemetry environment ladder (source of truth)</strong>.</li><li><strong>ADR-067</strong> · <a href="./../adr/ADR-067"><code>docs/adr/ADR-067.md</code></a> — Sentry SDK → GlitchTip config (+ ADR-082 amendment).</li><li><strong>ADR-081</strong> · <a href="./../adr/ADR-081"><code>docs/adr/ADR-081.md</code></a> — self-hosted Umami (+ ADR-082 amendment).</li><li><strong>ADR-068</strong> · <a href="./../adr/ADR-068"><code>docs/adr/ADR-068.md</code></a> — original Grafana-Cloud log-shipper pattern, <strong>superseded</strong> by the shared node Alloy → VictoriaLogs (ADR-121, infra repo).</li><li><strong>RFC-025</strong> · <a href="./../rfc/RFC-025"><code>docs/rfc/RFC-025.md</code></a> — historical rationale (see its post-closure note).</li><li><strong>README §Privacy</strong> — user-facing summary of what the browser telemetry collects + doesn&#39;t.</li><li><strong><code>src/lib/observability/sentry.ts</code></strong> / <strong><code>src/lib/analytics.ts</code></strong> — the scrubbers + env-ladder resolution.</li><li><strong><code>ops/observability/orrery.alloy</code></strong> + <strong><code>ops/observability/dashboards/</code></strong> — the log fragment + Grafana dashboards.</li><li><strong>podcast_scraper-infra ADR-121 / #1268</strong> — the shared node Alloy + VictoriaLogs migration this repo plugs into.</li></ul><hr><p><em>Orrery · docs/guides/observability.md · 2026-07-24 — realigned to self-hosted GlitchTip + Umami (ADR-082) and the shared node Alloy → VictoriaLogs log pipeline</em></p>`,66)])])}const u=o(a,[["render",i]]);export{g as __pageData,u as default};
