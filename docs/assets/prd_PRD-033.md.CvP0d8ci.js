import{_ as t,o as a,c as o,a2 as r}from"./chunks/framework.Cdp8-jB6.js";const p=JSON.parse('{"title":"PRD-033 — Physics Lab (the scientific base, liberated)","description":"","frontmatter":{},"headers":[],"relativePath":"prd/PRD-033.md","filePath":"prd/PRD-033.md"}'),s={name:"prd/PRD-033.md"};function i(n,e,l,h,d,c){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="prd-033-—-physics-lab-the-scientific-base-liberated" tabindex="-1">PRD-033 — Physics Lab (the scientific base, liberated) <a class="header-anchor" href="#prd-033-—-physics-lab-the-scientific-base-liberated" aria-label="Permalink to &quot;PRD-033 — Physics Lab (the scientific base, liberated)&quot;">​</a></h1><blockquote><p>Status · Draft — epic kickoff Sources · PRD-002 (Mission Configurator / <code>/plan</code>) · PRD-003 (Mission Arc) · PRD-032 (Launch) · PRD-008 (Science) · 01_Orrery_Vision.md §audiences · ADR-030 (fly-physics pure-function isolation) Audiences · STEM student, curious learner, the contributor (PA §audiences) — and a new fourth: the <strong>agent</strong> (an MCP client acting for any of the three) Promises · real physics not approximations that mislead · educational at every level · fail honestly · open source and contributable (PA §promises) Principles · physics first · fail honestly · attribution is design · data over code (PA §principles) Why this is a PRD · Orrery has quietly become a small, accurate, unit-tested spaceflight-physics engine — Lambert solvers, a JPL ephemeris, a powered-ascent integrator, per-body EDL, a J2 satellite propagator — but every formula is trapped <em>inside</em> a scripted surface. You can watch Curiosity fly to Mars; you cannot ask &quot;what if the Isp were 20 seconds higher?&quot; and see the answer. This proposes to liberate that engine into a <strong>shared physics kernel</strong> and give it two brand-new front doors: a <strong>Physics Lab</strong> where a human plays with the formulas in a live, media-rich, sketch-illustrated notebook, and a <strong>hosted MCP server</strong> where an agent calls the same formulas as tools. It is the difference between a museum where you watch, and a workshop where you build. Getting the honesty contract right — computed truth kept visibly distinct from generated illustration — is the make-or-break, because the moment we draw pretty pictures around numbers, we risk teaching fiction. That is a product decision, not a silent PR.</p></blockquote><hr><p>A STEM student opens <code>/lab</code>. Blank graph-paper canvas. She types a question into a card: <em>Tsiolkovsky — how much Δv does a mass ratio of 12 buy me at 350 s Isp?</em> A number appears — <strong>8.6 km/s</strong> — but so does a small hand-drawn-looking curve in the margin: Δv against mass ratio, her point marked with a circled dot, the vacuum-Isp of a real RL10 sketched in for scale. She drags the Isp slider; the curve redraws live and a pencil-style annotation updates: <em>&quot;+20 s Isp → +0.4 km/s here.&quot;</em> She pulls a second card and wires the <em>output orbit</em> of an ascent into a <em>transfer</em> — the canvas draws the parking orbit stretching into a heliocentric ellipse, periapsis circled, the Δv budget stacked as a little waterfall beside it. Nothing here is a cartoon: every line is integrated from the same equations <code>/fly</code> flies. She exports the whole page as a lab report and pastes it into her problem set. She never wrote a line of code.</p><h2 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The problem&quot;">​</a></h2><p>Orrery&#39;s physics is world-class and completely inaccessible as physics. It powers <code>/fly</code>, <code>/plan</code>, <code>/explore</code>, <code>/science</code> — but only along the rails those surfaces lay down. There is no way to:</p><ul><li><strong>Ask a free-form question</strong> — &quot;plot the entry corridor for Mars vs Titan,&quot; &quot;what mass ratio do I need for TLI,&quot; &quot;when is the next cheap Mars window.&quot; The numbers exist; the surface to pose the question does not.</li><li><strong>See the working, drawn.</strong> <code>/plan</code> draws one thing (the porkchop). The other twenty formulas produce numbers with no picture. A number without a diagram teaches far less than the same number <em>with</em> the transfer ellipse, the force diagram, the loss waterfall drawn around it.</li><li><strong>Reach the engine from outside.</strong> A developer, a researcher, or an agent cannot call <code>solveLambert</code> or <code>integrateAscent</code> without cloning the repo and booting SvelteKit. The contributor promise (PA §audiences) stops at the app&#39;s edge.</li></ul><p>The scientific base is a library trapped inside a building. This opens two doors.</p><h2 id="north-star" tabindex="-1">North star <a class="header-anchor" href="#north-star" aria-label="Permalink to &quot;North star&quot;">​</a></h2><p>Two references, held simultaneously:</p><ul><li><strong>An astrophysicist&#39;s notebook</strong> — the real thing: graph paper, integrals worked in the margin, a quick sketch of the geometry next to every result, the page a <em>record of thinking</em>, not a form with outputs. Feynman&#39;s diagrams were doodles that happened to be exact. That is the register: <strong>warm, hand-made, and rigorously true at the same time.</strong></li><li><strong>A live scientific instrument</strong> — every figure is computed, reproducible, exportable, and correct to the fidelity we honestly claim. The bar for the <em>look</em> is the same bar <code>/fly</code> holds for cinematics and <code>/posters</code> holds for print: this must feel like Orrery, top to bottom, not a bolted-on calculator.</li></ul><p>The tension between those two — hand-drawn warmth vs instrument-grade truth — is the whole design problem, and §&quot;The honesty line&quot; is how we resolve it.</p><h2 id="the-two-front-doors" tabindex="-1">The two front doors <a class="header-anchor" href="#the-two-front-doors" aria-label="Permalink to &quot;The two front doors&quot;">​</a></h2><p><strong>Door 1 — the Physics Lab (<code>/lab</code>, human-facing).</strong> A free-form canvas/notebook where each formula in the kernel is a card: inputs become live controls, outputs become numbers <em>and</em> auto-drawn figures. Cards can be compared, branched, and — the payoff — <strong>wired together</strong> into a full authored mission worksheet (ascent → injection → transfer → capture → descent), the whole pipeline drawn as one annotated storyboard. It is <code>/fly</code>&#39;s physics, authored by the user instead of scripted.</p><p><strong>Door 2 — the MCP server (<code>mcp.</code> subdomain, agent-facing).</strong> The same kernel, exposed as authenticated tools any MCP client (Claude.ai, Claude Code, third parties) can call over the network. An agent asks a natural-language question; the server runs the real formulas and returns structured results — including the <em>same figure descriptions</em> the Lab renders. This is the contributor promise extended from &quot;fork the repo&quot; to &quot;call it from anywhere,&quot; and it is where the Lab&#39;s own natural-language &quot;ask&quot; box eventually gets its brain.</p><p>Both doors open onto one room: the shared kernel. Neither reimplements physics.</p><h3 id="the-lab-is-a-subsystem-—-one-card-model-three-views" tabindex="-1">The Lab is a subsystem — one card model, three views <a class="header-anchor" href="#the-lab-is-a-subsystem-—-one-card-model-three-views" aria-label="Permalink to &quot;The Lab is a subsystem — one card model, three views&quot;">​</a></h3><p>Door 1 is not a single screen; it is a small subsystem built on one atom. The atom is a <strong>card</strong>: a formula instance = <em>(which formula, its bound inputs, any wiring to other cards, and the <code>FigureSpec</code> it emits)</em>. A card is portable data, so the same card renders in three views that match three distinct intents:</p><ul><li><strong>Canvas</strong> — <em>prototyping.</em> A spatial graph on the teal grid where cards are dragged freely and an output socket wires into another card&#39;s input. Ephemeral, exploratory, no narrative — the whiteboard.</li><li><strong>Notebook</strong> — <em>keeping.</em> An ordered, narrated sequence of cards; the durable, exportable artifact you&#39;re actually building. A card here can reference the result of the card above it.</li><li><strong>Focus</strong> — <em>drilling in.</em> One card, full-screen: big figure + full control rail + the <code>/science</code> derivation. A lens callable from either workspace, not a workspace itself.</li></ul><p>Cards move between views: you <strong>promote</strong> a card (or a wired subgraph) from Canvas into a Notebook. One model, three windows — this is what makes the Lab a subsystem rather than three routes. The genuinely novel/expensive parts are localized and named honestly: the Canvas <strong>wiring/graph engine</strong>, <strong>card serialization/portability</strong> across views and sessions, and the fact that a spatial Canvas has <strong>no honest mobile story</strong> (it degrades to read-only on phones). Those costs are why the sequence below front-loads the Notebook, not the Canvas.</p><h2 id="the-media-driven-layer-—-figures-that-draw-themselves" tabindex="-1">The media-driven layer — figures that draw themselves <a class="header-anchor" href="#the-media-driven-layer-—-figures-that-draw-themselves" aria-label="Permalink to &quot;The media-driven layer — figures that draw themselves&quot;">​</a></h2><p>The feature that makes the Lab <em>feel</em> like a notebook rather than a calculator: <strong>results auto-generate diagrams.</strong> Every kernel result carries a declarative description of what should be drawn (a <code>FigureSpec</code>), and the Lab renders it. The catalogue, all computed from pure functions, all free:</p><ul><li>transfer ellipse (departure / arrival / periapsis marked) · porkchop heatmap · Δv-budget waterfall · rocket free-body force diagram at altitude · ascent profile triptych (altitude · velocity · dynamic-pressure vs time) · EDL entry corridor per body · orbit / ground-track · sky chart (alt-az) · Tsiolkovsky sensitivity curve · cislunar ECI trajectory.</li></ul><p>Type a parameter, get a publication-grade annotated figure instantly. On top of that exact layer sits an optional <strong>illustrative layer</strong> — generative sketches and margin art (via the existing image pipeline) and grounded natural-language annotations that read like a physicist&#39;s notes — for warmth and shareability.</p><h2 id="the-honesty-line-make-or-break" tabindex="-1">The honesty line (make-or-break) <a class="header-anchor" href="#the-honesty-line-make-or-break" aria-label="Permalink to &quot;The honesty line (make-or-break)&quot;">​</a></h2><p>The moment we draw art around numbers, we can accidentally present fiction as fact — a violation of the deepest promise (PA §&quot;real physics, not approximations that mislead&quot;; §&quot;attribution is design&quot;; §&quot;fail honestly&quot;). The resolution has three parts:</p><ol><li><strong>Computed ≠ illustrated, always visibly.</strong> Exact figures (integrated from the kernel) and generative/illustrative art occupy visually and semantically distinct registers, and every artifact is labelled with its provenance — same discipline as the image pipeline and <code>/science</code> fact-checking.</li><li><strong>The &quot;sketchy-but-exact&quot; sweet spot.</strong> The default notebook aesthetic — graph paper, hand-annotated vectors, margin doodles — is rendered as <strong>deterministic SVG styled to look hand-drawn.</strong> The warmth is styling; the content is computed. This gets the notebook feel without generative cost, latency, or honesty risk. Generative art is a clearly-labelled layer <em>on top</em>, never on the data path.</li><li><strong>One authoritative ephemeris, honestly labelled.</strong> The kernel holds two planet-position models of different fidelity (a fast <code>/fly</code> approximation and a JPL-accurate solve). The Lab and MCP expose the <strong>accurate</strong> one as authoritative; the approximation, if exposed at all, is labelled <code>fast</code> with a stated error bound. And when the physics says <em>no</em> — Lambert finds no window, TWR &lt; 1, not enough Δv — the Lab <strong>explains the failure</strong>, it does not hide it (PA §&quot;fail honestly&quot;).</li></ol><h2 id="who-it-serves" tabindex="-1">Who it serves <a class="header-anchor" href="#who-it-serves" aria-label="Permalink to &quot;Who it serves&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Audience</th><th>What the Lab / MCP gives them</th></tr></thead><tbody><tr><td><strong>STEM student</strong></td><td>Real vis-viva / Lambert / Tsiolkovsky numbers <em>with</em> the figure drawn, verifiable, exportable into coursework. The tool that turns &quot;I know the Hohmann transfer in principle&quot; into &quot;I&#39;ve watched it respond to my inputs.&quot;</td></tr><tr><td><strong>Curious learner</strong></td><td>A playground: sweep a slider, watch the trajectory bend. Understanding through play, not a textbook.</td></tr><tr><td><strong>The contributor / developer</strong></td><td>The kernel as a real library + an MCP endpoint — call the physics from a script, a notebook, or an agent, without cloning the app.</td></tr><tr><td><strong>The agent</strong></td><td>Typed, authenticated physics tools over the network — the app&#39;s engine as an agent capability.</td></tr></tbody></table><h2 id="the-tier-ladder-this-is-the-roadmap" tabindex="-1">The tier ladder (this is the roadmap) <a class="header-anchor" href="#the-tier-ladder-this-is-the-roadmap" aria-label="Permalink to &quot;The tier ladder (this is the roadmap)&quot;">​</a></h2><ul><li><strong>T0 — parameterized figures.</strong> Every formula gets sliders → a live figure. (Generalizes <code>/plan</code>&#39;s porkchop to the whole kernel.)</li><li><strong>T1 — the notebook canvas.</strong> Linked cards, scenario comparison, export with Orrery&#39;s placard styling.</li><li><strong>T2 — composition.</strong> Wire formulas into a full authored mission worksheet, drawn as one storyboard.</li><li><strong>T3 — the media layer.</strong> Labelled generative sketches + grounded margin-notes; shareable lab-report artifacts.</li><li><strong>T4 — agentic.</strong> A natural-language &quot;ask&quot; box; the MCP server drives the kernel, the Lab renders. The loop closes.</li></ul><p>Mapped to the subsystem: <strong>v1 ships the shared card model + the Notebook + Focus views (T0–T1) in the sketchy-but-exact aesthetic</strong> — the durable, mobile-safe artifact and the PRD north star, which de-risks the kernel by exercising it in-process before the MCP server exposes it to strangers. <strong>The Canvas workspace + wiring + promote is T2</strong>, landed second once the card-graph engine is proven — deliberately not front-loaded, because the wiring engine and its (absent) mobile story are the expensive, novel parts. T3–T4 ride on the same kernel and <code>FigureSpec</code> contracts v1 establishes, so they cost a consumer, not a rewrite.</p><h2 id="why-now" tabindex="-1">Why now <a class="header-anchor" href="#why-now" aria-label="Permalink to &quot;Why now&quot;">​</a></h2><p>The engine is done and, per the 2026-08-08 inventory, already ~80% pure and unit-tested (ADR-030&#39;s discipline spread repo-wide). <code>/plan</code> already proves the &quot;formula → live figure&quot; loop with the porkchop. The image pipeline, the placard-overlay aesthetic, and the <code>lavish</code> artifact export all exist. The gap between &quot;we have the physics&quot; and &quot;you can play with the physics&quot; is now mostly packaging and one beautiful surface.</p><h2 id="success-looks-like" tabindex="-1">Success looks like <a class="header-anchor" href="#success-looks-like" aria-label="Permalink to &quot;Success looks like&quot;">​</a></h2><ul><li>A student computes a Δv budget in <code>/lab</code>, exports the drawn figure into a problem set, and it&#39;s <em>right</em>.</li><li>A learner drags the Isp slider and watches the sensitivity curve bend, and gets <em>why</em> staging matters without being told.</li><li>A developer points Claude at <code>mcp.orrerylearn.com</code> and asks for a 2033 Mars window, and gets a real Lambert answer with a figure.</li><li>Someone screenshots a <code>/lab</code> page and it is unmistakably Orrery — the notebook looks as considered as <code>/fly</code> looks cinematic.</li><li>Nobody ever mistakes a generated sketch for a computed result, because the line is always visible.</li></ul><h2 id="non-goals-v1" tabindex="-1">Non-goals (v1) <a class="header-anchor" href="#non-goals-v1" aria-label="Permalink to &quot;Non-goals (v1)&quot;">​</a></h2><ul><li><strong>Not a full CAS / Jupyter.</strong> No arbitrary user code execution; the palette is the kernel&#39;s vetted formulas, not a Python sandbox.</li><li><strong>No new physics.</strong> The Lab exposes today&#39;s fidelity, honestly labelled — it does not add N-body, CFD, or higher-precision propagation.</li><li><strong>Generative art is not v1-gating.</strong> The sketchy-but-exact deterministic figures ship first; the generative illustrative layer (T3) is a labelled follow-on.</li><li><strong>The agentic &quot;ask&quot; box (T4) is not v1.</strong> It depends on the MCP server, which lands after the Lab proves the kernel.</li><li><strong>MCP public multi-user auth is not v1.</strong> A private-beta credential precedes public OAuth.</li></ul><p><em>The look &amp; feel — notebook metaphor, the sketchy-but-exact figure language, layout, controls, export — is specified in the forthcoming <strong>UXS</strong> (visual-anchored on real screenshots, signed off before build). The technical architecture — the kernel package boundary, the <code>FigureSpec</code> contract, MCP hosting/auth, and the data strategy — is specified in <strong>RFC-037</strong>.</em></p>',39)])])}const g=t(s,[["render",i]]);export{p as __pageData,g as default};
