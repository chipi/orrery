import{_ as t,o as a,c as o,a2 as r}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"PRD-033 — Physics Lab (the scientific base, liberated)","description":"","frontmatter":{},"headers":[],"relativePath":"prd/PRD-033.md","filePath":"prd/PRD-033.md"}'),s={name:"prd/PRD-033.md"};function n(i,e,l,d,h,c){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="prd-033-—-physics-lab-the-scientific-base-liberated" tabindex="-1">PRD-033 — Physics Lab (the scientific base, liberated) <a class="header-anchor" href="#prd-033-—-physics-lab-the-scientific-base-liberated" aria-label="Permalink to &quot;PRD-033 — Physics Lab (the scientific base, liberated)&quot;">​</a></h1><blockquote><p>Status · Draft — epic kickoff Sources · PRD-002 (Mission Configurator / <code>/plan</code>) · PRD-003 (Mission Arc) · PRD-032 (Launch) · PRD-008 (Science) · 01_Orrery_Vision.md §audiences · ADR-030 (fly-physics pure-function isolation) Audiences · STEM student, curious learner, the contributor (PA §audiences) — and a new fourth: the <strong>agent</strong> (an MCP client acting for any of the three) Promises · real physics not approximations that mislead · educational at every level · fail honestly · open source and contributable (PA §promises) Principles · physics first · fail honestly · attribution is design · data over code (PA §principles) Why this is a PRD · Orrery has quietly become a small, accurate, unit-tested spaceflight-physics engine — Lambert solvers, a JPL ephemeris, a powered-ascent integrator, per-body EDL, a J2 satellite propagator — but every formula is trapped <em>inside</em> a scripted surface. You can watch Curiosity fly to Mars; you cannot ask &quot;what if the Isp were 20 seconds higher?&quot; and see the answer. This proposes to liberate that engine into a <strong>shared physics kernel</strong> and give it two brand-new front doors: a <strong>Physics Lab</strong> where a human plays with the formulas in a live, media-rich, sketch-illustrated notebook, and a <strong>hosted MCP server</strong> where an agent calls the same formulas as tools. It is the difference between a museum where you watch, and a workshop where you build. Getting the honesty contract right — computed truth kept visibly distinct from generated illustration — is the make-or-break, because the moment we draw pretty pictures around numbers, we risk teaching fiction. That is a product decision, not a silent PR.</p></blockquote><hr><p>A STEM student opens <code>/lab</code>. Blank graph-paper canvas. She types a question into a card: <em>Tsiolkovsky — how much Δv does a mass ratio of 12 buy me at 350 s Isp?</em> A number appears — <strong>8.6 km/s</strong> — but so does a small hand-drawn-looking curve in the margin: Δv against mass ratio, her point marked with a circled dot, the vacuum-Isp of a real RL10 sketched in for scale. She drags the Isp slider; the curve redraws live and a pencil-style annotation updates: <em>&quot;+20 s Isp → +0.4 km/s here.&quot;</em> She pulls a second card and wires the <em>output orbit</em> of an ascent into a <em>transfer</em> — the canvas draws the parking orbit stretching into a heliocentric ellipse, periapsis circled, the Δv budget stacked as a little waterfall beside it. Nothing here is a cartoon: every line is integrated from the same equations <code>/fly</code> flies. She exports the whole page as a lab report and pastes it into her problem set. She never wrote a line of code.</p><h2 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The problem&quot;">​</a></h2><p>Orrery&#39;s physics is world-class and completely inaccessible as physics. It powers <code>/fly</code>, <code>/plan</code>, <code>/explore</code>, <code>/science</code> — but only along the rails those surfaces lay down. There is no way to:</p><ul><li><strong>Ask a free-form question</strong> — &quot;plot the entry corridor for Mars vs Titan,&quot; &quot;what mass ratio do I need for TLI,&quot; &quot;when is the next cheap Mars window.&quot; The numbers exist; the surface to pose the question does not.</li><li><strong>See the working, drawn.</strong> <code>/plan</code> draws one thing (the porkchop). The other twenty formulas produce numbers with no picture. A number without a diagram teaches far less than the same number <em>with</em> the transfer ellipse, the force diagram, the loss waterfall drawn around it.</li><li><strong>Reach the engine from outside.</strong> A developer, a researcher, or an agent cannot call <code>solveLambert</code> or <code>integrateAscent</code> without cloning the repo and booting SvelteKit. The contributor promise (PA §audiences) stops at the app&#39;s edge.</li></ul><p>The scientific base is a library trapped inside a building. This opens two doors.</p><h2 id="north-star" tabindex="-1">North star <a class="header-anchor" href="#north-star" aria-label="Permalink to &quot;North star&quot;">​</a></h2><p>Two references, held simultaneously:</p><ul><li><strong>An astrophysicist&#39;s notebook</strong> — the real thing: graph paper, integrals worked in the margin, a quick sketch of the geometry next to every result, the page a <em>record of thinking</em>, not a form with outputs. Feynman&#39;s diagrams were doodles that happened to be exact. That is the register: <strong>warm, hand-made, and rigorously true at the same time.</strong></li><li><strong>A live scientific instrument</strong> — every figure is computed, reproducible, exportable, and correct to the fidelity we honestly claim. The bar for the <em>look</em> is the same bar <code>/fly</code> holds for cinematics and <code>/posters</code> holds for print: this must feel like Orrery, top to bottom, not a bolted-on calculator.</li></ul><p>The tension between those two — hand-drawn warmth vs instrument-grade truth — is the whole design problem, and §&quot;The honesty line&quot; is how we resolve it.</p><h2 id="the-two-front-doors" tabindex="-1">The two front doors <a class="header-anchor" href="#the-two-front-doors" aria-label="Permalink to &quot;The two front doors&quot;">​</a></h2><p><strong>Door 1 — the Physics Lab (<code>/lab</code>, human-facing).</strong> A free-form canvas/notebook where each formula in the kernel is a card: inputs become live controls, outputs become numbers <em>and</em> auto-drawn figures. Cards can be compared, branched, and — the payoff — <strong>wired together</strong> into a full authored mission worksheet (ascent → injection → transfer → capture → descent), the whole pipeline drawn as one annotated storyboard. It is <code>/fly</code>&#39;s physics, authored by the user instead of scripted.</p><p><strong>Door 2 — the MCP server (<code>mcp.</code> subdomain, agent-facing).</strong> The same kernel, exposed as authenticated tools any MCP client (Claude.ai, Claude Code, third parties) can call over the network. An agent asks a natural-language question; the server runs the real formulas and returns structured results — including the <em>same figure descriptions</em> the Lab renders. This is the contributor promise extended from &quot;fork the repo&quot; to &quot;call it from anywhere,&quot; and it is where the Lab&#39;s own natural-language &quot;ask&quot; box eventually gets its brain.</p><p>Both doors open onto one room: the shared kernel. Neither reimplements physics.</p><h3 id="the-lab-is-a-subsystem-—-one-card-model-three-views" tabindex="-1">The Lab is a subsystem — one card model, three views <a class="header-anchor" href="#the-lab-is-a-subsystem-—-one-card-model-three-views" aria-label="Permalink to &quot;The Lab is a subsystem — one card model, three views&quot;">​</a></h3><p>Door 1 is not a single screen; it is a small subsystem built on one atom. The atom is a <strong>card</strong>: a formula instance = <em>(which formula, its bound inputs, any wiring to other cards, and the <code>FigureSpec</code> it emits)</em>. A card is portable data, so the same card renders in three views that match three distinct intents:</p><ul><li><strong>Canvas</strong> — <em>prototyping.</em> A spatial graph on the teal grid where cards are dragged freely and an output socket wires into another card&#39;s input. Ephemeral, exploratory, no narrative — the whiteboard.</li><li><strong>Notebook</strong> — <em>keeping.</em> An ordered, narrated sequence of cards; the durable, exportable artifact you&#39;re actually building. A card here can reference the result of the card above it.</li><li><strong>Focus</strong> — <em>drilling in.</em> One card, full-screen: big figure + full control rail + the <code>/science</code> derivation. A lens callable from either workspace, not a workspace itself.</li></ul><p>Cards move between views: you <strong>promote</strong> a card (or a wired subgraph) from Canvas into a Notebook. One model, three windows — this is what makes the Lab a subsystem rather than three routes. The genuinely novel/expensive parts are localized and named honestly: the Canvas <strong>wiring/graph engine</strong>, <strong>card serialization/portability</strong> across views and sessions, and the fact that a spatial Canvas has <strong>no honest mobile story</strong> (it degrades to read-only on phones). Those costs are why the sequence below front-loads the Notebook, not the Canvas.</p><h2 id="the-media-driven-layer-—-figures-that-draw-themselves" tabindex="-1">The media-driven layer — figures that draw themselves <a class="header-anchor" href="#the-media-driven-layer-—-figures-that-draw-themselves" aria-label="Permalink to &quot;The media-driven layer — figures that draw themselves&quot;">​</a></h2><p>The feature that makes the Lab <em>feel</em> like a notebook rather than a calculator: <strong>results auto-generate diagrams.</strong> Every kernel result carries a declarative description of what should be drawn (a <code>FigureSpec</code>), and the Lab renders it. The catalogue, all computed from pure functions, all free:</p><ul><li>transfer ellipse (departure / arrival / periapsis marked) · porkchop heatmap · Δv-budget waterfall · rocket free-body force diagram at altitude · ascent profile triptych (altitude · velocity · dynamic-pressure vs time) · EDL entry corridor per body · orbit / ground-track · sky chart (alt-az) · Tsiolkovsky sensitivity curve · cislunar ECI trajectory.</li></ul><p>Type a parameter, get a publication-grade annotated figure instantly. On top of that exact layer sits an optional <strong>illustrative layer</strong> — generative sketches and margin art (via the existing image pipeline) and grounded natural-language annotations that read like a physicist&#39;s notes — for warmth and shareability.</p><h2 id="the-honesty-line-make-or-break" tabindex="-1">The honesty line (make-or-break) <a class="header-anchor" href="#the-honesty-line-make-or-break" aria-label="Permalink to &quot;The honesty line (make-or-break)&quot;">​</a></h2><p>The moment we draw art around numbers, we can accidentally present fiction as fact — a violation of the deepest promise (PA §&quot;real physics, not approximations that mislead&quot;; §&quot;attribution is design&quot;; §&quot;fail honestly&quot;). The resolution has three parts:</p><ol><li><strong>Computed ≠ illustrated, always visibly.</strong> Exact figures (integrated from the kernel) and generative/illustrative art occupy visually and semantically distinct registers, and every artifact is labelled with its provenance — same discipline as the image pipeline and <code>/science</code> fact-checking.</li><li><strong>The &quot;sketchy-but-exact&quot; sweet spot.</strong> The default notebook aesthetic — graph paper, hand-annotated vectors, margin doodles — is rendered as <strong>deterministic SVG styled to look hand-drawn.</strong> The warmth is styling; the content is computed. This gets the notebook feel without generative cost, latency, or honesty risk. Generative art is a clearly-labelled layer <em>on top</em>, never on the data path.</li><li><strong>One authoritative ephemeris, honestly labelled.</strong> The kernel holds two planet-position models of different fidelity (a fast <code>/fly</code> approximation and a JPL-accurate solve). The Lab and MCP expose the <strong>accurate</strong> one as authoritative; the approximation, if exposed at all, is labelled <code>fast</code> with a stated error bound. And when the physics says <em>no</em> — Lambert finds no window, TWR &lt; 1, not enough Δv — the Lab <strong>explains the failure</strong>, it does not hide it (PA §&quot;fail honestly&quot;).</li></ol><h2 id="who-it-serves" tabindex="-1">Who it serves <a class="header-anchor" href="#who-it-serves" aria-label="Permalink to &quot;Who it serves&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Audience</th><th>What the Lab / MCP gives them</th></tr></thead><tbody><tr><td><strong>STEM student</strong></td><td>Real vis-viva / Lambert / Tsiolkovsky numbers <em>with</em> the figure drawn, verifiable, exportable into coursework. The tool that turns &quot;I know the Hohmann transfer in principle&quot; into &quot;I&#39;ve watched it respond to my inputs.&quot;</td></tr><tr><td><strong>Curious learner</strong></td><td>A playground: sweep a slider, watch the trajectory bend. Understanding through play, not a textbook.</td></tr><tr><td><strong>The contributor / developer</strong></td><td>The kernel as a real library + an MCP endpoint — call the physics from a script, a notebook, or an agent, without cloning the app.</td></tr><tr><td><strong>The agent</strong></td><td>Typed, authenticated physics tools over the network — the app&#39;s engine as an agent capability.</td></tr></tbody></table><h2 id="the-tier-ladder-all-of-it-ships-in-v0-9-—-see-amendment-01-a01-5-a01-7" tabindex="-1">The tier ladder (ALL of it ships in v0.9 — see Amendment 01 A01.5/A01.7) <a class="header-anchor" href="#the-tier-ladder-all-of-it-ships-in-v0-9-—-see-amendment-01-a01-5-a01-7" aria-label="Permalink to &quot;The tier ladder (ALL of it ships in v0.9 — see Amendment 01 A01.5/A01.7)&quot;">​</a></h2><ul><li><strong>T0 — parameterized figures.</strong> Every formula gets sliders → a live figure. (Generalizes <code>/plan</code>&#39;s porkchop to the whole kernel.)</li><li><strong>T1 — the notebook canvas.</strong> Linked cards, scenario comparison, export with Orrery&#39;s placard styling.</li><li><strong>T2 — composition.</strong> Wire formulas into a full authored mission worksheet, drawn as one storyboard.</li><li><strong>T3 — the media layer.</strong> Labelled generative sketches + grounded margin-notes; shareable lab-report artifacts.</li><li><strong>T4 — agentic.</strong> A natural-language &quot;ask&quot; box; the MCP server drives the kernel, the Lab renders. The loop closes.</li></ul><p><strong>SUPERSEDED by Amendment 01 (operator &quot;all of it,&quot; 2026-08-29):</strong> the original plan shipped only T0–T1 in v1 and deferred T2/T3/T4. <strong>v0.9 now ships ALL tiers T0–T4 + the MCP (OAuth, gated beta).</strong> The <em>build order</em> still starts with the card model + Notebook + Focus (T0–T1, mobile-safe, de-risks the kernel in-process), then Canvas + wiring + promote (T2), then the generative media layer (T3, always in the labelled register), then the agentic ask-box (T4) — but all of them land in v0.9, not as a roadmap. Sequencing ≠ deferral. Details: A01.5 (goal ladder), A01.7 (T4 ask-box + MCP access model).</p><h2 id="why-now" tabindex="-1">Why now <a class="header-anchor" href="#why-now" aria-label="Permalink to &quot;Why now&quot;">​</a></h2><p>The engine is done and, per the 2026-08-08 inventory, already ~80% pure and unit-tested (ADR-030&#39;s discipline spread repo-wide). <code>/plan</code> already proves the &quot;formula → live figure&quot; loop with the porkchop. The image pipeline, the placard-overlay aesthetic, and the <code>lavish</code> artifact export all exist. The gap between &quot;we have the physics&quot; and &quot;you can play with the physics&quot; is now mostly packaging and one beautiful surface.</p><h2 id="success-looks-like" tabindex="-1">Success looks like <a class="header-anchor" href="#success-looks-like" aria-label="Permalink to &quot;Success looks like&quot;">​</a></h2><ul><li>A student computes a Δv budget in <code>/lab</code>, exports the drawn figure into a problem set, and it&#39;s <em>right</em>.</li><li>A learner drags the Isp slider and watches the sensitivity curve bend, and gets <em>why</em> staging matters without being told.</li><li>A developer points Claude at <code>mcp.orrerylearn.com</code> and asks for a 2033 Mars window, and gets a real Lambert answer with a figure.</li><li>Someone screenshots a <code>/lab</code> page and it is unmistakably Orrery — the notebook looks as considered as <code>/fly</code> looks cinematic.</li><li>Nobody ever mistakes a generated sketch for a computed result, because the line is always visible.</li></ul><h2 id="non-goals-v1" tabindex="-1">Non-goals (v1) <a class="header-anchor" href="#non-goals-v1" aria-label="Permalink to &quot;Non-goals (v1)&quot;">​</a></h2><ul><li><strong>Not a full CAS / Jupyter.</strong> No arbitrary user code execution; the palette is the kernel&#39;s vetted formulas, not a Python sandbox.</li><li><strong>No new physics.</strong> <s>The Lab exposes today&#39;s fidelity, honestly labelled — it does not add N-body, CFD, or higher-precision propagation.</s> <strong>SUPERSEDED by Amendment 01 (§below):</strong> the Lab <em>does</em> add a <strong>foundational classical-mechanics domain</strong> (F=ma, weight, momentum, energy) that the existing spaceflight formulas rest on. It still does <strong>not</strong> add N-body, CFD, or higher-precision propagation — the fidelity ceiling is unchanged; only the <em>floor</em> is extended downward to first principles.</li><li><s><strong>Generative art is not v1-gating.</strong></s> <strong>SUPERSEDED by Amendment 01 (operator: &quot;all of it&quot;).</strong> The generative illustrative layer (T3) ships in v0.9 — but <em>sequenced after</em> the deterministic figures and always in the labelled <code>illustration</code> register (never on the data path). Not gating; still in scope.</li><li><s><strong>The agentic &quot;ask&quot; box (T4) is not v1.</strong></s> <strong>SUPERSEDED — T4 ships in v0.9.</strong> It rides on the MCP server + the goal-aware registry, so it lands <em>after</em> those, but it is in scope.</li><li><s><strong>MCP public multi-user auth is not v1.</strong></s> <strong>SUPERSEDED — OAuth ships in v0.9.</strong> Still <em>sequenced</em> bearer-token private beta first, then public OAuth 2.1 — both in v0.9.</li><li><strong>Still genuinely out of scope:</strong> a full CAS / arbitrary code execution (above); npm publication of the kernel; and any <em>new numerical-fidelity</em> physics (N-body/CFD/higher-precision) — the catalogue grows with closed-form formulas only.</li></ul><p><em>The look &amp; feel — notebook metaphor, the sketchy-but-exact figure language, layout, controls, export — is specified in the forthcoming <strong>UXS</strong> (visual-anchored on real screenshots, signed off before build). The technical architecture — the kernel package boundary, the <code>FigureSpec</code> contract, MCP hosting/auth, and the data strategy — is specified in <strong>RFC-037</strong>.</em></p><hr><h1 id="amendment-01-2026-08-29-—-first-principles-the-milestone-ladder-full-coverage" tabindex="-1">Amendment 01 (2026-08-29) — first principles, the milestone ladder, full coverage <a class="header-anchor" href="#amendment-01-2026-08-29-—-first-principles-the-milestone-ladder-full-coverage" aria-label="Permalink to &quot;Amendment 01 (2026-08-29) — first principles, the milestone ladder, full coverage&quot;">​</a></h1><blockquote><p>Status · Draft, operator-approved shape (session 2026-08-29) · Pending adversarial review (advisor / Fable-5) before slicing · Sources · this session&#39;s design dialogue; kernel/<code>/science</code> capability audit · Companion notes · <code>docs/wip/2026-08-29-kernel-scope-expansion-classical-mechanics.md</code>, <code>docs/wip/2026-08-29-s1-kernel-boundary-manifest.md</code> · RFC delta · RFC-037 Amendment 01</p></blockquote><p>This amendment widens PRD-033 from <em>&quot;liberate the existing spaceflight engine&quot;</em> to <em>&quot;a first-principles physics learning environment that exposes the <strong>entire</strong> built physics library as a ladder of mission goals.&quot;</em> The original body (above) stands; this refines the <strong>audience</strong>, the <strong>pedagogy</strong>, the <strong>scope</strong> (one non-goal reversed), and the <strong>organizing model</strong>.</p><h2 id="a01-1-·-the-audience-widens-—-the-first-principles-learner" tabindex="-1">A01.1 · The audience widens — the first-principles learner <a class="header-anchor" href="#a01-1-·-the-audience-widens-—-the-first-principles-learner" aria-label="Permalink to &quot;A01.1 · The audience widens — the first-principles learner&quot;">​</a></h2><p>Add a first-class audience to the table in §&quot;Who it serves&quot;: <strong>the learner building up from zero</strong> — a curious high-schooler / early-undergrad who wants to understand <em>all of it</em> in one place, <strong>from first principles</strong>. Not &quot;I already know the Hohmann transfer, show me the numbers&quot; (the existing STEM-student framing) but <em>&quot;I don&#39;t yet know why a rocket leaves the ground — teach me, starting from why a ball falls.&quot;</em> Musk/Tesla framing: you reason up from bedrock (gravity pulls down, thrust pushes up, thrust must beat weight), never down from an answer. The emotional target: a learner finishes a goal and says <em>&quot;I understand how this works, fundamentally&quot;</em> — not &quot;I got the right number.&quot;</p><h2 id="a01-2-·-the-organizing-model-—-goals-not-a-formula-palette" tabindex="-1">A01.2 · The organizing model — goals, not a formula palette <a class="header-anchor" href="#a01-2-·-the-organizing-model-—-goals-not-a-formula-palette" aria-label="Permalink to &quot;A01.2 · The organizing model — goals, not a formula palette&quot;">​</a></h2><p>The Lab is not a grid of formulas you browse; it is a <strong>ladder of mission-goal milestones</strong>, each a scenario that lines up the physics and <em>adds new slices on top of the last</em>, so that <strong>completing the goals = understanding all of spaceflight.</strong> Each milestone is simultaneously three things — this triple identity is the core of the design:</p><ol><li><strong>A curriculum</strong> — the learner&#39;s ordered path through the goal.</li><li><strong>A validation scenario</strong> — the S2.5 &quot;workbench coverage&quot; end-to-end exercise of the kernel.</li><li><strong>An engineering anchor</strong> — the acceptance scenario that proves a slice actually works.</li></ol><p><strong>First principles compound:</strong> the foundation introduced in M1 (F=ma, gravity, thrust, momentum) <em>reappears</em> at every later milestone — landing is thrust-vs-gravity again, a gravity assist is momentum exchange again, an orbit is centripetal force again. Six escalating situations, four principles paying off — that is the pedagogy, not six unrelated lessons.</p><h2 id="a01-3-·-full-coverage-—-the-complete-goal-set-two-families" tabindex="-1">A01.3 · Full coverage — the complete goal set (two families) <a class="header-anchor" href="#a01-3-·-full-coverage-—-the-complete-goal-set-two-families" aria-label="Permalink to &quot;A01.3 · Full coverage — the complete goal set (two families)&quot;">​</a></h2><p>&quot;Full coverage&quot; is a hard requirement: <strong>every computable capability already built into the kernel must have a home in some goal.</strong> The audit (kernel exports + the authored <code>/science</code> topic taxonomy) yields two families:</p><p><strong>Family A — the spaceflight spine (&quot;get to space&quot;):</strong> M1 Launch a rocket → orbit · M2 Get to the Moon · M3 Land on the Moon (airless) · M4 Get to Mars · M5 Land on Mars (atmospheric EDL) · <strong>M-return Come home</strong> (deorbit → heat shield → splashdown) · M6 Leave the solar system (escape + gravity assist). <em>&quot;Land on any world&quot;</em> generalizes M3/M5 across <strong>every body <code>descent-physics</code> supports</strong> — atmospheric (Earth/Mars/Venus/Titan), airless (Moon/Mercury), <strong>gas-giant probe</strong> (Jupiter, pressure-end), and <strong>micro-g small bodies</strong> (comet 67P, Ryugu/Bennu/Eros/Itokawa). Full coverage (below) means <em>each</em> gets a pulling goal — the micro-g case is thrust-vs-gravity at 10⁻⁴ g, on-brand for the compounding first principles (Fable-5 round-2 NEW-1).</p><blockquote><p><strong>M-return added post-Fable-5 (MAJOR 5).</strong> <code>descent-physics</code> explicitly supports Earth as <em>the</em> return body (LEO deorbit → heat-shield + parachutes → splashdown) and <code>transEarthCoast</code> exists — yet the first draft&#39;s ladder launched, transferred, and landed <em>elsewhere</em> but nobody ever came home. A first-principles learner expects &quot;how did Apollo get home / why the heat shield,&quot; and it is the emotional mirror of M3/M5. Full coverage now includes it.</p></blockquote><p><strong>Family B — observe &amp; orbit (&quot;understand the sky&quot;):</strong> G7 Observe the sky (where/when to see planets) · <strong>G8 Moon phases</strong> <em>(ships — <code>moonPhase</code> is a built capability; <strong>eclipse geometry is an optional budgeted extension</strong>, new closed-form physics the kernel lacks — Fable-5 MINOR 4 — decided at goal-enumeration, not a full-coverage requirement since it isn&#39;t a built capability)</em> · G9 Track a satellite / catch the ISS · G10 Choose an orbit (LEO/GEO/SSO regimes).</p><p><strong>Cross-cutting:</strong> &quot;How a rocket engine works&quot; (the 8 <code>/science/propulsion</code> sub-topics — cycles, fuels, Isp, TWR; feeds M1) · &quot;Scales &amp; time&quot; (light-time, AU, sidereal day).</p><p><strong>Explicitly out of the Lab&#39;s computable scope</strong> (honest boundary — part of full coverage, not a gap): the editorial <code>/science</code> domains <code>cosmology</code>, <code>exoplanets</code>, <code>life-in-space</code>, <code>history</code>, <code>reading-list</code>, <code>watch-list</code> are <em>context content, not computable physics.</em> The Lab teaches what the kernel <strong>computes</strong>; these remain <code>/science</code> reading.</p><h2 id="a01-4-·-scope-change-—-a-foundational-classical-mechanics-kernel-domain" tabindex="-1">A01.4 · Scope change — a foundational classical-mechanics kernel domain <a class="header-anchor" href="#a01-4-·-scope-change-—-a-foundational-classical-mechanics-kernel-domain" aria-label="Permalink to &quot;A01.4 · Scope change — a foundational classical-mechanics kernel domain&quot;">​</a></h2><p>The one reversed non-goal (see the struck line in §Non-goals). The Lab adds a <strong>new pure kernel subdomain</strong> for classical mechanics — the primitives the spaceflight formulas already rest on but never exposed: kinematics (free-fall, projectile), dynamics (F=ma, weight, drag), momentum, energy &amp; work, gravitation. This is <strong>new physics</strong> relative to the original PRD and is adopted deliberately.</p><p><strong>The honest scope, corrected post-Fable-5 (BLOCKER 2).</strong> The first draft framed this as <em>&quot;only the floor extends downward; the ceiling is unchanged.&quot;</em> That is internally false — the milestone ladder also needs <strong>bounded new astrodynamics <em>sideways</em></strong> at higher rungs: SOI/patched-conic radius (M2), ignition-altitude/hover-thrust (M3), gravity-assist v∞-rotation</p><ul><li>Oberth (M6 — assists today are <em>authored waypoints, not computed</em>), eclipse geometry (G8). So the real scope is: <strong>new physics is added both downward (mechanics floor) and sideways (per-milestone astrodynamics), each a small named closed-form formula, budgeted per milestone and pulled demand-driven</strong> (RFC-037 A01.2b/A01.3). What stays out of bounds is unchanged: <strong>no N-body, CFD, or higher-precision <em>numerical propagation</em></strong> — the fidelity ceiling holds; what grows is the catalogue of exact/closed-form formulas.</li></ul><p><strong>Compose honestly (Fable-5 MAJOR 4).</strong> &quot;A drop through Titan is <em>the same</em> EDL integrator&quot; was wrong — a drop from rest is the integrator&#39;s singular case (it divides by velocity), and Titan isn&#39;t in the gravity table cited. The real reuse is the per-body helpers (<code>bodyGravity</code>/<code>bodyAirDensity</code>) under a purpose-built free-fall integrator; the <em>insight</em> (general mechanics and spaceflight are the same physics at different altitudes) holds, the <em>implementation claim</em> was corrected. Taxonomy + depth tiers: companion note; the &quot;too complex&quot; cut (friction, collisions, rotational, fluids, thermo) is <strong>demand-driven — nothing lands until a goal pulls it</strong> (RFC-037 A01.3), which is where the cut actually falls.</p><h2 id="a01-5-·-mvp-the-entire-ladder-operator-2026-08-29-—-corrects-an-earlier-misread" tabindex="-1">A01.5 · MVP = the ENTIRE ladder (operator, 2026-08-29 — corrects an earlier misread) <a class="header-anchor" href="#a01-5-·-mvp-the-entire-ladder-operator-2026-08-29-—-corrects-an-earlier-misread" aria-label="Permalink to &quot;A01.5 · MVP = the ENTIRE ladder (operator, 2026-08-29 — corrects an earlier misread)&quot;">​</a></h2><p><strong>The v0.9 MVP is the whole stacked ladder — every goal in both families, plus the kernel, the mechanics domain, the Lab views, and the MCP server. There is NO subset.</strong> &quot;Everything that respects this PRD + RFC-037 is the MVP.&quot; v1.0 is where the operator adds <em>even more</em>, beyond what these docs describe. (An earlier draft of this section wrongly said &quot;MVP = M1 only, rest is roadmap&quot; — that was a misunderstanding, now reversed. M1 is the first thing <strong>built</strong>, not the boundary of what <strong>ships</strong>.)</p><p><strong>Full coverage is therefore the definition of done, not an aspiration:</strong> every computable capability in the kernel — including gas-giant-probe and micro-g small-body descent (A01.3) — gets a pulling goal, because we ship every goal. This is the hard requirement RFC-037 A01.3&#39;s coverage ledger enforces.</p><p><strong>Build order still starts at M1, and additivity is still the architectural bet.</strong> M1 is built first because it proves the machine (kernel → registry → FigureSpec → Notebook → workbench validation) end to end on one goal. The bet — <em>each subsequent milestone is scenario-authoring + a budgeted bit of new physics, with no structural rebuild</em> — is then <strong>validated live at the first additivity checkpoint, M2</strong> (Fable-5 MAJOR 1): if M2 needs a <em>contract</em> change rather than just new <code>FormulaDef</code>s + <code>FigureSpec</code> kinds, the architecture failed and we learn it after one milestone, not ten. Every milestone from M1 through M-return and Family B ships in v0.9; the re-slicing step sizes the full ladder.</p><p>Progression UI stays <strong>minimum</strong> (no skill-tree); each goal&#39;s critical path lives as <em>data</em>, rendered as a plain authored Notebook.</p><h2 id="a01-6-·-the-natural-language-ask-t4-—-reaffirmed-as-the-payoff-still-not-v1" tabindex="-1">A01.6 · The natural-language &quot;ask&quot; (T4) — reaffirmed as the payoff, still not v1 <a class="header-anchor" href="#a01-6-·-the-natural-language-ask-t4-—-reaffirmed-as-the-payoff-still-not-v1" aria-label="Permalink to &quot;A01.6 · The natural-language &quot;ask&quot; (T4) — reaffirmed as the payoff, still not v1&quot;">​</a></h2><p><em>&quot;Explain what it takes to launch a rocket&quot;</em> typed in plain language, answered by selecting goal formulas, computing, and drawing — is <strong>T4</strong>, an upper layer over the MCP + the goal-aware registry. Unchanged from the original ladder: it lands after the MCP proves the kernel to strangers. The milestone/goal model is precisely what gives a future NL layer a structured target to resolve a human goal against.</p>',68)])])}const p=t(s,[["render",n]]);export{g as __pageData,p as default};
