import{_ as t,o,c as s,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"PRD-035 — Mobile offline: right-sized assets + a downloadable corpus","description":"","frontmatter":{},"headers":[],"relativePath":"prd/PRD-035.md","filePath":"prd/PRD-035.md"}'),r={name:"prd/PRD-035.md"};function i(n,e,l,d,h,p){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="prd-035-—-mobile-offline-right-sized-assets-a-downloadable-corpus" tabindex="-1">PRD-035 — Mobile offline: right-sized assets + a downloadable corpus <a class="header-anchor" href="#prd-035-—-mobile-offline-right-sized-assets-a-downloadable-corpus" aria-label="Permalink to &quot;PRD-035 — Mobile offline: right-sized assets + a downloadable corpus&quot;">​</a></h1><p><em>Status: Draft · 2026-08-25 · Implemented by: RFC-040 · target: v0.8 · Tracking: Part 1 = #482 (mobile image rung), Part 2 = filed after Part 1 lands (offline download)</em></p><blockquote><p><strong>One-liner.</strong> The mobile app should (a) only ever fetch phone-sized imagery, and (b) let the user pre-download the app for offline use — in a Basic or Full tier — from Settings.</p></blockquote><blockquote><p>Why this is a PRD · Orrery&#39;s reach depends on being usable where a curious person actually is — on a plane, in a classroom with weak wifi, on a metered connection. Today the mobile app silently over-fetches desktop-grade imagery a phone can&#39;t show, and goes dark the moment connectivity does. Making the app right-sized and deliberately downloadable turns &quot;needs a good connection&quot; into &quot;works anywhere you chose to take it&quot; — a change in who can use Orrery and when, not just a size optimization.</p></blockquote><h2 id="_1-·-the-problem-measured-2026-08-25" tabindex="-1">1 · The problem (measured, 2026-08-25) <a class="header-anchor" href="#_1-·-the-problem-measured-2026-08-25" aria-label="Permalink to &quot;1 · The problem (measured, 2026-08-25)&quot;">​</a></h2><p>Two findings from an asset audit, both real:</p><p><strong>A. Mobile over-fetches on a small, specific set — a targeted pipeline gap.</strong> The app has a responsive <code>srcset</code> ladder (<code>{id}-{width}.webp</code> rungs + a <code>.1x1.jpg</code> 512px thumb + an unsuffixed base = the largest). The browser picks the right rung per device. The <strong>manifest-measured</strong> reality (<code>scripts/mobile/measure-mobile-corpus.mjs</code>, 2026-08-25): <strong>3146 images are already mobile-OK</strong> (a rung ≤1280px is available). The genuine over-serve was <strong>141 images / 33 MB</strong> — deep-sky (13.7), launch-ground (8.3), essays (6.6), missions/gemini (4.9) — with no rung ≤1280, so a phone fetched the full base.</p><blockquote><p><strong>Scope correction.</strong> An earlier framing (this PRD&#39;s first draft) said &quot;the ladder covers only 859/3089 images&quot; and named galleries/missions/hotspots as the problem. That was a <strong>file-count artifact</strong> — an image whose base is already ≤1280 needs no separate rung <em>file</em>, so counting <code>-1280.webp</code> files massively undercounts what srcset already handles. The manifest (what the browser actually uses) shows galleries/missions/anatomy are already right-sized. <strong>hotspots (185 MB) are NOT over-serve either</strong> — the surface-scene tier dispatcher gates on projected screen px, so mobile already loads lower tiers per zoom. The real gap was the 141 above.</p></blockquote><p><strong>B. No offline mode.</strong> The mobile app is deliberately ~66 MB (bundle) and <strong>streams</strong> the heavy set (images 1.1 GB, audio 97 MB, 4K textures, non-en locales) from the CDN on demand. With no connectivity — a flight — the streamed content simply isn&#39;t there and the review/browse experience breaks. Operator hit this on a plane (2026-08).</p><p><strong>Corpus sizes (source, measured):</strong> images 1.1 GB (fleet-galleries 406 · missions 202 · hotspots 188 · anatomy 160 · rest ~150) · audio 97 MB · 4K textures ~40 MB prune-able. Crucially, the 1.1 GB is the <strong>whole ladder at every rung</strong> (base .webp alone = 512 MB, mostly desktop-only rungs). A phone displays one rung per image.</p><h2 id="_2-·-why-the-two-parts-are-one-initiative" tabindex="-1">2 · Why the two parts are one initiative <a class="header-anchor" href="#_2-·-why-the-two-parts-are-one-initiative" aria-label="Permalink to &quot;2 · Why the two parts are one initiative&quot;">​</a></h2><p>Offline depends on right-sizing. &quot;Download everything&quot; naively = ~1.3 GB of desktop-res a phone can&#39;t use. Once every image has a phone rung, the <strong>whole corpus at mobile resolution is ~300–450 MB</strong>, and offline tiers become sane. So Part 1 is the prerequisite for Part 2. They ship as two GitHub issues (Part 1 first), one RFC (RFC-040), one PRD (this).</p><h2 id="_3-·-part-1-—-right-sized-mobile-assets-482-—-done" tabindex="-1">3 · Part 1 — right-sized mobile assets (#482 — DONE) <a class="header-anchor" href="#_3-·-part-1-—-right-sized-mobile-assets-482-—-done" aria-label="Permalink to &quot;3 · Part 1 — right-sized mobile assets (#482 — DONE)&quot;">​</a></h2><p><strong>Goal:</strong> every displayed image has a phone-appropriate variant, so mobile never fetches a desktop rung — online or in the offline cache.</p><p>Shipped (#482, commit <code>5e83ca872a</code>):</p><ul><li><code>scripts/mobile/measure-mobile-corpus.mjs</code> — the reproducible audit (over-serve per bucket + the mobile-res corpus total, ~325 MB, which feeds Part 2&#39;s tier budget).</li><li><code>scripts/mobile/add-mobile-rung.mjs</code> — generated the ≤1280 rung for the <strong>79 webp-base</strong> over-serve images (essays 53 + missions/gemini 26) by downscaling the served base (no masters / only LFS stubs; one lanczos step avoids the full-corpus-regen degrade the guards prevent). Additive, idempotent, diff-gated, zero base images touched. <strong>Deferred:</strong> the 62 <code>.jpg</code>-base over-serve images (deep-sky, launch-ground, ~22 MB) can&#39;t join the webp-only ladder without a <code>.webp</code>-base format conversion first — a separate follow-up, not a ladder hack. So the live over-serve fix covers ~11.5 MB now; the <code>.jpg</code> ~22 MB remains until that conversion.</li><li><code>image-srcset.test.ts</code> invariant: every ladder entry has a rung ≤1280 (regression guard). <strong>Over-serve now 0.</strong></li></ul><p><strong>Non-goals (Part 1):</strong> changing desktop imagery; re-encoding already-right-sized buckets (galleries/missions/anatomy already are); capping hotspots (the tier dispatcher already handles mobile); the offline download itself.</p><h2 id="_4-·-part-2-—-the-offline-download-two-tiers-from-settings" tabindex="-1">4 · Part 2 — the offline download (two tiers, from Settings) <a class="header-anchor" href="#_4-·-part-2-—-the-offline-download-two-tiers-from-settings" aria-label="Permalink to &quot;4 · Part 2 — the offline download (two tiers, from Settings)&quot;">​</a></h2><p><strong>Goal:</strong> the user can pre-download the app for offline use, choosing how much.</p><p>Two tiers (operator decision — keep it simple):</p><table tabindex="0"><thead><tr><th>Tier</th><th>Contents</th><th>Est. size (mobile-res)</th></tr></thead><tbody><tr><td><strong>Basic</strong></td><td>Core browse: planets, moon/mars sites, science, essays, deep-sky, posters, modules, satellites, small bodies — at the mobile rung. Fully usable offline: browse the solar system, missions overview, read all content.</td><td>~150–250 MB</td></tr><tr><td><strong>Full</strong></td><td>Everything: all galleries + surface hotspots + audio tours (97 MB) + 4K textures. The whole corpus for a long flight.</td><td>~400–550 MB</td></tr></tbody></table><ul><li>Lives in the <strong>Settings panel</strong> (<code>QualitySettingsModal.svelte</code>, the nav ⚙) — a new &quot;Offline&quot; section, mobile-only (<code>Capacitor.isNativePlatform()</code>).</li><li>States: not-downloaded → downloading (progress + size) → downloaded (with &quot;Update&quot; + &quot;Remove offline data&quot;). Wi-Fi-only by default with an explicit cellular override; show the size before starting.</li><li>Default is unchanged: the app stays ~66 MB and streams on demand. Offline is strictly opt-in.</li></ul><p><strong>Success:</strong> on a fresh device with the chosen tier downloaded, airplane-mode browse works for that tier&#39;s surfaces with no missing imagery/audio.</p><p><strong>Non-goals (Part 2):</strong> offline for the <em>web</em> PWA (mobile-app only for now); partial/per-section downloads (just the two tiers); background/auto download.</p><h2 id="_5-·-open-decisions-locked-here" tabindex="-1">5 · Open decisions (locked here) <a class="header-anchor" href="#_5-·-open-decisions-locked-here" aria-label="Permalink to &quot;5 · Open decisions (locked here)&quot;">​</a></h2><ul><li><strong>Tiers:</strong> two — Basic / Full. Tours + 4K fold into Full (not separate toggles).</li><li><strong>Surface:</strong> the existing Settings panel, not a new route.</li><li><strong>Platform:</strong> mobile app only (iOS + Android).</li><li><strong>Part 1 scope:</strong> in — it&#39;s the prerequisite and fixes a live over-fetch.</li></ul><p>Architecture (persistence, manifests, width targets, Wi-Fi guard, storage budget) → RFC-040.</p>',27)])])}const u=t(r,[["render",i]]);export{g as __pageData,u as default};
