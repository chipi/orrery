import{_ as t,o,c as a,a2 as i}from"./chunks/framework.CpZKh-tG.js";const m=JSON.parse('{"title":"/fly Architectural Review — June 2026 Baseline","description":"","frontmatter":{},"headers":[],"relativePath":"reference/fly-architectural-review.md","filePath":"reference/fly-architectural-review.md"}'),s={name:"reference/fly-architectural-review.md"};function r(l,e,n,c,d,h){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="fly-architectural-review-—-june-2026-baseline" tabindex="-1"><code>/fly</code> Architectural Review — June 2026 Baseline <a class="header-anchor" href="#fly-architectural-review-—-june-2026-baseline" aria-label="Permalink to &quot;`/fly` Architectural Review — June 2026 Baseline&quot;">​</a></h1><p>A health check on the heliocentric + cislunar /fly subsystem before scaling out the iconic-shot pipeline across the remaining mission corpus. Captures what&#39;s good, what&#39;s at risk, and where the next round of investment pays off most. Paired with <code>fly-cinematic-state-machine.md</code> (the runtime spec) and <code>fly-manual-test-checklist.md</code> (the manual smoke list).</p><p><em>Snapshot date: 2026-06-12. Reviewed against commit <code>f8b472bdb</code>. Re-run the Explore inventory before re-baselining.</em></p><hr><h2 id="tl-dr" tabindex="-1">Tl;dr <a class="header-anchor" href="#tl-dr" aria-label="Permalink to &quot;Tl;dr&quot;">​</a></h2><p><strong>Healthy on math, light on orchestration.</strong> Pure-math layers (<code>flyby-camera-plan</code>, <code>fly-cinematic-beats</code>, <code>mission-arc</code>, <code>fly-mission-apply</code>, cislunar / interplanetary geometry) are cleanly separated from Three.js, exhaustively unit-tested (289 cases across 18 files), and reusable. <strong>The 9,517-line <code>+page.svelte</code> carries five distinct responsibilities, has 51 <code>$state</code> declarations + 19 <code>$effect</code> blocks, and zero behavioural test coverage above the JS-helper level.</strong> The iconic-shot pipeline is the most recently touched corner and the most likely to regress on the next /fly tweak; A1/A2/A3 just locked the math invariants but the integration surface (camera lerp + chrome suppression + Saturn-OI tilt + peakHold timing) is still e2e-untested.</p><p>Top three follow-ups, in leverage order:</p><ol><li><strong>Split <code>+page.svelte</code> along the five responsibility lines</strong> (post-#332 §3 /fly effect-ladder consolidation). The five chunks are well-bounded already; making them files would shrink the merge-conflict surface <ul><li>let the animate loop be tested in isolation.</li></ul></li><li><strong>Re-enable + stabilise <code>fly-iconic-peakhold.spec.ts</code></strong> (currently <code>test.fixme</code>). The math is locked; the missing layer is the timing- sensitive cinematic suppression behaviour. Cruise-hold timing race is the only blocker.</li><li><strong>Profile the animate loop</strong> (3 long-cruise missions × 1 flyby cinema × 1 cislunar lock). Today&#39;s code is allocation-free at the math layer but has never been measured under the dual-mode camera lerp.</li></ol><hr><h2 id="what-s-good" tabindex="-1">What&#39;s good <a class="header-anchor" href="#what-s-good" aria-label="Permalink to &quot;What&#39;s good&quot;">​</a></h2><h3 id="pure-math-layers-are-exemplary" tabindex="-1">Pure math layers are exemplary <a class="header-anchor" href="#pure-math-layers-are-exemplary" aria-label="Permalink to &quot;Pure math layers are exemplary&quot;">​</a></h3><ul><li><code>mission-arc</code>, <code>fly-mission-apply</code>, <code>fly-cinematic-beats</code>, <code>flyby-camera-plan</code>, <code>cislunar-geometry</code>, <code>interplanetary-geometry</code>, <code>jump-to-met-bias</code> — none import Three.js, the DOM, or each other in ways that cycle. The Explore audit confirms a clean DAG: every helper&#39;s transitive imports land at <code>lambert-grid.constants</code> or Three.js types only.</li><li>289 colocated unit tests across 18 files. Highest-density modules: <code>mission-arc</code> (35), <code>fly-mission-apply</code> (35), <code>cislunar-geometry</code> (31), <code>fly-cinematic-beats</code> (30).</li><li>The iconic-shot composition just got its own pure-math beachhead at <code>$lib/orbital/</code> with 13 + 9 + 8 new tests landing in this session (classify-shot baseline + jump-to-met-bias + arming round-trip).</li><li>The 2D <code>FlybyDebugViewer</code> consumes the same <code>planFlybyShot()</code> the 3D scene does — dual-use math, no drift possible.</li></ul><h3 id="test-coverage-at-the-math-layer-≈-best-in-class-for-this-codebase" tabindex="-1">Test coverage at the math layer ≈ best-in-class for this codebase <a class="header-anchor" href="#test-coverage-at-the-math-layer-≈-best-in-class-for-this-codebase" aria-label="Permalink to &quot;Test coverage at the math layer ≈ best-in-class for this codebase&quot;">​</a></h3><p>For comparison: <code>src/lib/orbital/</code> already exceeds the per-file test density of <code>src/lib/quality/</code>, <code>src/lib/launches/sources/</code>, or <code>src/lib/three/</code>. Future tweaks to camera math, peakHold timing, or mission-arc geometry land against a thick safety net. <strong>This is the floor the next polish wave can build on.</strong></p><h3 id="naming-file-organisation-tells-the-story" tabindex="-1">Naming + file organisation tells the story <a class="header-anchor" href="#naming-file-organisation-tells-the-story" aria-label="Permalink to &quot;Naming + file organisation tells the story&quot;">​</a></h3><p>The <code>fly-*</code> prefix consistently signals &quot;/fly-specific helper&quot; without leaking outside the route. <code>cislunar-*</code> is shared with <code>/moon</code> (Apollo flight path) but the API surface is the same. <code>interplanetary-*</code> is shared with <code>/missions</code> cards. Cross-route reuse without cross-route coupling.</p><hr><h2 id="what-s-at-risk" tabindex="-1">What&#39;s at risk <a class="header-anchor" href="#what-s-at-risk" aria-label="Permalink to &quot;What&#39;s at risk&quot;">​</a></h2><h3 id="page-svelte-is-doing-five-jobs-in-one-9-517-line-file" tabindex="-1"><code>+page.svelte</code> is doing five jobs in one 9,517-line file <a class="header-anchor" href="#page-svelte-is-doing-five-jobs-in-one-9-517-line-file" aria-label="Permalink to &quot;`+page.svelte` is doing five jobs in one 9,517-line file&quot;">​</a></h3><p>The Explore audit found five distinct concerns inside the entry-point:</p><ol><li><strong>Reactive state management</strong> — 51 <code>$state</code> declarations + 19 <code>$effect</code> blocks. The reactive graph has no documented invariants beyond what the Svelte compiler enforces.</li><li><strong>Scene initialisation &amp; lifecycle</strong> — calls <code>buildHelioScene</code> + <code>buildCislunarScene</code>, wires the quality tier, sets up the route lifecycle. ~500 lines.</li><li><strong>Real-time animation loop</strong> — lines ~2900–3600 (the 700-line <code>createAnimateLoop()</code> closure) + 800 lines of camera lerp math right after it. <strong>This is the largest single chunk and the highest- risk surface.</strong> Owns sim time advancement, phase detection, cinematic beats, dual-mode camera interpolation.</li><li><strong>Mission loading pathway</strong> — lines ~6800–7500. <code>applyMissionAsLoaded</code> / <code>applyScenarioAsLoaded</code> callsites, timeline reset, arc rebuild, cinematic state reset.</li><li><strong>User interaction</strong> — mouse orbit/pan, scroll scrub, keyboard shortcuts, HUD toggles. ~800 lines.</li></ol><p>The 700-line animate loop in particular is the bottleneck. It owns the sim-day gate, the camera lerp, the chrome suppression, the Saturn-OI roll, the FD banner timing, the cinematic peak/afterglow/cruise hold dispatch, and the per-frame <code>__flyDebug</code> mirror. Every cinematic behaviour we care about flows through it.</p><p><strong>Why it&#39;s a risk:</strong> any future tweak to one concern (e.g. quality-tier downgrade should cap camera lerp speed) involves reading 700 lines of unrelated logic to know it&#39;s safe. Post-#332 §3 (the deferred effect- ladder consolidation) called this out specifically: <em>&quot;Needs careful cinematic-timing analysis so consolidation doesn&#39;t break the iconic-shot / flyby choreography landed on main during the #332 rebase window.&quot;</em></p><h3 id="zero-behavioural-test-coverage-above-the-unit-layer" tabindex="-1">Zero behavioural test coverage above the unit layer <a class="header-anchor" href="#zero-behavioural-test-coverage-above-the-unit-layer" aria-label="Permalink to &quot;Zero behavioural test coverage above the unit layer&quot;">​</a></h3><ul><li><code>fly-iconic-peakhold.spec.ts</code> (just added) — <code>test.fixme</code> for cruise- hold timing race.</li><li><code>fly-apollo11-phase-markers.spec.ts</code> — covers phase-marker projection <ul><li>clickability, not iconic-shot composition or chrome suppression.</li></ul></li><li><code>fly-cislunar.spec.ts</code>, <code>fly-no-cislunar.spec.ts</code> — cover route loading + 2D mode toggle, not animation behaviour.</li><li><code>fly-render-validation.spec.ts</code> — counts Three.js material disposals; not behaviour.</li></ul><p>So if a future /fly refactor breaks peakHold arming, milestone-overlay suppression, Saturn-OI tilt, jumpToMet bias, or the v2 camera composition, the only safety net is the unit tests on the helpers — which won&#39;t catch &quot;the helper is correct but never called from the loop&quot;.</p><h3 id="performance-is-unverified" tabindex="-1">Performance is unverified <a class="header-anchor" href="#performance-is-unverified" aria-label="Permalink to &quot;Performance is unverified&quot;">​</a></h3><ul><li>The animate loop runs every <code>requestAnimationFrame</code> tick. No profiling marks. The cinematic state machine has no <code>performance.mark/measure</code> instrumentation despite owning sim-time control.</li><li><code>frame-monitor.ts</code> measures total frame time, not per-concern breakdown. A 16 ms budget overrun could be the camera lerp, the trajectory tube vertex update, the milestone-overlay re-render, the <code>__flyDebug</code> write, or the FD banner reveal — no way to tell from the existing telemetry.</li><li><code>quality-tier.ts</code> switches LOD but nothing inside the animate loop reads it to throttle work. If we downgrade to low-tier the loop still runs the same camera-math + cinematic dispatch + mission-arc sample every frame.</li></ul><h3 id="mission-dest-ts-ships-with-zero-tests" tabindex="-1"><code>mission-dest.ts</code> ships with zero tests <a class="header-anchor" href="#mission-dest-ts-ships-with-zero-tests" aria-label="Permalink to &quot;`mission-dest.ts` ships with zero tests&quot;">​</a></h3><p>The file is small (2.5 K), pure data formatting, and (per Explore) has 0 <code>it(...)</code> cases despite being colocated with a <code>.test.ts</code>. Probably not a regression risk in practice but it&#39;s the one outlier in the otherwise-uniform test-density picture and worth a 10-minute pass to either fill in or delete the test stub.</p><h3 id="the-cinematic-state-machine-has-hidden-invariants-the-test-suite-doesn-t-enforce" tabindex="-1">The cinematic state machine has hidden invariants the test suite doesn&#39;t enforce <a class="header-anchor" href="#the-cinematic-state-machine-has-hidden-invariants-the-test-suite-doesn-t-enforce" aria-label="Permalink to &quot;The cinematic state machine has hidden invariants the test suite doesn&#39;t enforce&quot;">​</a></h3><p><code>docs/reference/fly-cinematic-state-machine.md</code> documented seven invariants the iconic-shot pipeline depends on:</p><ul><li><code>isPlaying = false</code> after every <code>jumpToMet</code></li><li><code>jumpToMet</code> biases flyby targets onto <code>peakMet − 2</code></li><li><code>jumpToMet</code> clears <code>cine.peakHoldArmedForFlybyMet</code> + <code>peakHoldUntil</code></li><li><code>parseFlybyMetFromSubPhase</code> is the sole &quot;current flyby&quot; source</li><li><code>PLANET_COMPOSITION</code> is the only per-planet tuning surface</li><li>Camera target is the planet center, not the ship (v2)</li><li>Saturn-OI camera.up roll is gated on <code>saturnOIComposition</code> and reverts to <code>(0,1,0)</code> outside Saturn-OI</li></ul><p>A1/A2/A3 just locked invariants 2 and 3. The other five are still unenforced — a future refactor can quietly break any of them with no test signal.</p><hr><h2 id="recommended-follow-ups-leverage-ranked" tabindex="-1">Recommended follow-ups (leverage-ranked) <a class="header-anchor" href="#recommended-follow-ups-leverage-ranked" aria-label="Permalink to &quot;Recommended follow-ups (leverage-ranked)&quot;">​</a></h2><h3 id="high-leverage" tabindex="-1">High leverage <a class="header-anchor" href="#high-leverage" aria-label="Permalink to &quot;High leverage&quot;">​</a></h3><ol><li><p><strong>Split <code>+page.svelte</code> along the five responsibility lines.</strong></p><ul><li><code>src/lib/three/fly-animate-loop.ts</code> (~1500 lines once camera + lerp + cinematic dispatch land together) — the orchestration.</li><li><code>src/lib/fly-page-state.svelte.ts</code> — the 51 <code>$state</code> declarations <ul><li>19 <code>$effect</code> blocks as a typed state object.</li></ul></li><li><code>src/lib/three/fly-camera-controller.ts</code> — the camera lerp + spherical-coord arithmetic + auto-zoom.</li><li><code>src/routes/fly/+page.svelte</code> shrinks to: imports + mount/unmount wiring + template (HUD + CAPCOM + scrubber + overlays). 1500-2000 lines.</li><li><strong>Why now:</strong> the iconic-shot work just landed. The state machine doc captures the invariants. Splitting now means the next polish wave has a clean target instead of a 9,517-line one.</li></ul></li><li><p><strong>Re-enable <code>fly-iconic-peakhold.spec.ts</code> after stabilising the cruise-hold race.</strong> Three concrete approaches in the commit message — pick (b) suppress cruise-hold under <code>reducedMotion</code> (cleanest; preview build can pass <code>emulateMedia({reducedMotion:&#39;reduce&#39;})</code> and the spec just works). Net: 8 e2e tests for the load-bearing visual behaviour, recoverable in ~2 hours.</p></li><li><p><strong>Add unit tests for the five remaining cinematic-state-machine invariants.</strong> Specifically:</p><ul><li><code>jumpToMet</code> must always call <code>isPlaying = false</code> (assert on the state mutation after a synthetic jump).</li><li><code>parseFlybyMetFromSubPhase</code> is called from a single site in the animate loop (single-source assertion via grep, not runtime — a <code>vitest</code> test that reads the source file and counts call sites).</li><li><code>PLANET_COMPOSITION</code> is the only place pitch/camR/lead live (same grep-test pattern: any other <code>pitchRad = </code> or <code>camRMultiplier = </code> in /fly source is a regression).</li><li>Saturn-OI flag clears outside the cinema window (already locked by the existing fly-cinematic-beats test for <code>isAnyCinematicFreeze</code> but worth a dedicated test). These take an hour. Cheap insurance.</li></ul></li></ol><h3 id="medium-leverage" tabindex="-1">Medium leverage <a class="header-anchor" href="#medium-leverage" aria-label="Permalink to &quot;Medium leverage&quot;">​</a></h3><ol start="4"><li><p><strong>Instrument the animate loop with <code>performance.mark/measure</code> for the three biggest per-frame costs</strong>: camera lerp, mission-arc sample, cinematic dispatch. Wire the measurements through <code>frame-monitor.ts</code> so the existing struggle-detector can attribute over-budget frames. Five lines per concern, one-shot.</p></li><li><p><strong>Make <code>quality-tier.ts</code> actually throttle work inside the loop.</strong> At &quot;low&quot; tier today: same frame work, less LOD. At &quot;low&quot; tier should be: skip the milestone-overlay projection every other frame, skip the FD banner reveal animation, fall back from per-frame <code>__flyDebug</code> write to every-other-frame. Add an explicit <code>frameTier</code> integer to the cine state and gate on it.</p></li><li><p><strong>Visual regression baseline for one frozen frame per planet.</strong><code>playwright</code>&#39;s screenshot assertion with <code>mask: [.hud-stack, .capcom-panel]</code> plus a tight <code>pixelDiff</code> threshold. Caught the eye- regressions that <code>classifyShot</code> can&#39;t (e.g. Neptune-too-dark). Needs the cruise-hold race fix first (so it can be reproducible).</p></li></ol><h3 id="lower-leverage-good-when-there-s-slack" tabindex="-1">Lower leverage (good when there&#39;s slack) <a class="header-anchor" href="#lower-leverage-good-when-there-s-slack" aria-label="Permalink to &quot;Lower leverage (good when there&#39;s slack)&quot;">​</a></h3><ol start="7"><li><p><strong>Audit <code>mission-dest.ts</code></strong> — either fill in tests or delete the stub. 10 minutes.</p></li><li><p><strong>Move <code>mission-arc.ts</code> + <code>orbital.ts</code> + <code>fly-physics.ts</code> under <code>$lib/orbital/</code></strong> per the post-#332 §1 vision. The names hint at different concerns but they&#39;re all pure orbital math. Consolidating makes the post-#327 data-layer work easier to plan against and surfaces the natural <code>$lib/orbital/index.ts</code> barrel as the public API.</p></li><li><p><strong>Audit <code>fly-updaters.ts</code></strong> — currently type contracts only, no implementation. Either move the closures out of <code>+page.svelte</code> (so the file becomes real, with tests) or delete it.</p></li></ol><hr><h2 id="performance-budget-—-what-we-know-don-t-know" tabindex="-1">Performance budget — what we know / don&#39;t know <a class="header-anchor" href="#performance-budget-—-what-we-know-don-t-know" aria-label="Permalink to &quot;Performance budget — what we know / don&#39;t know&quot;">​</a></h2><p>What we know:</p><ul><li><code>planFlybyShot</code> is allocation-free (~30 multiplies / call, two trig).</li><li><code>projectToCameraFrame</code> is allocation-free.</li><li>Cinematic predicates (<code>isPeakHolding</code>, <code>isAfterglowing</code>, <code>isCruiseHolding</code>, <code>isFinaleLocked</code>) are O(1) and free.</li><li>The mission-arc Kepler sampler hits the trajectory spline cache for every animate frame (~150 frames at 60 Hz during peakHold) — not measured.</li><li><code>__flyDebug</code> write is gated on DEV and gets stripped in prod.</li></ul><p>What we don&#39;t know:</p><ul><li>Camera lerp cost. The dual-mode spherical-coord conversion runs every frame in both helio and cislunar modes.</li><li>Three.js scene churn during the animate loop (mesh visibility flips, material opacity tweens, sprite scale lerps).</li><li>Chrome suppression cost — the <code>inCinematicHeldBeat</code> flag is read by six <code>class:cinematic-hidden</code> bindings; Svelte 5&#39;s reactive graph should handle this efficiently but it&#39;s not profiled.</li><li>Mobile budget. Everything above is desktop-chromium.</li></ul><p>A 30-minute pass with <code>performance.mark</code> would surface 80% of the unknowns.</p><hr><h2 id="where-the-baseline-should-re-baseline" tabindex="-1">Where the baseline should re-baseline <a class="header-anchor" href="#where-the-baseline-should-re-baseline" aria-label="Permalink to &quot;Where the baseline should re-baseline&quot;">​</a></h2><p>Re-run this review when:</p><ul><li><code>+page.svelte</code> drops below 5,000 lines (post-split).</li><li>The Playwright iconic-shot spec hits ≥ 8 green planets.</li><li>The animate loop has at least one perf instrument shipped.</li><li>Any of the five remaining state-machine invariants gets test coverage.</li></ul><p>Each of those moves the floor up. Don&#39;t re-baseline before two of them land — the picture stays roughly the same.</p><hr><p><em>Orrery · docs/reference/fly-architectural-review.md · June 2026 · paired with <a href="./fly-cinematic-state-machine">fly-cinematic-state-machine.md</a> and <a href="./fly-manual-test-checklist">fly-manual-test-checklist.md</a>.</em></p>',56)])])}const p=t(s,[["render",r]]);export{m as __pageData,p as default};
