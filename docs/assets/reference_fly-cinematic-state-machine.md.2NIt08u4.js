import{_ as i,o as s,c as a,a2 as t}from"./chunks/framework.CpZKh-tG.js";const k=JSON.parse('{"title":"/fly Cinematic State Machine","description":"","frontmatter":{},"headers":[],"relativePath":"reference/fly-cinematic-state-machine.md","filePath":"reference/fly-cinematic-state-machine.md"}'),n={name:"reference/fly-cinematic-state-machine.md"};function o(r,e,l,d,h,c){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="fly-cinematic-state-machine" tabindex="-1"><code>/fly</code> Cinematic State Machine <a class="header-anchor" href="#fly-cinematic-state-machine" aria-label="Permalink to &quot;\`/fly\` Cinematic State Machine&quot;">​</a></h1><p>Reference spec for the heliocentric flyby + EDL/OI cinema engine. Captures every hidden invariant the iconic-shot composition relies on, so the <strong>deferred /fly effect-ladder consolidation</strong> (post-#332 punch-list item §3) can be planned and executed without regressing the on-screen result.</p><p>Most of these invariants are not enforced by tests today — the §Test coverage gaps section at the bottom lists what should be locked down before the next consolidator touches the file.</p><p><em>Last verified against <code>f48c76abf</code> → current. June 2026.</em></p><hr><h2 id="what-the-cinema-does" tabindex="-1">What the cinema does <a class="header-anchor" href="#what-the-cinema-does" aria-label="Permalink to &quot;What the cinema does&quot;">​</a></h2><p>When the spacecraft is within ±60 days before / +30 days after a <code>flyby</code> or <code>edl_or_oi</code> event in <code>mission.flight.events</code>, the heliocentric scene enters <strong>flyby cinema</strong>. The camera converges onto a planet-dominated Cassini-mission- art composition, freezes for 2.5 s (4 s on Earth), then dolly-recedes through a 6 s afterglow before returning to cruise. Chrome (HUD, CAPCOM, scrubber, phase markers, milestone label, trajectory tubes, bottom strips) hides during the held + afterglow window so the iconic frame reads as pure mission art.</p><p>This is implemented as one <code>requestAnimationFrame</code> loop driving a small fixed set of flags + lerp targets. There is no proper finite-state-machine library under the hood; the state is reconstructed implicitly from the relative timings of <code>performance.now()</code> vs four <code>cine.*</code> deadlines + four sub-phase strings.</p><hr><h2 id="the-four-held-deadlines" tabindex="-1">The four held deadlines <a class="header-anchor" href="#the-four-held-deadlines" aria-label="Permalink to &quot;The four held deadlines&quot;">​</a></h2><p>All live in <code>cine</code>, a typed <code>CinematicBeatState</code> from <code>$lib/fly-cinematic-beats.ts</code>. Each is a <code>performance.now()</code> millisecond value; “held” means <code>now &lt; deadline</code>.</p><table tabindex="0"><thead><tr><th>Flag</th><th>Set by</th><th>Cleared by</th><th>What it freezes</th><th>Default duration</th></tr></thead><tbody><tr><td><code>cine.peakHoldUntil</code></td><td>First animate frame inside the ±0.5d iconic-window</td><td>Expiry of <code>now</code></td><td>Sim time + camera lerp + chrome</td><td>2500 ms (4000 ms on Earth)</td></tr><tr><td><code>cine.afterglowUntil</code></td><td>Frame after <code>peakHoldUntil</code> expires</td><td>Expiry of <code>now</code></td><td>Sim time + chrome; camera dollies</td><td>6000 ms</td></tr><tr><td><code>cine.cruiseHoldUntil</code></td><td>One-shot when the active sub-phase enters <code>cruise-out</code> first time</td><td>Expiry of <code>now</code></td><td>Sim time + chrome</td><td>4000 ms</td></tr><tr><td><code>cine.finaleStartedAt</code></td><td>When the trajectory&#39;s last waypoint passes</td><td><code>CINEMATIC_TIMINGS.FINALE_DURATION_MS</code> after</td><td>Sim time + chrome</td><td>8000 ms</td></tr></tbody></table><p>The composite <code>isCinematicFreeze</code> = <code>isPeakHoldingFrame || isAfterglowing || isCruiseHolding</code>. <strong>Sim time only advances when <code>!isCinematicFreeze</code> and a few other gates</strong> (player not paused, past <code>launchDwellUntil</code>, not dragging the scrubber).</p><p><code>inCinematicHeldBeat</code> = <code>isPeakHoldingFrame || isAfterglowing || isCruiseHolding || finaleActive</code>. Drives the <code>.cinematic-hidden</code> class on chrome.</p><hr><h2 id="peakhold-arming" tabindex="-1">peakHold arming <a class="header-anchor" href="#peakhold-arming" aria-label="Permalink to &quot;peakHold arming&quot;">​</a></h2><p><code>peakHoldUntil</code> is <strong>not</strong> triggered by entering the flyby sub-phase. It is triggered by reaching <code>simDay ∈ [peakMet − 2 ± 0.5 d]</code>. That window is the &quot;iconic moment&quot; — 2 days before closest approach, where the trajectory data&#39;s +y lift on the ship glyph is maximised relative to the planet&#39;s xz position so the ship reads as an off-pole accent against the disc.</p><p>Arming logic, every animate frame:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> currentFrameFlybyMet</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> parseFlybyMetFromSubPhase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(lastHelioSubPhase);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// reset: scrubbed off the flyby OR drifted &gt;4 d from peak → re-arm-able</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (cine.peakHoldArmedForFlybyMet </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sameFlyby</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> currentFrameFlybyMet </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cine.peakHoldArmedForFlybyMet;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> outsidePeakDays</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">abs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(simDay </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (arcTimeline.dep_day </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cine.peakHoldArmedForFlybyMet)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> FLYBY_PEAK_DAYS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sameFlyby </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> outsidePeakDays) cine.peakHoldArmedForFlybyMet </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// arm: inside ±0.5d window AND not already armed for this flyby</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">isMoonMission </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">reducedMotion </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">isDrag </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> currentFrameFlybyMet </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> iconicPeakSimDay</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> arcTimeline.dep_day </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> currentFrameFlybyMet </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> inHeldWindow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">abs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(simDay </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> iconicPeakSimDay) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (inHeldWindow </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cine.peakHoldArmedForFlybyMet </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> currentFrameFlybyMet) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cine.peakHoldUntil </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> now </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (isEarthHold </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4000</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> :</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cine.peakHoldArmedForFlybyMet </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> currentFrameFlybyMet;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="invariants-the-arming-logic-depends-on-load-bearing-—-don-t-break" tabindex="-1">Invariants the arming logic depends on (load-bearing — don&#39;t break) <a class="header-anchor" href="#invariants-the-arming-logic-depends-on-load-bearing-—-don-t-break" aria-label="Permalink to &quot;Invariants the arming logic depends on (load-bearing — don&#39;t break)&quot;">​</a></h3><ul><li><strong><code>isPlaying = false</code> after every <code>jumpToMet</code> call</strong>. Otherwise sim advances past the ±0.5d window before the next animate frame can arm.</li><li><strong><code>jumpToMet</code> for a flyby event biases the target onto <code>peakMet − 2</code></strong>. Direct jumps to <code>peakMet</code> skip the window entirely (the user clicks &quot;Venus #1&quot; once, no freeze fires; this was the bug fixed in the post-v2-math commit).</li><li><strong><code>jumpToMet</code> clears <code>cine.peakHoldArmedForFlybyMet</code> + <code>cine.peakHoldUntil</code></strong>. Without this, re-clicking the same flyby button finds the prior arm still valid and never re-fires the hold.</li><li><strong><code>parseFlybyMetFromSubPhase</code> is the only authoritative source for &quot;current flyby&quot;</strong>. <code>__flyDebug.activeFlybyMet</code> is stale outside the flyby window and was the original source of the reset bug.</li></ul><hr><h2 id="the-peakhold-camera-composition" tabindex="-1">The peakHold camera composition <a class="header-anchor" href="#the-peakhold-camera-composition" aria-label="Permalink to &quot;The peakHold camera composition&quot;">​</a></h2><p>When peakHold is armed, the camera lerp <strong>freezes</strong> at whatever framing the prior animate frame asked for. The framing is computed once per frame by:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> plan</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> planFlybyShot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  planetId: flyby.id,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  planetPos: { x: bodyScene.x, z: bodyScene.z },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  planetRadius: flyby.size,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  shipPosAtMet,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  peakMet: activeFlybyMet,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>planFlybyShot</code> lives in <code>$lib/orbital/flyby-camera-plan.ts</code> and is <strong>the single source of truth for camera position + target during a flyby</strong>. The /fly cinema block converts its (cameraPos, cameraTarget) Vec3 pair into the scene&#39;s existing spherical-coords format <code>(R, P, T)</code> around <code>cameraTarget</code>. Saturn-OI is the only planet that gets a post-process: a 17° <code>camera.up</code> roll applied inline at the render block, driven by the <code>saturnOIComposition</code> flag.</p><h3 id="composition-invariants" tabindex="-1">Composition invariants <a class="header-anchor" href="#composition-invariants" aria-label="Permalink to &quot;Composition invariants&quot;">​</a></h3><ul><li><strong><code>PLANET_COMPOSITION[planetId]</code> is the only place pitch / camR / lead / side angle live</strong>. The pre-v2 inline <code>targetP = isSaturnOI ? 1.25 : ...</code> override was absorbed into <code>PLANET_COMPOSITION.saturn.pitchRad: 0.32</code>. Resist temptation to add new inline pitch overrides — extend <code>PLANET_COMPOSITION</code> instead, otherwise the 2D <code>FlybyDebugViewer</code> mockup diverges from the 3D scene and the iteration loop is broken.</li><li><strong>Camera target is the PLANET center</strong> for <code>targetBias = 0</code>, not the ship. v1 anchored the camera to the ship&#39;s position; the trajectory data clusters ship.xz around planet.xz for ±many days near peak, so a ship-anchored camera kept ship + planet collinear from the camera POV. v2 fixed this.</li><li><strong>The Saturn-OI <code>camera.up.set(sin(0.3), cos(0.3), 0)</code> roll is purely aesthetic</strong>, not part of <code>planFlybyShot</code>. Reverting to <code>(0, 1, 0)</code> outside Saturn-OI is gated on the <code>saturnOIComposition</code> flag. If you remove the flag during a refactor, the roll persists forever.</li></ul><hr><h2 id="chrome-suppression" tabindex="-1">Chrome suppression <a class="header-anchor" href="#chrome-suppression" aria-label="Permalink to &quot;Chrome suppression&quot;">​</a></h2><p>Five overlays / panels gate on <code>!inCinematicHeldBeat</code>:</p><table tabindex="0"><thead><tr><th>Element</th><th>Source line ref</th></tr></thead><tbody><tr><td><code>.hud-stack</code> (left mission identity / nav / flight-params / systems)</td><td><code>class:cinematic-hidden={inCinematicHeldBeat}</code></td></tr><tr><td><code>.scrubber</code> (timeline + speed group)</td><td><code>class:cinematic-hidden={inCinematicHeldBeat}</code></td></tr><tr><td><code>.capcom-panel</code> (right anomaly / comms / events)</td><td><code>class:cinematic-hidden={inCinematicHeldBeat}</code></td></tr><tr><td><code>.fly-bottom-strips</code></td><td><code>class:cinematic-hidden={inCinematicHeldBeat}</code></td></tr><tr><td><code>.phase-markers-overlay</code> (milestone diamonds + labels)</td><td><code>{#if … &amp;&amp; !inCinematicHeldBeat}</code></td></tr><tr><td><code>.fd-phase-markers-overlay</code> (FlightDirector phase chips)</td><td><code>{#if … &amp;&amp; !inCinematicHeldBeat}</code></td></tr><tr><td><code>.milestone-overlay</code> (active flyby label text)</td><td><code>{#if … &amp;&amp; !inCinematicHeldBeat}</code></td></tr></tbody></table><p>Outside the cinema, <code>outLine</code> + <code>retLine</code> trajectory tubes are visibility-gated on a separate <code>inFlybyCinemaForLines</code> boolean (parsed from <code>lastHelioSubPhase</code>). That&#39;s distinct from <code>inCinematicHeldBeat</code> — tubes hide during the <strong>entire</strong> flyby sub-phase, not just the held + afterglow.</p><h3 id="chrome-invariants" tabindex="-1">Chrome invariants <a class="header-anchor" href="#chrome-invariants" aria-label="Permalink to &quot;Chrome invariants&quot;">​</a></h3><ul><li><strong><code>updateChromeSuppression</code> only writes <code>inCinematicHeldBeat</code> when the composite flag actually flips</strong>. The animate loop fires 60–144 Hz; setting the <code>$state</code> every frame would needlessly re-run every dependent <code>$effect</code>.</li><li><strong>The <code>.cinematic-hidden</code> class uses CSS <code>opacity</code> + <code>pointer-events: none</code>, not <code>display: none</code></strong>, so the Svelte reactive graph stays stable across the held window. Switching to <code>{#if}</code> would unmount + remount sub-trees, losing measured scroll positions + scrubber drag state.</li></ul><hr><h2 id="saturn-oi-tilt-wernquist-grand-finale" tabindex="-1">Saturn-OI tilt (Wernquist Grand Finale) <a class="header-anchor" href="#saturn-oi-tilt-wernquist-grand-finale" aria-label="Permalink to &quot;Saturn-OI tilt (Wernquist Grand Finale)&quot;">​</a></h2><p>Saturn orbit insertion gets one extra post-process: the camera&#39;s up vector rolls 17° (<code>saturnOIComposition</code> flag set at the cinema-block end, consumed in the render block).</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (saturnOIComposition) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ROLL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  camera.up.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ROLL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), Math.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cos</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ROLL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  camera.up.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Without explicit ring geometry, the roll reads as &quot;ring-plane-edge-on&quot; — Saturn&#39;s disc appears askew across the frame, evoking the Wernquist Grand Finale art.</p><h3 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h3><ul><li><strong>The flag is <code>true</code> only when <code>activeEvt.type === &#39;edl_or_oi&#39; &amp;&amp; flyby.id === &#39;saturn&#39;</code></strong>. Cleared the moment the cinema block exits.</li><li><strong>The roll is applied AFTER <code>camera.position.set(...)</code> and BEFORE <code>camera.lookAt(camTarget)</code></strong>. Reordering breaks the lookAt&#39;s frame construction.</li></ul><hr><h2 id="mission-data-invariants" tabindex="-1">Mission data invariants <a class="header-anchor" href="#mission-data-invariants" aria-label="Permalink to &quot;Mission data invariants&quot;">​</a></h2><p>The iconic-shot pipeline depends on these mission-data shapes; breaking them silently skips the freeze or composes for the wrong planet:</p><ul><li><strong><code>flight.events[].type</code> is one of <code>&#39;launch&#39; | &#39;flyby&#39; | &#39;edl_or_oi&#39; | &#39;arrival&#39; | &#39;free_return&#39; | &#39;orbit&#39; | &#39;tei_direct&#39;</code></strong>. Only <code>&#39;flyby&#39;</code> + <code>&#39;edl_or_oi&#39;</code> enter the cinema block. Cislunar event types (<code>&#39;free_return&#39;</code> etc.) are handled by a separate cislunar camera path.</li><li><strong><code>flight.events[].label</code> resolves the planet ID</strong> via <code>findFlybyPlanetFromLabel</code> (parses keywords like <code>&#39;Venus #1 — gravity assist&#39;</code>). When the label is missing, <code>findClosestPlanetToShip(sc.pos)</code> is the fallback — but the closest planet to the ship&#39;s heliocentric position at the flyby moment is <strong>not always</strong> the actual flyby target (Juno&#39;s MET 749 was closer to Mars than Earth heliocentrically). Labels are required to get this right.</li><li><strong><code>flight.events[].met_days</code> is a positive integer</strong>. Negative or missing values are filtered out of the cinema-trigger loop.</li><li><strong><code>mission.timeline.dep_day</code> exists and is finite</strong>. <code>simDay = dep_day + met_days</code> is the universal time-mapping.</li></ul><hr><h2 id="performance-budget" tabindex="-1">Performance budget <a class="header-anchor" href="#performance-budget" aria-label="Permalink to &quot;Performance budget&quot;">​</a></h2><p>The cinema engine is in the <code>requestAnimationFrame</code> loop and runs every frame. Performance-sensitive paths:</p><ul><li><strong><code>planFlybyShot</code> is called once per frame inside the flyby sub-phase</strong>. Currently ~30 multiplies, two <code>Math.sin/cos</code>, no allocations. Should stay allocation-free — putting <code>new Vec3()</code> inside it would re-trigger GC pressure during the 2.5 s hold (~150 frames at 60 Hz).</li><li><strong><code>projectToCameraFrame</code> is allocation-free</strong>. Used by <code>classifyShot</code> in the 2D viewer, not the 3D scene. If a future visual classifier runs every frame in the 3D path, the per-frame allocation cost (3 Vec3s per call) becomes ~9 µs/frame at 144 Hz — still fine, but worth noting.</li><li><strong><code>__flyDebug</code> is written once per frame under DEV only</strong>. Production build&#39;s tree-shaker drops the writes.</li><li><strong><code>__flyJumpToMet</code> is wired only under DEV</strong>. Same gating; no production payload increase.</li><li><strong>Chrome suppression flip is once per state transition</strong>, not every frame — see invariant above.</li></ul><p>The cinema engine has never been profiled with <code>performance.mark/measure</code>. A profile pass during a future quality-tier deepen would be useful but isn&#39;t load-bearing today.</p><hr><h2 id="test-coverage-gaps" tabindex="-1">Test coverage gaps <a class="header-anchor" href="#test-coverage-gaps" aria-label="Permalink to &quot;Test coverage gaps&quot;">​</a></h2><p>Today&#39;s automated coverage:</p><ul><li><strong>Unit (<code>vitest</code>)</strong>: 11 tests for <code>planFlybyShot</code> invariants (camR / pitch / target-bias / shipPos / side angle / velocity recovery).</li><li><strong>E2e (<code>playwright</code>)</strong>: zero direct tests of peakHold arming, chrome suppression, or iconic-shot composition. The closest is the manual-test checklist (<code>docs/reference/fly-manual-test-checklist.md</code>).</li></ul><p>The gaps below are listed in priority order. Each blocks a class of future refactors from being safe.</p><ol><li><p><strong>Unit: arming round-trip on synthetic timeline</strong>. Drive a <code>peakHoldUntil</code> arm → expire → re-arm cycle through a fake clock, asserting <code>peakHoldArmedForFlybyMet</code> transitions correctly on (jump to flyby → wait → jump elsewhere → jump back). Locks down the regression that re-clicking the same flyby button silently dropped the freeze.</p></li><li><p><strong>Unit: jumpToMet biases flyby targets correctly</strong>. Given a mission with a <code>flyby</code>-typed event at MET 193, <code>jumpToMet(193)</code> should leave <code>simDay = dep_day + 191</code>. Locks down the bias gate so a future &quot;I&#39;ll just clean up <code>jumpToMet</code>&quot; refactor doesn&#39;t undo it.</p></li><li><p><strong>Unit: <code>classifyShot</code> returns ICONIC for every Cassini beat at default composition</strong>. Sample the Cassini mission&#39;s <code>outPts</code> via the same <code>predictShipPosAtMet</code> the page uses, feed <code>planFlybyShot</code> for each of Venus #1 / Venus #2 / Earth / Jupiter / Saturn-OI, assert <code>classifyShot(...).isIconic === true</code>. The five frames we&#39;ve validated visually become the safety net.</p></li><li><p><strong>E2e: peakHold lights up on each flyby event click</strong>. For each labeled mission (Cassini, Galileo, Juno, JUICE, Voyager 2), click each milestone-track button, assert within 1 s:</p><ul><li><code>[data-testid=&quot;hud-stack&quot;].classList.contains(&#39;cinematic-hidden&#39;)</code></li><li><code>[data-testid=&quot;milestone-overlay&quot;]</code> absent from DOM</li><li><code>window.__flyDebug.peakHoldRemainingMs &gt; 1000</code></li></ul></li><li><p><strong>E2e: peakHold semantic frame is reasonable</strong>. At held moment, take an a11y-tree snapshot and assert the Cassini glyph&#39;s visible-name + the planet ID are both readable text on the page. Catches regressions where the v2 math identifies the wrong planet (Juno-Earth-as- Mars era).</p></li><li><p><strong>Unit: <code>findFlybyPlanetFromLabel</code> × every shipped mission label</strong>. Parse the entire <code>static/data/missions/**/*.json</code> corpus, assert that every <code>&#39;flyby&#39; | &#39;edl_or_oi&#39;</code>-typed event&#39;s <code>label</code> resolves to a known <code>PlanetId</code> via <code>findFlybyPlanetFromLabel</code>. Catches future label drift.</p></li><li><p><strong>Visual regression (<code>playwright snapshot</code>)</strong>. Per-platform baselines (mac vs CI Ubuntu) for one held frame per planet — Cassini&#39;s Venus #1 + Venus #2 + Earth + Jupiter + Saturn-OI. This is the only gap that would catch a stylistic regression where the math still ships ICONIC but the composition LOOKS different. Defer to after the test harness lands per <code>feedback_e2e_pitfalls</code>.</p></li></ol><hr><h2 id="maintainability-resilience" tabindex="-1">Maintainability + resilience <a class="header-anchor" href="#maintainability-resilience" aria-label="Permalink to &quot;Maintainability + resilience&quot;">​</a></h2><p>A short list of properties that the cinematic engine relies on holding, so a future refactor agent can vet their plan against them:</p><ul><li><strong><code>planFlybyShot</code> is pure</strong>. No I/O, no DOM, no Three.js. A change that needs Three.js geometry doesn&#39;t belong in <code>$lib/orbital/</code>.</li><li><strong><code>PLANET_COMPOSITION</code> is the only per-planet tuning surface</strong>.</li><li><strong>The <code>cine.*</code> deadlines are write-once-per-event</strong>. <code>peakHoldUntil</code> is set when entering the arming window, never re-extended. <code>afterglowUntil</code> is set when peakHold expires, never re-set. The animate loop assumes these are monotonic-until-expiry — re-setting them mid-window would retroactively change the user-perceived freeze duration.</li><li><strong><code>__flyJumpToMet</code> is DEV-only</strong>. The chrome-devtools-mcp test harness depends on it for missions whose events lack labels; production builds must not expose it.</li><li><strong><code>saturnOIComposition</code> is the only inline post-process on the camera</strong>. Adding a second planet-specific roll / FoV change / etc. should extend <code>PLANET_COMPOSITION</code> with optional <code>rollRad</code> / <code>fovDegOverride</code> fields rather than adding a new flag here.</li></ul><hr><h2 id="when-this-doc-goes-stale" tabindex="-1">When this doc goes stale <a class="header-anchor" href="#when-this-doc-goes-stale" aria-label="Permalink to &quot;When this doc goes stale&quot;">​</a></h2><p>The state machine evolves; treat this doc as the spec the <strong>next</strong> consolidator should reconcile against, not a permanent fixture. The principles to preserve:</p><ul><li>One source of truth per concept (composition: <code>PLANET_COMPOSITION</code>; timing deadlines: <code>cine.*</code>; held-flag: <code>inCinematicHeldBeat</code>).</li><li>Pure math separated from rendering (orbital-math files have no Three.js imports).</li><li>Every screen-visible cinematic invariant has at least one automated test by the time the §Test coverage gaps section is checked off.</li><li>Test the boundary, not the implementation: a freeze test should assert &quot;the camera holds at the iconic frame for at least 2 s&quot; rather than &quot;the <code>peakHoldUntil</code> field equals <code>now + 2500</code>&quot;. Refactors are free to rewrite the latter; only the former should be load-bearing.</li></ul><hr><p><em>Orrery · docs/reference/fly-cinematic-state-machine.md · June 2026 · paired with <code>docs/reference/fly-manual-test-checklist.md</code>.</em></p>`,67)])])}const g=i(n,[["render",o]]);export{k as __pageData,g as default};
