import{c as t,Q as s,j as a,m as n}from"./chunks/framework.CdiUUa3G.js";const u=JSON.parse('{"title":"RFC-007 — Multi-destination porkchop","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-007.md","filePath":"rfc/RFC-007.md"}'),o={name:"rfc/RFC-007.md"};function i(r,e,l,d,p,c){return s(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="rfc-007-—-multi-destination-porkchop" tabindex="-1">RFC-007 — Multi-destination porkchop <a class="header-anchor" href="#rfc-007-—-multi-destination-porkchop" aria-label="Permalink to &quot;RFC-007 — Multi-destination porkchop&quot;">​</a></h1><blockquote><p>Status · Decided · closed by ADR-026 (2026-04-29) TA anchor · §components/lambert-worker · §components/plan-screen Related PRD · PRD-002 (Mission Configurator) Related UXS · UXS-002 (Mission Configurator) Related ADRs · ADR-008 (Lambert worker), ADR-022 (Lambert protocol), ADR-023 (mobile magnifier), ADR-026 (this RFC&#39;s closure) Why this is an RFC · The current <code>/plan</code> screen hardcodes Earth → Mars trajectories. Extending to &quot;Earth → any planet&quot; with optional landing/flyby semantics is a genuine open question with three interlocking sub-decisions: (1) which destinations to support and how to handle gas giants where landing isn&#39;t physical; (2) how the landing-vs-flyby toggle changes the porkchop&#39;s ∆v computation and the FLY-this-mission semantics; (3) how the date ranges scale per destination since outer-planet transfers are years long. Each has multiple defensible options.</p></blockquote><h2 id="the-question" tabindex="-1">The question <a class="header-anchor" href="#the-question" aria-label="Permalink to &quot;The question&quot;">​</a></h2><p>The current porkchop plot answers one question: &quot;if I want to go from Earth to Mars between 2026 and 2030, when&#39;s the cheapest launch window?&quot; The user feedback: &quot;can we extend porkchop to support Earth to any planet, and one selects what planet they want to go and is it a landing or flyby?&quot;</p><p>This expansion changes three things simultaneously: the data model (per-destination Lambert grids), the UI (destination + mission-type selectors), and the physics (landing arrival ∆v vs flyby arrival ∆v differ substantially).</p><h2 id="use-cases" tabindex="-1">Use cases <a class="header-anchor" href="#use-cases" aria-label="Permalink to &quot;Use cases&quot;">​</a></h2><ul><li><strong>STEM student</strong> wants to see why a Voyager-class flyby of Jupiter is dramatically cheaper than a Galileo-style orbiter (no orbit-insertion burn).</li><li><strong>Curious learner</strong> wants to understand why the next Saturn launch window is &quot;in 12 years&quot; and the Mars window is &quot;in 26 months&quot; — by seeing both porkchops side by side or sequentially.</li><li><strong>STEM student</strong> wants to verify that the C3 (departure energy) for a Mars Hohmann ≈ 8.6 km²/s² matches what they computed from textbook values.</li><li>All audiences: trying to plan a mission to Pluto reveals why New Horizons used a Jupiter gravity assist (no direct trajectory in the porkchop&#39;s transfer-time range).</li></ul><h2 id="goals" tabindex="-1">Goals <a class="header-anchor" href="#goals" aria-label="Permalink to &quot;Goals&quot;">​</a></h2><ul><li>Each major destination gets its own porkchop computed at build time or on demand.</li><li>Mission-type toggle (LANDING vs FLYBY) changes the ∆v calculation transparently — same Lambert solver, different arrival-leg cost.</li><li>The educational moment from PRD-002 (delta-v as felt reality) extends to outer-planet missions where the realisation is &quot;this is unreachable with current launchers.&quot;</li><li>No regression for the existing Earth→Mars experience.</li></ul><h2 id="constraints" tabindex="-1">Constraints <a class="header-anchor" href="#constraints" aria-label="Permalink to &quot;Constraints&quot;">​</a></h2><ul><li><strong>ADR-008</strong> locks the Lambert solver in a Web Worker. Adding destinations means more grids, not a different solver.</li><li><strong>ADR-019</strong> locks ajv schema validation on PR. New per-destination data files must validate.</li><li><strong>Lambert solver runtime</strong> — Earth→Mars grid is ~200 ms on fast hardware, 1–3 s on slow. Gas-giant transfer-time ranges are 5–10× wider; naive expansion would make the grid intolerable on mobile.</li><li><strong>Mobile magnifier (ADR-023)</strong> assumes a 112×100 grid. Per-destination grid resolutions need to keep this assumption valid or the magnifier needs adjusting.</li></ul><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><h3 id="what-destinations-to-support" tabindex="-1">What destinations to support <a class="header-anchor" href="#what-destinations-to-support" aria-label="Permalink to &quot;What destinations to support&quot;">​</a></h3><p><strong>Option D-A — All eight planets.</strong> Mercury, Venus, Mars, Jupiter, Saturn, Uranus, Neptune (skip Earth as origin). Full coverage at the cost of 8× the data and 8× compute. Mercury and Venus need careful handling: their porkchops are narrow because of the inner-planet phasing.</p><p><strong>Option D-B — Inner + Jupiter + Saturn (5 destinations).</strong> Skip Uranus and Neptune as v1 of multi-destination — their transfer times (8–12 years) make porkchops practically unreadable at mobile-friendly grid resolution, and they&#39;re rarely targeted (Voyager 2 was the only mission). Keeps the data and compute scope manageable.</p><p><strong>Option D-C — Mars + Venus + Jupiter (3 destinations).</strong> Most-targeted destinations only. Smallest scope; least educational coverage.</p><h3 id="landing-vs-flyby-semantics" tabindex="-1">Landing vs flyby semantics <a class="header-anchor" href="#landing-vs-flyby-semantics" aria-label="Permalink to &quot;Landing vs flyby semantics&quot;">​</a></h3><p><strong>Option L-A — Toggle switches the displayed grid.</strong> Two grids per destination: <code>landing.json</code> (includes orbit-insertion ∆v) and <code>flyby.json</code> (Lambert solution only). Toggle swaps the active grid. Storage doubles per destination but compute is the same as today (each grid is one Lambert run with a different arrival-cost convention).</p><p><strong>Option L-B — Toggle adds a delta to the displayed grid.</strong> One Lambert grid per destination (the flyby cost). A separately-stored per-destination &quot;orbit insertion delta-v&quot; gets added on the client when LANDING is toggled. Smaller storage; arithmetic on the client. Less precise — orbit insertion ∆v varies with arrival velocity.</p><p><strong>Option L-C — Per-destination decision based on physical possibility.</strong> Mars/Venus/Mercury support both LANDING and FLYBY. Gas giants support only FLYBY (landing on Jupiter is undefined; orbit-insertion is its own ∆v). Jupiter LANDING resolves to &quot;Europa orbit insertion&quot; or similar (out of scope). Cleaner conceptually but more rules to encode.</p><h3 id="transfer-time-ranges-per-destination" tabindex="-1">Transfer-time ranges per destination <a class="header-anchor" href="#transfer-time-ranges-per-destination" aria-label="Permalink to &quot;Transfer-time ranges per destination&quot;">​</a></h3><p><strong>Option T-A — Fixed 80–520 day range (current).</strong> Works for Mars and inner planets. Fails for outer planets: Jupiter Hohmann takes ~860 days, Saturn ~6 years. Not viable.</p><p><strong>Option T-B — Per-destination range, bigger for outer planets.</strong> Mars: 80–520 days. Jupiter: 400–1500 days. Saturn: 800–3000 days. Each per-destination JSON declares its range. Grid resolution stays 112×100 so the magnifier still works.</p><p><strong>Option T-C — Auto-scaled range based on Hohmann transit.</strong> <code>dep_range_days = [Hohmann × 0.5, Hohmann × 2.5]</code>. Calculated from the destination&#39;s semi-major axis. Less hand-tuning; could miss interesting non-Hohmann transfers (gravity-assist windows).</p><h2 id="proposed-approach" tabindex="-1">Proposed approach <a class="header-anchor" href="#proposed-approach" aria-label="Permalink to &quot;Proposed approach&quot;">​</a></h2><p><strong>Combine D-B + L-C + T-B.</strong></p><ul><li>5 destinations: Mercury, Venus, Mars, Jupiter, Saturn. Defers Uranus/Neptune to a v0.3 follow-up where the porkchop UX has been validated against multi-decade transfer windows.</li><li>LANDING + FLYBY for inner planets (Mercury, Venus, Mars). FLYBY only for Jupiter + Saturn — the toggle on those destinations is still rendered but disabled with an explanatory tooltip (&quot;Landing on a gas giant is not a defined manoeuvre — see Galileo orbit insertion in /missions&quot;).</li><li>Per-destination transfer-time range hardcoded in each destination&#39;s JSON. Mars stays 80–520 days (no regression); outer planets get ranges chosen to span the Hohmann transfer ± 60% so launch-window structure is visible.</li></ul><p><strong>Data model:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static/data/porkchop/</span></span>
<span class="line"><span>  earth-to-mars.json       # existing, unchanged</span></span>
<span class="line"><span>  earth-to-mercury.json    # NEW</span></span>
<span class="line"><span>  earth-to-venus.json      # NEW</span></span>
<span class="line"><span>  earth-to-jupiter.json    # NEW</span></span>
<span class="line"><span>  earth-to-saturn.json     # NEW</span></span></code></pre></div><p>Each JSON contains: <code>destination</code> (planet id), <code>dep_range_days</code>, <code>arr_range_days</code> (= dep + tof_range), <code>tof_range_days</code>, <code>steps</code> ([dep_steps, tof_steps]), <code>mission_types</code> (<code>[&quot;LANDING&quot;, &quot;FLYBY&quot;]</code> or <code>[&quot;FLYBY&quot;]</code>), <code>orbit_insertion_dv_km_s</code> (per type, used when LANDING is selected).</p><p><strong>Computation:</strong> Lambert worker takes a destination id alongside the existing range params and looks up the destination&#39;s mu, a, T, L0 from <code>static/data/planets.json</code>. No new physics — same solver, parameterised over the destination.</p><p><strong>UI:</strong> A <code>&lt;select&gt;</code> for destination above the porkchop, a radiogroup for LANDING / FLYBY beside it (mirroring <code>/missions</code> filter pills). Both filters write to the URL (<code>?dest=jupiter&amp;type=flyby</code>) so deep-links carry the configuration.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Compute on demand from a single solver instance</strong> — same solver code; the worker is already async-friendly. Already proposed.</li><li><strong>Per-destination separate Worker instances</strong> — overkill; one worker handles all destinations in series.</li><li><strong>Skip per-destination data and compute fresh on every visit</strong> — porkchop computation is the most expensive operation in the app; pre-computing is consistent with ADR-016 (assets resolved at build time). Build-time pre-compute is the right call.</li><li><strong>Drop the FLYBY toggle entirely</strong> — simpler UI but loses the educational moment about Jupiter-class missions where a flyby is the only currently-affordable option.</li></ul><h2 id="trade-offs" tabindex="-1">Trade-offs <a class="header-anchor" href="#trade-offs" aria-label="Permalink to &quot;Trade-offs&quot;">​</a></h2><ul><li><strong>5 grids ≈ 5× data on disk.</strong> ~110 KB current → ~550 KB total. Acceptable; below the existing 5.6 MB image bundle.</li><li><strong>Per-destination calibration</strong> required for LANDING ∆v values; needs sourcing from public NASA technical reports per destination. Some hand-tuning expected.</li><li><strong>Mobile magnifier</strong> (ADR-023) keeps working since per-destination grids stay at 112×100 cells. The transfer-time axis scales differently per destination but the magnifier is in cell-space, not date-space.</li><li><strong>Onboarding complexity</strong> — UXS-002&#39;s &quot;blue cells are cheap&quot; mental model still works, but users need to understand why Saturn&#39;s porkchop is mostly orange (because everything to Saturn is expensive). The porkchop educational overlay (added v0.1.2) needs an &quot;outer planets&quot; note.</li></ul><h2 id="open-questions-—-resolved-2026-04-29" tabindex="-1">Open questions — resolved 2026-04-29 <a class="header-anchor" href="#open-questions-—-resolved-2026-04-29" aria-label="Permalink to &quot;Open questions — resolved 2026-04-29&quot;">​</a></h2><ol><li><strong>FLY button for outer-planet flybys</strong> — RESOLVED: render an outbound-only arc using the v0.1.2 per-mission arc helper (the path already supports Mars one-way missions). The /fly HUD reflects the destination + mission type. Free-return is reserved for the ORRERY DEMO scenario.</li><li><strong>Build-time vs runtime grids</strong> — RESOLVED: pre-computed at build time per ADR-016. Five JSONs live in <code>static/data/porkchop/</code>. Trade-off accepted: ~550 KB on-disk for instant first paint and full offline capability. Runtime computation would re-do work on every visit and would block the porkchop&#39;s first frame on slow devices.</li><li><strong>Y-axis units</strong> — RESOLVED: auto-switch to years when the destination&#39;s <code>tof_range_days[1] &gt; 730</code> (≥2 years). Mercury, Venus, Mars stay in days; Jupiter and Saturn show years. The threshold sits per-destination in the JSON file so tests can fix it. Tick labels render as integers (&quot;1y&quot;, &quot;2y&quot;, &quot;3y&quot;) for legibility.</li><li><strong>C3-vs-tof axes</strong> — DROPPED: kept ∆v / dep-date axes for v0.2. C3 (characteristic energy = v_∞²) is the mission-planner convention but adds an axis the user has to learn before reading the plot. PRD-002&#39;s promise is &quot;intuitive before explanation&quot; (blue cheap, orange expensive). C3 axes belong in a v0.3+ &quot;advanced view&quot; toggle if the educational case emerges.</li><li><strong>Pluto + dwarf planets</strong> — DEFERRED to <strong>v0.3</strong> alongside Uranus + Neptune. All four share the same problem (multi-decade transfer-time axes that overwhelm the 112×100 grid resolution + Lambert convergence concerns at extreme distances). The same RFC frame applies; resolution will pull in the v0.2 lessons learned.</li></ol><h2 id="closing-evidence" tabindex="-1">Closing evidence <a class="header-anchor" href="#closing-evidence" aria-label="Permalink to &quot;Closing evidence&quot;">​</a></h2><p>This RFC closes when:</p><ul><li>5 per-destination porkchop grids are pre-computed and committed</li><li>The destination + mission-type selectors are visible on <code>/plan</code></li><li>Cell selection + FLY routing work for Jupiter and Saturn (the outer-planet edge cases)</li><li>A passing e2e covers the Mars regression + at least one outer-planet flyby</li><li>ADR-NNN locks the data model contract and selector UX</li></ul><h2 id="how-this-closes" tabindex="-1">How this closes <a class="header-anchor" href="#how-this-closes" aria-label="Permalink to &quot;How this closes&quot;">​</a></h2><p>One ADR locking:</p><ul><li>The 5-destination scope decision (D-B)</li><li>The LANDING/FLYBY semantics (L-C)</li><li>The per-destination transfer-time range pattern (T-B)</li><li>The URL parameter contract (<code>?dest=...&amp;type=...</code>)</li><li>The data file location + schema</li></ul><p>Then PRD-002 + UXS-002 get a v0.2 extension section once the ADR lands, mirroring the slice-3 closure pattern.</p>`,45)])])}const g=t(o,[["render",i]]);export{u as __pageData,g as default};
