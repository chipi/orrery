import{_ as i,o as e,c as a,a2 as n}from"./chunks/framework.7nMnW4XS.js";const k=JSON.parse('{"title":"RFC-025 · Observability — Sentry for client-side JS errors + Grafana Cloud Agent (env-var-gated, no committed secrets)","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-025.md","filePath":"rfc/RFC-025.md"}'),t={name:"rfc/RFC-025.md"};function r(l,s,o,p,h,d){return e(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="rfc-025-·-observability-—-sentry-for-client-side-js-errors-grafana-cloud-agent-env-var-gated-no-committed-secrets" tabindex="-1">RFC-025 · Observability — Sentry for client-side JS errors + Grafana Cloud Agent (env-var-gated, no committed secrets) <a class="header-anchor" href="#rfc-025-·-observability-—-sentry-for-client-side-js-errors-grafana-cloud-agent-env-var-gated-no-committed-secrets" aria-label="Permalink to &quot;RFC-025 · Observability — Sentry for client-side JS errors + Grafana Cloud Agent (env-var-gated, no committed secrets)&quot;">​</a></h1><p><em>Status: Closed (v0.7.x) · 2026-05-22 · Closes into: ADR-067 / ADR-068 · Reuses operator credentials from podcast_scraper RFC-081</em></p><blockquote><p><strong>Why this is an RFC.</strong> Five things bind. (1) Sentry&#39;s DSN is fundamentally a build-time public string (it gets baked into the bundle and shipped to every browser), but it is <em>still</em> a credential that ties errors to a specific Sentry project + organisation, and the project&#39;s stated posture in the README&#39;s Privacy section (&quot;no PII&quot;, &quot;aggregate only&quot;, &quot;zero analytics on forks by default&quot;) has to remain true after Sentry lands or we ship a regression on a load-bearing user promise. (2) The Sentry initialisation has to scrub PII (URLs with query params, IP addresses, breadcrumbs that reach into form fields) before the SDK serialises the event — once an event leaves the browser, the redaction is no longer in our hands. (3) Grafana Cloud Agent has nothing to ship from Orrery today (static SPA on GH Pages → no logs, no metrics surface), but the future VPS RFC (RFC-024 §10) will want it wired the moment we have nginx access logs + scheduled pipeline-runner runs to observe; landing the compose scaffolding <em>now</em> under env-var gates means the VPS RFC inherits a working integration rather than re-litigating it. (4) Every credential in this RFC — Sentry DSN, Grafana Cloud Prometheus / Loki URLs, Grafana Cloud API key — must NEVER be committed to the repo; the integration shape has to make accidental commits structurally hard, not just discouraged. (5) The pattern follows the operator&#39;s podcast_scraper observability stack (RFC-081 / Grafana Cloud free tier, same account, same credentials), so the operational burden across both projects is one Sentry org + one Grafana Cloud org + one set of access policies, not two.</p></blockquote><hr><h2 id="_1-·-scope-non-scope" tabindex="-1">1 · Scope + non-scope <a class="header-anchor" href="#_1-·-scope-non-scope" aria-label="Permalink to &quot;1 · Scope + non-scope&quot;">​</a></h2><h3 id="in-scope-this-rfc" tabindex="-1">In scope (this RFC) <a class="header-anchor" href="#in-scope-this-rfc" aria-label="Permalink to &quot;In scope (this RFC)&quot;">​</a></h3><ul><li><code>@sentry/sveltekit</code> SDK installation + a thin <code>src/lib/observability/sentry.ts</code> init module.</li><li><code>sentry.client.config.ts</code> (or equivalent SvelteKit-side hook) that initialises Sentry <strong>only if <code>PUBLIC_SENTRY_DSN</code> is set at build time</strong>. Empty DSN = no-op, no network requests, no console noise.</li><li>A <code>beforeSend</code> PII scrubber that strips URL query params, replaces full URLs with route-only, nulls <code>request.headers</code>, drops any breadcrumb whose category is <code>ui.input</code> (form fields).</li><li><code>sendDefaultPii: false</code>. <code>tracesSampleRate: 0</code>. Errors only — no performance tracing, no Web Vitals (per RFC-025 §scope agreed during design).</li><li><code>grafana-agent</code> service added to <code>docker-compose.yml</code> under a new <code>observability</code> compose profile. Default off; only spins up when explicitly invoked via <code>docker compose --profile observability up -d grafana-agent</code>.</li><li><code>ops/observability/grafana-agent.yaml</code> config — scrapes nothing today (no metrics endpoint), tails the <code>web</code> + <code>pipeline-runner</code> Docker container logs via the Docker socket discovery pattern from podcast_scraper RFC-081. Ships to Grafana Cloud Loki only if <code>GRAFANA_CLOUD_LOKI_URL</code> + <code>GRAFANA_CLOUD_API_KEY</code> are set.</li><li>Provisioned Grafana dashboards as JSON in <code>ops/observability/dashboards/</code> — committed (no secrets), imported into the operator&#39;s Grafana Cloud org via the dashboards-as-code pattern.</li><li><strong>Expand the existing <code>.env.example</code></strong> at repo root — already committed, already documents <code>ANTHROPIC_API_KEY</code> + <code>LL2_PATREON_KEY</code>. Append a new &quot;Observability (RFC-025)&quot; section with <code>PUBLIC_SENTRY_DSN</code>, <code>PUBLIC_SENTRY_ENVIRONMENT</code>, <code>PUBLIC_SENTRY_RELEASE</code>, <code>GRAFANA_CLOUD_LOKI_URL</code>, <code>GRAFANA_CLOUD_LOKI_USER</code>, <code>GRAFANA_CLOUD_API_KEY</code>, <code>GRAFANA_AGENT_ENV</code> — variable names + comments only, no values, no DSNs.</li><li><strong>Existing <code>.gitignore</code> already covers <code>.env</code> (line 6) + <code>.env.local</code> (line 7).</strong> Add <code>.env.production</code>, <code>.env.development</code>, <code>.env.*.local</code> for completeness (vite reads each variant on the matching mode). <code>.env.example</code> stays tracked so the template stays committed.</li><li><code>scripts/check-no-secrets.ts</code> — pre-commit / preflight scanner that greps the staged diff for Sentry DSN patterns + Grafana Cloud token patterns and fails the commit if any leaked through. Fast and dumb on purpose.</li><li>README Privacy section update — one new paragraph documenting Sentry&#39;s role + scrubbing + the fork-silent default, mirroring the existing Umami paragraph&#39;s honesty.</li><li><code>docs/guides/observability.md</code> operator guide — what to set up in Sentry + Grafana Cloud consoles, which env vars to populate in which environment, how to verify the integration is silent in local dev.</li></ul><h3 id="explicitly-out-of-scope-deferred" tabindex="-1">Explicitly out of scope (deferred) <a class="header-anchor" href="#explicitly-out-of-scope-deferred" aria-label="Permalink to &quot;Explicitly out of scope (deferred)&quot;">​</a></h3><ul><li><strong>Sentry release tracking + source-map upload</strong> — adds a CI step + a Sentry CLI auth token. Useful for production-debugging stack traces, but a follow-up once the base SDK + scrubbing land cleanly. Tracked as §11.</li><li><strong>Sentry performance tracing / Web Vitals.</strong> User chose &quot;errors only&quot; during the design pass; revisit only if real-user performance regressions become a recurring problem.</li><li><strong>Cookie-based opt-in consent banner.</strong> With <code>sendDefaultPii: false</code> + URL-scrubbing + IP-null, Sentry collects no PII — the standard GDPR carve-out for &quot;legitimate interest in service stability&quot; applies. Adding a banner is a separate UX decision that doesn&#39;t belong in this RFC.</li><li><strong>Grafana metrics scraping.</strong> Orrery has no <code>/metrics</code> endpoint today (no Node backend, nginx default doesn&#39;t expose Prometheus). When the future VPS RFC adds nginx-prometheus-exporter, this RFC&#39;s <code>grafana-agent.yaml</code> gets a scrape_configs block added then.</li><li><strong>Synthetic monitoring</strong> (uptime probes from Grafana Cloud Synthetic Monitoring). Costs nothing on free tier but configures one production hostname; lives more naturally in the future VPS RFC than here.</li><li><strong>OpenTelemetry / OTLP exporter.</strong> Grafana Agent supports OTLP, but Orrery has no traceable workload right now (static SPA, no server). Reconsider when the VPS adds nginx + the pipeline-runner has a daemon mode.</li><li><strong>Self-hosted Grafana / Loki / Prometheus.</strong> Container overhead + ops burden, and the operator already uses Grafana Cloud for podcast_scraper. Same posture for Orrery.</li></ul><hr><h2 id="_2-·-architecture-overview" tabindex="-1">2 · Architecture overview <a class="header-anchor" href="#_2-·-architecture-overview" aria-label="Permalink to &quot;2 · Architecture overview&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌────────────────────── browser (chipi.github.io) ──────────────────────┐</span></span>
<span class="line"><span>│                                                                       │</span></span>
<span class="line"><span>│  SvelteKit app                                                        │</span></span>
<span class="line"><span>│    ↓                                                                  │</span></span>
<span class="line"><span>│  Sentry SDK (@sentry/sveltekit)                                       │</span></span>
<span class="line"><span>│    if PUBLIC_SENTRY_DSN is &quot;&quot; → no-op (default for forks)             │</span></span>
<span class="line"><span>│    else → init with beforeSend scrubber + sendDefaultPii: false       │</span></span>
<span class="line"><span>│       ↓ HTTPS                                                         │</span></span>
<span class="line"><span>│       Sentry Cloud (saas.sentry.io · operator&#39;s org / orrery project) │</span></span>
<span class="line"><span>│                                                                       │</span></span>
<span class="line"><span>└───────────────────────────────────────────────────────────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>┌──────────────── docker-compose stack (local dev + future VPS) ────────┐</span></span>
<span class="line"><span>│                                                                       │</span></span>
<span class="line"><span>│  ┌──────────────┐    ┌─────────────────┐    ┌──────────────────────┐  │</span></span>
<span class="line"><span>│  │  web         │    │ pipeline-runner │    │ grafana-agent        │  │</span></span>
<span class="line"><span>│  │  nginx       │    │ (on-demand)     │    │ (observability       │  │</span></span>
<span class="line"><span>│  │              │    │                 │    │  profile, default    │  │</span></span>
<span class="line"><span>│  │  stdout +    │    │  stdout +       │    │  off in \`up\`)        │  │</span></span>
<span class="line"><span>│  │  /var/log    │    │  /var/log       │    │                      │  │</span></span>
<span class="line"><span>│  └──────┬───────┘    └────────┬────────┘    └──────────┬───────────┘  │</span></span>
<span class="line"><span>│         │                     │                        │              │</span></span>
<span class="line"><span>│         └──── docker logs ────┴──── docker_sd ─────────┘              │</span></span>
<span class="line"><span>│                                                        ↓              │</span></span>
<span class="line"><span>│                                            scrape labels: orrery_*    │</span></span>
<span class="line"><span>│                                                        ↓              │</span></span>
<span class="line"><span>│                                       if all GRAFANA_CLOUD_* set:     │</span></span>
<span class="line"><span>│                                       remote_write → Grafana Cloud    │</span></span>
<span class="line"><span>│                                       Loki (operator&#39;s org)           │</span></span>
<span class="line"><span>│                                            else: tail only, no ship   │</span></span>
<span class="line"><span>└───────────────────────────────────────────────────────────────────────┘</span></span></code></pre></div><p>Two independent integrations. Sentry covers the client browser surface (every visitor, immediately useful on GH Pages today). Grafana Agent covers the docker stack (silent today because GH Pages isn&#39;t docker-served, will be useful the moment the VPS RFC ships nginx + cron&#39;d pipeline runners). Neither writes anything to disk in the repo; both fail closed when env vars are unset.</p><hr><h2 id="_3-·-the-non-negotiable-contract-no-committed-secrets" tabindex="-1">3 · The non-negotiable contract: no committed secrets <a class="header-anchor" href="#_3-·-the-non-negotiable-contract-no-committed-secrets" aria-label="Permalink to &quot;3 · The non-negotiable contract: no committed secrets&quot;">​</a></h2><p>Everything credential-shaped lives behind an env var. The repo contains:</p><ul><li><strong><code>.env.example</code></strong> — already exists and committed (currently documents <code>ANTHROPIC_API_KEY</code> + <code>LL2_PATREON_KEY</code>). Observability adds an &quot;Observability (RFC-025)&quot; section to it — variable names + comments only. No values, no DSNs, no URLs.</li><li><strong><code>.gitignore</code></strong> — already excludes <code>.env</code> and <code>.env.local</code>. Add <code>.env.production</code>, <code>.env.development</code>, <code>.env.*.local</code> for vite-mode-specific variants. <code>.env.example</code> stays tracked.</li><li><code>scripts/check-no-secrets.ts</code> — runs in <code>npm run preflight</code> AND in <code>.husky/pre-commit</code>. Greps the staged diff for: <ul><li>Sentry DSN pattern: <code>https://[a-f0-9]{32}@[a-z0-9.]+\\.ingest\\.sentry\\.io/[0-9]+</code></li><li>Sentry public DSN pattern: <code>https://[a-zA-Z0-9]{32}@o[0-9]+\\.ingest\\.sentry\\.io/[0-9]+</code></li><li>Grafana Cloud API key pattern: <code>glc_[A-Za-z0-9+/=]{40,}</code></li><li>Generic high-entropy strings adjacent to <code>DSN</code>, <code>API_KEY</code>, <code>TOKEN</code>, <code>SECRET</code> (heuristic, with a configurable false-positive allowlist)</li></ul></li><li>Fail-closed: any match → exit code non-zero → the commit / push is rejected. No <code>--no-verify</code> recommended path; if the scanner flags a false positive, add it to the allowlist file.</li></ul><p>The env vars themselves (per environment):</p><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Sentry — client-side error reporting</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">PUBLIC_SENTRY_DSN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Empty in dev/CI, set in production build only.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">PUBLIC_SENTRY_ENVIRONMENT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># &#39;development&#39; | &#39;staging&#39; | &#39;production&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">PUBLIC_SENTRY_RELEASE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Optional: sha-&lt;short&gt; or pkg-version</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Grafana Cloud — log shipping from docker stack</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_CLOUD_LOKI_URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># https://logs-prod-XXX.grafana.net/loki/api/v1/push</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_CLOUD_LOKI_USER</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Numeric instance ID</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_CLOUD_API_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># glc_...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GRAFANA_AGENT_ENV</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># &#39;local-dev&#39; | &#39;staging&#39; | &#39;production-vps&#39;</span></span></code></pre></div><p><code>PUBLIC_</code> prefix on the Sentry vars is the SvelteKit convention for &quot;this gets exposed to the client at build time, treat as public.&quot; The Sentry DSN <em>is</em> public by Sentry&#39;s own docs (it doesn&#39;t authenticate, it identifies — the security model is project-side rate limits + IP allowlists), but documenting it with the <code>PUBLIC_</code> prefix surfaces the fact that this string ships to every browser.</p><hr><h2 id="_4-·-sentry-—-the-client-side-integration" tabindex="-1">4 · Sentry — the client-side integration <a class="header-anchor" href="#_4-·-sentry-—-the-client-side-integration" aria-label="Permalink to &quot;4 · Sentry — the client-side integration&quot;">​</a></h2><h3 id="_4-1-·-dependencies" tabindex="-1">4.1 · Dependencies <a class="header-anchor" href="#_4-1-·-dependencies" aria-label="Permalink to &quot;4.1 · Dependencies&quot;">​</a></h3><ul><li><code>@sentry/sveltekit</code> — adds the SDK + SvelteKit-specific hooks for <code>handleError</code> (server) and the client init.</li><li>That&#39;s it. No <code>@sentry/node</code> (no Node server), no <code>@sentry/vite-plugin</code> for now (defer source-map upload to §11), no <code>@sentry/replay</code> (session replay collects user UI — would violate the privacy posture).</li></ul><h3 id="_4-2-·-init-module-—-src-lib-observability-sentry-ts" tabindex="-1">4.2 · Init module — <code>src/lib/observability/sentry.ts</code> <a class="header-anchor" href="#_4-2-·-init-module-—-src-lib-observability-sentry-ts" aria-label="Permalink to &quot;4.2 · Init module — \`src/lib/observability/sentry.ts\`&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Sentry </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sentry/sveltekit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { env } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;$env/dynamic/public&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { dev } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;$app/environment&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Initialise Sentry client-side. No-op if PUBLIC_SENTRY_DSN is empty —</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * which is the default for forks, local dev, CI, and any environment</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * the operator hasn&#39;t explicitly opted in. Errors only — no</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * performance tracing, no Web Vitals, no session replay.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Privacy: sendDefaultPii: false explicitly, beforeSend scrubs URL</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * query params and IP, drops UI input breadcrumbs.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> initSentry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dsn</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> env.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PUBLIC_SENTRY_DSN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dsn) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Fork-silent default.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (dev) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Vite dev never reports.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Sentry.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    dsn,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    environment: env.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PUBLIC_SENTRY_ENVIRONMENT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;production&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    release: env.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PUBLIC_SENTRY_RELEASE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    tracesSampleRate: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Errors only, no perf tracing.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sendDefaultPii: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// No IP, no cookies, no user data.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    beforeSend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // Strip URL query string — bookmarkable mission deep-links could</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // carry user-typed search text in the future. Route-only is</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // enough for triage.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (event.request?.url) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">          const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> u</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event.request.url);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          event.request.url </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> u.origin </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> u.pathname;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* leave as-is */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // Null any auto-populated headers (Sentry SDK sometimes attaches</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // referer + cookie even with sendDefaultPii: false).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      event.request </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event.request </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event.request, headers: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, cookies: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // Server-side IP scrubbing: Sentry derives IP from the connecting</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // TCP socket; setting user.ip_address to &#39;0.0.0.0&#39; tells the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // server to discard it (per Sentry&#39;s PII discard rules).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      event.user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event.user, ip_address: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;0.0.0.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    beforeBreadcrumb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">crumb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // ui.input breadcrumbs contain the typed value — drop them.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (crumb.category </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;ui.input&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // Navigation breadcrumbs also carry full URLs; strip query.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (crumb.category </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;navigation&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> crumb.data?.to </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;string&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        crumb.data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">crumb.data, to: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stripQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(crumb.data.to) };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> crumb;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> stripQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">s</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> u</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(s); </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> u.pathname; } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> s; }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_4-3-·-wire-up-—-src-hooks-client-ts" tabindex="-1">4.3 · Wire-up — <code>src/hooks.client.ts</code> <a class="header-anchor" href="#_4-3-·-wire-up-—-src-hooks-client-ts" aria-label="Permalink to &quot;4.3 · Wire-up — \`src/hooks.client.ts\`&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { initSentry } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;$lib/observability/sentry&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { handleErrorWithSentry } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sentry/sveltekit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">initSentry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> handleError</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> handleErrorWithSentry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>The SvelteKit framework calls <code>handleError</code> on any unhandled error during client-side hydration, route navigation, or load function. <code>handleErrorWithSentry</code> is the SDK&#39;s drop-in wrapper that forwards through to Sentry.</p><h3 id="_4-4-·-why-no-hooks-server-ts" tabindex="-1">4.4 · Why no <code>hooks.server.ts</code> <a class="header-anchor" href="#_4-4-·-why-no-hooks-server-ts" aria-label="Permalink to &quot;4.4 · Why no \`hooks.server.ts\`&quot;">​</a></h3><p>Orrery uses <code>adapter-static</code>. There is no server runtime. Adding a server hook would be dead code; the SDK&#39;s server-side init never executes because the SPA is pre-rendered + served as static files.</p><h3 id="_4-5-·-build-time-dsn-injection" tabindex="-1">4.5 · Build-time DSN injection <a class="header-anchor" href="#_4-5-·-build-time-dsn-injection" aria-label="Permalink to &quot;4.5 · Build-time DSN injection&quot;">​</a></h3><p><code>@sveltejs/kit</code> reads <code>PUBLIC_*</code> env vars from <code>.env</code> files at build time and inlines them into the client bundle. So:</p><ul><li><strong>Local dev:</strong> <code>npm run dev</code> reads <code>.env</code> (gitignored); operator can leave <code>PUBLIC_SENTRY_DSN=</code> empty and Sentry is silent.</li><li><strong>Production build</strong> (GH Pages deploy): <code>.github/workflows/preview.yml</code> sets <code>PUBLIC_SENTRY_DSN: \${‌{ secrets.SENTRY_DSN_WEB }}</code> in the <code>env:</code> block of the build step. The bundle that lands on GH Pages has the DSN baked in.</li><li><strong>Fork builds:</strong> the secret doesn&#39;t exist in their fork → empty env → silent Sentry. The pattern matches Umami exactly.</li></ul><hr><h2 id="_5-·-grafana-cloud-agent-—-the-docker-side-integration" tabindex="-1">5 · Grafana Cloud Agent — the docker-side integration <a class="header-anchor" href="#_5-·-grafana-cloud-agent-—-the-docker-side-integration" aria-label="Permalink to &quot;5 · Grafana Cloud Agent — the docker-side integration&quot;">​</a></h2><h3 id="_5-1-·-new-compose-service" tabindex="-1">5.1 · New compose service <a class="header-anchor" href="#_5-1-·-new-compose-service" aria-label="Permalink to &quot;5.1 · New compose service&quot;">​</a></h3><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># docker-compose.yml — new service, profile-gated.</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">grafana-agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">grafana/agent:v0.43.4</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  profiles</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;observability&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  volumes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./ops/observability/grafana-agent.yaml:/etc/grafana-agent.yaml:ro</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/var/run/docker.sock:/var/run/docker.sock:ro</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  environment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">GRAFANA_CLOUD_LOKI_URL=\${GRAFANA_CLOUD_LOKI_URL:-}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">GRAFANA_CLOUD_LOKI_USER=\${GRAFANA_CLOUD_LOKI_USER:-}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">GRAFANA_CLOUD_API_KEY=\${GRAFANA_CLOUD_API_KEY:-}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">GRAFANA_AGENT_ENV=\${GRAFANA_AGENT_ENV:-local-dev}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;-config.file=/etc/grafana-agent.yaml&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p><code>profiles: [&#39;observability&#39;]</code> means <code>docker compose up -d web</code> does NOT start it. To run with observability locally:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> observability</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>When <code>GRAFANA_CLOUD_*</code> vars are empty (the local-dev default), the agent stays running but the Loki client config short-circuits (no remote_write target) and the agent tails logs to its own stdout only — useful for &quot;is my logging output structured the way I expect?&quot; but no external traffic.</p><h3 id="_5-2-·-ops-observability-grafana-agent-yaml" tabindex="-1">5.2 · <code>ops/observability/grafana-agent.yaml</code> <a class="header-anchor" href="#_5-2-·-ops-observability-grafana-agent-yaml" aria-label="Permalink to &quot;5.2 · \`ops/observability/grafana-agent.yaml\`&quot;">​</a></h3><p>Mirrors the podcast_scraper <code>compose/grafana-agent.yaml</code> pattern. Loki block:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">logs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  configs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">orrery</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      clients</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\${GRAFANA_CLOUD_LOKI_URL}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          basic_auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            username</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\${GRAFANA_CLOUD_LOKI_USER}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            password</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\${GRAFANA_CLOUD_API_KEY}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          external_labels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            env</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\${GRAFANA_AGENT_ENV}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">orrery</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      positions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        filename</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tmp/positions.yaml</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      scrape_configs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">job_name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">orrery-docker</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          docker_sd_configs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">host</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">unix:///var/run/docker.sock</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">              refresh_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">30s</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          relabel_configs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">source_labels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;__meta_docker_container_name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">              regex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/(orrery-web|orrery-pipeline-runner-.*)&#39;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">              action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">keep</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">source_labels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;__meta_docker_container_name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">              target_label</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">container</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">              regex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/(.*)&#39;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">              replacement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;$1&#39;</span></span></code></pre></div><p>Promtail&#39;s <code>docker_sd_configs</code> discovers containers via the docker socket; the <code>relabel_configs</code> filters to Orrery-specific names. Logs land in Grafana Cloud Loki keyed by <code>{env, app, container}</code>.</p><h3 id="_5-3-·-dashboards-as-code" tabindex="-1">5.3 · Dashboards as code <a class="header-anchor" href="#_5-3-·-dashboards-as-code" aria-label="Permalink to &quot;5.3 · Dashboards as code&quot;">​</a></h3><p><code>ops/observability/dashboards/*.json</code> — committed, no secrets. Two dashboards to start:</p><ul><li><code>orrery-web-access.json</code> — nginx access log volume + status code breakdown over time. Useful when nginx is actually serving traffic (i.e. on the VPS); silent today.</li><li><code>orrery-pipelines.json</code> — pipeline-runner invocation log lines, errors, duration. Useful when pipelines run on a host cron (i.e. on the VPS); silent today.</li></ul><p>Operator imports them into Grafana Cloud via <code>grafana-cli dashboard import</code> or the dashboards-as-code script in <code>ops/observability/dashboards/import.sh</code>. None of this runs from the app.</p><h3 id="_5-4-·-why-land-this-now-if-it-s-silent-today" tabindex="-1">5.4 · Why land this now if it&#39;s silent today? <a class="header-anchor" href="#_5-4-·-why-land-this-now-if-it-s-silent-today" aria-label="Permalink to &quot;5.4 · Why land this now if it&#39;s silent today?&quot;">​</a></h3><p>Two reasons. First, getting the env-var contract + compose profile + agent config right is the easy 80% — leaving it until the VPS RFC means re-litigating the shape under deploy-stress, which is the worst time to negotiate it. Second, the dashboards-as-code files committed today form the operator playbook for the VPS bring-up; they ARE the implementation of &quot;what does production observability look like&quot; and reviewing them now (when nothing is live) is the cheap moment to get the shape right.</p><hr><h2 id="_6-·-readme-privacy-section-update" tabindex="-1">6 · README Privacy section update <a class="header-anchor" href="#_6-·-readme-privacy-section-update" aria-label="Permalink to &quot;6 · README Privacy section update&quot;">​</a></h2><p>Adds one paragraph after the existing Umami paragraph:</p><blockquote><p><strong>Error tracking — <a href="https://sentry.io/" target="_blank" rel="noreferrer">Sentry</a>, errors only, no PII, fork-silent.</strong> Production builds on <code>chipi.github.io</code> initialise the Sentry SDK if <code>PUBLIC_SENTRY_DSN</code> is set at build time — captures unhandled JavaScript exceptions only (no performance tracing, no session replay, no Web Vitals). <code>sendDefaultPii: false</code> is set explicitly; a <code>beforeSend</code> hook strips URL query parameters, nulls request headers, and sets IP to <code>0.0.0.0</code> (Sentry&#39;s discard signal). Self-hosters that clone Orrery and build their own fork get <strong>zero Sentry traffic by default</strong> — the env var isn&#39;t in their CI secrets, so the SDK init returns early before any network request. The Sentry DSN, like Umami&#39;s website ID, is public-by-design (the security model is project-side rate limits, not DSN secrecy), but it&#39;s still injected via build-time env var so that forks don&#39;t accidentally report into Orrery&#39;s project.</p></blockquote><p>The Grafana side stays <em>un</em>mentioned in the user-facing Privacy section — it observes the docker stack (local + future VPS), never the browser, so it doesn&#39;t intersect with the user&#39;s privacy contract.</p><hr><h2 id="_7-·-implementation-slices" tabindex="-1">7 · Implementation slices <a class="header-anchor" href="#_7-·-implementation-slices" aria-label="Permalink to &quot;7 · Implementation slices&quot;">​</a></h2><ul><li><strong>Slice 1 — Sentry SDK + scrubbing + env-var wiring + secret guard.</strong> Add <code>@sentry/sveltekit</code>, write <code>src/lib/observability/sentry.ts</code>, wire <code>src/hooks.client.ts</code>, <strong>expand existing <code>.env.example</code></strong> with the Sentry + Grafana env-var rows, <strong>extend existing <code>.gitignore</code></strong> with <code>.env.production</code> / <code>.env.development</code> / <code>.env.*.local</code>, add <code>scripts/check-no-secrets.ts</code> to <code>preflight</code>. Update README Privacy section. Update <code>preview.yml</code> to thread <code>\${‌{ secrets.SENTRY_DSN_WEB }}</code> into the build env (operator sets this in repo settings once). Closes <strong>ADR-067</strong>.</li><li><strong>Slice 2 — Grafana Agent compose service + config + dashboards.</strong> Add the <code>grafana-agent</code> service, <code>ops/observability/grafana-agent.yaml</code>, the two dashboard JSONs, a one-page <code>ops/observability/dashboards/README.md</code> operator import flow. Closes <strong>ADR-068</strong>.</li><li><strong>Slice 3 — operator guide + flip ADRs.</strong> <code>docs/guides/observability.md</code> (set up Sentry org, set up Grafana Cloud, populate env vars, verify silence locally). Flip ADRs 067-068 Draft → Accepted. Update <code>AGENTS.md</code> pointer (per the established RFC-024 pattern).</li></ul><p>Total: ~2-3 days of focused work. No data migration. Zero behaviour change for users on <code>chipi.github.io</code> until the operator populates the <code>SENTRY_DSN_WEB</code> GitHub Actions secret. Zero behaviour change for the docker stack until the operator runs <code>docker compose --profile observability up -d</code> with <code>GRAFANA_CLOUD_*</code> env vars set.</p><hr><h2 id="_8-·-operator-setup-once-outside-the-repo" tabindex="-1">8 · Operator setup (once, outside the repo) <a class="header-anchor" href="#_8-·-operator-setup-once-outside-the-repo" aria-label="Permalink to &quot;8 · Operator setup (once, outside the repo)&quot;">​</a></h2><p>These steps live in <code>docs/guides/observability.md</code>; documented here for completeness so the RFC is self-contained.</p><ul><li><strong>Sentry account / org.</strong> Operator already has a Sentry org (per podcast_scraper RFC-081). Create a new project under that org: type <code>JavaScript / SvelteKit</code>, name <code>orrery-web</code>. Copy the DSN string from the project&#39;s Client Keys page. Set it in GitHub repo settings → Secrets → Actions as <code>SENTRY_DSN_WEB</code>. Optionally set it in your local <code>.env</code> for testing.</li><li><strong>Grafana Cloud account / org.</strong> Operator already has a Grafana Cloud free-tier stack (per podcast_scraper RFC-081). Create a new Access Policy with <code>logs:write</code> scope only; export the API key (one-time view). Note the Loki ingest URL from the stack&#39;s Loki integration page and the numeric instance ID. Put those into local <code>.env</code> for now (the VPS RFC will move them to the VPS host&#39;s environment).</li><li><strong>Verify silence in dev.</strong> <code>npm run dev</code> should show no Sentry network requests in the browser devtools, and <code>docker compose --profile observability up -d</code> should show the agent running but no outbound traffic in <code>docker logs grafana-agent</code>.</li></ul><hr><h2 id="_9-·-risks-mitigations" tabindex="-1">9 · Risks + mitigations <a class="header-anchor" href="#_9-·-risks-mitigations" aria-label="Permalink to &quot;9 · Risks + mitigations&quot;">​</a></h2><ul><li><strong>A future contributor adds <code>Sentry.captureMessage(&#39;user typed: &#39; + input)</code> somewhere.</strong> Captures PII despite the SDK config. Mitigation: the <code>beforeSend</code> hook scrubs known PII surfaces (URL, headers, IP) but cannot scrub free-text message arguments. Add a lint rule (or at least a <code>docs/guides/observability.md</code> §don&#39;t-do-this section) explicitly calling this out. Code review is the real defense.</li><li><strong>CI accidentally exposes the DSN.</strong> Mitigation: <code>PUBLIC_SENTRY_DSN</code> IS public-by-design, so the worst case is a fork builds with the operator&#39;s DSN and starts reporting into the operator&#39;s project. Not catastrophic — Sentry&#39;s project-side rate limits absorb the noise — but worth a follow-up &quot;what&#39;s our reaction&quot; runbook. The <code>check-no-secrets</code> scanner catches the <em>backend</em> Grafana Cloud API key (which IS genuinely secret) in the staged diff.</li><li><strong>Forks build and ship Sentry without realising.</strong> Mitigation: the SDK is no-op when <code>PUBLIC_SENTRY_DSN</code> is empty. Forks would have to actively set the env var. This is the same posture as Umami&#39;s hostname allowlist; the user is in control.</li><li><strong>Sentry quota exhaustion on a viral spike.</strong> Free tier is 5k errors/month. If Orrery goes viral and starts erroring, we&#39;d run out within hours. Mitigation: <code>tracesSampleRate: 0</code> keeps perf events out; for errors-only the volume tends to be bounded by <em>unique</em> bugs, not unique users. Add a follow-up to RFC-025 if quota becomes a real ceiling.</li><li><strong>Grafana Cloud free tier limits.</strong> 50 GB logs / month. The docker stack only logs in production-mode; nginx access logs at ~200 B / request, pipeline runners produce ~5-50 KB / invocation. At 100k req/day + 4 pipeline runs/day, we&#39;re at ~700 MB / month. Comfortable. The VPS RFC should add a <code>loki.client.batch_size_bytes</code> cap so a misconfigured app can&#39;t blow the budget overnight.</li><li><strong>Compose-profile drift between local + CI + VPS.</strong> Mitigation: the <code>observability</code> profile is opt-in everywhere. CI doesn&#39;t run with it (the docker-e2e workflow per ADR-066 only brings up <code>web</code>). VPS bring-up runs it explicitly. Local dev runs it explicitly when the operator wants to test.</li></ul><hr><h2 id="_10-·-validation-plan" tabindex="-1">10 · Validation plan <a class="header-anchor" href="#_10-·-validation-plan" aria-label="Permalink to &quot;10 · Validation plan&quot;">​</a></h2><ul><li><code>PUBLIC_SENTRY_DSN= npm run dev &amp;&amp; curl logs from dev tools</code> → no Sentry requests fire.</li><li><code>PUBLIC_SENTRY_DSN=&lt;dev DSN&gt; npm run dev &amp;&amp; trigger an unhandled error in /science</code> → one event in Sentry&#39;s dev project&#39;s issue list within ~30 s, with <code>request.url</code> stripped of query params, <code>request.headers</code> undefined, <code>user.ip_address</code> recorded as <code>0.0.0.0</code>.</li><li><code>docker compose --profile observability up -d grafana-agent</code> with all <code>GRAFANA_CLOUD_*</code> empty → agent runs, tails container logs to stdout, makes zero outbound network requests (verified via <code>docker exec grafana-agent ss -tn</code>).</li><li><code>docker compose --profile observability up -d grafana-agent</code> with all <code>GRAFANA_CLOUD_*</code> populated → log lines from <code>orrery-web</code> and any running pipeline-runner appear in Grafana Cloud Loki under <code>{env: &#39;local-dev&#39;, app: &#39;orrery&#39;, container: &#39;orrery-web&#39;}</code>.</li><li><code>scripts/check-no-secrets.ts</code> with a manually-injected DSN in the staged diff → script exits non-zero, blocks the commit.</li><li><code>npm run preflight</code> continues to pass on <code>main</code>. The docker-e2e CI workflow continues to pass.</li></ul><hr><h2 id="_11-·-future-work" tabindex="-1">11 · Future work <a class="header-anchor" href="#_11-·-future-work" aria-label="Permalink to &quot;11 · Future work&quot;">​</a></h2><ul><li><strong>Sentry release tracking + source-map upload.</strong> <code>@sentry/vite-plugin</code> integration in <code>vite.config.ts</code>. Auth token via <code>SENTRY_AUTH_TOKEN</code> GH Actions secret. CI workflow step that uploads source maps + tags the release with the <code>git rev-parse --short HEAD</code>. Materially improves the readability of production stack traces — defer until enough real errors warrant the operator setup. Own RFC if scope grows.</li><li><strong>Sentry performance tracing + Web Vitals.</strong> <code>tracesSampleRate: 0.1</code>, <code>browserTracingIntegration</code>. Adds page-load + route-transition timings to the issue list. Defer until a real-user performance regression motivates the bandwidth + free-tier cost.</li><li><strong>OpenTelemetry / OTLP exporter.</strong> Grafana Agent supports OTLP; once any Node-side workload exists (the VPS RFC&#39;s pipeline-runner cron, for example), structured traces could ship via OTLP. Cleaner than parsing log lines; defer until the VPS RFC.</li><li><strong>Synthetic uptime probe.</strong> Grafana Cloud Synthetic Monitoring free tier supports one production hostname. Configure for <code>chipi.github.io</code> + the future VPS hostname. Drops nicely into the VPS RFC&#39;s checklist.</li><li><strong>Privacy banner / cookie-based consent.</strong> If the project ever needs to surface Sentry collection to users (regulatory shift, new data type added), drop in a banner. With current scope (errors only + scrubbed), GDPR legitimate-interest covers it.</li><li><strong>A <code>disable-observability</code> URL parameter</strong> for operator debugging — <code>?orrery_telemetry=off</code> short-circuits Sentry init for the session. Trivial to add; punted because it&#39;s a tool you only need when you&#39;re actively debugging Sentry itself.</li></ul><hr><h2 id="_12-·-open-questions" tabindex="-1">12 · Open questions <a class="header-anchor" href="#_12-·-open-questions" aria-label="Permalink to &quot;12 · Open questions&quot;">​</a></h2><ul><li><strong>Should the Sentry release-tag include the GH Pages deploy SHA or the upstream Orrery release tag?</strong> Probably both — <code>release: vX.Y.Z+gha-&lt;sha&gt;</code>. Decided during Slice 1 when the CI env block is written.</li><li><strong>Should the Grafana Cloud Loki labels include the Orrery git SHA?</strong> Yes if it&#39;s available cheaply via <code>git rev-parse</code> at compose bring-up; punt to runtime label injection (vs static config) if it&#39;s not.</li><li><strong>Should pipeline-runner logs use a structured logger (pino, winston)</strong> to make Loki queries cleaner, or do we trust unstructured stdout for now? Punt to a follow-up — pipelines today print human-readable lines and that&#39;s fine for a first dashboard pass.</li></ul><hr><p><em>RFC-025 · 2026-05-22 · Marko + Claude · v0.7.x slice gate · ADRs 067-068 · reuses podcast_scraper RFC-081&#39;s Grafana Cloud account</em></p>`,78)])])}const g=i(t,[["render",r]]);export{k as __pageData,g as default};
