import{_ as i,o as e,c as t,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const k=JSON.parse('{"title":"RFC-027 — List-route search on /missions + /fleet (v0.7)","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-027.md","filePath":"rfc/RFC-027.md"}'),n={name:"rfc/RFC-027.md"};function l(r,s,o,h,p,c){return e(),t("div",null,[...s[0]||(s[0]=[a(`<h1 id="rfc-027-—-list-route-search-on-missions-fleet-v0-7" tabindex="-1">RFC-027 — List-route search on /missions + /fleet (v0.7) <a class="header-anchor" href="#rfc-027-—-list-route-search-on-missions-fleet-v0-7" aria-label="Permalink to &quot;RFC-027 — List-route search on /missions + /fleet (v0.7)&quot;">​</a></h1><blockquote><p>Status · Draft Date · 2026-06-13 Target · v0.7 Tracked by · GH issue #338</p></blockquote><blockquote><p><strong>Why this is an RFC.</strong> /missions (98 entries) and /fleet (245 entries) both carry a rich filter strip, but the filter chips alone leave the user scrolling once a category is broad (e.g. &quot;FLOWN crewed NASA&quot; is still 30+ Apollo + Skylab + Shuttle / Crew-Dragon entries). The audit-blessed addition is a single text input that narrows the card set across name + agency + description, and bidirectionally re-counts the filter chips so the filter UI keeps telling the truth about what the user is looking at. This RFC locks the matching contract, the search↔filter interaction model, the URL state, and the slice plan before either route&#39;s <code>+page.svelte</code> is touched.</p></blockquote><p><strong>Gating sentence</strong>: Introduces a per-route text-search input on /missions + /fleet, with single-corpus matching across name + agency + description, AND-semantics interaction with the existing filter chips, and deep-linkable <code>?q=</code> URL state — tracked by <a href="https://github.com/chipi/orrery/issues/338" target="_blank" rel="noreferrer">issue #338</a>.</p><h2 id="goal" tabindex="-1">Goal <a class="header-anchor" href="#goal" aria-label="Permalink to &quot;Goal&quot;">​</a></h2><p>Let a user narrow the card set on /missions + /fleet by typing a search term that matches any of the three text fields the cards already render (name, agency-short, description), while keeping the existing filter chips fully bidirectional with the matched set.</p><h2 id="scope-v0-7" tabindex="-1">Scope (v0.7) <a class="header-anchor" href="#scope-v0-7" aria-label="Permalink to &quot;Scope (v0.7)&quot;">​</a></h2><ul><li>One text input per route — <code>/missions</code> and <code>/fleet</code> only — positioned at the same break point as the FILTERS button (above the card grid).</li><li>Match against three resolved-locale fields per card: <code>name</code>, <code>agency_short</code> (or <code>agency</code> if no short form), and <code>description</code>.</li><li>AND-semantics with filters: the visible set is <code>(matches(q) AND matches(filters))</code>. Order of application is irrelevant.</li><li>Filter chip counts re-compute on every keystroke against the searched subset so the chips never overstate.</li><li>URL state via <code>?q=&lt;term&gt;</code> on top of existing filter params. Empty <code>q</code> drops from the URL.</li><li>i18n: input placeholder, &quot;Showing N of M&quot;, and &quot;No matches&quot; copy each get one message-bundle key, matched across all 14 locales.</li><li>Empty state: when no card matches, show &quot;No matches for <code>&lt;q&gt;</code>&quot; + an inline &quot;Clear search&quot; button. Filter chips remain visible (filters can independently widen the result if the user clears the query).</li><li>Mobile: the input collapses into the filter strip&#39;s sticky header so it never costs vertical pixels on a phone.</li></ul><h2 id="non-goals-v0-7" tabindex="-1">Non-goals (v0.7) <a class="header-anchor" href="#non-goals-v0-7" aria-label="Permalink to &quot;Non-goals (v0.7)&quot;">​</a></h2><ul><li><strong>Pagination</strong> — the existing <code>content-visibility: auto</code> pattern (TA.md §&quot;Long-list routes&quot;) already keeps off-screen cards out of paint. 98 missions + 245 fleet entries × scroll is not a real perf cost; the filter + search combination is the cognitive-load tool. Re-visit if a real complaint lands.</li><li><strong>Fuzzy / typo-tolerant matching</strong> — substring-case-insensitive is enough for the corpus size. A future RFC can add fzy/fuse.js if the empty-state hit rate is non-trivial.</li><li><strong>Autocomplete dropdown</strong> — the matched cards are the autocomplete.</li><li><strong>Search history / &quot;recent&quot; suggestions</strong> — privacy-coherent with the &quot;no tracking&quot; line in the footer; not worth the local-storage footprint.</li><li><strong>Cross-route search</strong> — typing &quot;perseverance&quot; on /missions doesn&#39;t surface the /fleet entry. A future v0.8 &quot;global search&quot; RFC can pick that up if the routes start needing it; for now the route-scoped pattern matches the user&#39;s already-route-scoped intent.</li><li><strong>Search against gallery alt-text, learn-link titles, science cross-refs</strong> — the corpus is bounded to what the card itself renders so the empty state never confuses (&quot;I see no Apollo card but it matched??&quot;).</li></ul><h2 id="matching-contract" tabindex="-1">Matching contract <a class="header-anchor" href="#matching-contract" aria-label="Permalink to &quot;Matching contract&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> matchesQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">card</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MissionIndex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FleetEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">q</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">q) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> needle</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> q.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">trim</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toLowerCase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">needle) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> haystack</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    card.name,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    card.agency_short </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> card.agency,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    card.description </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">join</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39; &#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toLowerCase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> haystack.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">includes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(needle);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li>Case-insensitive substring; no token splitting, no Unicode normalization in v1 (Latin-script corpus across all 14 locales currently — the few CJK locales fall back to en-US for these fields, so substring still hits).</li><li>Empty / whitespace-only <code>q</code> is a no-op (every card matches).</li><li>Pure function — same input always produces the same output, no module state.</li></ul><h2 id="search-↔-filter-interaction" tabindex="-1">Search ↔ filter interaction <a class="header-anchor" href="#search-↔-filter-interaction" aria-label="Permalink to &quot;Search ↔ filter interaction&quot;">​</a></h2><p>The two are commutative. The route&#39;s <code>filtered</code> derivation becomes:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> q </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// bound to &lt;input&gt; via useUrlParam(&#39;q&#39;)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> filtered </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $derived</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  allCards</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> matchesQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(c, q))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> matchesFilters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(c, activeFilters))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Filter chip counts come from a second pass against the searched-but-not-yet-filter-narrowed set:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> searched </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $derived</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(allCards.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> matchesQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(c, q)));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> agencyCounts </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> $derived</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">countBy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(searched, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> c.agency));</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// chips render \`\${label} \${count}\` from agencyCounts</span></span></code></pre></div><p>Selecting an agency filter when a query is active keeps the query intact; clearing the query keeps the filter chips intact. The two state slices are independent.</p><h2 id="url-state" tabindex="-1">URL state <a class="header-anchor" href="#url-state" aria-label="Permalink to &quot;URL state&quot;">​</a></h2><ul><li><code>?q=&lt;term&gt;</code> — written by the new <code>useUrlParam(&#39;q&#39;)</code> instance (RFC-024 rune), debounced 200 ms, <code>replaceState: true</code> so back-button doesn&#39;t fill with keystrokes.</li><li>Empty/whitespace-only <code>q</code> drops from the URL on the next debounce tick.</li><li>Coexists with every existing filter param. Order of params is unstable but URL.searchParams round-trips losslessly.</li><li>Deep-linkable: <code>/missions?dest=MARS&amp;q=2020</code> opens /missions filtered to Mars + searched for &quot;2020&quot; — both apply on first paint.</li></ul><h2 id="i18n" tabindex="-1">i18n <a class="header-anchor" href="#i18n" aria-label="Permalink to &quot;i18n&quot;">​</a></h2><p>Three new message keys per route family (six total across /missions + /fleet):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>search_input_placeholder = &quot;Search missions…&quot; / &quot;Search the fleet…&quot;</span></span>
<span class="line"><span>search_results_count = &quot;Showing {n} of {total}&quot;</span></span>
<span class="line"><span>search_empty_state = &quot;No matches for &quot;{query}&quot;&quot;</span></span></code></pre></div><p>The matching corpus uses the <strong>active locale&#39;s</strong> resolved overlay (<code>static/data/i18n/&lt;locale&gt;/missions/&lt;id&gt;.json</code> shallow-merged onto base per ADR-017) — the same object the card already renders. So typing &quot;Mond&quot; on the German /missions route matches Moon missions because the German overlay translates the destination label inside the description. No fallback to en-US for matching (would be confusing — user can&#39;t see en-US strings).</p><h2 id="slice-plan" tabindex="-1">Slice plan <a class="header-anchor" href="#slice-plan" aria-label="Permalink to &quot;Slice plan&quot;">​</a></h2><ol><li><strong>Slice A — extract matching contract + tests.</strong> New <code>src/lib/list-search.ts</code> with <code>matchesQuery()</code> + unit tests. No UI changes. Lands as <code>feat(list-search): matcher contract</code>.</li><li><strong>Slice B — wire /missions.</strong> Add <code>&lt;input&gt;</code>, <code>useUrlParam(&#39;q&#39;)</code>, update <code>filtered</code> derivation, re-bind chip counts to the <code>searched</code> set. e2e: deep-link <code>?q=apollo</code> shows ≥6 Apollo missions; chip counts reflect.</li><li><strong>Slice C — wire /fleet.</strong> Same pattern. e2e parity.</li><li><strong>Slice D — i18n strings.</strong> Add 3 message keys × 14 locales = 42 strings (de/es/fr/it/pt-BR/nl/sr-Cyrl/zh-CN/ja/ko/hi/ar/ru). Run through translation pipeline.</li></ol><p>Each slice is one commit. Adoption sweep stops at /missions + /fleet — the other long lists (/credits, /library) re-use the helper if/when a user complaint lands.</p><h2 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h2><ul><li><strong>Filter-chip flicker on every keystroke</strong> — every input change re-derives chip counts. Mitigation: $derived is reactive, no manual setState; 200 ms input debounce on the URL write avoids the back-button history flood but the visual chip update is intentionally instant (matches the user&#39;s mental model: &quot;I see what I&#39;m searching&quot;).</li><li><strong><code>?q=</code> collision with other route params</strong> — none of the existing params on /missions or /fleet use <code>q</code>; verified via <code>grep -nE &quot;searchParams.get\\(&#39;q&#39;&quot; src/routes/{missions,fleet}/+page.svelte</code> (zero hits).</li><li><strong>Substring false positives on short queries</strong> — <code>?q=es</code> matches &quot;ESA&quot;, &quot;es&quot; inside &quot;esa-jaxa joint&quot;, &quot;tess&quot;, &quot;messages&quot;, etc. Acceptable; the visible cards are the disambiguator. Two-char minimum was considered + rejected (excludes useful queries like &quot;Q1&quot; or &quot;MA&quot;).</li></ul><h2 id="acceptance" tabindex="-1">Acceptance <a class="header-anchor" href="#acceptance" aria-label="Permalink to &quot;Acceptance&quot;">​</a></h2><ul><li><code>/missions?q=apollo</code> and <code>/fleet?q=dragon</code> paint the matched subset on first frame, chip counts reflect.</li><li>Deep-link <code>/missions?dest=MARS&amp;q=2020</code> opens with both filter + query applied.</li><li>Clearing the query restores the full filter-chip-counted set without a route reload.</li><li>e2e covers: typing narrows; clearing widens; filter-while-searching narrows further; URL round-trips.</li><li>preflight green; line coverage hold (the new helper is pure + tested).</li><li>42 new i18n strings land across all 14 locales.</li></ul><h2 id="open-questions-carried-into-the-issue" tabindex="-1">Open questions (carried into the issue) <a class="header-anchor" href="#open-questions-carried-into-the-issue" aria-label="Permalink to &quot;Open questions (carried into the issue)&quot;">​</a></h2><ul><li>Does <code>agency_short</code> exist on every fleet entry? If not, fall back to <code>agency</code>.</li><li>Should the search input keep focus after URL hydration on first paint (so deep-linked <code>?q=</code> lands the user already typing more)? Default: yes.</li><li>Keyboard shortcut to focus the input (<code>/</code>, <code>Cmd-K</code>)? Defer to a follow-up — not all routes have it and it&#39;s a separate UX decision.</li></ul>`,34)])])}const u=i(n,[["render",l]]);export{k as __pageData,u as default};
