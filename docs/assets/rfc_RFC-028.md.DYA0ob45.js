import{_ as t,o as s,c as a,a2 as i}from"./chunks/framework.7nMnW4XS.js";const g=JSON.parse('{"title":"RFC-028 — data.ts split + DAL architecture review (v0.7)","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-028.md","filePath":"rfc/RFC-028.md"}'),o={name:"rfc/RFC-028.md"};function r(n,e,l,d,c,h){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-028-—-data-ts-split-dal-architecture-review-v0-7" tabindex="-1">RFC-028 — data.ts split + DAL architecture review (v0.7) <a class="header-anchor" href="#rfc-028-—-data-ts-split-dal-architecture-review-v0-7" aria-label="Permalink to &quot;RFC-028 — data.ts split + DAL architecture review (v0.7)&quot;">​</a></h1><blockquote><p>Status · Draft Date · 2026-06-13 Target · v0.7 Tracked by · GH issue #327</p></blockquote><blockquote><p><strong>Why this is an RFC.</strong> <code>src/lib/data.ts</code> is 1,565 lines, 83 exports, 37 caller files — one module owning every JSON fetch the app does, every locale overlay, every gallery walker, plus four module-level singletons for provenance + audio + episode-source manifests. The issue tracker frames this as a split, but the real ask is an architecture review: cache shape, locale-overlay handling, where module state belongs, and whether provenance is a DAL concern at all. This RFC locks the answers to those questions BEFORE Phase 1 (the mechanical split) starts, so the slices don&#39;t have to re-litigate design choices mid-flight.</p></blockquote><p><strong>Gating sentence</strong>: Locks the post-split DAL architecture — shared cache behind a <code>getCached&lt;T&gt;()</code> accessor, a single <code>withLocaleOverlay&lt;T&gt;</code> helper, lazy-singleton domain state with explicit per-domain reset hooks, provenance moved out of the DAL into <code>$lib/provenance/</code>, a new <code>galleries-data.ts</code> extraction that breaks the only cross-domain edges in the current call graph, and a 10-slice Phase 1 sequence keeping the public-API barrel — tracked by <a href="https://github.com/chipi/orrery/issues/327" target="_blank" rel="noreferrer">issue #327</a>.</p><h2 id="goal" tabindex="-1">Goal <a class="header-anchor" href="#goal" aria-label="Permalink to &quot;Goal&quot;">​</a></h2><p>End up with ~9 domain modules under <code>$lib/</code> (plus <code>$lib/provenance/</code>) that each own a single subject area, a <code>data-core.ts</code> that owns the cache + the locale-overlay helper, and a thin <code>data.ts</code> barrel that re-exports the public API so the 37 existing callers don&#39;t change. Lock the architectural choices now so the file split itself is mechanical.</p><h2 id="scope-v0-7" tabindex="-1">Scope (v0.7) <a class="header-anchor" href="#scope-v0-7" aria-label="Permalink to &quot;Scope (v0.7)&quot;">​</a></h2><ul><li>Phase 0 (this RFC) — answer the 7 architectural questions from #327, plus the cross-domain gallery question surfaced during the assessment.</li><li>Phase 1 — the mechanical split itself: ~10 slices, one per domain module (+ one for <code>data-core</code>, one for the provenance move). Each slice independently revertable + browser-verified.</li><li>Caller sweep (drop the barrel + migrate the 37 <code>from &#39;$lib/data&#39;</code> imports to per-domain modules) is <strong>out of scope for this RFC</strong>. File a separate issue if bundle measurements show tree-shaking actually moves the needle once the split lands.</li></ul><h2 id="non-goals-v0-7" tabindex="-1">Non-goals (v0.7) <a class="header-anchor" href="#non-goals-v0-7" aria-label="Permalink to &quot;Non-goals (v0.7)&quot;">​</a></h2><ul><li><strong>TanStack-Query / query-key migration</strong> — a real architecture change with SSR + hydration implications. Document the migration path here, but defer to v0.9 alongside the i18n caption pipeline (#257).</li><li><strong>Cache TTL / LRU bound</strong> — at ~100 distinct URLs cached the leak is theoretical. Document the static-site assumption; revisit only if we ever go SSR or per-user.</li><li><strong>Per-domain caches</strong> — accessor pattern (see Q1 answer) keeps the cache shared without exposing the Map. Per-domain caches are an unnecessary indirection at this corpus size.</li><li><strong>Schema/type consolidation</strong> — <code>$types/*</code> already owns shape definitions; this RFC only moves runtime code.</li><li><strong>Caller sweep</strong> — Phase 1 ships a thin barrel; sweep is a separate issue, gated on a real tree-shaking win.</li></ul><h2 id="survey-—-current-data-ts-shape-verified-2026-06-13" tabindex="-1">Survey — current <code>data.ts</code> shape (verified 2026-06-13) <a class="header-anchor" href="#survey-—-current-data-ts-shape-verified-2026-06-13" aria-label="Permalink to &quot;Survey — current \`data.ts\` shape (verified 2026-06-13)&quot;">​</a></h2><ul><li><strong>1,565 lines, 83 exports, 37 caller files</strong> (matches #327&#39;s audit).</li><li><strong>Layers:</strong><ul><li>Core: one <code>cache = new Map&lt;string, unknown&gt;()</code> + <code>async function get&lt;T&gt;(path, fetchFn)</code>. 25 LOC.</li><li>Domain getters: 83 exports across 8+ subject areas. ~1,480 LOC.</li><li>Module state: 3 lazy singletons (<code>provenanceManifest</code>, <code>audioProvenance</code>, <code>episodeSources</code>) + 1 derived index (<code>provenanceIndex</code>).</li></ul></li><li><strong>Inter-getter call graph (audited):</strong><ul><li>Intra-domain calls are common and acyclic: <code>getMissionsForLibrary</code> → <code>getMission</code>, <code>getPlanets</code> → <code>planets()</code>, etc.</li><li><strong>Cross-domain calls all live in gallery code:</strong><ul><li><code>getMoonSiteGallery</code> → <code>getMissionGallery</code> + <code>getFleetGallery</code></li><li><code>getMarsSiteGallery</code> → <code>getMissionGallery</code> + <code>getFleetGallery</code></li></ul></li><li>4 of 4 cross-domain edges are gallery → gallery fan-out to the right &quot;source&quot; domain. Removing galleries from the per-domain modules makes the rest of the graph a clean DAG.</li></ul></li><li><strong>Internal helpers</strong> (not exported): <code>surfaceHotspotsSidecar()</code>, <code>hotspotMetadataOverlay()</code>, <code>getCategoryGallery()</code>. Used only by surface-site + gallery getters.</li><li><strong>Latent bug worth flagging</strong>: <code>__resetCache()</code> clears the URL cache + <code>provenanceManifest</code> + <code>provenanceIndex</code> but <strong>not</strong> <code>audioProvenance</code> or <code>episodeSources</code>. Tests that rely on cold-state audio/episode-source loading would silently get stale state if run after another test exercised those getters. Phase 1&#39;s per-domain reset hook design fixes this for free.</li></ul><h2 id="the-7-architectural-questions-—-locked-answers" tabindex="-1">The 7 architectural questions — locked answers <a class="header-anchor" href="#the-7-architectural-questions-—-locked-answers" aria-label="Permalink to &quot;The 7 architectural questions — locked answers&quot;">​</a></h2><h3 id="q1-·-per-domain-caches-vs-shared-cache-singleton" tabindex="-1">Q1 · Per-domain caches vs shared cache singleton? <a class="header-anchor" href="#q1-·-per-domain-caches-vs-shared-cache-singleton" aria-label="Permalink to &quot;Q1 · Per-domain caches vs shared cache singleton?&quot;">​</a></h3><p><strong>Answer: shared cache, behind a <code>getCached&lt;T&gt;()</code> accessor.</strong> Don&#39;t export the Map.</p><p><code>data-core.ts</code> exports:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getCached</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">fetchFn</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FetchLike</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fetch)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">T</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> clearAllCaches</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// umbrella — calls per-domain reset hooks too</span></span></code></pre></div><p>The internal <code>Map&lt;string, unknown&gt;</code> is module-private. Domain modules can&#39;t accidentally re-declare it (foot-gun Q7) because they don&#39;t have access to it. Invalidation policy stays in one place. URL-keyed cache is naturally global — at our static-site scale ~100 distinct URLs, per-domain partitioning would add complexity without any measurable benefit.</p><h3 id="q2-·-lazy-module-singletons-or-query-key-pattern" tabindex="-1">Q2 · Lazy module singletons or query-key pattern? <a class="header-anchor" href="#q2-·-lazy-module-singletons-or-query-key-pattern" aria-label="Permalink to &quot;Q2 · Lazy module singletons or query-key pattern?&quot;">​</a></h3><p><strong>Answer: keep lazy singletons for v0.7.</strong> Document the migration path to a TanStack-Query-style pattern as a v0.9 follow-up.</p><p>Rationale: switching the 4 manifest-style getters to a query-key pattern is a real architecture change with SSR/hydration implications, and <code>+page.server.ts</code>-shaped data flow we don&#39;t have today. Out of scope for &quot;split the file.&quot; But the split itself must preserve the lazy-singleton pattern cleanly + with proper reset coverage (see Q5 + the latent-bug note above).</p><h3 id="q3-·-locale-overlay-merge-—-shared-transformer-or-inline" tabindex="-1">Q3 · Locale-overlay merge — shared transformer or inline? <a class="header-anchor" href="#q3-·-locale-overlay-merge-—-shared-transformer-or-inline" aria-label="Permalink to &quot;Q3 · Locale-overlay merge — shared transformer or inline?&quot;">​</a></h3><p><strong>Answer: shared.</strong> New helper in <code>data-core.ts</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> WithLocaleOverlayOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TBase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TOverlay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TOut</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  basePath</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  overlayPath</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  locale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  merge</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TBase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">overlay</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TOverlay</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TOut</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> withLocaleOverlay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TBase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TOverlay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TOut</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  opts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> WithLocaleOverlayOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TBase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TOverlay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TOut</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TOut</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><p>Audit shows 7+ getters re-implementing the same <code>fetch base → fetch overlay (catch + ignore 404) → merge</code> pattern (missions, planets, fleet, science, sun, ISS, Tiangong). Three different merge styles exist (shallow, deep, key-based) — the explicit <code>merge</code> callback covers all three without forcing a single strategy.</p><h3 id="q4-·-barrel-forever-or-sweep-callers" tabindex="-1">Q4 · Barrel forever, or sweep callers? <a class="header-anchor" href="#q4-·-barrel-forever-or-sweep-callers" aria-label="Permalink to &quot;Q4 · Barrel forever, or sweep callers?&quot;">​</a></h3><p><strong>Answer: barrel for Phase 1; sweep gated on measurement.</strong></p><p>Phase 1 ships <code>data.ts</code> as a thin barrel re-exporting from the 9 domain modules — zero caller-side changes, zero blast radius. After Phase 1 lands:</p><ol><li>Run a baseline bundle measurement on /explore (a route that today pulls in <code>$lib/data</code> for satellites + belts + small-body galleries but doesn&#39;t need missions/fleet/station JSON).</li><li>If dropping the barrel saves &gt;50 KB on the route&#39;s initial JS, file a separate sweep issue.</li><li>If the saving is &lt;50 KB, the readability win from the split is enough — keep the barrel.</li></ol><p>Tree-shaking is the speculative benefit. Readability is the certain one. The split&#39;s value doesn&#39;t depend on the sweep.</p><h3 id="q5-·-provenance-source-logos-audio-provenance-—-dal-or-separate-concern" tabindex="-1">Q5 · Provenance / source-logos / audio-provenance — DAL or separate concern? <a class="header-anchor" href="#q5-·-provenance-source-logos-audio-provenance-—-dal-or-separate-concern" aria-label="Permalink to &quot;Q5 · Provenance / source-logos / audio-provenance — DAL or separate concern?&quot;">​</a></h3><p><strong>Answer: separate concern. Move out of the DAL entirely.</strong></p><p>Create <code>$lib/provenance/</code> (not <code>provenance-data.ts</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$lib/provenance/</span></span>
<span class="line"><span>  image-manifest.ts      — getImageProvenanceManifest + getImageProvenance + the singleton + reset hook</span></span>
<span class="line"><span>  source-logos.ts         — getSourceLogos + getTextSources + (tiny) singleton + reset</span></span>
<span class="line"><span>  audio-manifest.ts       — getAudioProvenanceManifest + singleton + reset</span></span>
<span class="line"><span>  episode-sources.ts      — getEpisodeSourcesManifest + singleton + reset</span></span>
<span class="line"><span>  index.ts                — barrel for the 5-7 getters callers actually import</span></span></code></pre></div><p>Rationale: these are <strong>meta-data about other data</strong> — which mission a photo belongs to, who licensed it, which TTS provider generated which clip. Different shape from &quot;fetch + locale-merge a content JSON.&quot; Lumping them in the DAL was a copy-paste accident more than a design choice. Moving them out:</p><ul><li>Makes the DAL definition tighter (&quot;fetches + merges content JSON; nothing else&quot;).</li><li>Each provenance module owns its own singleton + reset hook explicitly. Fixes the latent <code>__resetCache</code> bug (audio + episode-source state was leaking across tests).</li><li>Caller impact: 5-10 components (<code>SourceCredit</code>, <code>SourceLogo</code>, <code>LibraryCard</code>, …) import provenance from <code>$lib/data</code> today. They migrate to <code>$lib/provenance</code> in Phase 1&#39;s last slice (concurrent with the barrel re-export so no caller breaks).</li></ul><h3 id="q6-·-cache-invalidation-policy" tabindex="-1">Q6 · Cache invalidation policy. <a class="header-anchor" href="#q6-·-cache-invalidation-policy" aria-label="Permalink to &quot;Q6 · Cache invalidation policy.&quot;">​</a></h3><p><strong>Answer: static-forever for v0.7.</strong> Document the assumption.</p><p><code>data-core.ts</code> header comment + an inline comment on the cache Map explicitly state: &quot;Static-site assumption — the cache lives for the SPA&#39;s lifetime. No TTL, no LRU. At ~100 distinct URLs (mission JSONs + fleet + planets + station + provenance), the memory footprint is bounded by the static asset count. Revisit if we ever go SSR or per-user state.&quot;</p><p>Phase 1 also exports an unused-by-default <code>cache.size</code> accessor that the rendering Debug tab (#334) can surface as a follow-up — gives visibility without committing to a policy.</p><h3 id="q7-·-cache-as-footgun-—-accessor-pattern" tabindex="-1">Q7 · Cache as footgun — accessor pattern. <a class="header-anchor" href="#q7-·-cache-as-footgun-—-accessor-pattern" aria-label="Permalink to &quot;Q7 · Cache as footgun — accessor pattern.&quot;">​</a></h3><p><strong>Answer: yes, see Q1.</strong> The <code>getCached&lt;T&gt;()</code> API hides the Map. Domain modules can&#39;t re-declare it because they don&#39;t have access to the underlying instance. No <code>import { cache } from &#39;./data-core&#39;</code> anywhere in the codebase by design.</p><h2 id="new-finding-—-the-cross-domain-gallery-edges" tabindex="-1">New finding — the cross-domain gallery edges <a class="header-anchor" href="#new-finding-—-the-cross-domain-gallery-edges" aria-label="Permalink to &quot;New finding — the cross-domain gallery edges&quot;">​</a></h2><p>The audit surfaced one architectural decision the issue didn&#39;t pre-answer: <strong>gallery code is the only place where domain modules call into each other</strong>.</p><p>Specifically: <code>getMoonSiteGallery</code> and <code>getMarsSiteGallery</code> (both in surface-site) call <code>getMissionGallery</code> + <code>getFleetGallery</code> for fallback. Naive per-domain split puts surface-site upstream of missions + fleet, which forces a one-way DAG that&#39;s coherent but constraining.</p><p><strong>Answer: extract a separate <code>galleries-data.ts</code> module</strong> that owns every gallery getter:</p><ul><li><code>getMissionGallery</code>, <code>getFleetGallery</code>, <code>getPlanetGallery</code>, <code>getSunGallery</code>, <code>getEarthObjectGallery</code>, <code>getMoonSiteGallery</code>, <code>getMarsSiteGallery</code>, <code>getSmallBodyGallery</code>, <code>getSatelliteGallery</code>, <code>getBeltGallery</code>, <code>getSiteStory</code></li><li><code>getCategoryGallery</code> (the shared category-walker helper)</li><li>The <code>loadHeroOverrides</code> + <code>applyHeroOverride</code> integration with <code>$lib/image-hero</code></li></ul><p>Galleries are a <strong>vertical concern</strong> in the codebase: they all share the same walker pattern (category index → per-id JSON → hero-override application → mission/fleet fallback). They&#39;re tied together by the image-hero pipeline more than by their owning subject area.</p><p>After extraction:</p><ul><li>Domain modules (missions, fleet, surface-site, …) have <strong>zero cross-domain calls</strong>.</li><li>Galleries depends on <code>data-core</code> + <code>$lib/image-hero</code>. Nobody depends on galleries.</li><li>The call graph becomes a clean directed tree rooted at <code>data-core</code>.</li></ul><h2 id="module-breakdown-final-9-modules-lib-provenance" tabindex="-1">Module breakdown (final, 9 modules + <code>$lib/provenance/</code>) <a class="header-anchor" href="#module-breakdown-final-9-modules-lib-provenance" aria-label="Permalink to &quot;Module breakdown (final, 9 modules + \`$lib/provenance/\`)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>New module</th><th>Owns</th><th>Approx LOC</th></tr></thead><tbody><tr><td><code>data-core.ts</code></td><td><code>getCached&lt;T&gt;()</code>, <code>clearAllCaches()</code>, <code>withLocaleOverlay&lt;T&gt;()</code>, internal cache</td><td>~80</td></tr><tr><td><code>missions-data.ts</code></td><td><code>getMissionIndex</code>, <code>getMission</code>, <code>getMissionsForLibrary</code>, <code>MissionFilter</code>, <code>filterMissions</code>, <code>getPorkchopGrid</code></td><td>~200</td></tr><tr><td><code>fleet-data.ts</code></td><td><code>getFleetIndex</code>, <code>getFleet</code>, <code>getFleetByCategory</code></td><td>~150</td></tr><tr><td><code>science-data.ts</code></td><td><code>SCIENCE_TABS</code>, <code>getScienceTab</code>, <code>getScienceSection</code>, <code>getScienceLanding</code>, <code>getScienceTabIntro</code></td><td>~120</td></tr><tr><td><code>surface-site-data.ts</code></td><td><code>planets</code>, <code>getPlanets</code>, <code>earthObjects</code>, <code>getEarthObjects</code>, <code>moonSites</code>, <code>getMoonSites</code>, <code>marsSites</code>, <code>getMarsSites</code>, <code>getMarsTraverse</code>, <code>getSun</code>, <code>getRockets</code>, internal <code>surfaceHotspotsSidecar</code> + <code>hotspotMetadataOverlay</code></td><td>~400</td></tr><tr><td><code>station-data.ts</code></td><td>8 <code>getIss*</code> getters + 8 <code>getTiangong*</code> getters</td><td>~250</td></tr><tr><td><code>scenarios-data.ts</code></td><td><code>getScenario</code></td><td>~30</td></tr><tr><td><code>galleries-data.ts</code> <em>(NEW — see &quot;Cross-domain gallery edges&quot; above)</em></td><td>All gallery getters + <code>getCategoryGallery</code> + <code>getSiteStory</code> + image-hero integration</td><td>~350</td></tr><tr><td><code>small-bodies-data.ts</code></td><td><code>getSatellites</code>, <code>getSatelliteI18n</code>, <code>getBelts</code>, <code>getBeltI18n</code> + their gallery getters move to <code>galleries-data.ts</code></td><td>~150</td></tr><tr><td><code>$lib/provenance/</code> <em>(5 files — see Q5)</em></td><td>All provenance + source-logos + audio + episode-source manifests + their 4 singletons + reset hooks</td><td>~250</td></tr><tr><td><code>data.ts</code> <em>(thin barrel)</em></td><td>Re-exports of the public API across the 9 modules</td><td>~80</td></tr></tbody></table><p>Net: ~2,060 LOC across 11 files (vs 1,565 LOC in one file today). The 25% overhead is the per-file header comments + module barrels + the new <code>withLocaleOverlay</code> helper. Worth it for the readability win.</p><h2 id="slice-plan-—-phase-1" tabindex="-1">Slice plan — Phase 1 <a class="header-anchor" href="#slice-plan-—-phase-1" aria-label="Permalink to &quot;Slice plan — Phase 1&quot;">​</a></h2><p>10 commits, one per logical extraction. Suggested order (smallest/least-connected first):</p><table tabindex="0"><thead><tr><th>Slice</th><th>Commit</th><th>What lands</th></tr></thead><tbody><tr><td><strong>1</strong></td><td><code>feat(data-core)</code></td><td><code>data-core.ts</code> — <code>getCached&lt;T&gt;</code> + <code>clearAllCaches</code> + <code>withLocaleOverlay&lt;T&gt;</code>. <code>data.ts</code> switches to use them (private — no public-API change yet)</td></tr><tr><td><strong>2</strong></td><td><code>refactor(scenarios)</code></td><td><code>scenarios-data.ts</code>. <code>data.ts</code> re-exports.</td></tr><tr><td><strong>3</strong></td><td><code>refactor(science)</code></td><td><code>science-data.ts</code>.</td></tr><tr><td><strong>4</strong></td><td><code>refactor(station)</code></td><td><code>station-data.ts</code> (largest single move; isolated).</td></tr><tr><td><strong>5</strong></td><td><code>refactor(missions)</code></td><td><code>missions-data.ts</code>.</td></tr><tr><td><strong>6</strong></td><td><code>refactor(fleet)</code></td><td><code>fleet-data.ts</code>.</td></tr><tr><td><strong>7</strong></td><td><code>refactor(surface-site)</code></td><td><code>surface-site-data.ts</code>. (After slices 5+6 land so surface-site can no-op import them later if needed.)</td></tr><tr><td><strong>8</strong></td><td><code>refactor(galleries)</code></td><td><code>galleries-data.ts</code> — extracts gallery getters from surface-site + missions + fleet + small-bodies, including the cross-domain fallback logic.</td></tr><tr><td><strong>9</strong></td><td><code>refactor(small-bodies)</code></td><td><code>small-bodies-data.ts</code>.</td></tr><tr><td><strong>10</strong></td><td><code>refactor(provenance)</code></td><td><code>$lib/provenance/</code> (5 files). <code>data.ts</code> re-exports for back-compat. Last slice — most state-heavy + the latent-bug fix.</td></tr></tbody></table><p>After all 10: <code>data.ts</code> is a pure barrel of re-exports. 37 callers untouched.</p><p>Each slice runs:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>npx svelte-check --tsconfig ./tsconfig.json     # 0 errors</span></span>
<span class="line"><span>npx vitest run src/lib/data.test.ts             # all pass after each slice</span></span>
<span class="line"><span>npx vitest run                                  # full suite — 0 regressions</span></span></code></pre></div><p>Plus browser-verify on the routes most affected by the touched domain (e.g. /missions after slice 5; /iss + /tiangong after slice 4; etc.).</p><h2 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h2><ul><li><p><strong>Module-state leaks across slices.</strong> When a singleton moves out of <code>data.ts</code> into its domain module, its reset hook must be added to <code>clearAllCaches</code>&#39;s umbrella. Miss it and <code>__resetCache()</code>-relying tests get stale state silently. Mitigation: every per-domain module exports a <code>_reset()</code> for tests; <code>clearAllCaches</code> calls every one. Codify as the slice template.</p></li><li><p><strong>Import cycles between domain modules.</strong> Eliminated by extracting <code>galleries-data.ts</code> (see the cross-domain finding above). Audit the graph after each slice to confirm no new edge sneaks in — <code>madge --circular src/lib/</code> in CI catches it.</p></li><li><p><strong>Vite + dynamic imports.</strong> <code>$lib/image-hero</code> is imported dynamically (lines 837 + 1198 of current <code>data.ts</code>). Moving the call sites into <code>galleries-data.ts</code> keeps the dynamic-import pattern; Vite handles it the same way. Verify with a production build (<code>npm run build</code>) after slice 8.</p></li><li><p><strong>Caller miss in the barrel.</strong> A re-export typo in <code>data.ts</code> post-split fails typecheck (most cases) but not always at runtime (interfaces vs values). Mitigation: the existing 988-line <code>data.test.ts</code> exercises 80+ public APIs; running it after every slice catches a dropped re-export instantly.</p></li><li><p><strong>Latent <code>__resetCache</code> bug fix changes test outcomes.</strong> Two known singletons (<code>audioProvenance</code>, <code>episodeSources</code>) currently don&#39;t get reset between tests. After slice 10 (provenance) lands, the umbrella reset becomes complete. If any test was silently relying on stale state, it&#39;ll surface as a new failure when slice 10 lands. Treat that as a discovered bug, not a regression — fix the test, not the umbrella.</p></li></ul><h2 id="acceptance" tabindex="-1">Acceptance <a class="header-anchor" href="#acceptance" aria-label="Permalink to &quot;Acceptance&quot;">​</a></h2><ul><li>[ ] All 7 Q-answers above ratified by @chipi at draft review.</li><li>[ ] Galleries-extraction approach (option A) ratified by @chipi.</li><li>[ ] Phase 1 — 10 slices land on <code>main</code>; each independently revertable.</li><li>[ ] <code>data.ts</code> is a pure barrel of re-exports after slice 10.</li><li>[ ] <code>npx svelte-check</code> 0 errors.</li><li>[ ] <code>npx vitest run</code> passes (count grows only by the per-module test exports if we split <code>data.test.ts</code> too — not required).</li><li>[ ] <code>npm run preflight</code> exit 0.</li><li>[ ] <code>madge --circular src/lib/</code> reports no cycles.</li><li>[ ] Browser-verify after each slice on the routes most affected.</li><li>[ ] Bundle measurement on /explore as a follow-up gate for the optional caller-sweep issue.</li></ul><h2 id="open-questions-carried-into-the-implementation-slices" tabindex="-1">Open questions (carried into the implementation slices) <a class="header-anchor" href="#open-questions-carried-into-the-implementation-slices" aria-label="Permalink to &quot;Open questions (carried into the implementation slices)&quot;">​</a></h2><ul><li><strong>Should <code>data.test.ts</code> (988 lines) also split?</strong> Not required for Phase 1 — the umbrella test file can stay together and import from the new domain modules. Splitting it later (one test file per domain module) is a clean-up follow-up.</li><li><strong>Should <code>__resetCache()</code> keep its underscore-prefixed name post-split?</strong> The underscore signalled &quot;internal / test-only.&quot; The new umbrella <code>clearAllCaches()</code> could keep the same convention, or graduate to a public name now that the cache contract is locked. Recommend keeping <code>__</code> for the umbrella; expose a non-underscored <code>clearCache(domain)</code> if a route ever needs targeted invalidation.</li><li><strong>Bundle measurement: which tool, which routes?</strong> <code>vite-bundle-visualizer</code> is on the dev deps already; baseline on <code>/explore</code>, <code>/missions</code>, <code>/fly</code>, <code>/iss</code> since they&#39;re the heavy <code>$lib/data</code> consumers. Run pre-split + post-split.</li><li><strong><code>$lib/provenance/</code> index module — single barrel or per-concern barrels?</strong> Default: single barrel + 5 underlying files. If a caller pattern emerges where one concern dominates (e.g. components only ever need source-logos), split the barrel later.</li></ul>`,66)])])}const u=t(o,[["render",r]]);export{g as __pageData,u as default};
