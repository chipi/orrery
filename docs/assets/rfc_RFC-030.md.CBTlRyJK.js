import{_ as t,o,c as s,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const p=JSON.parse('{"title":"RFC-030 — Responsive image delivery + weight reduction (mobile · web · Google TV)","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-030.md","filePath":"rfc/RFC-030.md"}'),i={name:"rfc/RFC-030.md"};function r(n,e,d,l,c,h){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="rfc-030-—-responsive-image-delivery-weight-reduction-mobile-·-web-·-google-tv" tabindex="-1">RFC-030 — Responsive image delivery + weight reduction (mobile · web · Google TV) <a class="header-anchor" href="#rfc-030-—-responsive-image-delivery-weight-reduction-mobile-·-web-·-google-tv" aria-label="Permalink to &quot;RFC-030 — Responsive image delivery + weight reduction (mobile · web · Google TV)&quot;">​</a></h1><blockquote><p>Status · Partially implemented (Slices 0–3 + WebP-only shipped, #383; Slice 4 auto-derive invariant pending) — decision locked in ADR-080 Date · 2026-07-08 Gates · #383 (tracking) · #379 (image weight) · <strong>evolves ADR-079 Slice C</strong> (masters store + WebP + derivatives) · mobile epic #149 · Google TV target Precondition · <strong>git-LFS masters store</strong> — nothing destructive lands until it exists (D0) Supersedes/extends · ADR-079 D2 (source/derived) + D3 (WebP delivery) — this is the concrete delivery-size design Slice C was deferring to</p></blockquote><blockquote><p><strong>Why this is an RFC.</strong> Cutting image weight touches the whole delivery spine at once: how three clients with a 3× display spread (mobile / web / 4K Google TV) each ask for the right size, whether that&#39;s <code>srcset</code> or an image server, where the permanent originals live (git-LFS masters) vs the churning derivatives, how the pipeline re-derives on every image change so churn can&#39;t re-bloat, and — surfaced during Slice 1b — how a bulk re-encode coexists with the hand-curated byte-dupe / phash <strong>image-authorization</strong> system that keys on derived bytes. Those are architecture questions across the crop pipeline, the components, <code>validate-data</code>, and the deploy; this RFC locks the answers before the slices so they don&#39;t re-litigate mid-flight.</p></blockquote><p><strong>Gating sentence</strong>: display images ship <strong>WebP-only</strong> as a size ladder — an unsuffixed base <code>NN.webp</code> (the canonical, capped ≤ 3072 px q80) plus smaller width-suffixed <code>NN-&lt;w&gt;.webp</code> rungs — that DOM <code>&lt;img&gt;</code> selects via <code>srcset</code>/<code>sizes</code> (viewport × DPR, so phone/desktop/4K-TV each fetch the size they render) while 3D textures select by quality-tier, all URLs resolving through the <code>assetUrl</code> spine; every <strong>full-resolution original is kept permanently in the git-LFS <code>masters/</code> store</strong> (so any rung / format regenerates losslessly), and the ladder is derived through the pipeline — <strong><code>hotspots/</code> (zoomable 3D tiers) and <code>posters/</code> (downloadable art) stay full-res JPEG</strong>. (The auto-derive <code>validate-data</code> invariant that would fail preflight on an oversized/ladder-incomplete image is Slice 4, pending.)</p><p><strong>Shipped result (2026-07-08):</strong> deploy image tree <strong>~1.9 GB → ~897 MB</strong>; masters (~1 GB) in git-LFS ($0 tier). The journey went cap → additive WebP ladder → WebP-only (jpg dropped); the ~4% ancient-web-browser gap is accepted (ADR-079, all app targets are WebP-native).</p><hr><h2 id="goal" tabindex="-1">Goal <a class="header-anchor" href="#goal" aria-label="Permalink to &quot;Goal&quot;">​</a></h2><p>Two coupled outcomes:</p><ol><li><strong>Weight</strong> — cut the served image bulk (<code>build/images</code> ~1.9 GB → ~750 MB; deploy ~2.2 GB → ~1.1 GB) so the VPS deploy and the Capacitor mobile CDN stream are light.</li><li><strong>Per-client sizing</strong> — each of the three targets fetches a size appropriate to its display, with <strong>no per-platform build flag and no image server</strong>: <ul><li><strong>mobile</strong> ~1280 px (streamed over cellular),</li><li><strong>web</strong> ~1440–2560 px,</li><li><strong>Google TV</strong> (40–50″ = 4K 3840×2160; a 90vw lightbox ≈ 3456 px) ~3072 px.</li></ul></li></ol><p>And — because images churn constantly — the whole thing must be <strong>self-maintaining in the workflow</strong>, not a manual pass.</p><h2 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h2><ul><li>An on-the-fly image server (imgproxy / Cloudflare Images) — see D1 alternatives; reconsider only if the ladder becomes unwieldy.</li><li>Re-sizing the zoomable <code>hotspots/</code> 3D surface tiers (they keep their existing tier system).</li><li>A separate WebP slice — WebP (ADR-079 D3) is folded directly into the ladder derivation (the rungs are encoded as WebP), not deferred.</li></ul><h2 id="survey-—-measured-current-state-2026-07-08" tabindex="-1">Survey — measured current state (2026-07-08) <a class="header-anchor" href="#survey-—-measured-current-state-2026-07-08" aria-label="Permalink to &quot;Survey — measured current state (2026-07-08)&quot;">​</a></h2><p><code>build/images</code> ≈ 1.9 GB. Two populations plus one exclusion:</p><table tabindex="0"><thead><tr><th>Population</th><th>Size</th><th>Files</th><th>Status</th></tr></thead><tbody><tr><td>1×1 thumbnails</td><td>~76 MB (was 653)</td><td>2,523</td><td><strong>Done</strong> — Slice 0 (crop-variants 512 px cap + in-place migration this cycle)</td></tr><tr><td>Full-size (hero / gallery / lightbox)</td><td>1,049 MB</td><td>3,067</td><td>this RFC</td></tr><tr><td><code>hotspots/</code> (3D surface tier2/tier3 pan/zoom)</td><td>176 MB</td><td>232</td><td><strong>excluded</strong> — genuinely need resolution</td></tr></tbody></table><p><strong>Key measurements:</strong></p><ul><li><p>The full-size tail is broken: gallery photos at <strong>5000–6720 px / 7.5 MB</strong>, displayed in a <strong>fit-to-viewport lightbox</strong> (<code>PanelLightbox</code>: <code>max-width:90vw; object-fit:contain; cursor:zoom-out</code>) — <strong>no pinch-zoom</strong>. So resolution beyond viewport is pure waste.</p></li><li><p><strong>The weight is quality, not pixels.</strong> Capping non-hotspot images at various sizes + q85 mozjpeg (dry-run over 2,835 files):</p><table tabindex="0"><thead><tr><th>Cap</th><th>Quality</th><th>Saves</th><th>Files actually resized</th></tr></thead><tbody><tr><td>2048 px</td><td>q82</td><td>435 MB</td><td>301</td></tr><tr><td>2560 px</td><td>q85</td><td>348 MB</td><td>158</td></tr><tr><td>3072 px</td><td>q85</td><td>326 MB</td><td>69</td></tr><tr><td><strong>3840 px</strong></td><td><strong>q85</strong></td><td><strong>312 MB</strong></td><td><strong>40</strong></td></tr></tbody></table><p>Going all the way to <strong>native 4K (3840)</strong> costs only 123 MB vs the aggressive 2048 cap — because almost all the win is <strong>re-encoding bloated-quality JPEGs</strong> (saved at q90–95+), not downsizing. <strong>TV resolution is therefore NOT in tension with size.</strong></p></li></ul><h2 id="decisions" tabindex="-1">Decisions <a class="header-anchor" href="#decisions" aria-label="Permalink to &quot;Decisions&quot;">​</a></h2><h3 id="d0-—-masters-are-permanent-everything-served-is-a-regenerable-derivative" tabindex="-1">D0 — Masters are permanent; everything served is a regenerable derivative <a class="header-anchor" href="#d0-—-masters-are-permanent-everything-served-is-a-regenerable-derivative" aria-label="Permalink to &quot;D0 — Masters are permanent; everything served is a regenerable derivative&quot;">​</a></h3><p><strong>Invariant (never relaxed):</strong> the full-resolution <strong>original</strong> of every image is kept forever in a non-served <code>masters/</code> store, <strong>never mutated</strong>. Every file in <code>static/images/</code> — thumbnail, ladder rung, WebP — is a <strong>derivative</strong> that can be regenerated from its master at any time, losslessly. So any future change is cheap: a new rung, a higher-quality re-encode, WebP→AVIF, a different crop — all re-derive from masters, never from an already-lossy served copy.</p><p>This RFC <strong>de-facto executes ADR-079&#39;s deferred Slice C</strong> (masters store + derivatives), and <strong>git-LFS is the hard precondition</strong> — masters (~1–2 GB of originals) live in LFS, out of main git; pointers only in the tree. <strong>Nothing destructive lands until LFS + <code>masters/</code> exist</strong> (Slice 1a), because today <code>static/images</code> is the <em>only</em> copy — capping in place first would forfeit the very regenerability this invariant exists to guarantee. (Slice 0 needed no master: it touched only derived 1×1 crops, leaving the plain <code>.jpg</code> intact.)</p><p><strong>Split:</strong> only <strong>masters → LFS</strong>. The <strong>derived</strong> ladder stays in the served <code>static/images/</code> tree so the deploy serves it directly (ADR-079: CI validates + deploys, never encodes — derivatives must be pre-committed). Open question 6 revisits whether <em>derived</em> should also move to LFS now that GH Pages is retired and the VPS can LFS-pull on deploy — attractive because derived binaries churn and otherwise grow main-git history unbounded.</p><h3 id="d1-—-pre-generated-size-ladder-srcset-not-an-on-the-fly-image-server" tabindex="-1">D1 — Pre-generated size ladder + <code>srcset</code> (not an on-the-fly image server) <a class="header-anchor" href="#d1-—-pre-generated-size-ladder-srcset-not-an-on-the-fly-image-server" aria-label="Permalink to &quot;D1 — Pre-generated size ladder + `srcset` (not an on-the-fly image server)&quot;">​</a></h3><p>Delivery <code>&lt;img&gt;</code> images carry a <code>srcset</code> of pre-generated widths; the browser asks for the right one per client:</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!-- WebP-only; the largest rung is the unsuffixed base NN.webp (also the src). --&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">img</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  srcset</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…/NN-1280.webp 1280w, …/NN-2048.webp 2048w, …/NN.webp 3072w&quot;</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  sizes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;90vw&quot;</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  src</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…/NN.webp&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> alt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div><p>The browser picks the smallest candidate satisfying <strong>viewport × DPR</strong> — phone → 1280, desktop → 2048, 4K-TV lightbox (3840 CSS px, DPR 1, 90vw ≈ 3456) → 3072. This <strong>is</strong> &quot;ask for a size per client,&quot; done natively — no platform detection, and mobile streams the small candidate automatically.</p><p><strong>Why not an image server</strong> (imgproxy / thumbor / Cloudflare Images serving <code>?w=</code>): the app is static-origin (VPS nginx + Capacitor streaming from a static CDN). srcset is browser-native, composes with the <code>assetUrl</code> spine + SW caching, and needs no service / CPU / cache infra. An image server doesn&#39;t fit Capacitor&#39;s stream-from-static-origin model without making the CDN itself that server. Deferred, not chosen.</p><h3 id="d2-—-served-derivative-policy-cap-the-largest-rung-at-3840-px-q85-mozjpeg" tabindex="-1">D2 — Served-derivative policy: cap the largest rung at 3840 px + q85 mozjpeg <a class="header-anchor" href="#d2-—-served-derivative-policy-cap-the-largest-rung-at-3840-px-q85-mozjpeg" aria-label="Permalink to &quot;D2 — Served-derivative policy: cap the largest rung at 3840 px + q85 mozjpeg&quot;">​</a></h3><p>The <strong>original master</strong> (in <code>masters/</code>, D0) keeps its full resolution. The largest <strong>served</strong> rung is capped at <strong>3840 px</strong> (native 4K — trims only the ~40 absurd &gt;4K files, nothing a 50″ panel can show) and re-encoded <strong>q85 mozjpeg</strong> (the visually-lossless floor; q85 on a hero at 3840 px is imperceptible — gated by a visual sign-off on a TV-sized viewport, Slice 1). This is −312 MB with full TV quality, and because the original is retained it is fully reversible.</p><h3 id="d3-—-client-selects-by-viewport-never-by-build-flag" tabindex="-1">D3 — Client selects by viewport, never by build-flag <a class="header-anchor" href="#d3-—-client-selects-by-viewport-never-by-build-flag" aria-label="Permalink to &quot;D3 — Client selects by viewport, never by build-flag&quot;">​</a></h3><p><code>srcset</code> / <code>sizes</code> on DOM <code>&lt;img&gt;</code>; the browser (mobile WebView / desktop / TV WebView) chooses by viewport × DPR. <strong>No <code>__MOBILE__</code> / <code>__TV__</code> branching for images</strong> — a build flag is too coarse (it can&#39;t tell a phone-on-web from a 4K monitor, and TV-as-Capacitor-Android would collide with mobile&#39;s flag). Viewport handles every gradation.</p><h3 id="d4-—-3d-programmatic-textures-select-by-quality-tier" tabindex="-1">D4 — 3D / programmatic textures select by quality-tier <a class="header-anchor" href="#d4-—-3d-programmatic-textures-select-by-quality-tier" aria-label="Permalink to &quot;D4 — 3D / programmatic textures select by quality-tier&quot;">​</a></h3><p><code>srcset</code> can&#39;t reach <code>TextureLoader</code>. Surface / hotspot skybox textures pick their resolution at runtime by <strong>quality-tier</strong> — the existing pattern (the <code>4k</code>/<code>2k</code> gate under <code>__MOBILE__</code>, ADR-079). <code>&lt;img&gt;</code> → srcset; textures → tier-pick. Two mechanisms, both already have a home.</p><h3 id="d5-—-exclude-hotspots-and-posters-from-the-ladder" tabindex="-1">D5 — Exclude <code>hotspots/</code> and <code>posters/</code> from the ladder <a class="header-anchor" href="#d5-—-exclude-hotspots-and-posters-from-the-ladder" aria-label="Permalink to &quot;D5 — Exclude `hotspots/` and `posters/` from the ladder&quot;">​</a></h3><p>Two categories stay full-res JPEG (excluded from the WebP ladder + the cap, in both <code>build-display-ladder.mjs</code> and <code>cap-display-images.mjs</code>, and skipped by the provenance re-key):</p><ul><li><strong><code>hotspots/</code></strong> — the tier2/tier3 pan/zoom panoramas keep full resolution + their tier system.</li><li><strong><code>posters/</code></strong> — downloadable art; the <code>/posters</code> page links them for download, so a capped WebP would degrade the download and JPEG is more universally openable + print-friendly (decided 2026-07-08).</li></ul><p>The ladder + cap apply only to lightbox/hero/gallery display images.</p><h3 id="d6-—-origin-through-the-asseturl-spine" tabindex="-1">D6 — Origin through the <code>assetUrl</code> spine <a class="header-anchor" href="#d6-—-origin-through-the-asseturl-spine" aria-label="Permalink to &quot;D6 — Origin through the `assetUrl` spine&quot;">​</a></h3><p>Every <code>srcset</code> candidate resolves through <code>assetUrl()</code> (ADR-079 D1) → CDN-absolute under <code>__MOBILE__</code>, local otherwise. The SW caches whichever size the browser fetched. No new streaming logic.</p><h3 id="d7-—-automatic-derivation-a-validate-data-invariant-the-churn-guarantee" tabindex="-1">D7 — Automatic derivation + a <code>validate-data</code> invariant (the churn guarantee) <a class="header-anchor" href="#d7-—-automatic-derivation-a-validate-data-invariant-the-churn-guarantee" aria-label="Permalink to &quot;D7 — Automatic derivation + a `validate-data` invariant (the churn guarantee)&quot;">​</a></h3><p>Masters and the ladder are <strong>source vs derived</strong> (ADR-079): the committed master is the source; the ladder (1280/2048/3072) + 512 thumbnail are <strong>derived</strong>, generated by the pipeline (<code>crop-variants</code> / <code>images:score</code>), cache-keyed so unchanged images are free.</p><ul><li><strong>On ingest</strong> (<code>/dev/staging</code> promote + <code>fetch-assets</code>): cap the master (3840/q85) and derive the ladder + thumbnail automatically — no oversized image enters the shipped tree.</li><li><strong><code>validate-data</code> invariant</strong> (new gate): every shipped delivery image ≤ 3840 px, every master has its <strong>complete, fresh</strong> ladder + thumbnail, and thumbnails ≤ 512 px. <strong>Fails preflight otherwise</strong> — a manually-dropped or swapped image can&#39;t ship un-resized. Fix = one <code>npm run images:derive</code>.</li><li><strong>Churn:</strong> add → derived; replace → cache-key miss → re-derived + the gate catches staleness; delete → derived pruned (orphan check). This is what makes the size win permanent instead of a one-off that decays as missions/fleet grow.</li></ul><h2 id="the-size-ladder" tabindex="-1">The size ladder <a class="header-anchor" href="#the-size-ladder" aria-label="Permalink to &quot;The size ladder&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Rung</th><th>Width</th><th>Consumer</th><th>Rationale</th></tr></thead><tbody><tr><td>thumbnail</td><td>512 px</td><td>1×1 cards / mobile rows</td><td>≤140 px display × DPR3 (<strong>done</strong>, Slice 0)</td></tr><tr><td>display-s</td><td>1280 px</td><td>mobile lightbox/hero (streamed)</td><td>~400 CSS px × DPR3; light over cellular</td></tr><tr><td>display-m</td><td>2048 px</td><td>desktop / laptop</td><td>≤1440p retina lightbox</td></tr><tr><td>display-l</td><td>3072 px</td><td>4K TV / 4K monitor</td><td>90vw of 3840 ≈ 3456 → 3072 nearest</td></tr><tr><td><strong>original (master)</strong></td><td><strong>full res</strong></td><td><strong>not served</strong> — source in <code>masters/</code> (D0)</td><td>regenerate any rung / WebP / AVIF losslessly</td></tr></tbody></table><p>The <strong>interim</strong> (Slice 1b, before the ladder exists) serves a single image capped at <strong>3840 px</strong> per slot; Slice 2–3 replace that with the <code>srcset</code> ladder above. Exact rungs are an open question — 1536 could be added; keep the ladder short to bound file-count.</p><h2 id="sizes-per-component-contract-load-bearing" tabindex="-1"><code>sizes</code>-per-component contract (load-bearing) <a class="header-anchor" href="#sizes-per-component-contract-load-bearing" aria-label="Permalink to &quot;`sizes`-per-component contract (load-bearing)&quot;">​</a></h2><p>The one thing that must be right — a wrong <code>sizes</code> makes the browser over/under-fetch:</p><table tabindex="0"><thead><tr><th>Component</th><th><code>sizes</code></th></tr></thead><tbody><tr><td><code>PanelHeroImage</code> (detail hero)</td><td><code>100vw</code></td></tr><tr><td><code>PanelLightbox</code> (full view)</td><td><code>90vw</code></td></tr><tr><td>gallery card / fleet row</td><td><code>140px</code> (→ thumbnail, already 1×1)</td></tr><tr><td>mission/fleet grid tile</td><td><code>minmax(96–140px)</code> → its rendered width</td></tr></tbody></table><p>A lint/test flags any delivery <code>&lt;img&gt;</code> without a <code>sizes</code> attribute (Slice 3).</p><h2 id="slice-plan" tabindex="-1">Slice plan <a class="header-anchor" href="#slice-plan" aria-label="Permalink to &quot;Slice plan&quot;">​</a></h2><ul><li><p><strong>Slice 0 — thumbnails · DONE.</strong> <code>crop-variants</code> capped at 512 px + in-place migration (−533 MB, committed this cycle). Safe without masters — only touched derived 1×1 crops.</p></li><li><p><strong>Slice 1a — git-LFS masters store + $0-cost config (the precondition · ADR-079 Slice C).</strong> In order:</p><ol><li><code>git lfs install</code>; <code>git lfs track &quot;masters/**&quot;</code> (→ <code>.gitattributes</code>).</li><li>Commit <strong><code>.lfsconfig</code></strong> with <code>fetchexclude = masters/**</code> — makes <strong>skip-masters the default for every clone/CI/VPS checkout</strong>, so LFS bandwidth stays inside the free tier (~$0/mo, RFC §cost). The audit (2026-07-08) confirmed CI is already safe (all <code>actions/checkout</code> default <code>lfs: false</code>; the static deploy rsyncs <code>build/</code>, never git-LFS); <code>.lfsconfig</code> makes that structural + covers the VPS-side <code>git fetch+reset</code> checkout.</li><li>Copy the current full-res <code>static/images</code> originals into <code>masters/</code> (LFS-tracked), preserving structure — the permanent originals, <strong>before</strong> any derivative is capped.</li><li>Convention: the <strong>only</strong> thing that fetches masters is the local encode step, explicitly <code>git lfs pull --include=&#39;masters/**&#39;</code>; nothing else ever pulls them.</li></ol><p><strong>Must precede 1b</strong> — it&#39;s the only non-lossy path, and the D0 invariant depends on it.</p></li><li><p><strong>Slice 1b — served cap + backfill.</strong> Cap non-hotspot served images at 3840/q85, derived from masters (−312 MB). Immediate deploy win, no delivery change yet. Gated by a hero visual sign-off on a 3840 px viewport.</p></li><li><p><strong>Slice 2 — derive the ladder, as WebP.</strong> Extend <code>crop-variants</code> to emit the 1280/2048/3072 rungs <strong>encoded WebP</strong> (ADR-079 D3 folded in here, not deferred) next to each master; wire into <code>images:score</code> + promote (D7). Stacks ~30 % on top of the cap.</p></li><li><p><strong>Slice 3 — wire <code>srcset</code>.</strong> <code>PanelHeroImage</code>, <code>PanelLightbox</code>, gallery/grid components emit <code>srcset</code> + <code>sizes</code>; the contract above; the lint/test guard.</p></li><li><p><strong>Slice 4 — validate + gate.</strong> <code>validate-data</code> invariant (D7); Layer-1 mobile e2e asserts the mobile viewport fetches the small candidate; verify per-client fetch at mobile/desktop/<strong>4K-TV</strong> viewports.</p></li></ul><h2 id="migration" tabindex="-1">Migration <a class="header-anchor" href="#migration" aria-label="Permalink to &quot;Migration&quot;">​</a></h2><p>Slice 1 reuses the Slice-0 pattern: bump the <code>crop-variants</code> cache key (already includes cap + quality) so the pipeline re-derives, plus an in-place <code>optimize</code> pass for the committed masters. Idempotent — a master already ≤ cap and reasonably sized is skipped.</p><p><strong>File-count note:</strong> the ladder adds ~3 derived files per master (~9 k new files). We are <strong>VPS-only</strong> now (GH Pages retired), so file-count is far less critical than under GH Pages&#39; soft limit — but the ladder is still kept short (3 rungs) to bound it.</p><h2 id="risks" tabindex="-1">Risks <a class="header-anchor" href="#risks" aria-label="Permalink to &quot;Risks&quot;">​</a></h2><ul><li><strong><code>sizes</code> correctness</strong> — per-component; wrong values silently over/under-fetch. Mitigate with the Slice-3 lint/test guard.</li><li><strong>q85 on heroes at TV scale</strong> — visual sign-off on a 3840 px viewport before Slice 1 commits.</li><li><strong>Hotspot exclusion must be airtight</strong> — the cap/ladder must never touch <code>hotspots/</code> (an accidental downsize breaks zoom). Enforced by path exclusion + a test.</li><li><strong>Derived-orphan drift</strong> — deleted masters leaving stale ladder files; the D7 orphan check catches it.</li><li><strong>WebP-only, no <code>&lt;picture&gt;</code>/jpg fallback (shipped decision)</strong> — all three app targets are WebP-native (Capacitor iOS 15 WKWebView, Android + Google-TV Chromium WebView) and web is ~96% (caniuse); the ~4% ancient-web-browser gap is accepted (ADR-079). The base <code>src</code> is itself a <code>.webp</code>, so browsers ignoring <code>srcset</code> still get WebP.</li></ul><h2 id="acceptance" tabindex="-1">Acceptance <a class="header-anchor" href="#acceptance" aria-label="Permalink to &quot;Acceptance&quot;">​</a></h2><ul><li><code>build/images</code> ≤ ~750 MB; deploy ≤ ~1.1 GB.</li><li>At mobile / desktop / 4K-TV viewports, the network panel shows each fetching its appropriate ladder rung.</li><li>Heroes are sharp on a 50″ 4K viewport; q85 passes visual sign-off.</li><li><code>hotspots/</code> bytes unchanged.</li><li>Dropping an oversized image and running <code>validate-data</code> <strong>fails</strong> until re-derived (the churn guarantee).</li><li>Layer-1 mobile e2e asserts the mobile viewport fetches the small candidate.</li></ul><h2 id="decisions-locked-2026-07-08-were-open-questions" tabindex="-1">Decisions locked 2026-07-08 (were open questions) <a class="header-anchor" href="#decisions-locked-2026-07-08-were-open-questions" aria-label="Permalink to &quot;Decisions locked 2026-07-08 (were open questions)&quot;">​</a></h2><ul><li><strong>Ladder rungs = 1280 / 2048 / 3072</strong> (3 rungs; no 1536).</li><li><strong>WebP is folded into this work</strong>, not deferred — the ladder is derived <em>as WebP</em> (Slice 2 encodes WebP directly; ADR-079 D3). No separate Slice 5.</li><li><strong>Google TV = Capacitor Android</strong> (Android-TV target). So TV is a Capacitor build like mobile → it <strong>streams</strong> images from the CDN (shares the mobile stream-heavy path); <code>srcset</code> still picks the large rung by the TV&#39;s 4K viewport.</li><li><strong>git-LFS backing store = GitHub LFS.</strong> Cost analysis below.</li></ul><h2 id="git-lfs-cost-estimate-github-metered-billing-2026" tabindex="-1">git-LFS cost estimate (GitHub, metered billing 2026) <a class="header-anchor" href="#git-lfs-cost-estimate-github-metered-billing-2026" aria-label="Permalink to &quot;git-LFS cost estimate (GitHub, metered billing 2026)&quot;">​</a></h2><p>Rates (current — data packs removed, now metered): <strong>10 GiB storage + 10 GiB bandwidth free/month</strong> (Free/Pro; 250 GiB Team/Enterprise); overage <strong>$0.07/GiB-mo storage</strong>, <strong>$0.0875/GiB bandwidth</strong>.</p><p>Our profile: <strong>masters ≈ 1.05 GB / 3 067 files</strong>, high churn (~154 image commits / 60 days).</p><ul><li><strong>Storage:</strong> masters ~1 GB, growing ~0.5–1 GB/yr from churn + new missions/fleet — <strong>well under the 10 GiB free tier for years → $0.</strong></li><li><strong>Bandwidth (the variable):</strong> LFS bytes are billed on <em>download</em>. Masters are needed <strong>only by the machine that encodes derivatives</strong> (local). <strong>CI + deploy use the committed <em>derived</em> tree (regular git) and must skip the LFS smudge for masters</strong> (<code>lfs.fetchexclude</code>/<code>GIT_LFS_SKIP_SMUDGE=1</code>) — they never pull masters. With that, bandwidth is just occasional dev/agent clones (~1–5 GB/mo) → <strong>under free → $0.</strong></li><li><strong>Anticipated monthly cost ≈ $0</strong>, <em>conditional on</em> CI/agents not smudging masters. The failure mode is uncontrolled: if every CI run / fresh agent env pulls the ~1 GB of masters, bandwidth blows past 10 GiB fast (e.g. 40 GB over = ~$3.50/mo). <strong>Mitigation is mandatory config, not optional.</strong></li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol start="0"><li><strong>RESOLVED (2026-07-08) — bulk re-encode vs the image-authorization system.</strong> <code>validate-image-dupes</code> + <code>validate-image-phash-dupes</code> are hand-curated authorization gates (byte <code>ALLOWLIST</code> keyed on 8-char SHA-256 + a phash baseline) that scanned the <strong>derived</strong> <code>static/images</code> tree, so a bulk re-encode (Slice 1b cap; Slice 2 WebP) invalidated the whole allowlist (Slice 1b produced 140 un-allowlisted byte groups + 68 phash pairs). <strong>Fix (option a): the validators + <code>compute-phash</code> now key on <code>masters/</code></strong> — the stable source of truth, byte-identical to the pre-cap originals, so the <em>existing</em> allowlist matches unchanged and re-encoding the derived tree no longer touches dedup. Masters are LFS <code>fetchexclude</code>d, so each validator <strong>skips when masters are pointer stubs</strong> (CI / fresh clones) and runs only where smudged (<code>git lfs pull -I &#39;masters/**&#39;</code>) — dedup is now a local authoring check, not a CI gate. Verified: after the −311 MB cap, <code>validate-data</code> stays green. Content triage of the dupes themselves stays in #384.</li><li>Does <code>validate-data</code>&#39;s invariant check dimensions cheaply enough at scale (~9 k derived files), or does it need a cached manifest of derived sizes?</li><li><strong>Do <em>derived</em> images also go to LFS</strong>, or only masters? Default (chosen for now) is <strong>masters-only</strong> — derived committed to regular git, served from the checkout, and CI/deploy skip master smudge (keeps LFS bandwidth ≈ $0). Moving derived to LFS too would keep main-git history lean against churn, but the VPS would LFS-pull derived on every deploy — bandwidth = derived-size × deploys/mo (~750 MB × N), which <em>does</em> incur cost and pulls masters-skip logic apart. Revisit only if main-git history growth becomes the bigger pain.</li></ol><hr><p><em>Orrery · RFC-030 · v0.8 — responsive image delivery. Extends ADR-079; locked decision in ADR-080.</em></p>',68)])])}const u=t(i,[["render",r]]);export{p as __pageData,u as default};
