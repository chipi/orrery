import{_ as t,o as s,c as a,a2 as i}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"RFC-034 — Launch / powered-ascent engine + Scene 0 (the ascent act of /fly)","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-034.md","filePath":"rfc/RFC-034.md"}'),n={name:"rfc/RFC-034.md"};function o(r,e,c,d,l,h){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="rfc-034-—-launch-powered-ascent-engine-scene-0-the-ascent-act-of-fly" tabindex="-1">RFC-034 — Launch / powered-ascent engine + Scene 0 (the ascent act of <code>/fly</code>) <a class="header-anchor" href="#rfc-034-—-launch-powered-ascent-engine-scene-0-the-ascent-act-of-fly" aria-label="Permalink to &quot;RFC-034 — Launch / powered-ascent engine + Scene 0 (the ascent act of \`/fly\`)&quot;">​</a></h1><p><em>Status: Draft (epic kickoff) · 2026-07 · Closes: PRD-032 · target: v2.x</em></p><blockquote><p><strong>Why this is an RFC.</strong> Prepending a powered-ascent act to <code>/fly</code> binds architectural commitments that must be right from slice 0 or every later slice forces a rewrite: (1) a <strong>new launch-site-relative coordinate frame</strong> (Scene 0) — you cannot render a launchpad in the AU-scale heliocentric world without float-precision death; (2) a <strong>multi-scale clock</strong> that runs seconds-resolution ascent and days-resolution cruise on <em>one</em> continuous scrubbable timeline; (3) an <strong>integrated ascent physics engine</strong> (equations of motion, not keyframes) that must validate in CI the way <code>fly-physics</code> does today; (4) a <strong>per-vehicle LaunchProfile data library</strong> keyed on the existing <code>fleet_refs</code> launcher id, with a generic fallback; and (5) a <strong>re-basing scene handoff</strong> from the ascent frame into the existing heliocentric / cislunar scene at orbit injection. The existing <code>/fly</code> cruise must not regress. UX/HUD detail beyond this doc is captured against PRD-032.</p></blockquote><h2 id="_1-·-architecture-overview" tabindex="-1">1 · Architecture overview <a class="header-anchor" href="#_1-·-architecture-overview" aria-label="Permalink to &quot;1 · Architecture overview&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/fly route — mission timeline extended to one continuous pad→destination arc</span></span>
<span class="line"><span> └─ AscentContext (Scene 0) — NEW launch-site-relative frame (ENU, m→km world units)</span></span>
<span class="line"><span> │    • integrated planar (2-DOF) ascent EOM from a per-vehicle LaunchProfile</span></span>
<span class="line"><span> │    • owns MET 0 → injection; then re-bases (warp) into the helio/cislunar scene</span></span>
<span class="line"><span> ├─ MultiScaleClock — seconds-res ascent act ⊕ days-res cruise, ONE scrubber</span></span>
<span class="line"><span> ├─ LaunchProfile library — static/data/launch-profiles/&lt;launcher-id&gt;.json</span></span>
<span class="line"><span> │    • flagship: hand-authored wet/dry mass, Isp(SL/vac), thrust, mdot, staging,</span></span>
<span class="line"><span> │      fairing jettison, pitch program · generic: parameterized 2-stage fallback</span></span>
<span class="line"><span> ├─ Ascent engine — src/lib/orbital/ascent-physics.ts (pure fns, CI-validated)</span></span>
<span class="line"><span> ├─ AscentCameras — REUSE the #371 montage shot-cutting (flyby-shots.ts pattern):</span></span>
<span class="line"><span> │    pad-wide → tower-clear → tracking → onboard-down → staging → MECO/orbit</span></span>
<span class="line"><span> └─ Telemetry HUD + Science-Lens ascent layers (TWR · q · gravity/drag/steering loss · force vectors)</span></span></code></pre></div><p>Conceptually this is the <em>innermost context</em> below RFC-032&#39;s context stack: <code>LaunchSite ⊂ SolarSystem ⊂ …</code>. The re-basing warp at injection is the same move as RFC-032&#39;s context boundaries and ADR-058&#39;s helio→cislunar SOI handoff — established precedent, not a new pattern.</p><h2 id="_2-·-design-decisions-to-lock" tabindex="-1">2 · Design decisions (to lock) <a class="header-anchor" href="#_2-·-design-decisions-to-lock" aria-label="Permalink to &quot;2 · Design decisions (to lock)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>id</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td><strong>L-A</strong></td><td><strong>New launch-site-relative frame (Scene 0), not the AU scene</strong></td><td>A launchpad is metres; the heliocentric world is 10¹¹ m. One float space can&#39;t hold both. Scene 0 is Earth-surface-relative (ENU), world units m→km, disposed after handoff.</td></tr><tr><td><strong>L-B</strong></td><td><strong>One continuous timeline via a multi-scale clock</strong></td><td>PRD-032&#39;s core promise is scrubbing pad→destination in one drag. The clock is seconds-dense in the ascent window, days-dense in cruise — a non-linear map over the existing time-control (#351/#387), not two clocks.</td></tr><tr><td><strong>L-C</strong></td><td><strong>Fully integrated EOM; hybrid data-anchor as the escape hatch</strong></td><td>Physics-first: the trajectory emerges from thrust/gravity/drag on real mass. If integration proves too complex or the ride reads worse than reality, fall back to anchoring the integration to published milestones (MECO/staging alt+vel) — the same philosophy as the shipping <code>tier_1_5_hybrid</code> trajectory data. Decision is reversible per-vehicle.</td></tr><tr><td><strong>L-D</strong></td><td><strong>Per-vehicle LaunchProfile keyed on <code>fleet_refs</code> launcher id; generic 2-stage fallback</strong></td><td>Ascent shape belongs to the rocket, not the payload. Missions already name their launcher. Flagship profiles hand-authored; every other launcher gets a parameterized generic profile so no mission is launch-less.</td></tr><tr><td><strong>L-E</strong></td><td><strong>Reuse the #371 montage engine for the shot sequence</strong></td><td>Launch has the most codified shot vocabulary in spaceflight; <code>flyby-shots.ts</code> (<code>composeShot</code>) + the MET-relative schedule already do cut-on-beats camera work. Author an ascent shot schedule; do <strong>not</strong> build a second camera system.</td></tr><tr><td><strong>L-F</strong></td><td><strong>Scene 0 lazy-loaded; cruise must not regress</strong></td><td>Mirror RFC-032 C-F: the ascent scene + its physics/data are a dynamic import gated on the mission having a launch profile and the clock being in the ascent window. <code>/fly</code> bundle weight and cruise perf are held.</td></tr><tr><td><strong>L-G</strong></td><td><strong>Cinematic + scrubber + HUD, not a game</strong></td><td>PRD-032 non-goal. Interactive fly-it-yourself is deferred (§9) on the <em>same</em> engine — the EOM core is authored so a control input can drive it later.</td></tr><tr><td><strong>L-H</strong></td><td><strong>2-DOF planar integration first; 3-DOF (yaw/dogleg) deferred</strong></td><td>The launch plane carries all the teachable physics. Azimuth steering / doglegs add cost without new pedagogy; defer unless a flagship profile visibly needs it.</td></tr><tr><td><strong>L-I</strong></td><td><strong><code>ascent-physics.ts</code> is a headless core reused by both <code>/fly</code> and <code>/plan</code></strong></td><td>Same pattern as the shared <code>fly-physics.ts</code>. The engine ships stepwise integration (animated scene) <em>and</em> summary solvers (<code>ascentToOrbit</code>) so the configurator can plan the take-off (§8 S10) without duplicating physics. Pure fns, no Three.js/DOM.</td></tr></tbody></table><h2 id="_3-·-coordinate-frame-scene-handoff" tabindex="-1">3 · Coordinate frame + scene handoff <a class="header-anchor" href="#_3-·-coordinate-frame-scene-handoff" aria-label="Permalink to &quot;3 · Coordinate frame + scene handoff&quot;">​</a></h2><p><strong>Scene 0 frame.</strong> East-North-Up local tangent frame at the launch site, world units km (÷ some scale for render). Integration runs in the launch plane: state <code>= { downrange, altitude, v_downrange, v_up, mass, t }</code>. Earth curvature is applied for the visual (the horizon drops away) and for the orbit-relative velocity readout; the integration itself is planar over a rotating spherical Earth (Coriolis/centrifugal from Earth rotation optional, small, add if a flagship needs it).</p><p><strong>Handoff at injection.</strong> The ascent ends in a parking orbit / on an injection trajectory, expressed in <strong>ECI</strong> (km) — the same frame Scene 3 (cislunar) already uses. Two exit paths:</p><ol><li><strong>Moon missions</strong> → hand into Scene 3 (cislunar ECI) directly; the parking-orbit → TLI already lives there via <code>cislunar_profile</code>. The ascent extends that scene&#39;s timeline earlier rather than replacing it.</li><li><strong>Interplanetary missions</strong> → the ascent zooms out until Earth becomes the AU-scale point, then warps into Scene 2 (heliocentric). The current heliocentric anchor (<code>met_days: 0</code> at Earth&#39;s position) is unchanged <em>in space</em>; the ascent act simply plays first and the helio MET0 is re-labelled &quot;post-injection.&quot; No trajectory data changes for existing missions.</li></ol><p>The warp is a 1–2 s cinematic re-base (camera + clock), reusing the RFC-032 / ADR-058 handoff choreography.</p><h3 id="_3-1-·-the-injection-burn-beat-shipped-2026-07-18" tabindex="-1">3.1 · The injection-burn beat (shipped 2026-07-18) <a class="header-anchor" href="#_3-1-·-the-injection-burn-beat-shipped-2026-07-18" aria-label="Permalink to &quot;3.1 · The injection-burn beat (shipped 2026-07-18)&quot;">​</a></h3><p>The kick / upper stage that actually <em>leaves</em> parking orbit and builds the transfer speed (S-IVB, Centaur, Blok D, Star 48B, Briz-M…) used to be silent — it happened inside the warp flash. It is now an explicit beat LaunchScene plays <strong>after SECO, before the warp</strong>, in its existing Earth-local frame (the only frame that can host a km-scale parking orbit — 185 km ≈ 0.0000044 AU, sub-pixel in the heliocentric scene). This closes the gap where interplanetary transfers began already on the escape ellipse with the injection stage unshown.</p><ul><li><strong>Resolver (pure):</strong> <code>src/lib/orbital/injection-burn.ts</code> — <code>resolveInjectionBurn(launcherId, vehicle_stage, tli_or_tmi_dv_km_s, dest)</code> → <code>{ stageName, dvKms|null, burnType }</code> or <code>null</code> (LEO-direct / no injection stage ⇒ no beat). The stage name prefers the mission&#39;s authored <code>flight.launch.vehicle_stage</code> (e.g. New Horizons&#39; STAR 48B), else the launcher&#39;s standard injection stage from a small map (saturn-v→S-IVB, atlas-v→Centaur, proton-k→Blok D, proton-m→Briz-M, falcon-9→S2, …). <code>burnType</code> is inferred from <code>dest</code>: MOON→<code>TLI</code>, MARS→<code>TMI</code>, else <code>INJECTION</code>. Δv reuses the already-authored <code>flight.totals.tli_or_tmi_dv_km_s</code>.</li><li><strong>Timing:</strong> <code>INJECTION_COAST_S</code> (parking-orbit coast) + <code>INJECTION_BURN_S</code> (kick-stage burn) in <code>ascent-hud.ts</code>, appended to the ascent duration. The unified scrubber&#39;s ascent slice (<code>[0, ascentScrubberFraction]</code>) absorbs the extra seconds via <code>launchDurationS</code>; the seam now lands <em>after</em> injection. No <code>ascent-clock.ts</code> change.</li><li><strong>Render:</strong> LaunchScene shows a status (&quot;PARKING ORBIT&quot; → the burn label via <code>injectionPhaseStatus</code>) + an amber callout (stage name + Δv); <code>ascent-scene.ts</code> re-lights the plume on the upper bell for the burn window via <code>setInjectionBurn(on)</code> (clock-driven, since <code>sampleAscentAt</code> freezes the physics state at SECO). Reduced-motion (ADR-025): the beat is fully scrubbable, no extra gating. Absent for missions with no injection stage — zero regression.</li></ul><h2 id="_4-·-the-multi-scale-clock" tabindex="-1">4 · The multi-scale clock <a class="header-anchor" href="#_4-·-the-multi-scale-clock" aria-label="Permalink to &quot;4 · The multi-scale clock&quot;">​</a></h2><p>Today the <code>/fly</code> clock is days-only (<code>simDay</code>, day/sec speed pills). The ascent act is ~600 s ≈ 0.007 days — invisible at day resolution.</p><ul><li>Introduce a <strong>piecewise timeline map</strong>: <code>T_ascent ∈ [0, ~600 s]</code> occupies a fixed fraction of the scrubber; <code>T_cruise ∈ [injection, arrival]</code> occupies the rest. The scrubber position → MET is a monotone non-linear function; the readout shows <code>T+MM:SS</code> in the ascent window and calendar date/<code>T+NNNd</code> in cruise.</li><li>Speed pills gain a seconds regime (e.g. 1× / 5× / 20× real-time) active in the ascent window; the existing day/sec pills govern cruise. Regime switches at the injection seam.</li><li><code>prefers-reduced-motion</code> still hard-freezes per ADR-025.</li><li>Test hooks extend the ADR-077 pattern: <code>window.__flySetAscentT(seconds)</code> mirrors <code>__flySetSimDay</code> for the ascent window (sets + pauses, skips intro bias) so e2e can pin a beat deterministically.</li></ul><h2 id="_5-·-ascent-physics-engine" tabindex="-1">5 · Ascent physics engine <a class="header-anchor" href="#_5-·-ascent-physics-engine" aria-label="Permalink to &quot;5 · Ascent physics engine&quot;">​</a></h2><p><code>src/lib/orbital/ascent-physics.ts</code> — pure functions, no Three.js, unit-tested + CI-validated exactly like <code>fly-physics.ts</code>.</p><p><strong>Reusability is a hard requirement (L-I).</strong> The engine is a headless physics core with <strong>two consumers</strong>: (1) <code>/fly</code> Scene 0 (renders the integrated trajectory), and (2) <code>/plan</code> — the mission configurator (see §8 S10), which today plans only the <em>transfer</em> and should also plan the <em>take-off</em>: launch-vehicle → Δv-to-orbit, payload-to-orbit, TWR margin, gravity/drag/steering losses feeding the mission&#39;s total Δv budget. This mirrors how <code>fly-physics.ts</code> (Tsiolkovsky, transfer math) is already shared between <code>/fly</code> and <code>/plan</code>. So <code>ascent-physics.ts</code> exposes both <em>stepwise integration</em> (for the animated scene) and <em>summary solvers</em> (<code>ascentToOrbit(profile) → { dv_to_leo, losses, payload_margin }</code>) for the configurator — no rendering assumptions, no Three.js, no DOM.</p><p>Per integration step (fixed small <code>dt</code>, e.g. RK4 or semi-implicit Euler):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>thrust   = stage.thrust(altitude)                 // SL↔vac Isp interpolation on ambient pressure</span></span>
<span class="line"><span>drag     = ½ · ρ(altitude) · v² · Cd · A          // ρ from a standard atmosphere table (US Std 1976)</span></span>
<span class="line"><span>weight   = m · g(altitude)                          // g = μ⊕ / r²</span></span>
<span class="line"><span>a_along  = (thrust − drag)/m − g·sinθ               // θ = flight-path angle (pitch program / gravity turn)</span></span>
<span class="line"><span>a_perp   = −g·cosθ + (thrust steering component)</span></span>
<span class="line"><span>m       -= mdot · dt                                // mdot = thrust / (Isp·g0)</span></span>
<span class="line"><span>q        = ½ · ρ · v²                                // dynamic pressure → Max-Q detection</span></span></code></pre></div><p>Discrete events fire when their condition trips: staging at <code>mass ≤ stage_dry</code> (or scheduled MET), fairing jettison at altitude/<code>q</code> threshold, MECO/SECO at target orbital velocity. Loss accounting integrates <code>∫g·sinθ dt</code> (gravity), <code>∫(drag/m) dt</code> (drag), and the steering component (steering) across the flight for the HUD.</p><p><strong>Validation (the CI gate).</strong> Mirror the <code>fly-physics-validation</code> harness: each flagship profile ships committed expected milestones (MECO altitude &amp; velocity, staging MET, orbit insertion velocity) with tolerances; a regression in the math or the profile data fails CI. This is how &quot;physics first, prototype is ground truth&quot; stays honest.</p><p><strong>Hybrid escape hatch (L-C).</strong> If a vehicle&#39;s pure integration drifts from reality or reads poorly, a per-profile <code>anchor</code> block supplies target milestones and the integrator tunes the pitch program / a small thrust-fudge to hit them — logged and captioned, never silent.</p><h3 id="_5-1-·-insertion-guidance-—-two-phase-steering-415" tabindex="-1">5.1 · Insertion guidance — two-phase steering (#415) <a class="header-anchor" href="#_5-1-·-insertion-guidance-—-two-phase-steering-415" aria-label="Permalink to &quot;5.1 · Insertion guidance — two-phase steering (#415)&quot;">​</a></h3><p>The open-loop <code>pitch_program</code> alone reaches a <em>genuine</em> orbit only when the table happens to place the vehicle horizontal at apoapsis at circular speed; higher-energy stacks loft and cross circular speed on the way <strong>down</strong> — a suborbital lob, not an orbit (staging mass bookkeeping is correct; this is a guidance problem). So the integrator steers in two phases (<code>commandedPitchRad</code>):</p><ul><li><strong>Atmospheric phase</strong> (<code>altitude &lt; GUIDANCE.handoverAltM</code>, ≈ 55 km): the <code>pitch_program</code> drives the aero-safe gravity turn (low angle-of-attack in dense air).</li><li><strong>Guidance phase</strong> (above handover, powered): an <strong>altitude-hold autopilot</strong> — the real technique (Saturn V IGM, Shuttle/PEG) in miniature. It commands the vertical acceleration <code>a_des = kAlt·(target−alt) − kVy·vy</code> and solves for the thrust pitch that delivers it after gravity and the <strong>centrifugal relief</strong> of horizontal speed: <code>pitch = asin((a_des + g − vₓ²/r) / (T/m))</code>. As horizontal speed builds toward orbital, <code>vₓ²/r</code> cancels gravity and the commanded pitch falls to zero on its own — the vehicle arrives level at circular speed. The two phases blend over <code>GUIDANCE.blendM</code>. The orbit gate additionally requires <code>|vy| &lt; GUIDANCE.orbitVyMaxMs</code>, so a residual lob can&#39;t trip it while descending.</li></ul><p><strong>True 2-body dynamics (#415 Track 2).</strong> The integrator works in a launch-local Earth-centred frame — position from the centre is <code>(x, y+R⊕)</code> — with <strong>gravity always pulling toward the centre</strong> (<code>−μ/r³·P</code>) and <strong>altitude = |P| − R⊕</strong>. Orbits are therefore real curved arcs: at circular speed the ground falls away as fast as gravity pulls, so a vehicle <strong>holds its orbit after cutoff</strong> instead of lobbing back (an earlier flat-frame + <code>vx²/r</code> patch approximated this; the real 2-body is the honest model). The orbit gate is now perigee-based — SECO fires when the osculating perigee reaches a stable altitude — identical for a high-TWR direct ascent and a low-TWR upper stage that circularises over a long burn. This lands <strong>saturn-v</strong> (S-IVB) and every other adequately-powered serial stack in a stable orbit.</p><p>One shared gain set now flies every adequately-powered <strong>serial</strong> stack to a stable orbit — falcon-9, titan-ii-glv, saturn-ib, proton-k, <strong>saturn-v</strong> — with no per-vehicle tuning. The <code>flagship-profiles.test.ts</code> GENUINE_ORBIT tier is the regression guard.</p><h3 id="_5-2-·-parallel-boost-stage-415-track-3" tabindex="-1">5.2 · Parallel-boost stage (#415 Track 3) <a class="header-anchor" href="#_5-2-·-parallel-boost-stage-415-track-3" aria-label="Permalink to &quot;5.2 · Parallel-boost stage (#415 Track 3)&quot;">​</a></h3><p>Strap-on boosters ignite at liftoff and burn <strong>in parallel</strong> with the core (stage 0), then jettison when spent — the core fires throughout. <code>LaunchProfile.boosters</code> gives the per-booster masses/thrust/Isp and a <code>count</code>; the integrator combines them (<code>combinedBoosterStage</code>), adds their thrust to the core&#39;s during the boost phase, carries their mass, burns their propellant on their own Isp, and drops them (a staging beat) at depletion. Covers Ariane 5 EAP, H-IIA SRB-A, the R-7 strap-ons, Delta II GEM-40 solids, the Atlas half-stage. <strong>vostok-k</strong> (R-7 + 4 strap-ons + Blok-E) now reaches a genuine orbit.</p><p><strong>Still needing PEG (soft-insert to space meanwhile, #416):</strong> vehicles whose UPPER stage is too low-TWR to circularise — <strong>atlas-v</strong> (Centaur ~0.32 TWR), <strong>ariane-5</strong> (ESC-A), <strong>h-iia</strong> (LE-5B). Each reaches apoapsis far below circular speed and can&#39;t add the ~2.5 km/s of horizontal Δv at apoapsis before the eccentric arc re-enters; it has the Δv but not the thrust profile. Closing them needs <strong>PEG (Powered Explicit Guidance)</strong> — the boost phase and everything up to insertion is already accurate. (The Delta IV / Titan III–IV upper stages in #414 are the same class.)</p><h2 id="_6-·-launchprofile-data-schema" tabindex="-1">6 · LaunchProfile data schema <a class="header-anchor" href="#_6-·-launchprofile-data-schema" aria-label="Permalink to &quot;6 · LaunchProfile data schema&quot;">​</a></h2><p><code>static/data/launch-profiles/&lt;launcher-id&gt;.json</code>, <code>&lt;launcher-id&gt;</code> = the <code>fleet_refs[].id</code> with <code>role: &quot;launcher&quot;</code>.</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;falcon-9&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Falcon 9 Block 5&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;agency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SpaceX&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;source_tier&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;flagship&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;flagship&quot; | &quot;generic&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;launch_site&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;lat&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">28.56</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;lon&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">-80.58</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SLC-40, Cape Canaveral&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;stages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;S1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;wet_kg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">433100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;dry_kg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;thrust_sl_kN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">7607</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;thrust_vac_kN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8227</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;isp_sl_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">283</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;isp_vac_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">312</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;burn_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">162</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;S2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;wet_kg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">111500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;dry_kg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;thrust_vac_kN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">981</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;isp_vac_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">348</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;burn_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">397</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;fairing&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;jettison_alt_km&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">110</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;mass_kg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1900</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;pitch_program&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">90</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">88</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">40</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">70</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">120</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">45</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">180</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] ],  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [t_s, flight-path-angle_deg]</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;events&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;t_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">72</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;max_q&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;t_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">162</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;meco&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">              { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;t_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">165</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;staging&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;t_s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">559</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;seco&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;anchor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;meco_alt_km&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">66</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;meco_vel_kms&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.35</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// optional, hybrid mode</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;provenance&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;l&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SpaceX Falcon 9 Users Guide&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;u&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Validated fail-closed by a schema (like existing mission data), provenance-tracked, i18n-overlaid for user-facing strings like <code>/science</code>. <strong>Generic fallback:</strong> when a launcher has no flagship profile, <code>source_tier: &quot;generic&quot;</code> is synthesized from published per-stage Δv / mass fractions and a canned pitch program, captioned as representative.</p><h2 id="_7-·-rendering-scene-0" tabindex="-1">7 · Rendering (Scene 0) <a class="header-anchor" href="#_7-·-rendering-scene-0" aria-label="Permalink to &quot;7 · Rendering (Scene 0)&quot;">​</a></h2><ul><li><strong>Vehicle:</strong> a stylised procedural multi-stage body is the S1 placeholder; the target is a <strong>per-vehicle accurate 3D model</strong> so each rocket looks like the real thing (Falcon 9 ≠ Saturn V ≠ Long March 5 ≠ Soyuz), exactly as the spacecraft fleet is individually modelled. Each flagship gets its own GLB via the <strong>fleet 3D-model pipeline (RFC-016 / ADR-052)</strong> — real silhouette, stage divisions, engine count, fairing, livery — with stages that visibly separate, a fairing that splits, and an engine plume that scales with throttle. This is a dedicated slice (§8 S11); the procedural body carries every earlier slice so the physics/camera/HUD work isn&#39;t blocked on modelling.</li><li><strong>World:</strong> launch pad + tower (simple), curved lit Earth surface that recedes, sky gradient darkening to black as altitude climbs, star field fading in. Atmosphere shell for the visual <code>ρ</code> cue.</li><li><strong>Launch site as a place (candidate slice S8):</strong> rather than a bare pad geometry, the T-minus scene can reuse the <strong>surface-scene panorama renderer</strong> (RFC-017 / ADR-072) — the same zoomable/panoramic treatment <code>/moon</code> and <code>/mars</code> landing sites get — to make each flagship&#39;s pad a recognizable, explorable place (LC-39A, SLC-40, Baikonur, Sriharikota, Kourou, Wenchang). The pipeline already exists; this points it at Earth launch complexes. A <strong>stylised pad ships first (S1)</strong>; the full per-site panorama is a candidate later slice so it never gates the ascent physics.</li><li><strong>Broadcast-grade bar:</strong> the render + camera + plume + sound target a live-launch webcast look (PRD-032 north star), not a schematic. Direction is locked in S0 before any of this is built.</li><li><strong>Budget:</strong> governed by the same <code>detect-gpu</code> tiering as the rest of the 3D routes; Scene 0 is short-lived and disposed on handoff (<code>forceContextLoss</code> per the surface-perf lesson) so it never competes with the cruise scene.</li></ul><h2 id="_8-·-slice-plan-each-slice-ships-something-watchable" tabindex="-1">8 · Slice plan (each slice ships something watchable) <a class="header-anchor" href="#_8-·-slice-plan-each-slice-ships-something-watchable" aria-label="Permalink to &quot;8 · Slice plan (each slice ships something watchable)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Slice</th><th>Locks</th><th>Deliverable</th></tr></thead><tbody><tr><td><strong>S0</strong></td><td><strong>visual direction</strong> (gate) — ✅ <strong>LOCKED 2026-07-16</strong></td><td>Art-directed concept mockups for the key beats — direction <strong>approved</strong>. Reference frames + the locked art-direction spec live in <a href="./../wip/2026-07-16-launch-mockups/README"><code>docs/wip/2026-07-16-launch-mockups/</code></a>: photoreal-leaning 3D scene + restrained cyan/monospace broadcast-register HUD + a constant bottom timeline scrubber + on-vehicle force vectors. Broadcast-grade is the bar (PRD-032 north star).</td></tr><tr><td><strong>S1</strong></td><td>frame + clock + engine skeleton</td><td>Scene 0 renders a pad; the multi-scale clock scrubs; a single hard-coded profile integrates and the vehicle rises. No camera/HUD polish.</td></tr><tr><td><strong>S2</strong></td><td><code>ascent-physics.ts</code> + validation harness</td><td>Full EOM (thrust/gravity/drag/staging/losses) + CI-validated milestones for one flagship (Falcon 9).</td></tr><tr><td><strong>S3</strong></td><td>LaunchProfile schema + loader + 2–3 flagships</td><td>Falcon 9 + Saturn V + one non-US flagship; schema + fail-closed validation.</td></tr><tr><td><strong>S4</strong></td><td>camera montage (reuse #371)</td><td>pad → tower-clear → tracking → onboard → staging → MECO shot sequence, cut to the S0-locked direction.</td></tr><tr><td><strong>S5</strong></td><td>telemetry HUD + Science-Lens ascent layers</td><td>live TWR/q/losses + force vectors + per-beat captions &amp; ScienceChips.</td></tr><tr><td><strong>S6</strong></td><td>scene handoff</td><td>injection → warp into helio (interplanetary) and into cislunar (Moon missions); one continuous timeline end-to-end.</td></tr><tr><td><strong>S7</strong></td><td>flagship tier complete + generic fallback</td><td>remaining hero vehicles; generic 2-stage model for every other launcher.</td></tr><tr><td><strong>S8</strong></td><td>launch-site panorama (candidate)</td><td>per-flagship explorable zoomable pad scene via the surface-scene renderer (RFC-017/ADR-072) — LC-39A / SLC-40 / Baikonur / Sriharikota / Kourou / Wenchang. Deferrable behind a stylised pad if it stretches the epic.</td></tr><tr><td><strong>S9</strong></td><td>i18n + a11y + polish</td><td>14-locale strings, reduced-motion, keyboard/D-pad, mobile (applies across S1–S10).</td></tr><tr><td><strong>S10</strong></td><td><strong><code>/plan</code> take-off planning</strong></td><td>Extend the mission configurator to plan the <em>launch</em>, not just the transfer — reuse <code>ascent-physics.ts</code> summary solvers to surface launch-vehicle → Δv-to-orbit, payload-to-orbit, TWR margin, and ascent losses as part of the mission&#39;s Δv budget. Gated on the engine being cleanly reusable (L-I); no new physics, only a new consumer of the S2 core.</td></tr><tr><td><strong>S11</strong></td><td><strong>per-vehicle 3D models</strong></td><td>Accurate GLB per flagship rocket via the fleet model pipeline (RFC-016 / ADR-052) — real silhouette, stage divisions, engines, fairing, livery — replacing the procedural placeholder, so a launch shows the actual vehicle (the spacecraft-fleet treatment applied to launchers). Data-authoring effort, sequenced after the physics + render + wiring are solid.</td></tr></tbody></table><h2 id="_9-·-deferred-explicit-non-goals-for-this-epic" tabindex="-1">9 · Deferred (explicit non-goals for this epic) <a class="header-anchor" href="#_9-·-deferred-explicit-non-goals-for-this-epic" aria-label="Permalink to &quot;9 · Deferred (explicit non-goals for this epic)&quot;">​</a></h2><ul><li><strong>Interactive fly-it-yourself mode</strong> — throttle/pitch control on the same EOM core (a later epic; L-G keeps the engine control-input-ready).</li><li><strong>Landing / booster recovery act</strong> — RTLS / droneship return.</li><li><strong>3-DOF azimuth steering / doglegs</strong> (L-H) — add only if a flagship visibly needs it.</li><li><strong>Abort / failure modes</strong> — pad abort, MaxQ abort, engine-out; rich but out of v1 scope.</li></ul><p><strong>Forward-compatibility (the sibling epic).</strong> This epic is the <strong>ascent</strong> bookend; the planned sibling is <strong>descent &amp; landing</strong> (Moon/Mars EDL → the existing surface scenes) + <strong>orbital rendezvous / docking</strong> (Earth &amp; Moon orbit insertions, separations, joinings — ISS/Tiangong/Apollo/Gateway). The full arc is <code>pad → orbit → cruise → arrival → descent → surface</code>. Three architecture choices here are made to <strong>extend downward</strong>, not just ship ascent: (a) the integrated EOM core (§5) is a general powered-flight integrator, not ascent-specific — a descent is the same equations with the thrust vector reversed and a target of zero velocity at the surface; (b) the multi-scale clock (§4) already models a seconds-dense window inside a days-dense timeline — landings are another seconds-dense window; (c) the re-basing scene handoff (§3) is the general mechanism for stitching an Earth/Moon/Mars-relative frame into the cruise frame — descent is the inverse handoff (cruise → surface-relative). Building these general now avoids a rewrite when the sibling epic lands.</p><h3 id="_9-1-·-concrete-extension-seams-shipped-by-the-launch-consolidation-epic-2026-07-18" tabindex="-1">9.1 · Concrete extension seams (shipped by the launch-consolidation epic, 2026-07-18) <a class="header-anchor" href="#_9-1-·-concrete-extension-seams-shipped-by-the-launch-consolidation-epic-2026-07-18" aria-label="Permalink to &quot;9.1 · Concrete extension seams (shipped by the launch-consolidation epic, 2026-07-18)&quot;">​</a></h3><p>The launch-consolidation work turned the three &quot;extend-downward&quot; intentions above into named, tested contracts. A future descent/arrival epic implements against these seams — it does <strong>not</strong> re-architect them. The seams (in scope now); the descent <em>feature</em> is out of scope (build the seams + docs, not the feature).</p><ol><li><strong>The phase-scene contract — <code>src/lib/three/flight-phase-scene.ts</code>.</strong> <code>interface FlightPhaseScene&lt;TState&gt;</code> is the structural contract every <code>/fly</code> flight-phase render obeys: <code>scene</code> / <code>camera</code> / <code>setState(s)</code> / <code>setAspect</code> / <code>setForceVisible(force, on)</code> / <code>setForcesVisible</code> / <code>snapCamera</code> / <code>reset</code> / <code>dispose</code>, plus <code>type ForceKey = &#39;thrust&#39;|&#39;weight&#39;|&#39;drag&#39;|&#39;velocity&#39;</code>. <code>AscentScene</code> implements it today; the broadcast composer (<code>ascent-renderer.ts</code>, <code>createAscentRenderer</code>) consumes only the render slice (<code>scene</code>/<code>camera</code>/<code>setAspect</code>), so it drives any <code>FlightPhaseScene</code> unchanged. <strong>A future <code>DescentScene</code> implements the same interface and is rendered by the same composer</strong> — no renderer change.</li><li><strong>The headless physics core — <code>src/lib/orbital/ascent-physics.ts</code>.</strong> <code>integrateAscent(profile) → AscentSummary</code> is a pure, browser-free powered-flight integrator (fully unit-tested; excluded from nothing). <strong>A future <code>descent-physics.ts</code> is the same EOM with the thrust vector reversed and a zero-velocity-at-surface target</strong> — it produces the same <code>AscentState</code>-shaped samples the scene already consumes.</li><li><strong>The clock <code>Phase</code> union — <code>src/lib/orbital/ascent-clock.ts</code>.</strong> <code>type Phase = &#39;ascent&#39; | &#39;cruise&#39;</code> today. <strong>Widen to <code>&#39;ascent&#39; | &#39;cruise&#39; | &#39;arrival&#39; | &#39;descent&#39;</code></strong>; <code>makeTimeline</code> / <code>scrubberToPoint</code> / <code>advanceClock</code> / <code>defaultRegimeFor</code> already localise every phase-specific branch, so widening is additive (add the new arms; the exhaustiveness of the <code>Phase</code>-switch return types flags every site that must handle the new members).</li></ol><p><strong>Named future files</strong> (do not exist yet — this is the map, not a promise): <code>src/lib/orbital/descent-physics.ts</code> (the reversed-thrust integrator), <code>src/lib/three/descent-scene.ts</code> (<code>DescentScene implements FlightPhaseScene&lt;DescentState&gt;</code>), <code>src/lib/components/DescentScene.svelte</code> (clock-driven exactly as <code>LaunchScene</code> — <code>t</code>/<code>playing</code>/<code>speed</code> fed by the master clock, <code>showDescent = pt.phase === &#39;descent&#39;</code>). The descent hands the arrival frame back to the existing surface scenes (<code>SurfaceScene</code>), the inverse of the ascent → cruise re-basing warp.</p><h2 id="_10-·-open-questions" tabindex="-1">10 · Open questions <a class="header-anchor" href="#_10-·-open-questions" aria-label="Permalink to &quot;10 · Open questions&quot;">​</a></h2><ol><li><strong>Timeline seam feel</strong> — does the seconds→days regime switch at injection want a hard cut, an eased ramp, or a &quot;warp&quot; pause? Prototype in S5.</li><li><strong>Vehicle model sourcing</strong> — procedural stylised bodies vs. per-flagship GLBs; how far the RFC-016 fleet-model pipeline covers launchers vs. spacecraft.</li><li><strong>Atmosphere/<code>ρ</code> model</strong> — US Standard 1976 table is ample for Earth; is any non-Earth launch (none today) ever in scope? Assume Earth-only.</li><li><strong>Where flagship numbers come from</strong> — per-vehicle users&#39; guides / Spaceflight101 / agency press kits; provenance gate as with all Orrery data.</li></ol><h2 id="_11-·-stitch-into-fly-—-event-vocabulary-·-science-lens-·-transition" tabindex="-1">11 · Stitch into <code>/fly</code> — event vocabulary · Science Lens · transition <a class="header-anchor" href="#_11-·-stitch-into-fly-—-event-vocabulary-·-science-lens-·-transition" aria-label="Permalink to &quot;11 · Stitch into \`/fly\` — event vocabulary · Science Lens · transition&quot;">​</a></h2><p>The launch act is not a separate feature bolted onto <code>/fly</code>; it&#39;s the opening of the <em>same</em> mission on the <em>same</em> clock. Three contracts make it continuous.</p><h3 id="_11-1-·-one-event-vocabulary" tabindex="-1">11.1 · One event vocabulary <a class="header-anchor" href="#_11-1-·-one-event-vocabulary" aria-label="Permalink to &quot;11.1 · One event vocabulary&quot;">​</a></h3><p><code>/fly</code>&#39;s mission schema already carries a closed <strong><code>FlightEventType</code></strong> union (<code>fly-event-labels.ts</code>). The launch beats reuse what exists and add only the genuinely-new ones:</p><ul><li><strong><code>launch</code></strong> — reuse verbatim; the ascent act <em>is</em> this event, expanded from a zero-duration dot into ~8 minutes.</li><li><strong><code>separation</code></strong> — reuse verbatim for <strong>stage separation</strong> (do NOT introduce a parallel <code>staging</code>).</li><li><strong><code>parking_orbit_exit</code></strong> / <strong><code>tli_or_tmi</code></strong> — reuse; these ARE the launch→flight handoff (the injection burn).</li><li>⚠️ <strong><code>ascent</code></strong> already means <em>lunar-surface ascent</em> (Apollo LM). The launch act must NOT reuse it.</li><li><strong>Add</strong>: <code>max_q</code>, <code>meco</code>, <code>fairing_jettison</code>, <code>seco</code> (a.k.a. <code>orbit_insertion</code>). The engine&#39;s <code>AscentEvent</code> maps 1:1 onto the widened union.</li></ul><p>Result: pad beats → cruise events → arrival are <strong>one <code>FlightEvent</code> stream on one MET clock</strong>; the seconds-vs-days unit gap is bridged by the multi-scale clock (§4). Widening the union touches the schema + <code>defaultEventLabel</code> + 14-locale message keys (same pattern as any event addition).</p><h3 id="_11-2-·-science-lens-layers-the-forces-free-body-diagram" tabindex="-1">11.2 · Science Lens layers (the forces free-body diagram) <a class="header-anchor" href="#_11-2-·-science-lens-layers-the-forces-free-body-diagram" aria-label="Permalink to &quot;11.2 · Science Lens layers (the forces free-body diagram)&quot;">​</a></h3><p>The lens is a registry (<code>science-layers.ts</code>: <code>LayerKey</code> + <code>LAYER_ORDER</code> + <code>LAYER_DEFAULTS</code> + <code>metaFor()</code> + overlay builders keyed on <code>userData.layerKey</code>). Launch <strong>reuses</strong> <code>gravity</code> (weight vector), <code>velocity</code>, and <code>atmosphere</code> (density/<code>q</code> shell), and <strong>adds</strong> launch-phase layers, gated the way <code>coast</code>/<code>conics</code> are &quot;/fly only&quot;:</p><table tabindex="0"><thead><tr><th>Layer</th><th>New/reuse</th><th>Draws in Scene 0</th></tr></thead><tbody><tr><td><code>gravity</code> · <code>velocity</code> · <code>atmosphere</code></td><td>♻️ reuse</td><td>weight arrow · velocity arrow · density shell</td></tr><tr><td><strong><code>thrust</code></strong></td><td>➕ new</td><td>thrust vector up the body axis</td></tr><tr><td><strong><code>drag</code></strong></td><td>➕ new</td><td>drag vector opposing velocity + Max-Q emphasis</td></tr><tr><td><strong><code>ascent-losses</code></strong></td><td>➕ new</td><td>running gravity/drag/steering Δv ledger (panel)</td></tr></tbody></table><p>Each new layer adds a <code>LAYER_ORDER</code> + <code>LAYER_DEFAULTS</code> + <code>metaFor()</code> entry (the exhaustiveness tests enforce completeness) and a <code>?name=</code> <strong>ScienceChip</strong> cross-link to new <code>/science</code> articles: <strong>thrust-to-weight</strong>, <strong>gravity-turn</strong>, <strong>max-q</strong>, <strong>tsiolkovsky / rocket-equation</strong>, <strong>staging &amp; mass-ratio</strong>. The teachable core is the <strong>thrust-vs-weight-vs-drag free-body diagram on the vehicle</strong>.</p><p><strong>Status (2026-07-18, launch-consolidation epic).</strong> ✅ <strong>Shipped:</strong> the <strong><code>thrust</code></strong> + <strong><code>drag</code></strong> layers (<code>science-layers.ts</code>, 31 layers), weight via the reused <strong><code>gravity</code></strong> layer and velocity via <strong><code>velocity</code></strong> — wired to the scene&#39;s force arrows through the pure <code>LAUNCH_FORCE_LAYERS</code> map (<code>src/lib/orbital/launch-force-layers.ts</code>) which LaunchScene subscribes to via <code>onLayerChange</code> and drives <code>setForceVisible(force, on)</code> per <code>FlightPhaseScene</code>. The lens panel is the force legend (no separate legend). ⏳ <strong>Pending:</strong> the <strong><code>ascent-losses</code></strong> Δv-ledger layer + panel, the <code>atmosphere</code> density-shell reuse, and the five <code>/science</code> articles + ScienceChip cross-links (that content runs through the <code>/science</code> translate toolchain — largest remaining piece).</p><h3 id="_11-3-·-the-transition-launch-→-flight" tabindex="-1">11.3 · The transition (launch → flight) <a class="header-anchor" href="#_11-3-·-the-transition-launch-→-flight" aria-label="Permalink to &quot;11.3 · The transition (launch → flight)&quot;">​</a></h3><ul><li><strong>Where (the seam):</strong> the <strong>injection / <code>parking_orbit_exit</code></strong> event — the vehicle established in a parking orbit / on its escape trajectory (~T+9 min, the engine&#39;s <code>seco</code>/<code>orbit</code> beat). This is exactly where <code>/fly</code> begins today (its MET-0 ≡ post-injection). It is already modelled as the <strong><code>ascent-clock</code> seam</strong> where <code>phase</code> flips <code>&#39;ascent&#39; → &#39;cruise&#39;</code>; that flip is the transition trigger.</li><li><strong>How (the mechanism):</strong> reuse the <strong>ADR-058 scene-handoff</strong> (helio→cislunar-at-SOI) plus a <code>view</code> mode param. Scene 0 plays pad → SECO/parking orbit; at the seam the camera does the <strong>re-basing warp</strong> (§3) — zoom out until Earth collapses to the AU-scale point — and cross-fades into the next scene: <code>view=ascent</code> → <code>view=helio</code> (interplanetary) or <code>view=cislunar</code> (Moon). The heliocentric scene picks up at its existing MET-0 anchor, unchanged, now merely <em>preceded</em> by the ascent act.</li><li><strong>Edge case:</strong> a <strong>LEO-only</strong> mission has no interplanetary cruise, so it hands to an Earth-orbit view or simply ends at orbit. The Orrery corpus is mostly lunar/interplanetary, where the helio/cislunar handoff applies.</li></ul><hr><h2 id="_12-·-descent-landing-—-the-closing-bookend-§9-·-track-d-shipped-2026-07-18" tabindex="-1">12 · Descent &amp; Landing — the closing bookend (§9 · Track D, shipped 2026-07-18) <a class="header-anchor" href="#_12-·-descent-landing-—-the-closing-bookend-§9-·-track-d-shipped-2026-07-18" aria-label="Permalink to &quot;12 · Descent &amp; Landing — the closing bookend (§9 · Track D, shipped 2026-07-18)&quot;">​</a></h2><p>The flight ended at destination orbit. For the <strong>37 Moon / Mars / Venus landers</strong> it now continues through <strong>Entry, Descent &amp; Landing</strong> to the surface, closing the arc <code>pad → orbit → cruise → arrival → **descent → surface**</code>. This is the <strong>inverse of the launch act</strong>, built on the three &quot;extend-downward&quot; seams (§9): the <code>FlightPhaseScene</code> contract, the multi-scale clock, and the re-basing scene handoff — implemented against them, not re-architecting them.</p><p><strong>1:1 mirror of the ascent stack</strong> (the ascent counterpart → the descent equivalent):</p><table tabindex="0"><thead><tr><th>Ascent</th><th>Descent</th></tr></thead><tbody><tr><td><code>ascent-physics.ts</code> · <code>integrateAscent</code></td><td><code>descent-physics.ts</code> · <code>integrateDescent</code></td></tr><tr><td><code>ascent-physics-constants.ts</code></td><td><code>descent-physics-constants.ts</code> (per-body μ / R / atmosphere)</td></tr><tr><td><code>launch-profile-registry.ts</code> + <code>static/data/launch-profiles/</code></td><td><code>descent-profile-registry.ts</code> + <code>static/data/descent-profiles/</code></td></tr><tr><td><code>launcher-models.ts</code> · <code>LauncherModel</code></td><td><code>descent-models.ts</code> · <code>DescentModel</code></td></tr><tr><td><code>ascent-scene.ts</code></td><td><code>descent-scene.ts</code></td></tr><tr><td><code>ascent-renderer.ts</code></td><td><strong>reused unchanged</strong> (drives any <code>FlightPhaseScene</code>)</td></tr><tr><td><code>LaunchScene.svelte</code></td><td><code>DescentScene.svelte</code></td></tr><tr><td><code>ascent-clock</code> <code>Phase=&#39;ascent&#39;|&#39;cruise&#39;</code></td><td>widened → <code>+&#39;descent&#39;</code> (the scrubber TAIL)</td></tr><tr><td><code>/fly</code> <code>showLaunch</code>/<code>launchT</code></td><td><code>showDescent</code>/<code>descentT</code></td></tr></tbody></table><ul><li><strong>Physics (integrated EOM + EDL phases).</strong> <code>integrateDescent(profile)</code> is a 1-DOF along-track integrator: per-body gravity + exponential drag + a per-phase flight-path angle. Aerodynamic phases (entry / parachute / aeroshell / coast / airbag) are force-integrated; powered phases (<code>powered_retro</code> / <code>skycrane</code>) are <strong>throttle-guided</strong> — the descent rate tracks <code>gain·altitude</code> down to a soft terminal velocity, and the retro thrust to fly that schedule is back-solved for the fuel gauge + felt-g. Honest telemetry (altitude, velocity, deceleration-g, Mach, dynamic pressure) without a 3-DOF trajectory the scene never renders. <strong><code>flightPathAngleDeg</code> is the effective descent (collapse) angle</strong>, tuned so the speed-bleed + timeline match the published EDL (Mars ~7–8° → Mach ~2 at chute deploy, ~6-min entry; Venus steep ~65° → ~160 g peak, ~50-min descent; Moon models the terminal powered descent). Per-body atmospheres: Moon vacuum (no drag), Mars ~0.02 kg·m⁻³ / 11 km scale height, Venus ~65 kg·m⁻³ / 15.9 km.</li><li><strong>Registry + 6 EDL archetypes.</strong> Thin per-mission JSON (<code>&lt;missionId&gt;.json</code>) carries the numbers; six embedded TS archetype templates supply the phase sequence: <code>LUNAR_POWERED</code> (Apollo / Luna / Chang&#39;e / Chandrayaan / SLIM / Beresheet / Artemis / Blue Moon), <code>LUNA_DIRECT_IMPACT</code> (Luna 9), <code>MARS_PARACHUTE_RETRO</code> (Viking / Phoenix / InSight / Mars-3 / Schiaparelli / Zhurong), <code>MARS_AIRBAG</code> (Pathfinder / Spirit / Opportunity), <code>MARS_SKYCRANE</code> (Curiosity / Perseverance), <code>VENUS_AEROSHELL</code> (Venera-13 / Vega-1 / Vega-2). <code>hasDescentProfile(missionId)</code> gates entry — orbiters never descend. Crash reconstructions (Beresheet / Schiaparelli / Mars 3) model a propellant cutout → honest hard impact (<code>touchdownSuccess:false</code>, HUD reads &quot;IMPACT&quot;).</li><li><strong>Model + scene.</strong> <code>buildDescentModel(siteId, body, vehLen)</code> composes the EDL stack (aeroshell = heat-shield + backshell, parachute, skycrane / retro / airbag descent stage) wrapped around the mission&#39;s <strong>real landed model</strong> (<code>buildMoon/MarsLanderModel</code> / <code>buildVenera13</code>) as the terminal part — so a descent and its <code>/moon</code> <code>/mars</code> surface view show the same craft. <code>descent-scene.ts</code> inverts <code>ascent-scene.ts</code>: the destination body (textured) <strong>grows below</strong> the descending lander; a per-body sky (airless black / Martian salmon / Venusian amber); EDL separations (heat-shield jettison, chute deploy + cut, backshell sep, skycrane lower, airbag inflate) are scrub-safe pure functions of the event METs; a retro plume; a smooth per-phase camera.</li><li><strong>Clock + scrubber.</strong> <code>ascent-clock</code> <code>Phase</code> widened to <code>&#39;ascent&#39;|&#39;cruise&#39;|&#39;descent&#39;</code> with <code>descentDurationS</code> / <code>descentScrubberFraction</code> (default 0 ⇒ orbiters keep the byte-identical 2-segment bar). The descent owns the tail <code>[1−fD, 1]</code>; <code>E+MM:SS</code> readout; <code>DESCENT_SPEED_MULTIPLIERS = [1,3,10]</code>. In <code>/fly</code>, at arrival a landing mission enters <code>showDescent</code> instead of looping the cruise; <code>?descent=1</code> deep-links straight to the EDL act.</li><li><strong>Surface handoff.</strong> At touchdown <code>DescentScene</code> flashes and fires <code>onComplete</code> → <code>/fly</code> navigates to <code>/\${body}?site=\${siteId}&amp;from=descent</code>. <code>SurfaceScene</code> already reads <code>?site=</code> and rotates the body so the landing site faces the camera — the circle closes ON the site.</li><li><strong>Science Lens.</strong> Reuses the four generic lens layers (<code>thrust</code> / <code>gravity</code> / <code>drag</code> / <code>velocity</code>) via <code>descent-force-layers.ts</code> — no new registration. Pedagogically rich: under the parachute drag dominates; in the skycrane thrust ≈ weight; in the airbag bounce only weight remains.</li></ul><p><strong>Source-of-truth decision (supersedes the planned mission-JSON backfill).</strong> The descent-profile registry is the <strong>single source of truth</strong> for EDL data (entry state, system, duration). The <code>/fly</code> descent dossier reads entry velocity + EDL system from the profile, NOT from a duplicated <code>flight.arrival.edl_system</code> / <code>entry_velocity_km_s</code> in the 37 mission JSONs — duplicating would risk divergence. The <code>FlightArrival</code> schema is left unchanged; a future external consumer can derive EDL facts from the profile registry.</p><p><strong>Status (2026-07-18).</strong> ✅ <strong>Shipped:</strong> <code>descent-physics</code> (+ constants + tests), <code>descent-profile-registry</code> (+ 6 archetypes), 37 descent-profile JSONs, <code>descent-models</code>, <code>descent-scene</code>, <code>DescentScene.svelte</code>, the 3-segment clock, the <code>/fly</code> wiring + <code>?descent=1</code> deep-link, the surface handoff, and the descent force-lens mapping. Verified end-to-end in-browser (Apollo 11 Moon → <code>/moon</code>; Curiosity Mars EDL). ⏳ <strong>Pending / deferred:</strong> asteroid / comet / Jupiter-probe / Titan landers (out of scope this epic); a dedicated <code>/venus</code> surface route (Venus touchdown currently rests on the landed frame, no surface handoff); <code>/science</code> EDL articles.</p><h3 id="_12-1-·-phase-2-—-outer-small-body-edl-shipped-2026-07-18" tabindex="-1">12.1 · Phase 2 — outer + small-body EDL (shipped 2026-07-18) <a class="header-anchor" href="#_12-1-·-phase-2-—-outer-small-body-edl-shipped-2026-07-18" aria-label="Permalink to &quot;12.1 · Phase 2 — outer + small-body EDL (shipped 2026-07-18)&quot;">​</a></h3><p><strong>Phase 1</strong> (above) covers the 37 Moon / Mars / Venus landers. <strong>Phase 2</strong> extends the same engine to four body regimes that Phase 1 left out, each requiring distinct constants and archetypes: micro-g airless small bodies, comet nuclei, Titan&#39;s thick cold N₂ atmosphere, and Jupiter&#39;s gas column with no solid surface.</p><p><strong>New body constants.</strong> <code>descent-physics-constants.ts</code> gains real μ / R / atmospheric parameters for each regime (Itokawa, Ryugu, Bennu, Eros, comet 67P, Titan, Jupiter). <code>bodyAirDensity</code> no longer clamps at the body&#39;s 1-bar datum — Jupiter probes sink well below it into denser gas, so the integrator follows the real exponential column until the end-trigger fires.</p><p><strong>Four new archetypes</strong> added to <code>descent-profile-registry.ts</code>:</p><table tabindex="0"><thead><tr><th>Archetype</th><th>Regime</th><th>Sequence</th></tr></thead><tbody><tr><td><code>ASTEROID_TOUCH_AND_GO</code></td><td>micro-g airless</td><td>guided micro-g descent → sample contact (1–5 s) → departure burn; no landing — terminal state <code>SAMPLE COLLECTED</code></td></tr><tr><td><code>COMET_HARPOON</code></td><td>micro-g airless</td><td>unpowered coast → harpoon deploy → bounce / settle sequence; terminal state <code>SETTLED</code></td></tr><tr><td><code>TITAN_PARACHUTE</code></td><td>thick cold N₂</td><td>aeroshell → drogue → stabiliser chute; ~2.5 h descent; terminal state <code>TOUCHDOWN</code></td></tr><tr><td><code>JUPITER_PROBE</code></td><td>gas column / no surface</td><td>hypersonic entry → chute → passive pressure descent; ends when ambient pressure exceeds <code>pressure_pa</code> end-trigger (probe_signal_lost at ~22 bar); terminal state <code>SIGNAL LOST</code></td></tr></tbody></table><p><strong>Jupiter-probe physics.</strong> A new <code>pressure_pa</code> end-trigger fires when <code>bodyAmbientPressurePa(altitude, body)</code> crosses the profile&#39;s crush limit — the probe does not &quot;land&quot;, it is overwhelmed by increasing pressure. The <code>SIGNAL LOST</code> terminal state is honest: the final screen reads &quot;PROBE SIGNAL LOST&quot;, matching the Galileo mission record. Integration accumulates to ~230 g peak deceleration, matching the published ~228 g measurement. No surface handoff is attempted.</p><p><strong>Terminal states by regime</strong> — none of the Phase 2 endings hand off to a surface route (they rest on the landed frame as Venus does):</p><table tabindex="0"><thead><tr><th>Regime</th><th>Terminal state</th><th>HUD reads</th></tr></thead><tbody><tr><td>Asteroid touch-and-go</td><td><code>SAMPLE COLLECTED</code></td><td>&quot;SAMPLE COLLECTED — DEPARTURE&quot;</td></tr><tr><td>Comet harpoon</td><td><code>SETTLED</code></td><td>&quot;SETTLED ON SURFACE&quot;</td></tr><tr><td>Titan parachute</td><td><code>TOUCHDOWN</code></td><td>&quot;TOUCHDOWN&quot;</td></tr><tr><td>Jupiter probe</td><td><code>SIGNAL LOST</code></td><td>&quot;PROBE SIGNAL LOST&quot;</td></tr></tbody></table><p><strong>Shipped missions (5 of 7, all browser-verified):</strong> hayabusa1 (Itokawa), hayabusa2 (Ryugu), osiris-rex (Bennu), rosetta / Philae (67P), galileo (Jupiter). <strong>Not yet shipped (2 of 7):</strong> Cassini / Huygens (Titan) — Titan is not currently a flyable <code>/fly</code> destination and must be added as one first. NEAR Shoemaker (Eros) — no Eros mission data in the repo at all. Both gaps tracked in GH <strong>#418</strong>; plan in <code>docs/wip/2026-07-18-descent-phase2-outer-bodies.md</code>.</p><p><strong>Reference:</strong> archetype implementations in <code>src/lib/orbital/descent-profile-registry.ts</code>; body constants + <code>bodyAmbientPressurePa</code> / <code>bodyAirDensity</code> in <code>src/lib/orbital/descent-physics-constants.ts</code>; WebGL builders in <code>src/lib/three/descent-models.ts</code>; per-mission profile JSONs in <code>static/data/descent-profiles/</code>.</p>`,83)])])}const u=t(n,[["render",o]]);export{g as __pageData,u as default};
