import{_ as s,o as t,c as o,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"RFC-036 — God-file route restructure: testable controllers + decomposed 3D scene layers (/fly, then /explore)","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-036.md","filePath":"rfc/RFC-036.md"}'),n={name:"rfc/RFC-036.md"};function i(r,e,l,c,d,h){return t(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="rfc-036-—-god-file-route-restructure-testable-controllers-decomposed-3d-scene-layers-fly-then-explore" tabindex="-1">RFC-036 — God-file route restructure: testable controllers + decomposed 3D scene layers (<code>/fly</code>, then <code>/explore</code>) <a class="header-anchor" href="#rfc-036-—-god-file-route-restructure-testable-controllers-decomposed-3d-scene-layers-fly-then-explore" aria-label="Permalink to &quot;RFC-036 — God-file route restructure: testable controllers + decomposed 3D scene layers (\`/fly\`, then \`/explore\`)&quot;">​</a></h1><p><em>Status: Shipped (v0.8; on prod) · 2026-08-05 · Closes: architectural-review R1 · builds on RFC-034 (ascent), RFC-033 (launch/live), ADR-084 (the data.ts decomposition, as the pattern precedent) · Tracking: #440 (WS-A <code>/fly</code> controller) · #441 (WS-B <code>/fly</code> scene) · #443 (WS-C <code>/explore</code>) — all shipped v0.8</em></p><blockquote><p><strong>Scope note.</strong> This RFC establishes the <strong>god-file route decomposition pattern</strong> — a route&#39;s tangled orchestration + 3D scene-build split into (1) a pure, unit-tested <strong>controller</strong> and (2) a <strong>scene layer</strong> driven one-way by it, meeting at one <strong>seam contract</strong> (§4). <code>/fly</code> is the proving ground (WS-A + WS-B). <code>/explore</code> — the second-largest route file and R1&#39;s sibling — applies the <em>same</em> pattern once proven (WS-C, §8). One RFC because the seam contract is designed once and reused.</p></blockquote><blockquote><p><strong>Why this is an RFC.</strong> <code>src/routes/fly/+page.svelte</code> is the largest, highest-churn, least-protected file in the app — <strong>10,890 lines</strong>, a <strong>7,365-line <code>&lt;script&gt;</code></strong>, <strong>74 <code>$lib</code> imports</strong>, <strong>63 <code>$state</code> + 48 <code>$derived</code> + 12 <code>$effect</code></strong>, and a <strong>~4,870-line <code>onMount</code> closure</strong> (lines 2486→7360) that builds both 3D scenes and holds the per-frame <code>onFrame</code>. It is <strong>coverage-excluded</strong> (blanket <code>src/routes/**/+page.svelte</code>), so the phase orchestration — the launch→coast→cruise→cislunar→flyby→descent state machine we debugged all session — has <strong>zero unit tests</strong>; every fix this session was verified by hand in the browser. The file also documents its own booby-trap (line ~3519): a variable declared below a synchronous use hits a TDZ ReferenceError and &quot;the animate loop would never start, the whole canvas would render blank.&quot; Reshaping a prod-live file this size and this fragile binds architectural commitments — the controller/scene <strong>seam contract</strong> must be designed once, up front, or the two workstreams fight each other. Hence an RFC, not a silent PR.</p></blockquote><h2 id="_1-·-the-problem-measured-not-asserted" tabindex="-1">1 · The problem (measured, not asserted) <a class="header-anchor" href="#_1-·-the-problem-measured-not-asserted" aria-label="Permalink to &quot;1 · The problem (measured, not asserted)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Symptom</th><th>Evidence</th><th>Consequence</th></tr></thead><tbody><tr><td>Phase logic untested</td><td>coverage-excluded; only Playwright touches it, and Playwright doesn&#39;t gate preflight</td><td>Regressions in scene routing are invisible to CI. The earth-orbit routing bug (crewed → heliocentric instead of launch→coast→reentry) was pure phase logic and only a human eye caught it.</td></tr><tr><td>Ordering-fragile</td><td>one ~4,870-line <code>onMount</code>; documented TDZ hazard at ~3519</td><td>A declaration-order slip silently blanks the canvas. This is why debugging meant bolting on temp <code>window.__fly*</code> hooks instead of reasoning about the code.</td></tr><tr><td>Change-risky</td><td>74 imports, 63 <code>$state</code>, one reactive scope</td><td>Any edit risks the whole prod route; there is no seam to change one concern in isolation.</td></tr></tbody></table><p>The two tangled concerns:</p><ul><li><strong>Phase orchestration</strong> (~the first 2,486 lines): which scene/act is active, phase derivation, deep-link routing (<code>?launch</code>/<code>?descent</code>), scrub-position → phase mapping, launch/coast/descent entry+exit, <code>isMoonMission</code> → cislunar-vs-heliocentric. <strong>Pure logic. Currently untested.</strong></li><li><strong>3D scene rendering</strong> (the ~4,870-line <code>onMount</code>): heliocentric + cislunar scene assembly and the per-frame <code>onFrame</code>, partly delegating to the existing <code>$lib/three/fly-helio-scene</code>, <code>fly-cislunar-scene</code>, <code>fly-updaters</code>, <code>ascent-scene</code>, <code>descent-scene</code>. <strong>WebGL glue. Hard to unit-test; needs an architectural seam, not test coverage.</strong></li></ul><p>These are <strong>separate concerns</strong>, but they meet at one <strong>seam</strong>: the controller decides <em>what act/scene is active and where the clock is</em>; the scene layer <em>renders that</em>. Get the seam wrong and each workstream forces a rewrite of the other — which is the whole reason this is one RFC.</p><h2 id="_2-·-target-architecture" tabindex="-1">2 · Target architecture <a class="header-anchor" href="#_2-·-target-architecture" aria-label="Permalink to &quot;2 · Target architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/routes/fly/+page.svelte   ← THIN page: wires DOM/HUD to the controller + the scene host,</span></span>
<span class="line"><span> │                               owns Svelte reactivity only. No phase logic, no scene build.</span></span>
<span class="line"><span> ├─ $lib/fly/flight-phase-controller.ts   ← WORKSTREAM A (R1): PURE state machine.</span></span>
<span class="line"><span> │     inputs:  { deepLink, scrubU, mission flags (isMoonMission, earthCoast, launchAvailable,</span></span>
<span class="line"><span> │                descentAvailable), event times (secoT, coastDur, …), clock }</span></span>
<span class="line"><span> │     outputs: { act: &#39;opening&#39;|&#39;ascent&#39;|&#39;coast&#39;|&#39;cruise&#39;|&#39;cislunar&#39;|&#39;flyby&#39;|&#39;descent&#39;|&#39;recovery&#39;,</span></span>
<span class="line"><span> │                viewMode, activeScene, legal transitions, phase-derived flags }</span></span>
<span class="line"><span> │     NO svelte / three / dom import. Fully unit-tested.</span></span>
<span class="line"><span> └─ $lib/three/fly-scene-host.ts (+ the existing fly-*-scene modules)  ← WORKSTREAM B (3D SIM):</span></span>
<span class="line"><span>       owns scene assembly + the per-frame onFrame, driven by the controller&#39;s act/clock.</span></span>
<span class="line"><span>       The ~4,870-line onMount is decomposed into: buildScene(act) + updateFrame(state) units,</span></span>
<span class="line"><span>       each a plain function over a scene handle — testable via the existing jsdom scene harnesses.</span></span></code></pre></div><p>The page becomes a thin adapter: Svelte <code>$state</code>/<code>$derived</code>/<code>$effect</code> translate URL + user input into controller inputs, read the controller&#39;s outputs, and hand the active act + clock to the scene host. <strong>Behavior is byte-identical throughout — this is a structural refactor, not a feature change.</strong></p><h2 id="_3-·-the-two-workstreams-both-committed-—-this-rfc-is-the-full-solution" tabindex="-1">3 · The two workstreams (both committed — this RFC is the <em>full</em> solution) <a class="header-anchor" href="#_3-·-the-two-workstreams-both-committed-—-this-rfc-is-the-full-solution" aria-label="Permalink to &quot;3 · The two workstreams (both committed — this RFC is the *full* solution)&quot;">​</a></h2><p>This RFC commits to <strong>both</strong>. Sequencing below is <em>execution order</em>, not deferral — WS-B is not a &quot;maybe later,&quot; it is the second half of the same decomposition and ships as part of closing R1&#39;s parent concern.</p><h3 id="workstream-a-—-phase-controller-was-r1" tabindex="-1">Workstream A — Phase controller (was &quot;R1&quot;) <a class="header-anchor" href="#workstream-a-—-phase-controller-was-r1" aria-label="Permalink to &quot;Workstream A — Phase controller (was &quot;R1&quot;)&quot;">​</a></h3><p>Extract the phase state machine into <code>$lib/fly/flight-phase-controller.ts</code> — a pure, framework-agnostic module. <code>+page.svelte</code> consumes it; the scattered <code>showLaunch</code>/<code>showCoast</code>/<code>showDescent</code>/<code>showRecovery</code>/<code>openingActive</code>/<code>viewMode</code> booleans become derived from one controller state.</p><p><strong>Success criteria (all measurable):</strong></p><ol><li><code>flight-phase-controller.ts</code> exists, pure (no svelte/three/dom import), and is the single source of the active act + legal transitions.</li><li>Unit tests cover the transitions we hit this session and would have caught the bugs: crewed/suborbital → <code>ascent</code> (not heliocentric cruise); <code>isMoonMission</code> → cislunar vs heliocentric; scrub-U → act mapping; <code>?launch=1</code> / <code>?descent=1</code> deep-links; touchdown → <code>recovery</code>; opening → skip. <strong>Coverage-gated</strong> (pure lib → counts toward the frozen thresholds).</li><li><code>+page.svelte</code>&#39;s phase flags derive from the controller; the ordering-sensitive phase state moves into deterministic module init → the TDZ hazard <em>class</em> is removed for phase state.</li><li><code>/fly</code> Playwright e2e stays green; manual in-browser confirm of launch→coast→descent, a cislunar moon mission, and a flyby.</li></ol><h3 id="workstream-b-—-3d-scene-decomposition-the-3d-sim" tabindex="-1">Workstream B — 3D scene decomposition (the &quot;3D SIM&quot;) <a class="header-anchor" href="#workstream-b-—-3d-scene-decomposition-the-3d-sim" aria-label="Permalink to &quot;Workstream B — 3D scene decomposition (the &quot;3D SIM&quot;)&quot;">​</a></h3><p>Decompose the ~4,870-line <code>onMount</code> into a <code>fly-scene-host</code> that assembles the scene per active act and runs <code>onFrame</code>, built over the existing <code>fly-helio-scene</code> / <code>fly-cislunar-scene</code> / <code>fly-updaters</code> seam. The inline scene objects (moon-frame groups, phase lines, trajectory tubes — the last already extracted in R2) move into scene modules; <code>onFrame</code> becomes a composed set of per-frame updaters keyed on the controller&#39;s act.</p><p><strong>Success criteria (all measurable):</strong></p><ol><li><code>+page.svelte</code>&#39;s <code>onMount</code> shrinks to: create renderer/host, subscribe to the controller, wire input listeners, register cleanup — target <strong>&lt; ~600 lines</strong> (from ~4,870). No inline <code>new THREE.*</code> scene assembly left in the page.</li><li>Scene assembly + frame updates live in <code>$lib/three/fly-*</code> modules, each a plain function over a scene handle, testable via the existing jsdom scene/dispose harnesses (like <code>dispose-leak.test.ts</code>, <code>descent-models.test.ts</code>).</li><li>The TDZ hazard is <em>structurally</em> gone — no giant closure with order-coupled declarations; each scene module has deterministic construction.</li><li>Byte-identical render: <code>/fly</code> Playwright e2e green on desktop + mobile; manual confirm of every act (opening, ascent, coast, cruise, cislunar, flyby, descent, recovery) + a visual diff against pre-refactor screenshots for the flyby-hero and cislunar framings.</li></ol><h2 id="_4-·-the-seam-contract-why-one-rfc-designed-once" tabindex="-1">4 · The seam contract (why one RFC, designed once) <a class="header-anchor" href="#_4-·-the-seam-contract-why-one-rfc-designed-once" aria-label="Permalink to &quot;4 · The seam contract (why one RFC, designed once)&quot;">​</a></h2><p>The controller exposes a <strong>read-only</strong> view the scene host consumes each frame:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FlightPhaseState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  act</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FlyAct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;opening&#39;|&#39;ascent&#39;|&#39;coast&#39;|&#39;cruise&#39;|&#39;cislunar&#39;|&#39;flyby&#39;|&#39;descent&#39;|&#39;recovery&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  viewMode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;heliocentric&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;cislunar&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  clock</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">simDay</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">launchT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">coastMetDays</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">descentT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">playing</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  scene</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">activeScene</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FlySceneId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">transitionInto</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FlySceneId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The scene host <strong>never</strong> mutates phase; it renders <code>state</code> and reports frame events (touchdown, ascent-complete) back through callbacks the page routes into controller transitions. This one-way data-flow (controller → scene, events → controller) is the invariant both workstreams must honor. Fixing it now is why the two can be built in sequence without rework.</p><h2 id="_5-·-non-goals" tabindex="-1">5 · Non-goals <a class="header-anchor" href="#_5-·-non-goals" aria-label="Permalink to &quot;5 · Non-goals&quot;">​</a></h2><ul><li><strong>No behavior change.</strong> Not a visual, timing, cinematic, or UX change. Pixel- and frame-parity with the current <code>/fly</code> is a hard requirement (§3 e2e + visual diff gates).</li><li><strong>Not rewriting the physics</strong> (<code>fly-physics</code>, <code>ascent-physics</code>, <code>descent-profile</code>) or the cinematic quality bar ([[project_fly_cinematic_vision]]).</li><li><strong>Not re-homing the existing <code>fly-*-scene</code> modules</strong> — they stay; WS-B moves the <em>inline</em> page scene-build down to join them.</li><li><strong>Not touching <code>/explore</code></strong> (the other god-file) — a separate future concern.</li></ul><h2 id="_6-·-sequencing-risk-rollback" tabindex="-1">6 · Sequencing, risk, rollback <a class="header-anchor" href="#_6-·-sequencing-risk-rollback" aria-label="Permalink to &quot;6 · Sequencing, risk, rollback&quot;">​</a></h2><ol><li><strong>WS-A first</strong> — highest value (makes the buggy logic testable), lowest risk (pure logic, no WebGL), and it defines the seam WS-B consumes. Ship + validate before starting WS-B.</li><li><strong>WS-B second</strong> — larger, WebGL, higher regression risk; gated by the WS-A seam being real and the full e2e + visual-diff suite.</li></ol><p><strong>Risk:</strong> it&#39;s a prod-live file. <strong>Mitigation:</strong> each workstream lands behind green <code>/fly</code> e2e (desktop+mobile) + manual per-act browser confirm + a visual diff on the two most fragile framings (flyby-hero, cislunar); commits are small and revertible; the controller lands first with zero scene change so any regression is isolated to one layer. <strong>Rollback:</strong> each workstream is independently <code>git revert</code>-able because the seam keeps the layers decoupled.</p><h2 id="_7-·-decision-resolved-2026-08-05" tabindex="-1">7 · Decision (resolved 2026-08-05) <a class="header-anchor" href="#_7-·-decision-resolved-2026-08-05" aria-label="Permalink to &quot;7 · Decision (resolved 2026-08-05)&quot;">​</a></h2><p><strong>One RFC, sequenced workstreams</strong> — approved. The seam contract (§4) is designed once here; WS-A, WS-B, and WS-C are tracked as GitHub issues that reference this RFC. The two-RFC alternative was rejected because it would re-litigate §4 across files and risk divergence. Implementation of WS-A begins once the in-flight <code>main</code> push (the ADR-084 decomposition + review fixes) is docker-e2e-green and stable.</p><h2 id="_8-·-workstream-c-—-explore-same-pattern-after-fly-proves-it" tabindex="-1">8 · Workstream C — <code>/explore</code> (same pattern, after <code>/fly</code> proves it) <a class="header-anchor" href="#_8-·-workstream-c-—-explore-same-pattern-after-fly-proves-it" aria-label="Permalink to &quot;8 · Workstream C — \`/explore\` (same pattern, after \`/fly\` proves it)&quot;">​</a></h2><p><code>/explore/+page.svelte</code> is R1&#39;s sibling god-file — <strong>8,816 lines</strong>, a <strong>6,102-line <code>&lt;script&gt;</code></strong>, <strong>67 <code>$lib</code> imports</strong>. Its &quot;acts&quot; are the <strong>scale-shells</strong> (solar system → stellar neighbourhood → galactic → Local Group → universe), driven by a <code>contextId</code> state machine + <code>?context=</code> / <code>?galaxy=</code> deep-links + scale transitions (<code>scaleReadout</code>, <code>activeScale</code>, causality shells). Structurally identical to <code>/fly</code>: a tangled <strong>scale-shell orchestration</strong> (which shell/context is active, transitions, deep-link routing) welded to a <strong>per-scale 3D scene layer</strong>.</p><p>Apply the <strong>same</strong> decomposition — the seam contract (§4) generalizes (<code>act</code> → <code>scale-shell</code>; the controller decides the active shell + camera target, the scene layer renders it one-way):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/routes/explore/+page.svelte  ← THIN page</span></span>
<span class="line"><span> ├─ $lib/explore/scale-shell-controller.ts   ← pure, unit-tested: contextId + scale-shell</span></span>
<span class="line"><span> │     inputs:  { deepLink (?context/?galaxy), activeScale, selected ids, transitions }</span></span>
<span class="line"><span> │     outputs: { shell, viewScale, activeContext, camera target, legal transitions }</span></span>
<span class="line"><span> └─ $lib/three/explore-scene-host.ts (+ per-shell scene modules)   ← scene assembly + frame,</span></span>
<span class="line"><span>       driven by the controller&#39;s shell/scale. No orchestration in the host.</span></span></code></pre></div><p><strong>Success criteria (measurable, mirroring WS-A/WS-B):</strong></p><ol><li><code>$lib/explore/scale-shell-controller.ts</code> — pure (no svelte/three/dom), the single source of the active shell + legal transitions; <strong>unit-tested</strong> (scale-shell transitions, <code>?context=</code>/<code>?galaxy=</code> deep-links, reset-to-solar, causality-shell state). Coverage-gated.</li><li><code>explore-scene-host</code> + per-shell scene modules own scene assembly + frame updates; the page&#39;s scene-build/<code>onMount</code> reduces to wiring.</li><li><strong>Byte-identical behavior</strong> — <code>/explore</code> Playwright e2e green + manual per-shell confirm (solar, neighbourhood, galaxy, Local Group, universe) + visual diff on each shell boundary-crossing.</li></ol><p><strong>Sequencing:</strong> WS-C is <strong>blocked-by WS-A + WS-B</strong> — do <code>/fly</code> first so the controller/scene-host pattern + the seam are proven before applying them to the second file. WS-C reuses the WS-B <code>fly-scene-host</code> shape (not the code — the <em>pattern</em>). Tracked as <strong>#443</strong>.</p><p>Non-goals unchanged (§5): no behavior change, no <code>/explore</code> feature work (the scale-toggle #258, cosmos #259, message-trajectories #410, grand-tour #411 are separate roadmap items, not this refactor).</p>`,41)])])}const u=s(n,[["render",i]]);export{g as __pageData,u as default};
