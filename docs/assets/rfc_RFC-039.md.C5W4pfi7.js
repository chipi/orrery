import{_ as o,o as t,c as s,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const p=JSON.parse('{"title":"RFC-039 — The Known Universe: extending the /explore shell ladder to the observable universe","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-039.md","filePath":"rfc/RFC-039.md"}'),r={name:"rfc/RFC-039.md"};function n(l,e,i,c,d,h){return t(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="rfc-039-—-the-known-universe-extending-the-explore-shell-ladder-to-the-observable-universe" tabindex="-1">RFC-039 — The Known Universe: extending the /explore shell ladder to the observable universe <a class="header-anchor" href="#rfc-039-—-the-known-universe-extending-the-explore-shell-ladder-to-the-observable-universe" aria-label="Permalink to &quot;RFC-039 — The Known Universe: extending the /explore shell ladder to the observable universe&quot;">​</a></h1><p><em>Status: Shipped (v0.8.0, 2026-08-27) · Implements: PRD-034 · builds on: #258 (scale picker) · #410 (message-objects) · RFC-032 (/explore v2 shells) · RFC-036 (/explore scene-host decomposition seam) · PRD-030 (Known Universe principles) · the 8-shell ladder, Milky-Way-first milestone, scale picker, time playback and cosmology lens all landed — see CHANGELOG [0.8.0].</em></p><blockquote><p><strong>Why this is an RFC.</strong> The /explore scale ladder is a hardcoded 4-rung contract touched by at least eight files in lockstep — <code>scale-shell-controller.ts</code> (<code>CTX_ORDER</code>), <code>context-graph.ts</code> (parent chain), <code>explore-scene-host.ts</code> (per-shell camera constants + <code>crossOut*/crossIn*</code> + the wheel-handler ceiling + the <code>contextDeepLinkFn</code> walker), <code>ExploreScalePicker.svelte</code> (<code>ShellId</code> switch), and <code>+page.svelte</code> (honesty badges + breadcrumb). Adding <strong>four</strong> new shells and a <strong>science lens on every shell</strong> is not four PRs that can each improvise — if the shell-seam contract, the coordinate system, the data schema, and the lens-wiring aren&#39;t frozen once, each tier PR will re-litigate them and the workstreams will fight (the exact failure RFC-036 was written to prevent for <code>/fly</code>). This also binds a <strong>new coordinate system</strong> (supergalactic), a <strong>schema extension</strong> (redshift/cluster-affiliation), and a <strong>taxonomy change</strong> (which chips are &quot;science lens&quot; vs &quot;service&quot;) across the whole route. Freeze first, then slice.</p></blockquote><h2 id="_1-·-the-problem-measured" tabindex="-1">1 · The problem (measured) <a class="header-anchor" href="#_1-·-the-problem-measured" aria-label="Permalink to &quot;1 · The problem (measured)&quot;">​</a></h2><p>Per the PRD-034 audit (3 recon passes, 2026-08-08): the ladder terminates at Local Group (<code>context-graph.ts:84</code> — <code>parent: null</code>); the wheel handler at the ceiling just clamps (<code>explore-scene-host.ts:~1922</code>); there is <strong>no cosmology data</strong> past the Local Group; the <strong>Milky Way shell has 2 clickable objects, no scale HUD, 1 science article, and zero science lenses</strong>; and cosmology is nearly absent from the 162-article learning surface. Decisions locked in PRD-034: <strong>extend /explore</strong> (not a new route), <strong>scale UX = extended 8-rung ladder</strong> (grow #258), <strong>Milky Way first</strong>.</p><h2 id="_2-·-contract-a-—-the-add-a-shell-seam-frozen-recipe" tabindex="-1">2 · Contract A — the &quot;add a shell&quot; seam (frozen recipe) <a class="header-anchor" href="#_2-·-contract-a-—-the-add-a-shell-seam-frozen-recipe" aria-label="Permalink to &quot;2 · Contract A — the &quot;add a shell&quot; seam (frozen recipe)&quot;">​</a></h2><p>Every new shell (Local Sheet, Virgo, Laniakea, Cosmic Web) is added by the <strong>same</strong> eight-touch recipe. This RFC freezes it so each tier PR is mechanical and identical:</p><ol><li><strong><code>scale-shell-controller.ts</code></strong> — append the id to <code>CTX_ORDER</code> (the <code>ShellId</code> union + <code>planShellJump</code> bound auto-extend; raise the 6-step guard to N).</li><li><strong><code>context-graph.ts</code></strong> — add a <code>Context</code> entry (units, <code>scenePerUnit</code>, inner/outer boundary); re-point the previous outermost shell&#39;s <code>parent</code> to the new id; the new outermost gets <code>parent: null</code>.</li><li><strong><code>explore-scene-host.ts</code></strong> — add camera constants (<code>*_CAM_R_MIN/MAX</code>, far plane), an <code>ensure&lt;Shell&gt;()</code> lazy scene builder, <code>crossOutTo&lt;Shell&gt;()</code> + <code>crossInTo&lt;Prev&gt;()</code>, a <strong>new branch in the wheel-handler ceiling</strong> (replace the clamp at <code>~1922</code> with a <code>crossOutTo&lt;Shell&gt;()</code> call), and new <code>cur === N</code> cases in the <code>contextDeepLinkFn</code> walker.</li><li><strong><code>ExploreScalePicker.svelte</code></strong> — the rail auto-grows from <code>CTX_ORDER</code>; add the <code>labelFor()</code> switch case (exhaustive union → TS enforces this).</li><li><strong><code>+page.svelte</code></strong> — add the honesty-badge <code>{#if contextId === &#39;&lt;shell&gt;&#39;}</code> block (with a learn-more link — see Contract D) + breadcrumb crumb.</li><li><strong>Scene module</strong> — <code>src/lib/universe/&lt;shell&gt;-scene.ts</code> (the <code>local-group-scene.ts</code> template: sprites + labels + a &quot;you are here&quot; anchor for the inner shell).</li><li><strong>Data file</strong> — <code>static/data/universe/&lt;shell&gt;.json</code> (Contract B schema) + schema + <code>validate-data</code> registration.</li><li><strong>i18n</strong> — new <code>m.explore_ctx_&lt;shell&gt;()</code> label key + the schematic-badge key, 14 locales.</li></ol><p><strong>Invariant:</strong> the scale picker (#258) remains the single scale control; no per-tier bespoke navigation. Mobile = the same collapse-to-&quot;Scale ▾&quot; popover, now 8 rungs.</p><h2 id="_3-·-contract-b-—-coordinate-system-data-schema-frozen" tabindex="-1">3 · Contract B — coordinate system + data schema (frozen) <a class="header-anchor" href="#_3-·-contract-b-—-coordinate-system-data-schema-frozen" aria-label="Permalink to &quot;3 · Contract B — coordinate system + data schema (frozen)&quot;">​</a></h2><ul><li><strong>Supergalactic coordinates (SGL/SGB)</strong> are the frame for all super-Local-Group tiers (the natural frame for the supercluster plane). A new <code>src/lib/universe/supergalactic.ts</code> provides RA/Dec/dist → SG cartesian. Equatorial J2000 stays the frame for solar/neighborhood/MW as today.</li><li><strong>Every position past the Local Group is schematic</strong> (real catalogue distance, illustrative direction), carrying a <code>_note</code> + provenance, exactly as <code>local-group.json</code> does. Honest-not-to-scale is non-negotiable (PRD-030 principle 2).</li><li><strong>Schema extensions</strong> (additive, back-compat): <ul><li><code>deep-sky-objects.schema.json</code> / a new <code>galaxy-cluster</code> record: add optional <code>cluster_id</code>, <code>supercluster_id</code>, <code>is_bcg</code>, <code>dist_mpc</code>, <code>redshift</code>, <code>recession_kms</code>. Backfill the 34 null-distance Virgo galaxies (<code>deep-sky-objects.json</code>).</li><li>New per-tier data schemas (<code>local-sheet.schema.json</code>, <code>virgo-cluster.schema.json</code>, <code>laniakea.schema.json</code>, <code>cosmic-web.schema.json</code>) with a shared core (<code>id, name, kind, dist_mpc, diam_kly, sgx/sgy/sgz, headliner</code>) + tier-specifics (BCG flag, filament backbone points, void boundaries).</li><li>Provenance: <code>data-sources.json</code> entries for the catalogues used (McConnachie, Tully Cosmicflows, 2MRS/HyperLeda, Virgo Cluster Catalogue) — real citations, <code>validate-data</code>-checked.</li></ul></li></ul><h2 id="_4-·-contract-c-—-milestone-1-level-the-milky-way-frozen-scope" tabindex="-1">4 · Contract C — Milestone 1: level the Milky Way (frozen scope) <a class="header-anchor" href="#_4-·-contract-c-—-milestone-1-level-the-milky-way-frozen-scope" aria-label="Permalink to &quot;4 · Contract C — Milestone 1: level the Milky Way (frozen scope)&quot;">​</a></h2><p>The first build after this RFC. Raises the emptiest shell to the completeness bar and <strong>establishes the per-shell science-lens pattern</strong> the rest of the epic reuses. Scope:</p><ul><li><strong>Scene:</strong> add the <strong>galactic bar</strong> (the article already claims it), a <strong>globular-cluster layer</strong> (pin the ~30 already in <code>deep-sky-objects.json</code> as globular-cluster type at galactocentric positions), the <strong>Magellanic Clouds</strong> at the disc edge, and <strong>arm interactivity</strong> (each arm pickable → panel/article).</li><li><strong>HUD:</strong> stop suppressing the scale HUD in the MW shell (<code>+page.svelte:~2563</code>); show a kpc scale readout.</li><li><strong>Panels:</strong> fix the near-empty Sun panel (populate galactocentric distance from the schema root); give arms + globulars panels.</li><li><strong>First MW science lens</strong> (Contract D): rotation-curve, dark-matter-halo, stellar-populations overlays behind the science lens.</li><li><strong>Content:</strong> new MW science articles (galactic rotation, the galactic centre, stellar populations) + fill <code>see_in_app</code> cross-links; add the learn-more link the LG badge is missing.</li><li><strong>Cadence:</strong> its own <strong>mock-gate</strong> for the enrichment look before build; science-reviewer on all new claims; 14-locale i18n at the end; e2e desktop+mobile.</li></ul><h2 id="_5-·-contract-d-—-science-lens-on-every-shell-chip-reclassification-frozen" tabindex="-1">5 · Contract D — science lens on every shell + chip reclassification (frozen) <a class="header-anchor" href="#_5-·-contract-d-—-science-lens-on-every-shell-chip-reclassification-frozen" aria-label="Permalink to &quot;5 · Contract D — science lens on every shell + chip reclassification (frozen)&quot;">​</a></h2><ul><li><strong>Every /explore shell gets a science lens.</strong> Wire <code>ScienceLayersPanel</code> (or the neighborhood&#39;s inline lens-chip pattern) into the <strong>Milky Way, Local Group, and each new cosmology shell</strong> — today they have none. The lens is the single consistent home for &quot;this teaches you something.&quot;</li><li><strong>Chip taxonomy (frozen):</strong><ul><li><strong>Service chips (always-on, no lens gate):</strong> PLANETS, DWARFS, COMETS, INTERSTELLAR — pure visibility toggles.</li><li><strong>Teaching chips (move into / stay in the science lens):</strong> DEEP-SKY, CONSTELLATIONS, HR-DIAGRAM, LIGHT-CONES, and all new cosmology lenses (distance-ladder, Hubble-flow, dark-matter-web, redshift). PATHS is editorial-utility — stays a service chip but gains a <code>→ science</code> learn link.</li><li>The neighborhood&#39;s inline lens chips (HR, causality) are refactored to the standard lens surface so the pattern is uniform.</li></ul></li><li><strong>New cosmology lenses</strong> (per tier): distance-ladder (parallax→Cepheid→SN Ia rungs), Hubble-flow (recession vectors), dark-matter-web (filament mass overlay), redshift (colour-by-z). Each is a <code>LayerKey</code> with a <code>learn: {tab, section}</code> link (the existing <code>metaFor()</code> contract in <code>ScienceLayersPanel</code>).</li></ul><h2 id="_6-·-contract-e-—-the-learning-surface-frozen-set-tab-decision" tabindex="-1">6 · Contract E — the learning surface (frozen set + tab decision) <a class="header-anchor" href="#_6-·-contract-e-—-the-learning-surface-frozen-set-tab-decision" aria-label="Permalink to &quot;6 · Contract E — the learning surface (frozen set + tab decision)&quot;">​</a></h2><ul><li><strong>New cosmology articles</strong> (full shape: formula, diagram, narrative, tiered links, <code>see_in_app</code> into the new tiers; 14 locales; science-reviewer-gated): Big Bang · primordial + stellar nucleosynthesis · <strong>the role of carbon / CNO cycle + carbon-based life</strong> · stellar evolution (full life cycle) · supernovae + standard candles · the <strong>cosmic distance ladder</strong> · redshift &amp; Hubble&#39;s law · dark matter (dedicated) · dark energy · the CMB · large-scale structure / cosmic web.</li><li><strong>Tab decision (RESOLVED 2026-08-08 → new <code>cosmology</code> tab):</strong> the ~11 cosmology articles land in a <strong>new <code>cosmology</code> science tab</strong> (the 15th), not an <code>observation</code> expansion — the topics are a coherent group and <code>SCIENCE_TABS</code> + the drift-test (<code>science-tabs-vs-filesystem.test.ts</code>) already support adding a tab cleanly. Per the WS-5-first sequencing (also resolved 2026-08-08), the tab is created with the first tier that needs it (Virgo/WS-5b), and each new tier ships <strong>with</strong> its backing article rather than front-loading all of WS-4.</li><li>Each new /explore tier ships <strong>with</strong> its backing article(s) — no tier without a story.</li></ul><h2 id="_7-·-workstreams" tabindex="-1">7 · Workstreams <a class="header-anchor" href="#_7-·-workstreams" aria-label="Permalink to &quot;7 · Workstreams&quot;">​</a></h2><ul><li><strong>WS-1 · seam + schema</strong> (Contracts A/B) — the extension recipe made real once (context-graph, supergalactic util, schemas, the first new crossing) so tiers are mechanical after.</li><li><strong>WS-2 · Milky Way leveling</strong> (Contract C) — Milestone 1; blocks nothing, proves the bar + first lens.</li><li><strong>WS-3 · science-lens framework + chip reclassification</strong> (Contract D) — shared, unblocks per-tier lenses.</li><li><strong>WS-4 · cosmology learning surface</strong> (Contract E) — foundational articles lead; per-tier articles follow their tiers.</li><li><strong>WS-5 · the four new tiers</strong> (Local Sheet → Virgo → Laniakea → Cosmic Web) — each a mechanical application of Contract A + its data + panel + lens + article, one at a time, mock-gated.</li><li><strong>WS-6 · #411 grand-tour narration</strong> — LAST, on the completed arc.</li></ul><h2 id="_8-·-non-goals-risks" tabindex="-1">8 · Non-goals / risks <a class="header-anchor" href="#_8-·-non-goals-risks" aria-label="Permalink to &quot;8 · Non-goals / risks&quot;">​</a></h2><ul><li><strong>Non-goals:</strong> real N-body cosmology; photorealistic to-scale rendering of 10²⁷ m; redesigning the solar-system shell; #411 (deferred to WS-6).</li><li><strong>Risk — 8-rung rail height:</strong> if the extended #258 rail is too tall (esp. mobile), fall back to the C-hybrid spectrum (PRD-034 §UX) — scoped as a contained swap, not a redesign.</li><li><strong>Risk — data honesty at Gpc scale:</strong> the cosmic-web tier must not imply precision; heavy schematic badging + a &quot;this is a model&quot; learn link are mandatory.</li><li><strong>Risk — i18n + science-review volume:</strong> every tier adds strings + claims across 14 locales; budget the science-reviewer + translate passes into each tier&#39;s slice (the #410 cadence).</li></ul><h2 id="_9-·-open-questions-resolve-before-within-ws-1" tabindex="-1">9 · Open questions (resolve before/within WS-1) <a class="header-anchor" href="#_9-·-open-questions-resolve-before-within-ws-1" aria-label="Permalink to &quot;9 · Open questions (resolve before/within WS-1)&quot;">​</a></h2><ol><li><s><code>cosmology</code> tab vs <code>observation</code> expansion (§6) — recommend new tab.</s> <strong>RESOLVED 2026-08-08: new <code>cosmology</code> tab.</strong> Sequencing also resolved: WS-5 tiers first, each with its backing article (tab created with Virgo/WS-5b), rather than front-loading all of WS-4.</li><li>Cosmic-web data source: schematic hand-authored vs a decimated public catalogue (2MRS / IllustrisTNG) — provenance + weight trade-off.</li><li>Proper-motion lens for the neighborhood (needs the star-shell schema to carry pm) — in this epic or deferred to a neighborhood-specific follow-up?</li></ol><hr><p><em>RFC-039 · draft · 2026-08-08 — freezes the shell-extension seam, the coordinate/data contract, the Milky-Way-first milestone, the science-lens-everywhere + chip taxonomy, and the cosmology learning set. UXS + the GitHub epic/children follow.</em></p>',26)])])}const u=o(r,[["render",n]]);export{p as __pageData,u as default};
