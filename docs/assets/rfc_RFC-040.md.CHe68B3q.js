import{_ as t,o,c as a,a2 as r}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"RFC-040 — Mobile offline: the mobile image rung + tiered offline caching","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-040.md","filePath":"rfc/RFC-040.md"}'),i={name:"rfc/RFC-040.md"};function s(n,e,l,d,c,u){return o(),a("div",null,[...e[0]||(e[0]=[r(`<h1 id="rfc-040-—-mobile-offline-the-mobile-image-rung-tiered-offline-caching" tabindex="-1">RFC-040 — Mobile offline: the mobile image rung + tiered offline caching <a class="header-anchor" href="#rfc-040-—-mobile-offline-the-mobile-image-rung-tiered-offline-caching" aria-label="Permalink to &quot;RFC-040 — Mobile offline: the mobile image rung + tiered offline caching&quot;">​</a></h1><p><em>Status: Shipped (v0.8.0, 2026-08-27) · Implements: PRD-035 · builds on: the image srcset ladder (<code>src/lib/image-srcset.ts</code>), the mobile prune pipeline (<code>scripts/mobile/prune-streamed-assets.mjs</code>), the streaming SW (<code>vite.config.ts</code> runtimeCaching) · mobile rung + tiered offline caching live (CHANGELOG [0.8.0]). Leftover: #482 — 62 <code>.jpg</code> still to convert to a webp base.</em></p><blockquote><p><strong>Why this is an RFC.</strong> Two design decisions bind multiple subsystems and must be frozen once. Part 1 changes the <strong>image pipeline contract</strong> (which rungs exist, how wide the mobile rung is, how surface tiles are capped) — get the width wrong and we either still over-serve or regress visual quality, and a naive regen risks the LFS-stub / full-corpus-regen footguns (see the image-pipeline guards). Part 2 introduces <strong>new persistence</strong> (a large <strong>durable</strong> on-device store — deliberately not an evictable browser cache), a <strong>per-tier manifest contract</strong> that must stay in lockstep with the prune manifest, and a <strong>download engine</strong> with progress/out-of-space/network handling. Freeze the width target, the manifest shape, and the persistence layer before slicing.</p></blockquote><h2 id="_1-·-the-problem-measured-—-see-prd-035-§1" tabindex="-1">1 · The problem (measured — see PRD-035 §1) <a class="header-anchor" href="#_1-·-the-problem-measured-—-see-prd-035-§1" aria-label="Permalink to &quot;1 · The problem (measured — see PRD-035 §1)&quot;">​</a></h2><p>Manifest-measured (<code>measure-mobile-corpus.mjs</code>): <strong>3146 images already mobile-OK</strong>; the genuine over-serve was <strong>141 images / 33 MB</strong> (deep-sky, launch-ground, essays, missions/gemini) with no ≤1280 rung. hotspots (185 MB) are viewport-adaptive via the surface-scene tier dispatcher — not over-serve. (An earlier &quot;859/3089&quot; framing was a file-count artifact — see PRD-035 §1&#39;s scope correction.) No offline mode; the streamed set is unavailable without connectivity. The 1.1 GB image corpus is the whole ladder at every rung; a phone shows one rung (mobile-res corpus ~325 MB).</p><h2 id="_2-·-part-1-—-the-mobile-image-rung" tabindex="-1">2 · Part 1 — the mobile image rung <a class="header-anchor" href="#_2-·-part-1-—-the-mobile-image-rung" aria-label="Permalink to &quot;2 · Part 1 — the mobile image rung&quot;">​</a></h2><h3 id="contract-a-—-the-frozen-width-target" tabindex="-1">Contract A — the frozen width target <a class="header-anchor" href="#contract-a-—-the-frozen-width-target" aria-label="Permalink to &quot;Contract A — the frozen width target&quot;">​</a></h3><p>A phone&#39;s widest full-bleed image is ~430pt logical × 3 DPR ≈ <strong>1290px physical</strong>. So:</p><ul><li><strong>Standard browse imagery</strong> (galleries, missions, anatomy, etc.): the existing <strong><code>-1280.webp</code> rung is the mobile rung.</strong> Part 1 = <em>generate the missing <code>-1280.webp</code> (+ <code>.1x1.jpg</code> thumb where absent)</em> for every base image whose base exceeds ~1280px and that lacks the rung. Buckets already ≤1280px base (e.g. many galleries at 960px, missions at 1025px) need nothing — they&#39;re already mobile-sized.</li><li><strong>Surface hotspot tiles</strong> (<code>static/images/hotspots/*.jpg</code>, the 2048–3072px <code>tier2*</code>/<code>tier3*</code> panoramas + LROC): these are deep-zoom detail, not <code>srcset</code> images, so they don&#39;t get a <code>-1280</code> rung. Instead cap the mobile variant at <strong>≤1536px</strong> (a <code>-m.jpg</code> sibling) and have the surface-scene loader pick it under <code>Capacitor.isNativePlatform()</code>. The full-res tile stays for desktop + the Full offline tier.</li></ul><h3 id="regeneration-—-served-base-downscale-guarded-done-482" tabindex="-1">Regeneration — served-base downscale, guarded (DONE, #482) <a class="header-anchor" href="#regeneration-—-served-base-downscale-guarded-done-482" aria-label="Permalink to &quot;Regeneration — served-base downscale, guarded (DONE, #482)&quot;">​</a></h3><p>The over-serve buckets have <strong>no masters</strong> (essays/deep-sky/launch-ground) or only <strong>LFS stubs</strong> (missions/gemini — pulling them risks the full-corpus-regen / stub-degrade the guards prevent). So <code>add-mobile-rung.mjs</code> downscales the <strong>served base</strong> to the ≤1280 rung — one lanczos step from an already-web-encoded 1360–1920px base is visually lossless here. Enumerated exactly the 141 missing a rung, generated only those, diff-gated (141 new <code>-1280.webp</code> + manifest, zero base images touched). Idempotent (skips an existing rung); re-run when new large images land.</p><h3 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h3><ul><li><code>srcset</code> on every browse surface resolves to ≤1280px on a mobile viewport (unit test extends <code>image-srcset.test.ts</code>; e2e asserts no &gt;1280px image request on <code>mobile-chromium</code>).</li><li>Surface scenes request the <code>-m</code> tile on native.</li><li>A <code>scripts/mobile/measure-mobile-corpus.mjs</code> emits the mobile-res total per bucket → feeds Part 2&#39;s manifest + size budget.</li></ul><h2 id="_3-·-part-2-—-tiered-offline-caching" tabindex="-1">3 · Part 2 — tiered offline caching <a class="header-anchor" href="#_3-·-part-2-—-tiered-offline-caching" aria-label="Permalink to &quot;3 · Part 2 — tiered offline caching&quot;">​</a></h2><h3 id="contract-b-—-persistence-the-capacitor-filesystem-durable-app-data" tabindex="-1">Contract B — persistence: the Capacitor Filesystem (durable app data) <a class="header-anchor" href="#contract-b-—-persistence-the-capacitor-filesystem-durable-app-data" aria-label="Permalink to &quot;Contract B — persistence: the Capacitor Filesystem (durable app data)&quot;">​</a></h3><p>The offline download writes the tier&#39;s files into the <strong>app&#39;s durable data container</strong> via <code>@capacitor/filesystem</code> (<code>Directory.Data</code> on both platforms — a NEW plugin dependency, needs approval). This is deliberately NOT a browser cache.</p><p><strong>Why NOT the Service Worker Cache API</strong> (the obvious-looking choice — reuse the streaming SW): Cache Storage is <strong>OS-evictable</strong>. iOS/Android treat WebView Cache Storage / IndexedDB as <em>reclaimable</em> — under storage pressure the OS can silently purge it, and a user&#39;s offline download vanishes mid-flight (the exact failure the feature exists to prevent). <code>navigator.storage.persist()</code> requests non-eviction but its reliability inside iOS WKWebView is historically unreliable. For a &quot;download it for the plane&quot; feature, durability is the whole point, so evictable storage is disqualifying.</p><p><strong>Why Filesystem <code>Directory.Data</code> gives the operator&#39;s desired model</strong> (verified as the intent, 2026-08):</p><ul><li><strong>Durable</strong> — app-owned data, not OS-evictable; it survives storage pressure and relaunches.</li><li><strong>Visible + user-cleared as real storage</strong> — on iOS it counts under <strong>Settings → General → iPhone Storage → Orrery → &quot;Documents &amp; Data&quot;</strong> (the size the app takes); on Android it&#39;s <strong>User data</strong> in the app&#39;s storage breakdown. Cleared deliberately via our in-app &quot;Remove offline data&quot; (a Filesystem <code>rmdir</code> of the offline tree) or delete-app — NOT by an accidental Android &quot;Clear cache&quot; tap or an OS purge. This is exactly the behaviour we want: the user sees how much space the download takes and removes it on purpose.</li></ul><p><strong>Cost of Filesystem over Cache API</strong> (the tradeoff we accept): the <code>&lt;img src&gt;</code> / audio URLs no longer resolve transparently, so we add a thin resolver — on native, rewrite a streamed URL to its local <code>Filesystem.getUri(...)</code> (a <code>capacitor://</code> / <code>file://</code> path) when the file is present in the offline tree, else fall through to the network. One small <code>$lib/native/offline-assets.ts</code> shim consulted by the image-src + audio-src helpers; a miss is a normal online fetch, so it&#39;s safe when nothing is downloaded.</p><p><strong>Guardrails:</strong> handle write failures / out-of-space gracefully (surface it, leave the partial tree usable — never half-write silently); a tier&#39;s files live under a single <code>offline/&lt;tier&gt;/…</code> root so removal + accounting are one operation.</p><blockquote><p><strong>Pre-build verification (do before Part 2 code):</strong> confirm on a real device that <code>Directory.Data</code> files (a) show under iOS &quot;Documents &amp; Data&quot; and Android &quot;User data&quot;, (b) are NOT wiped by Android &quot;Clear cache&quot;, and (c) survive a storage-pressure / relaunch cycle. This RFC asserts platform behaviour from knowledge, not an on-device test — verify, then build.</p></blockquote><h3 id="contract-c-—-the-per-tier-manifest" tabindex="-1">Contract C — the per-tier manifest <a class="header-anchor" href="#contract-c-—-the-per-tier-manifest" aria-label="Permalink to &quot;Contract C — the per-tier manifest&quot;">​</a></h3><p>A build step emits <code>static/offline-manifest.json</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{ &quot;version&quot;: &quot;0.8.0&quot;,</span></span>
<span class="line"><span>  &quot;basic&quot;: { &quot;bytes&quot;: &lt;n&gt;, &quot;urls&quot;: [ &quot;&lt;mobile-rung url&gt;&quot;, ... ] },</span></span>
<span class="line"><span>  &quot;full&quot;:  { &quot;bytes&quot;: &lt;n&gt;, &quot;urls&quot;: [ ... ] } }</span></span></code></pre></div><p>Generated from the SAME bucket taxonomy the prune script uses (single source of truth — Part 1&#39;s <code>measure-mobile-corpus.mjs</code> feeds it), so tiers can&#39;t drift from what&#39;s actually streamed. <code>basic</code> = core browse buckets at mobile rung; <code>full</code> = every streamed URL at mobile rung + audio + 4K. URLs are absolute against <code>STREAM_ORIGIN</code>.</p><h3 id="the-download-engine" tabindex="-1">The download engine <a class="header-anchor" href="#the-download-engine" aria-label="Permalink to &quot;The download engine&quot;">​</a></h3><ul><li>Fetch the manifest tier → iterate URLs → <code>fetch</code> each → <code>Filesystem.writeFile</code> into <code>offline/&lt;tier&gt;/&lt;path&gt;</code> with a bounded concurrency (e.g. 6), reporting <code>{done, total, bytes}</code> progress.</li><li><strong>Network guard:</strong> <code>@capacitor/network</code> — Wi-Fi-only by default; on cellular, confirm with the size shown. Pause/resume on connectivity loss; the loop is idempotent (skip files already written) so a re-run resumes.</li><li><strong>Idempotent + updatable:</strong> &quot;Update&quot; re-runs against the current manifest version, adding new/changed files.</li><li>Errors: per-URL failures collected and retried once; an out-of-space error stops with a clear message + the partial tree stays usable.</li></ul><h3 id="ui-—-settings-panel-section-mobile-only" tabindex="-1">UI — Settings panel section (mobile only) <a class="header-anchor" href="#ui-—-settings-panel-section-mobile-only" aria-label="Permalink to &quot;UI — Settings panel section (mobile only)&quot;">​</a></h3><p>New &quot;Offline&quot; section in <code>QualitySettingsModal.svelte</code>, gated <code>Capacitor.isNativePlatform()</code>:</p><ul><li>Radio: <strong>Off</strong> (default) · <strong>Basic (~N MB)</strong> · <strong>Full (~N MB)</strong> — sizes from the manifest.</li><li>State machine: idle → downloading (progress bar + <code>X / Y MB</code>) → done (✓ + &quot;Update&quot; + &quot;Remove offline data&quot;).</li><li>A one-line &quot;Wi-Fi recommended&quot; note + cellular override.</li></ul><h2 id="_4-·-slice-plan" tabindex="-1">4 · Slice plan <a class="header-anchor" href="#_4-·-slice-plan" aria-label="Permalink to &quot;4 · Slice plan&quot;">​</a></h2><p><strong>Part 1 (issue #482):</strong></p><ol><li><code>measure-mobile-corpus.mjs</code> — enumerate base images lacking a ≤1280 rung + surface tiles &gt;1536px; report sizes.</li><li>Generate the missing <code>-1280.webp</code> + <code>.1x1.jpg</code> (guarded, diff-gated).</li><li>Surface-tile <code>-m</code> variant + native loader pick.</li><li>Verify: srcset unit + <code>mobile-chromium</code> e2e (no &gt;1280 request); commit.</li></ol><p><strong>Part 2 (issue filed after Part 1):</strong> 5. On-device pre-build check: <code>Directory.Data</code> durability + storage-accounting behaviour (Contract B verification note). 6. <code>offline-manifest.json</code> build step (basic/full from the bucket taxonomy). 7. <code>@capacitor/filesystem</code> download engine (write to <code>offline/&lt;tier&gt;/</code>) + <code>@capacitor/network</code> guard + progress store + the <code>offline-assets.ts</code> URL resolver. 8. Settings &quot;Offline&quot; section + state machine + &quot;Remove offline data&quot; (<code>rmdir</code> the tier tree). 9. Verify: airplane-mode e2e per tier (mock manifest); on-device confirmation the download shows under iOS &quot;Documents &amp; Data&quot; / Android &quot;User data&quot;.</p><h2 id="_5-·-non-goals" tabindex="-1">5 · Non-goals <a class="header-anchor" href="#_5-·-non-goals" aria-label="Permalink to &quot;5 · Non-goals&quot;">​</a></h2><p>Web-PWA offline (mobile app only); per-section/partial downloads; background/auto download; changing desktop imagery or the default streaming behaviour.</p>`,37)])])}const p=t(i,[["render",s]]);export{g as __pageData,p as default};
