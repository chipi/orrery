import{_ as t,o,c as s,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const p=JSON.parse('{"title":"RFC-041 — AR night sky: one sky, everything on it (constellations + stars + deep-sky + horizon)","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/RFC-041.md","filePath":"rfc/RFC-041.md"}'),n={name:"rfc/RFC-041.md"};function r(i,e,l,d,c,h){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="rfc-041-—-ar-night-sky-one-sky-everything-on-it-constellations-stars-deep-sky-horizon" tabindex="-1">RFC-041 — AR night sky: one sky, everything on it (constellations + stars + deep-sky + horizon) <a class="header-anchor" href="#rfc-041-—-ar-night-sky-one-sky-everything-on-it-constellations-stars-deep-sky-horizon" aria-label="Permalink to &quot;RFC-041 — AR night sky: one sky, everything on it (constellations + stars + deep-sky + horizon)&quot;">​</a></h1><p><em>Status: Implemented · 2026-08-27 · builds on: the sky-pointing AR scene (<code>src/lib/ar/sky-scene.ts</code>, #393/#51), the astronomy layer (<code>src/lib/astronomy/</code>, <code>equatorialToHorizontal</code>), the existing celestial data (<code>static/data/universe/constellation-lines.json</code>, <code>named-stars.json</code>, <code>deep-sky-objects.json</code>) · target: v0.8 · Tracking: #487, #488</em></p><blockquote><p><strong>Why this is an RFC.</strong> It freezes an <strong>architecture decision</strong> that would otherwise be re-litigated per feature: the AR sky is <strong>one sky with additive content layers</strong>, not a planets-sky vs a constellations-sky vs a per-<code>/explore</code>- scale sky. That decision binds several subsystems in lockstep — the AR scene (<code>sky-scene.ts</code>), the shared <strong>coordinate contract</strong> (equatorial data → <code>equatorial ToHorizontal</code> → <code>skyDirectionENU</code>, the <em>same</em> transform the planets use, so a new layer can never drift into its own frame), the celestial <strong>data contracts</strong> (<code>constellation-lines.json</code> / <code>named-stars.json</code> shapes), and the AR HUD&#39;s <strong>layer model</strong> (what&#39;s toggleable, defaults). Get the frame wrong and constellations silently misalign from the planets; split the sky per scale and the &quot;see everything on one screen&quot; intent is lost. Freeze the one-sky model + the vertex→ENU contract once, then slice.</p></blockquote><h2 id="_1-·-the-idea-operator-2026-08-26" tabindex="-1">1 · The idea (operator, 2026-08-26) <a class="header-anchor" href="#_1-·-the-idea-operator-2026-08-26" aria-label="Permalink to &quot;1 · The idea (operator, 2026-08-26)&quot;">​</a></h2><p>When you hold the phone up in AR &quot;SKY&quot; mode you are looking at the <strong>one real sky</strong>. Today it marks only the Solar-System bodies (Sun, Moon, planets) + the ISS/Tiangong. The operator wants to <strong>see everything we have data for on that one screen</strong> — the planets <strong>and</strong> the constellations <strong>and</strong> the bright named stars — all at once, entered from <strong>one place</strong>, not a planets-sky vs a constellations-sky.</p><blockquote><p>&quot;I don&#39;t wanna have one sky with planets, another one with constellations… we wanna have only the sky on the first one, and we see everything on the sky there coming from all the other tops as well.&quot;</p></blockquote><h2 id="_2-·-decision" tabindex="-1">2 · Decision <a class="header-anchor" href="#_2-·-decision" aria-label="Permalink to &quot;2 · Decision&quot;">​</a></h2><p><strong>One AR Sky feature with additive content layers, all on by default.</strong> The <code>/explore</code> scale (Solar System → Neighborhood → Milky Way) is a navigation concept in the flat/3D view; it is <strong>not</strong> a property of the real sky. So the sky is one thing, and the scale-specific topics (planets, constellations, stars) are just <em>layers on that one sky</em>:</p><ul><li><strong>Planets / Sun / Moon / stations</strong> — already built (#393/#51).</li><li><strong>Constellation figures + names</strong> — Slice 1 (shipped).</li><li><strong>Bright named stars + labels</strong> — Slice 2 (shipped).</li><li><strong>Deep-sky objects (nebulae / galaxies / clusters)</strong> — soft category-coloured glows + labels, #488 (shipped). Big diffuse &quot;clouds in the distance&quot; to orient by.</li><li><strong>Horizon line + hide-below-horizon</strong> (default hidden, with a toggle) + <strong>sunrise/sunset horizon markers</strong> — #488 (shipped).</li></ul><p>Entry stays the single existing <strong>SKY</strong> affordance (solar-system scale). We do <strong>not</strong> add per-scale sky variants or per-scale entry points — the operator explicitly wants one screen showing everything. Layer toggles exist only as <em>optional decluttering</em>; the default is everything visible.</p><h2 id="_3-·-how-reuses-the-pipeline-we-just-hardened-—-no-new-astronomy" tabindex="-1">3 · How (reuses the pipeline we just hardened — no new astronomy) <a class="header-anchor" href="#_3-·-how-reuses-the-pipeline-we-just-hardened-—-no-new-astronomy" aria-label="Permalink to &quot;3 · How (reuses the pipeline we just hardened — no new astronomy)&quot;">​</a></h2><p>The AR scene already anchors a marker at a body&#39;s real direction via <code>skyPosition(body,date,lat,lon) → skyDirectionENU</code>. Constellations and stars plug into the <strong>same</strong> pipe, because the celestial data is already in the equatorial frame:</p><ul><li><code>constellation-lines.json</code> — 89 constellations; each is a flat list of <strong>equatorial XYZ</strong> vertices (built from RA/Dec by <code>build-constellation-lines</code>), every 2 points = one figure segment.</li><li><code>named-stars.json</code> — 62 bright named stars (Sirius, Vega, …) with proper name, magnitude, and equatorial XYZ (standard HYG frame: x=vernal equinox, z=NCP).</li></ul><p>For any vertex <code>(x,y,z)</code> (equatorial): <code>raRad = atan2(y,x)</code>, <code>decRad = asin(z/‖v‖)</code> → <code>equatorialToHorizontal({raRad,decRad,distanceAu:∞}, jd, lat, lon)</code> → alt/az → <code>skyDirectionENU</code> → the <strong>same</strong> world direction the planets use (distance set effectively infinite ⇒ zero topocentric parallax). Render: constellation segments as <code>THREE.LineSegments</code> anchored at <code>camPos + dir·R</code>; stars as point sprites sized by magnitude with labels on the brightest. Recompute at the existing ~2 Hz ephemeris cadence so the field tracks sidereal rotation.</p><p>Because constellations ride the <strong>same</strong> ENU transform as the planets, a familiar constellation landing where you actually see it is also a <strong>free correctness check</strong> on the whole sky alignment.</p><h2 id="_4-·-non-goals" tabindex="-1">4 · Non-goals <a class="header-anchor" href="#_4-·-non-goals" aria-label="Permalink to &quot;4 · Non-goals&quot;">​</a></h2><ul><li><strong>No per-scale skies / per-scale entry.</strong> One sky, one entry (§2).</li><li><strong>No new imagery or new catalogs.</strong> Uses the existing <code>constellation-lines.json</code>, <code>named-stars.json</code> + <code>deep-sky-objects.json</code> only. A deeper star catalog (fainter mags) is out of scope — 62 named stars + the figure lines + 141 deep-sky objects are the &quot;everything we have&quot;.</li><li><strong>No zodiac artwork layer</strong> (the drawn zodiac characters over the figures). The operator scoped it out of this batch (&quot;everything except for the zodiac&quot;); it remains a possible later layer.</li><li><strong>Not</strong> touching the non-AR <code>/explore</code> constellation science layer.</li></ul><h2 id="_5-·-risks-design-constraints" tabindex="-1">5 · Risks / design constraints <a class="header-anchor" href="#_5-·-risks-design-constraints" aria-label="Permalink to &quot;5 · Risks / design constraints&quot;">​</a></h2><ul><li><strong>Clutter.</strong> 88 figures + stars can overwhelm a phone FOV. Mitigate with subtle line weight/opacity, labels on major/brightest only, and an optional per-layer declutter toggle. Tune on-device.</li><li><strong>Correctness rides on the planet alignment</strong>, still unconfirmed on-device (the #51 AR retest). If planets are off, constellations are off the same way.</li><li><strong>Data availability on native.</strong> <code>data/universe/*</code> is small (constellation-lines ≈ 39 KB) and already loaded by <code>/explore</code>; reuse the <code>universe.ts</code> <code>get()</code> loader (streams from the asset origin) so no bundle change.</li></ul><h2 id="_6-·-slices" tabindex="-1">6 · Slices <a class="header-anchor" href="#_6-·-slices" aria-label="Permalink to &quot;6 · Slices&quot;">​</a></h2><ul><li><strong>S1</strong> — constellation lines + names in the AR sky + a Constellations toggle. ✓</li><li><strong>S2</strong> — bright named stars + labels + a Stars toggle. ✓</li><li><strong>S3</strong> — everything on by default (one screen); wire the optional toggles into the AR HUD. (Originally &quot;ungate entry across scales&quot; — dropped per §2.) ✓</li><li><strong>S4 (#488)</strong> — deep-sky / nebula glows + labels + a Nebulas toggle. ✓</li><li><strong>S5 (#488)</strong> — horizon line, hide-below-horizon (default on, toggle), and sunrise/sunset horizon markers + a Rise/set toggle. ✓</li></ul><p>Correctness is unit-tested in isolation (<code>celestial-sky.test.ts</code>): RA/Dec→ENU via the north celestial pole, the deep-sky filter, and sunrise/sunset azimuths (equinox equator ⇒ rises due-E / sets due-W). Visual framing is the operator&#39;s on-device check alongside the #51 AR retest; desktop framing verified via the <code>/dev/sky-preview</code> harness (a mock <code>SkyView</code> at a fixed location + heading).</p><h2 id="_7-·-editorial-redesign-2026-08-27-—-advisor-led" tabindex="-1">7 · Editorial redesign (2026-08-27) — advisor-led <a class="header-anchor" href="#_7-·-editorial-redesign-2026-08-27-—-advisor-led" aria-label="Permalink to &quot;7 · Editorial redesign (2026-08-27) — advisor-led&quot;">​</a></h2><p>The first-cut layers were functional but cluttered and off-brand. A design pass (Fable-5 advisor review) pulled the whole sky to a <strong>&quot;printed star atlas / WIRED&quot;</strong> direction, aligned to <code>tokens.css</code> + the /fly + ConstellationFinder idioms:</p><ul><li><strong>Labels</strong> — no chip pills. <code>makeTextSprite</code> renders box-free UPPERCASE tracked labels with a dark shadow-halo (the /fly milestone idiom), in tiers: constellations whisper (faint/wide), stars small, planets a bold <strong>display</strong> tier, cardinals/sun quiet. Typography carries the hierarchy.</li><li><strong>Label budget + declutter</strong> — only &quot;primary&quot; labels show by default (brightest stars mag&lt;1.5 + real-named deep-sky); bare catalog IDs (<code>NGC…</code>) and faint stars wait for an <strong>All names</strong> toggle. A screen-space pass projects every label, priority-sorts (planets &gt; sun &gt; stars &gt; stations &gt; figures &gt; deep-sky), rejects overlaps and requires the whole box on-screen (kills the Orion&#39;s-belt smear + edge clip).</li><li><strong>Starfield / deep-sky</strong> — hard-core star texture + diffraction spikes on the brightest; deep-sky as small <strong>shaped</strong> ice-blue glows (galaxy ellipse / cluster speckle / planetary ring / nebula cloud), not grey smudges.</li><li><strong>Planets (<code>planet-bake.ts</code>, advisor Path B)</strong> — baked from the shipped <code>static/textures/2k_*.jpg</code> maps: each body rendered once as a real 3D sphere lit along its true Sun direction into a <code>WebGLRenderTarget</code> → sprite map. Real albedo, Jupiter&#39;s belts + Great Red Spot, <strong>Saturn&#39;s rings</strong> (real 3D occlusion), correct <strong>Mercury/Venus/Moon phases</strong> from the light geometry, a granulated Sun. Marker <strong>size tracks apparent angular diameter</strong> (log-compressed from live <code>distanceAu</code>: Sun≈Moon; Mars grows toward opposition) and <strong>brightness drives a per-body aura</strong> — one honest quantity per channel (size = geometry, glow = brightness, phase = Sun geometry). Procedural canvas markers remain the pre-bake fallback.</li><li><strong>Stations</strong> — ISS/Tiangong render as a small coloured diamond glyph (hardware, not worlds), and respect the below-horizon toggle like the planets.</li><li><strong>HUD</strong> — one dark-glass strip of text buttons (no emoji, teal status dots), editorial serif hint, chip + scene-fade motion (reduced-motion-safe).</li></ul><p>Deferred: per-layer canvas cross-fade on toggle (multi-material + <code>setDrawRange</code> packing make it fight the framework); the true bright-limb <em>orientation</em> of inner- planet crescents (the lit fraction is correct, the terminator tilt is simplified); a daytime &quot;clean disc&quot; Sun variant. All redesign work is desktop-verified via <code>/dev/sky-preview</code>; <strong>on-device</strong> (WKWebView render-target bakes over the live camera feed) remains the operator&#39;s smoke test.</p><h2 id="_0-8-1-amendment-—-precession-to-the-equinox-of-date-m1" tabindex="-1">0.8.1 amendment — precession to the equinox of date (M1) <a class="header-anchor" href="#_0-8-1-amendment-—-precession-to-the-equinox-of-date-m1" aria-label="Permalink to &quot;0.8.1 amendment — precession to the equinox of date (M1)&quot;">​</a></h2><p>An independent physics review found the sky pipeline mixed reference frames: <code>astronomy/planets.ts</code> outputs J2000 ecliptic positions and the HYG star catalogue is J2000 equatorial, yet <code>equatorialToHorizontal</code> already used the <strong>of-date</strong> obliquity and <strong>of-date</strong> local sidereal time — a ~0.36°-in-2026 framing error (growing ~0.014°/yr) between the two ends of the pipeline. Both source frames are now precessed to the equinox <strong>of date</strong> before the of-date steps consume them, so planets and stars share one consistent sky:</p><ul><li><strong>Planets + Sun</strong> — <code>precessEclipticJ2000ToDate</code> rotates the ecliptic vector about the ecliptic pole by the general precession in longitude (<code>index.ts</code>, before <code>eclipticToEquatorial</code>). The Schlyter <strong>Moon</strong> is already ecliptic-of- date and is left untouched.</li><li><strong>Stars / constellations / deep-sky</strong> — <code>precessEquatorialJ2000ToDate</code> (rigorous IAU 1976 ζ/z/θ rotation matrix) precesses the HYG equatorial directions in <code>celestial-sky.ts</code> before RA/Dec.</li></ul><p>Verified against Meeus worked examples: planet alt/az ex. 13.b (Venus 248.031°/15.130° vs 248.03°/15.12°) and equatorial precession ex. 21.b (θ Persei, arcsecond agreement modulo the star&#39;s proper motion).</p>',30)])])}const u=t(n,[["render",r]]);export{p as __pageData,u as default};
