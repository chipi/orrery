import{_ as s,o as a,c as t,a2 as n}from"./chunks/framework.Cdp8-jB6.js";const u=JSON.parse('{"title":"IA — Information Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"uxs/IA.md","filePath":"uxs/IA.md"}'),o={name:"uxs/IA.md"};function r(i,e,l,d,c,p){return a(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="ia-—-information-architecture" tabindex="-1">IA — Information Architecture <a class="header-anchor" href="#ia-—-information-architecture" aria-label="Permalink to &quot;IA — Information Architecture&quot;">​</a></h1><p><em>Orrery · Reference document · v1.2 · August 2026</em></p><blockquote><p><strong>Note (v1.2):</strong> the app has grown well past the original &quot;six screens&quot; this doc opens with. §overview / §surfaces below are historical; <strong>§nav-groups (2026-08)</strong> is the current top-level information architecture and supersedes §navigation for the nav bar + landing.</p></blockquote><p>This is the reference document for the UX plane. UXSes anchor to it by section. When surfaces, navigation, tokens, or shell regions change, this document is amended.</p><hr><h2 id="§overview" tabindex="-1">§overview <a class="header-anchor" href="#§overview" aria-label="Permalink to &quot;§overview&quot;">​</a></h2><p>Six screens, one persistent nav bar, SvelteKit&#39;s History API router. No login state. No user data. No persistence between sessions.</p><p>The narrative arc: Moon Map (prologue) → Solar System Explorer (Act 1) → Mission Configurator (Act 2) → Mission Arc (Act 3) → Mission Catalog (archive) → Earth Orbit (context). The nav bar presents all six destinations at all times. There is no enforced sequence. The Mission Catalog page was previously called &quot;Mission Library&quot;; under ADR-051 it was renamed to free the word <em>Library</em> for the outbound-link inventory at <code>/library</code>.</p><hr><h2 id="§surfaces" tabindex="-1">§surfaces <a class="header-anchor" href="#§surfaces" aria-label="Permalink to &quot;§surfaces&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Surface</th><th>Route</th><th>File</th><th>Primary canvas</th></tr></thead><tbody><tr><td>Moon Map</td><td><code>/moon</code></td><td><code>src/routes/moon/+page.svelte</code></td><td>Three.js sphere + Canvas 2D flat map</td></tr><tr><td>Solar System Explorer</td><td><code>/explore</code></td><td><code>src/routes/explore/+page.svelte</code></td><td>Three.js 3D + Canvas 2D toggle</td></tr><tr><td>Mission Configurator</td><td><code>/plan</code></td><td><code>src/routes/plan/+page.svelte</code></td><td>Canvas 2D porkchop plot</td></tr><tr><td>Mission Arc</td><td><code>/fly</code></td><td><code>src/routes/fly/+page.svelte</code></td><td>Three.js 3D + Canvas 2D toggle</td></tr><tr><td>Mission Catalog</td><td><code>/missions</code></td><td><code>src/routes/missions/+page.svelte</code></td><td>CSS grid card layout</td></tr><tr><td>Earth Orbit</td><td><code>/earth</code></td><td><code>src/routes/earth/+page.svelte</code></td><td>Canvas 2D logarithmic scale</td></tr><tr><td>Physics Lab</td><td><code>/lab</code></td><td><code>src/routes/lab/+page.svelte</code></td><td>SVG figures on the <code>station-blueprint</code> teal grid (Notebook / Focus; Canvas at T2)</td></tr></tbody></table><p><em>(This table predates several later surfaces — <code>/missions</code>, <code>/science</code>, <code>/iss</code>, <code>/programs</code>, etc. — and is maintained additively; the &quot;six screens&quot; framing in §overview is historical.)</em></p><hr><h2 id="§lab" tabindex="-1">§lab <a class="header-anchor" href="#§lab" aria-label="Permalink to &quot;§lab&quot;">​</a></h2><p>The Physics Lab (<code>/lab</code>) is a subsystem, not a single screen — <strong>one card model rendered in three views</strong> (specified in UXS-015; built per RFC-037).</p><ul><li>A <strong>card</strong> is a formula instance: a kernel formula + bound inputs + optional wiring + the <code>FigureSpec</code> it emits. It is portable, serializable data.</li><li><strong>Notebook</strong> (v1 home) — an ordered, narrated, exportable column of cards. Mobile-first.</li><li><strong>Focus</strong> (v1 detail) — one card full-screen: big figure stage + control rail + <code>/science</code> derivation. Uses the <code>Panel</code> bottom-sheet on mobile.</li><li><strong>Canvas</strong> (T2, second workspace) — a spatial graph with output→input wiring; desktop-first, read-only fallback on mobile.</li></ul><p>Cards <strong>promote</strong> from Canvas into a Notebook. The Lab reuses the shared tokens (§shared-tokens) and components (<code>Panel</code>, <code>ScienceChip</code>, <code>WhyPopover</code>, <code>.readout-grid</code>, <code>ImageCredit</code>) wholesale; the teal figure grid is <code>station-blueprint.ts</code>. Colour discipline: teal = figure/selection, <strong>gold = explanation</strong>, mars-red = fail-honest. The Lab is the first surface to persist state (see §state-persistence).</p><hr><h2 id="§topbar-patterns" tabindex="-1">§topbar-patterns <a class="header-anchor" href="#§topbar-patterns" aria-label="Permalink to &quot;§topbar-patterns&quot;">​</a></h2><p>One topbar pattern: the persistent 52px nav bar.</p><p><strong>Persistent nav bar</strong> — present on all six screens without exception.</p><ul><li>Height: <code>52px</code></li><li>Background: <code>rgba(4,4,12,0.92)</code></li><li>Border-bottom: <code>1px solid rgba(255,255,255,0.07)</code></li><li>Backdrop-filter: <code>blur(14px)</code></li><li>Z-index: <code>20</code></li></ul><p>Left region: ORRERY wordmark (Bebas Neue 26px, letter-spacing 4px) + &quot;MISSION SIMULATOR&quot; subtitle (Space Mono 8px, rgba(255,255,255,0.22)).</p><p>Centre region: six screen links. Active screen: white text, <code>rgba(68,102,255,0.28)</code> background, <code>rgba(68,102,255,0.55)</code> border. Inactive: <code>rgba(255,255,255,0.28)</code>, transparent border, hover to <code>rgba(255,255,255,0.85)</code>.</p><p>Right region: screen-specific controls (3D/2D toggle on explore and fly, CAPCOM toggle on fly, status indicator on fly).</p><hr><h2 id="§navigation" tabindex="-1">§navigation <a class="header-anchor" href="#§navigation" aria-label="Permalink to &quot;§navigation&quot;">​</a></h2><p>History API routing via SvelteKit&#39;s built-in router. Format: <code>/[screen]</code> with optional query params for mission IDs.</p><table tabindex="0"><thead><tr><th>Pattern</th><th>Example</th><th>Used by</th></tr></thead><tbody><tr><td><code>/[screen]</code></td><td><code>/explore</code></td><td>All screens</td></tr><tr><td><code>/fly?mission=[id]</code></td><td><code>/fly?mission=curiosity</code></td><td>Mission Arc (loads specific mission)</td></tr><tr><td><code>/missions?dest=[MARS|MOON]</code></td><td><code>/missions?dest=MARS</code></td><td>Mission Catalog (pre-filtered)</td></tr></tbody></table><p>Navigation triggers: nav bar links, &quot;PLAN A MISSION&quot; button in explore screen, &quot;FLY THIS MISSION&quot; button in missions screen.</p><p>History API routing is locked in ADR-013 (superseding ADR-004). RFC-001 is closed — SvelteKit&#39;s router replaces the hand-written router originally proposed.</p><hr><h2 id="§nav-groups-2026-08-—-current-top-level-ia" tabindex="-1">§nav-groups (2026-08 — current top-level IA) <a class="header-anchor" href="#§nav-groups-2026-08-—-current-top-level-ia" aria-label="Permalink to &quot;§nav-groups (2026-08 — current top-level IA)&quot;">​</a></h2><p>The nav bar (<code>src/lib/components/Nav.svelte</code>) is <strong>grouped</strong>, not flat. There are six top-level slots: two standalone links and four dropdown groups.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>HOME  ·  EXPLORE ▾  ·  WORLDS ▾  ·  FLY  ·  PLAN  ·  CATALOG ▾  ·  LEARN ▾</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Slot</th><th>Kind</th><th>Members (children)</th><th>Group hub (TV)</th></tr></thead><tbody><tr><td>Home</td><td>link → <code>/</code></td><td>—</td><td>—</td></tr><tr><td><strong>Explore</strong></td><td>group</td><td>Solar System · Stellar Neighborhood · Milky Way · Local Group (the <code>/explore</code> scale shells, via <code>?context=</code>)</td><td><code>/explore/hub</code></td></tr><tr><td><strong>Worlds</strong></td><td>group</td><td>Earth · Moon · Mars · Venus (the <code>SurfaceScene</code> routes)</td><td><code>/worlds</code></td></tr><tr><td>Fly</td><td>link → <code>/fly</code></td><td>—</td><td>—</td></tr><tr><td>Plan</td><td>link → <code>/plan</code></td><td>—</td><td>—</td></tr><tr><td><strong>Catalog</strong></td><td>group</td><td>Programs · Missions · Fleet · ISS · Tiangong · Live</td><td><code>/catalog</code></td></tr><tr><td><strong>Learn</strong></td><td>group</td><td>Essays · Science</td><td><code>/learn</code></td></tr></tbody></table><p><strong>Two axes — Explore vs Worlds (2026-08 split).</strong> <code>/explore</code> was doing two jobs: the zoom-out <em>scale</em> ladder (solar system → cosmic web) <strong>and</strong> the close-up of a single <em>body</em>. These are different questions — &quot;how far out am I looking&quot; vs &quot;which world am I on&quot; — so the nav splits them: <strong>Explore = scope</strong> (the scale shells), <strong>Worlds = bodies</strong> (only bodies with their own dedicated page, i.e. a <code>SurfaceScene</code>: Earth/Moon/Mars/Venus today; the group grows only as new surface scenes ship). <code>/explore</code> itself is <strong>not renamed</strong> — the 375+ <code>SeeInApp</code><code>route:&quot;/explore&quot;</code> records, the <code>orrery://explore</code> deep-link scheme, and e2e all keep working. Cosmology content is <strong>not</strong> an Explore nav item: it lives under <code>/science</code> (a cosmology tab) in the <strong>Learn</strong> group; the scale shells link to it contextually via each shell&#39;s science-lens &quot;→ Read in /science&quot; link.</p><p><strong>TV (10-foot / D-pad).</strong> A dropdown is unusable with a D-pad, so on TV each group renders as a single link to its <strong>big-box hub</strong> (<code>/explore/hub</code>, <code>/worlds</code>, <code>/catalog</code>, <code>/learn</code>), each a <code>SectionHub</code> tile grid of the group&#39;s members. Desktop + mobile are unchanged (gated on <code>isTv</code>).</p><h2 id="§home-cards-2026-08-—-the-landing-ia" tabindex="-1">§home-cards (2026-08 — the landing IA) <a class="header-anchor" href="#§home-cards-2026-08-—-the-landing-ia" aria-label="Permalink to &quot;§home-cards (2026-08 — the landing IA)&quot;">​</a></h2><p>The landing (<code>src/routes/+page.svelte</code>) card grid is a <strong>discovery</strong> surface, and that is a <em>different job</em> from the nav&#39;s <strong>wayfinding</strong>. Principle: the nav is persistent and must stay compact (hence dropdowns); the landing has room and should reveal breadth. So the two need not be identical — only <strong>reconcilable</strong> (no card leads somewhere the nav can&#39;t reach). Applied:</p><ul><li>The landing shows <strong>the whole nav tree, unpacked and sectioned</strong> — every group is a labeled section, every leaf its own rich card (with a hand-drawn glyph): <strong>Explore</strong> (the 4 scale shells) · <strong>Worlds</strong> (Earth/Moon/Mars/Venus) · <strong>Plan &amp; Fly</strong> · <strong>Catalog</strong> (all six) · <strong>Learn</strong> (Essays · Science).</li><li>Section headers <strong>reuse the nav group labels</strong>, so the landing <em>teaches</em> the nav&#39;s grouping by showing it — the two surfaces reinforce each other.</li><li>Why unpacked (not one card per group): <strong>information scent.</strong> &quot;Missions&quot;, &quot;Science&quot;, &quot;Milky Way&quot; are concrete, high-scent cues; &quot;Catalog&quot;/&quot;Learn&quot; are abstract. On a discovery surface, concrete leaf labels win; progressive disclosure belongs in the nav, not the front door.</li><li>Cards that share a route (the Explore scale shells all live at <code>/explore</code>) carry a <code>name</code> + <code>glyph</code> id so they render a distinct label + icon and a unique <code>{#each}</code> key. Everything else keys/labels off its <code>/route</code>.</li></ul><p><strong>Nav width note (measured 2026-08).</strong> The seven top-level items clear the right-side controls comfortably at ≥1280px in every locale, but at ~1100–1300px the wide locales (fr/es/pt-BR/ru) crunch — French essentially overflowed at 1150px. Fixed with a scoped <code>@media (min-width:641px) and (max-width:1319px)</code> that tightens <code>.link</code> padding (10px → 6px) in that band; wide screens keep the roomier spacing. Consolidating Fly+Plan into one group was considered and <strong>rejected</strong> — it&#39;s a blunt tool for a narrow-band problem and both are core features that warrant top-level prominence.</p><hr><h2 id="§url-structure" tabindex="-1">§url-structure <a class="header-anchor" href="#§url-structure" aria-label="Permalink to &quot;§url-structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/explore                                       Solar System Explorer</span></span>
<span class="line"><span>/plan                                          Mission Configurator (Mars default)</span></span>
<span class="line"><span>/plan?dest=jupiter&amp;type=flyby                  Multi-destination (ADR-026): dest=mercury|venus|mars|jupiter|saturn; type=landing|flyby</span></span>
<span class="line"><span>/fly                                           Mission Arc (default scenario: ORRERY DEMO free-return)</span></span>
<span class="line"><span>/fly?mission=id                                Mission Arc (specific mission from library)</span></span>
<span class="line"><span>/fly?dest=jupiter&amp;type=flyby&amp;dep=N&amp;tof=N       Mission Arc (synthesised from /plan selection, ADR-026)</span></span>
<span class="line"><span>/missions                                      Mission Catalog (all missions)</span></span>
<span class="line"><span>/missions?dest=MARS&amp;status=ACTIVE&amp;agency=NASA  Mission Catalog filtered (ADR-024)</span></span>
<span class="line"><span>/earth                                         Earth Orbit</span></span>
<span class="line"><span>/moon                                          Moon Map</span></span>
<span class="line"><span>/lab                                           Physics Lab (empty Notebook)</span></span>
<span class="line"><span>/lab?doc=&lt;compact&gt;                             Shared worksheet (URL-encoded card graph; grammar frozen in RFC-037 D-serialize)</span></span></code></pre></div><p>Routes resolve client-side via SvelteKit&#39;s router. Servers must serve <code>index.html</code> for unmatched paths (<code>try_files $uri /index.html</code> in nginx, <code>404.html</code> SPA-redirect on GitHub Pages — see ADR-014).</p><hr><h2 id="§shell-regions" tabindex="-1">§shell-regions <a class="header-anchor" href="#§shell-regions" aria-label="Permalink to &quot;§shell-regions&quot;">​</a></h2><p>Regions that persist across or within a screen.</p><p><strong>Nav bar</strong> — <code>height: 52px</code>, <code>top: 0</code>, <code>left: 0</code>, <code>right: 0</code>. Present on all screens. Z-index 20. Contains wordmark, screen links, screen-specific controls.</p><p><strong>Right detail panel</strong> — <code>width: 314px</code>, <code>top: 52px</code>, <code>right: 0</code>, <code>bottom: 68px</code> (68px on fly screen) or <code>bottom: 0</code>. Slides in on click. Present on: explore (planet/body panels), missions (mission detail), moon (site panels). Absent on: plan, fly, earth (these have dedicated HUD layouts).</p><p><strong>Bottom bar</strong> — <code>height: 68px</code>, <code>bottom: 0</code>, <code>left: 0</code>, <code>right: 0</code>. Present on: fly (timeline scrubber, play/pause, speed controls). Absent on all other screens.</p><p><strong>HUD panels</strong> — fly screen has four fixed-position panels (left identity, right navigation, right systems, CAPCOM panels). These are screen-specific, not shared shell regions.</p><hr><h2 id="§shared-tokens" tabindex="-1">§shared-tokens <a class="header-anchor" href="#§shared-tokens" aria-label="Permalink to &quot;§shared-tokens&quot;">​</a></h2><p>Tokens that apply across all surfaces. UXSes inherit these by name and do not redefine them locally.</p><h3 id="colour-tokens" tabindex="-1">Colour tokens <a class="header-anchor" href="#colour-tokens" aria-label="Permalink to &quot;Colour tokens&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>color-bg:         #04040c    Background — near-black with blue undertone</span></span>
<span class="line"><span>color-accent:     #4466ff    Primary accent — blue</span></span>
<span class="line"><span>color-teal:       #4ecdc4    Secondary accent — teal (CAPCOM, active states)</span></span>
<span class="line"><span>color-mars:       #c1440e    Mars red — danger, Mars-specific</span></span>
<span class="line"><span>color-earth:      #4b9cd3    Earth blue — information, Earth-specific</span></span>
<span class="line"><span>color-gold:       #ffc850    Gold — Sun, perihelion, caution</span></span>
<span class="line"><span>color-text:       #ffffff    Primary text</span></span>
<span class="line"><span>color-text-dim:   rgba(255,255,255,0.35)   Dimmed text</span></span>
<span class="line"><span>color-text-faint: rgba(255,255,255,0.15)   Very faint text / labels</span></span>
<span class="line"><span>color-border:     rgba(255,255,255,0.07)   Standard border</span></span>
<span class="line"><span>color-panel-bg:   rgba(4,4,12,0.97)        Panel background</span></span>
<span class="line"><span>color-hud-bg:     rgba(5,5,20,0.88)        HUD panel background</span></span></code></pre></div><h3 id="link-tier-colours" tabindex="-1">Link tier colours <a class="header-anchor" href="#link-tier-colours" aria-label="Permalink to &quot;Link tier colours&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>color-tier-intro: #4ecdc4    Introductory links (Wikipedia, agency pages)</span></span>
<span class="line"><span>color-tier-core:  #7b9cff    Core links (MIT OCW, technical overviews)</span></span>
<span class="line"><span>color-tier-deep:  #ff9966    Deep links (peer-reviewed papers)</span></span></code></pre></div><h3 id="typography-tokens" tabindex="-1">Typography tokens <a class="header-anchor" href="#typography-tokens" aria-label="Permalink to &quot;Typography tokens&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>font-display:   &#39;Bebas Neue&#39;, sans-serif    Titles, screen names, mission names</span></span>
<span class="line"><span>font-mono:      &#39;Space Mono&#39;, monospace     All data, labels, HUD values, UI chrome</span></span>
<span class="line"><span>font-editorial: &#39;Crimson Pro&#39;, serif        Editorial descriptions, fact text (always italic)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>size-nav-title:   26px    Nav bar wordmark</span></span>
<span class="line"><span>size-panel-title: 32–42px Panel planet/mission name (varies by name length)</span></span>
<span class="line"><span>size-hud-value:   10–16px HUD telemetry values</span></span>
<span class="line"><span>size-label:       7–8px   UI labels (Space Mono, letter-spacing 2px)</span></span>
<span class="line"><span>size-data:        9–11px  Data values</span></span>
<span class="line"><span>size-editorial:   12–13px Crimson Pro italic body text</span></span></code></pre></div><h3 id="spacing-tokens" tabindex="-1">Spacing tokens <a class="header-anchor" href="#spacing-tokens" aria-label="Permalink to &quot;Spacing tokens&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>nav-height:     52px</span></span>
<span class="line"><span>panel-width:    314px</span></span>
<span class="line"><span>bottom-bar-h:   68px</span></span>
<span class="line"><span>panel-pad:      14px 18px</span></span>
<span class="line"><span>hud-pad:        12px 16px</span></span>
<span class="line"><span>border-radius:  4–6px   Panels and cards</span></span></code></pre></div><h3 id="agency-colours" tabindex="-1">Agency colours <a class="header-anchor" href="#agency-colours" aria-label="Permalink to &quot;Agency colours&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>color-nasa:       #0B3D91</span></span>
<span class="line"><span>color-esa:        #003299</span></span>
<span class="line"><span>color-cnsa:       #DE2910</span></span>
<span class="line"><span>color-isro:       #FF9933</span></span>
<span class="line"><span>color-roscosmos:  #8B0000</span></span>
<span class="line"><span>color-jaxa:       #003087</span></span>
<span class="line"><span>color-spacex:     #005288</span></span></code></pre></div><hr><h2 id="§state-persistence" tabindex="-1">§state-persistence <a class="header-anchor" href="#§state-persistence" aria-label="Permalink to &quot;§state-persistence&quot;">​</a></h2><p>No state is persisted between sessions on the original surfaces. All screen state is held in memory and reset on navigation.</p><p>The one cross-surface exception is URL-encoded state: mission ID and filter params allow a specific view to be bookmarked and shared.</p><p><strong>The Physics Lab (<code>/lab</code>) is the deliberate exception</strong> — a worksheet is a document the user builds, so it persists: URL-compact for sharing, <code>localStorage</code> for the working session, and a <code>.orrlab.json</code> export/import for durable documents (one codec, RFC-037 §5 / D-serialize). This is scoped to <code>/lab</code>; the other surfaces remain memory-only.</p><hr><h2 id="§entry-points" tabindex="-1">§entry-points <a class="header-anchor" href="#§entry-points" aria-label="Permalink to &quot;§entry-points&quot;">​</a></h2><p>SvelteKit&#39;s standard entry: <code>src/app.html</code> is the document shell; <code>src/routes/+layout.svelte</code> is the root layout that wraps all routes. Deep-linking to any route is supported by the server-side catch-all (<code>try_files $uri /index.html</code> for nginx, <code>404.html</code> SPA-redirect for GitHub Pages per ADR-014).</p><hr><h2 id="§surface-map" tabindex="-1">§surface-map <a class="header-anchor" href="#§surface-map" aria-label="Permalink to &quot;§surface-map&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                 ┌─────────────────────────────────────────┐</span></span>
<span class="line"><span>                 │         Persistent nav bar               │</span></span>
<span class="line"><span>                 │  MOON  EXPLORE  PLAN  FLY  MISSIONS  EARTH │</span></span>
<span class="line"><span>                 └─────────────────────────────────────────┘</span></span>
<span class="line"><span>                          │       │       │</span></span>
<span class="line"><span>              ┌───────────┘       │       └──────────┐</span></span>
<span class="line"><span>              ▼                   ▼                  ▼</span></span>
<span class="line"><span>         Moon Map          Solar System        Mission Catalog</span></span>
<span class="line"><span>         3D sphere         Explorer            card grid</span></span>
<span class="line"><span>         2D flat map       3D/2D toggle        → fly screen</span></span>
<span class="line"><span>              │                   │</span></span>
<span class="line"><span>              │            Mission Config</span></span>
<span class="line"><span>              │            porkchop plot</span></span>
<span class="line"><span>              │                   │</span></span>
<span class="line"><span>              └────────────►  Mission Arc ◄──────────┘</span></span>
<span class="line"><span>                           3D/2D toggle</span></span>
<span class="line"><span>                           CAPCOM mode</span></span>
<span class="line"><span></span></span>
<span class="line"><span>         Earth Orbit — standalone, no inbound navigation</span></span></code></pre></div><hr><h2 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Version</th><th>Date</th><th>Change</th></tr></thead><tbody><tr><td>v1.0</td><td>April 2026</td><td>Initial version — surfaces, tokens, shell regions extracted from 05_Design_System.md and six prototypes</td></tr><tr><td>v1.1</td><td>April 2026</td><td>§overview, §surfaces, §navigation, §url-structure, §entry-points updated to reflect History API routing (ADR-013) and SvelteKit routes/components (ADR-012). Hash-routing examples replaced with clean URLs.</td></tr><tr><td>v1.2</td><td>August 2026</td><td>Added §lab (Physics Lab surface — one card model, three views; UXS-015 / RFC-037 / PRD-033), §surfaces + §url-structure rows for <code>/lab</code>, and the §state-persistence exception for Lab worksheet persistence.</td></tr><tr><td>v1.3</td><td>August 2026</td><td>Added §nav-groups (EXPLORE / WORLDS / PLAN &amp; FLY / CATALOG / LEARN top-level split; the 4 surface bodies moved out of EXPLORE into a new WORLDS group + <code>/worlds</code> TV hub) and §home-cards (the landing rebuilt as the whole nav tree, sectioned; every leaf a rich hand-drawn card). RFC-038. Known Universe epic renumbered to PRD-034 / RFC-039 / UXS-016 to clear the Physics Lab numbers.</td></tr></tbody></table>`,80)])])}const g=s(o,[["render",r]]);export{u as __pageData,g as default};
