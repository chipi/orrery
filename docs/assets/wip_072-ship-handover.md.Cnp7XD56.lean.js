import{_ as o,o as t,c as a,a2 as r}from"./chunks/framework.CpZKh-tG.js";const u=JSON.parse('{"title":"0.7.2 ship + release handover (2026-07-05)","description":"","frontmatter":{},"headers":[],"relativePath":"wip/072-ship-handover.md","filePath":"wip/072-ship-handover.md"}'),s={name:"wip/072-ship-handover.md"};function l(i,e,n,d,c,h){return t(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="_0-7-2-ship-release-handover-2026-07-05" tabindex="-1">0.7.2 ship + release handover (2026-07-05) <a class="header-anchor" href="#_0-7-2-ship-release-handover-2026-07-05" aria-label="Permalink to &quot;0.7.2 ship + release handover (2026-07-05)&quot;">​</a></h1><p>End-of-session state after fixing the i18n CI blocker, deploying 0.7.2, and backfilling the v0.7.1 / v0.7.2 GitHub Releases. Worktree: <code>~/.treehouse/orrery-311982/1/orrery</code>, branch <code>pwa-i18n-bundling-precache</code> (pushes to <code>origin/main</code>; Orrery uses direct-to-main, no PRs).</p><h2 id="status-everything-shipped-green" tabindex="-1">Status: everything shipped + green <a class="header-anchor" href="#status-everything-shipped-green" aria-label="Permalink to &quot;Status: everything shipped + green&quot;">​</a></h2><ul><li><code>origin/main</code> HEAD = <code>c8180380b</code>. Working tree clean.</li><li>Full pipeline green on <code>c8180380b</code>: CI · CodeQL · docker-e2e · Deploy preview · Deploy docs · Auto-publish GH Release — all success.</li><li><strong>GH Pages</strong>: live — <a href="https://chipi.github.io/orrery/" target="_blank" rel="noreferrer">https://chipi.github.io/orrery/</a></li><li><strong>VPS prod</strong>: deployed (run #28735992312, healthcheck passed) — live at <a href="https://prod-podcast.tail6d0ed4.ts.net:8443/" target="_blank" rel="noreferrer">https://prod-podcast.tail6d0ed4.ts.net:8443/</a> (tailnet-only).</li></ul><h2 id="releases-both-live-green-via-release-yml" tabindex="-1">Releases (both live, green, via release.yml) <a class="header-anchor" href="#releases-both-live-green-via-release-yml" aria-label="Permalink to &quot;Releases (both live, green, via release.yml)&quot;">​</a></h2><ul><li><strong>v0.7.2</strong> — tag → commit <code>c8180380b</code>; marked <strong>Latest</strong>. Published by the standard tag-push path. <a href="https://github.com/chipi/orrery/releases/tag/v0.7.2" target="_blank" rel="noreferrer">https://github.com/chipi/orrery/releases/tag/v0.7.2</a></li><li><strong>v0.7.1</strong> — tag → commit <code>4b82dd113</code>. Published by <code>workflow_dispatch</code> (retroactive path). <a href="https://github.com/chipi/orrery/releases/tag/v0.7.1" target="_blank" rel="noreferrer">https://github.com/chipi/orrery/releases/tag/v0.7.1</a></li></ul><h3 id="release-process-fixes-made-this-session" tabindex="-1">Release-process fixes made this session <a class="header-anchor" href="#release-process-fixes-made-this-session" aria-label="Permalink to &quot;Release-process fixes made this session&quot;">​</a></h3><ol><li><strong>CHANGELOG.md</strong> — <code>[Unreleased]</code> was actually 0.7.0&#39;s content (never renamed on release). Renamed it to <code>## [0.7.0]</code>, added curated <code>## [0.7.1]</code> + <code>## [0.7.2]</code> sections. <code>release.yml</code> extracts the release body from these.</li><li><strong>release.yml</strong> — added a <code>workflow_dispatch</code> path (<code>tag</code> + optional <code>target</code>) that reads the section from the CHANGELOG on the default branch, so a retroactive tag on a historical commit publishes <em>through the workflow</em>. Re-publish any release with <code>gh workflow run release.yml -f tag=vX.Y.Z [-f target=&lt;full-40char-sha&gt;]</code>. (Captured in memory <code>feedback_release_workflow</code>.)</li></ol><p>Gotchas learned: <code>--target</code> needs the full 40-char SHA (short SHA → HTTP 422); backfilling an older release with <code>--latest</code> steals the Latest marker (reset with <code>gh release edit &lt;newest&gt; --latest</code>).</p><h2 id="what-the-i18n-fix-was-context" tabindex="-1">What the i18n fix was (context) <a class="header-anchor" href="#what-the-i18n-fix-was-context" aria-label="Permalink to &quot;What the i18n fix was (context)&quot;">​</a></h2><p>The 0.7.2 mobile layout hid the translated <code>site-footer</code> on touch (links moved into the nav drawer). That footer was the only reliable visible locale-script text on canvas routes (<code>/explore</code>,<code>/fleet</code>,<code>/fly</code>), so <code>i18n-all-routes.spec.ts</code>&#39;s innerText <code>&gt;10</code> script-char check fell to 5-9 on mobile-chromium. Fix (<code>d7fa45337</code>): count locale-script chars across visible text AND accessible names (aria-label / title / placeholder / alt). Captured in memory <code>feedback_e2e_pitfalls</code>.</p><h2 id="open-follow-up-only-one-non-blocking" tabindex="-1">Open follow-up (only one, non-blocking) <a class="header-anchor" href="#open-follow-up-only-one-non-blocking" aria-label="Permalink to &quot;Open follow-up (only one, non-blocking)&quot;">​</a></h2><ul><li><strong>Mobile dev-test over Tailscale isn&#39;t wired for orrery.</strong> This worktree&#39;s vite dev port is <strong>5273</strong> (default; no <code>VITE_DEV_PORT</code> in <code>.env.local</code>). The laptop&#39;s active <code>tailscale serve</code> (:443 →:8080) belongs to <strong>podcast-scraper</strong>, not orrery. To test orrery on phone: <code>npm run dev -- --host</code> (0.0.0.0:5273) then <code>tailscale serve --bg --https=8443 5273</code> → <code>https://markos-macbook-pro-1.tail6d0ed4.ts.net:8443/</code>. <ul><li>UNVERIFIED: <code>vite.config.ts</code> sets no <code>server.allowedHosts</code>; vite 5.x may block the tailnet hostname. If the phone gets &quot;host not allowed&quot;, add the FQDN to <code>server.allowedHosts</code>. Not tested.</li></ul></li></ul><h2 id="optional-not-done-out-of-scope" tabindex="-1">Optional (not done, out of scope) <a class="header-anchor" href="#optional-not-done-out-of-scope" aria-label="Permalink to &quot;Optional (not done, out of scope)&quot;">​</a></h2><ul><li><code>v0.7.0</code> has one pre-existing failed <code>release.yml</code> run (2026-07-03, before the CHANGELOG had a <code>## [0.7.0]</code> section). Its release is fine. Now that the section exists, <code>gh workflow run release.yml -f tag=v0.7.0</code> would refresh its body + go green — but it&#39;d replace the curated title/body. Left untouched.</li></ul>',15)])])}const g=o(s,[["render",l]]);export{u as __pageData,g as default};
