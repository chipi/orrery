import{_ as t,o,c as i,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"AR device-testing plan — #150 Immersive Mode (for the home session)","description":"","frontmatter":{},"headers":[],"relativePath":"wip/2026-07-12-ar-device-testing-plan.md","filePath":"wip/2026-07-12-ar-device-testing-plan.md"}'),s={name:"wip/2026-07-12-ar-device-testing-plan.md"};function r(n,e,d,c,l,h){return o(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="ar-device-testing-plan-—-150-immersive-mode-for-the-home-session" tabindex="-1">AR device-testing plan — #150 Immersive Mode (for the home session) <a class="header-anchor" href="#ar-device-testing-plan-—-150-immersive-mode-for-the-home-session" aria-label="Permalink to &quot;AR device-testing plan — #150 Immersive Mode (for the home session)&quot;">​</a></h1><p><em>Written 2026-07-12 after merging <code>three-upgrade</code> → <code>main</code> (<code>9d455af4f5</code>). Everything here is the stuff CI/jsdom <strong>cannot</strong> validate — the real XR session, on-surface placement, spatial-audio pan, and the ARKit Swift path that never compiled on this laptop.</em></p><p>Two devices, two independent AR paths:</p><table tabindex="0"><thead><tr><th>Device</th><th>Path</th><th>Blocker to first run</th></tr></thead><tbody><tr><td><strong>Moto Nav rugged (Android, 7&quot;)</strong></td><td>WebXR (<code>src/lib/ar/webxr.ts</code>)</td><td>(a) is it ARCore-certified? (b) needs an <strong>HTTPS</strong> origin</td></tr><tr><td><strong>iPhone</strong> (Apple dev acct, no Xcode yet)</td><td>ARKit Swift plugin (<code>ios/.../ar-bridge/</code>, #206)</td><td>needs <strong>Xcode</strong> on the Mac to build the plugin into the app</td></tr></tbody></table><p>Start with <strong>Path A (Android)</strong> — it&#39;s the fast one. Path B needs an Xcode install first.</p><hr><h2 id="pre-flight-do-these-before-touching-a-globe-route" tabindex="-1">Pre-flight (do these before touching a globe route) <a class="header-anchor" href="#pre-flight-do-these-before-touching-a-globe-route" aria-label="Permalink to &quot;Pre-flight (do these before touching a globe route)&quot;">​</a></h2><h3 id="a0-·-is-the-moto-nav-arcore-certified" tabindex="-1">A0 · Is the Moto Nav ARCore-certified? <a class="header-anchor" href="#a0-·-is-the-moto-nav-arcore-certified" aria-label="Permalink to &quot;A0 · Is the Moto Nav ARCore-certified?&quot;">​</a></h3><p>Rugged tablets often are <strong>not</strong>. If it isn&#39;t, <code>isArSessionSupported()</code> returns false → the &quot;View in AR&quot; button is <strong>hidden by design</strong> and the flat scene is the (correct) fallback. That&#39;s not a bug — it&#39;s the graceful-degrade path (mirrors WebGL).</p><ul><li>Check: install <strong>&quot;Google Play Services for AR&quot;</strong> from the Play Store, then open a known WebXR AR demo (e.g. <code>immersive-web.github.io/webxr-samples/immersive-ar-session.html</code>). If that sample can&#39;t start an AR session, our button will (correctly) never appear.</li><li>If unsupported: Android AR is <strong>untestable on this device</strong> → all real-AR verification falls to the iPhone (Path B). Note it and move on; don&#39;t chase a hidden button.</li></ul><h3 id="a1-·-webxr-needs-a-secure-context-https-—-the-1-gotcha" tabindex="-1">A1 · WebXR needs a secure context (HTTPS) — the #1 gotcha <a class="header-anchor" href="#a1-·-webxr-needs-a-secure-context-https-—-the-1-gotcha" aria-label="Permalink to &quot;A1 · WebXR needs a secure context (HTTPS) — the #1 gotcha&quot;">​</a></h3><p><code>navigator.xr</code> is <strong>only exposed over HTTPS or localhost</strong>. Pointing the phone at the laptop dev server as <code>http://&lt;lan-ip&gt;:5173</code> will make AR silently unavailable even on a supported device. Options, best first:</p><ol><li><strong>Test the deployed site</strong> once the main→prod deploy is live (HTTPS). Simplest.</li><li><strong>Tailscale serve / <code>cap</code>-less HTTPS tunnel</strong> to the dev server (you already run Tailscale). <code>tailscale serve https / http://localhost:5173</code> → open the <code>*.ts.net</code> URL on the phone.</li><li><code>vite --https</code> with a local cert (phone must trust it — fiddlier).</li></ol><hr><h2 id="path-a-—-android-webxr-moto-nav" tabindex="-1">Path A — Android WebXR (Moto Nav) <a class="header-anchor" href="#path-a-—-android-webxr-moto-nav" aria-label="Permalink to &quot;Path A — Android WebXR (Moto Nav)&quot;">​</a></h2><p>Open each of the <strong>4 globe routes</strong> and tap <strong>&quot;View in AR&quot;</strong>: <code>/explore</code> · <code>/earth</code> · <code>/moon</code> · <code>/mars</code></p><p>For each, verify the on-device chain (this is the wiring hardened this session):</p><ol><li><strong>Session starts</strong> — camera passthrough appears, flat scene is replaced.</li><li><strong>Tap-to-place</strong> — tap a real surface (floor/table). The scene should drop onto the <em>tapped point</em>, not screen-centre (this is the new transient-input hit-test). Move around it — it should stay anchored to the spot (real <code>frame.createAnchor</code>).</li><li><strong>Placement haptic</strong> — a short buzz the instant it places (<code>arHaptic(&#39;anchor-placed&#39;)</code>).</li><li><strong>Spatial audio</strong> — quiet per-body tones that <strong>pan</strong> as you physically walk around the scene (each body has its own HRTF voice at its world position). With headphones the panning should be crisp (HRTF); on the speaker it auto-switches to equal-power.</li><li><strong>Narration</strong> — ~2 s after placement the <strong>Guide</strong> episode auto-plays, and the body tones <strong>duck</strong> under it (via the audio-bus). A short success buzz when narration ends.</li><li><strong>Walk-around orientation</strong> — surfaces should be lit/oriented correctly. <em>(We fixed a flipped-sign bug in the hit-normal math this session — confirm nothing looks inside-out or mis-shaded at the placement point.)</em></li></ol><h3 id="things-to-watch-hard-device-only-unknowns" tabindex="-1">Things to watch hard (device-only unknowns) <a class="header-anchor" href="#things-to-watch-hard-device-only-unknowns" aria-label="Permalink to &quot;Things to watch hard (device-only unknowns)&quot;">​</a></h3><ul><li><strong>Audio won&#39;t start without a user gesture</strong> — the tap that places <em>should</em> be the gesture that unlocks the AudioContext. If tones/narration are silent, note whether a second tap fixes it (autoplay-policy edge).</li><li><strong>Enter → exit → re-enter AR several times</strong> — watch for audio getting louder each time or the app hitching (oscillator/GPU leak). The <code>stop()</code> re-entrancy guard added this session should prevent double-teardown; verify.</li><li><strong>Frame rate</strong> — target is ~72 fps / smooth. Simplified scene (100 stars, no trails).</li><li><strong>Anchor drift</strong> — does the scene hold its spot over ~30 s, or slide?</li></ul><hr><h2 id="path-b-—-iphone-arkit-needs-xcode-first" tabindex="-1">Path B — iPhone ARKit (needs Xcode first) <a class="header-anchor" href="#path-b-—-iphone-arkit-needs-xcode-first" aria-label="Permalink to &quot;Path B — iPhone ARKit (needs Xcode first)&quot;">​</a></h2><p>This path has <strong>never been compiled</strong> (no Xcode here). One-time setup on the Mac:</p><ol><li><strong>Install Xcode</strong> (App Store) + open it once to accept the license / install components.</li><li>Sign into your <strong>Apple Developer account</strong> in Xcode → Settings → Accounts.</li><li>From the repo:<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>npm run build:mobile        # or: npm run build &amp;&amp; npx cap sync ios</span></span>
<span class="line"><span>npx cap open ios            # opens ios/App/App.xcworkspace in Xcode</span></span></code></pre></div></li><li><strong>Add the AR-bridge plugin to the App target</strong> — the Swift files under <code>ios/App/App/Plugins/ar-bridge/</code> (#206) must be members of the App target (Xcode → select files → File Inspector → Target Membership → App). They&#39;re committed but may not be wired into the <code>.pbxproj</code> target yet.</li><li>Set the <strong>signing team</strong> (App target → Signing &amp; Capabilities → your team) and a bundle id; plug in the iPhone, trust the Mac, select it as the run destination.</li><li><strong>Build &amp; run to the device.</strong> Grant the <strong>camera permission</strong> when prompted (<code>NSCameraUsageDescription</code> is already in Info.plist, #214).</li></ol><p>Then run the <strong>same 6-step verification</strong> as Path A on <code>/explore</code> <code>/earth</code> <code>/moon</code><code>/mars</code>. On iPhone the backend is <code>arkit-capacitor</code> (the app applies the ARKit pose to our camera; the same scene/audio/haptic wiring rides on top).</p><h3 id="iphone-specific-watch-items" tabindex="-1">iPhone-specific watch items <a class="header-anchor" href="#iphone-specific-watch-items" aria-label="Permalink to &quot;iPhone-specific watch items&quot;">​</a></h3><ul><li><strong>iOS haptics</strong> go through <code>@capacitor/haptics</code> (Taptic) — should feel crisper than Android&#39;s <code>navigator.vibrate</code>.</li><li><strong>Safari (not the wrapped app)</strong> on iPhone will show the <strong>greyed &quot;App Store&quot; button</strong> (<code>ios-fallback</code>) — that&#39;s correct; real AR only exists in the wrapped app. <em>(The App-Store URL is still a <code>#217</code> placeholder — a dead link until the app is published. Don&#39;t treat the 404 as a bug.)</em></li></ul><hr><h2 id="what-to-write-down-so-the-report-is-actionable" tabindex="-1">What to write down (so the report is actionable) <a class="header-anchor" href="#what-to-write-down-so-the-report-is-actionable" aria-label="Permalink to &quot;What to write down (so the report is actionable)&quot;">​</a></h2><p>Per device + route, note: did the button appear? did placement land on the tapped point? haptic? audio (and did it pan / duck)? narration autoplay? any leak on re-entry? fps feel? Screenshots/screen-recordings where possible. A &quot;works&quot; / &quot;broke at step N&quot; per route is enough to turn into fixes.</p><hr><h2 id="epic-slices-gated-on-this-session-s-results" tabindex="-1">Epic slices gated on this session&#39;s results <a class="header-anchor" href="#epic-slices-gated-on-this-session-s-results" aria-label="Permalink to &quot;Epic slices gated on this session&#39;s results&quot;">​</a></h2><ul><li><strong>#216</strong> · cross-platform parity test (you + 2 reviewers, Android + iPhone) — this device pass <em>is</em> the input to it.</li><li><strong>#217</strong> · TestFlight roll-out (needs a real App Store id → also unblocks the <code>APP_STORE_URL</code> placeholder).</li><li><strong>#218</strong> · v0.8 release gate (full Playwright e2e on <strong>both</strong> desktop-chromium <strong>and</strong> mobile-chromium locally, then tag — see AGENTS.md &quot;Before tagging&quot;).</li><li><strong>#195</strong> · per-scene WebGL <code>reinit()</code> — <em>deferred pending exactly this device pass</em>: only build it if you observe the route-reload recovery (<code>webgl-recovery.ts</code>) is actually insufficient on the real WKWebView. Don&#39;t build blind.</li></ul><h2 id="where-the-code-lives-quick-map" tabindex="-1">Where the code lives (quick map) <a class="header-anchor" href="#where-the-code-lives-quick-map" aria-label="Permalink to &quot;Where the code lives (quick map)&quot;">​</a></h2><ul><li>Backend seam + gate: <code>src/lib/ar.ts</code> (<code>getArBackend</code>, <code>isArSessionSupported</code>)</li><li>WebXR: <code>src/lib/ar/webxr.ts</code> · ARKit JS: <code>src/lib/ar/arkit-capacitor.ts</code> · Swift: <code>ios/App/App/Plugins/ar-bridge/</code></li><li>Scene + sensory wiring: <code>src/lib/ar/ar-scene.ts</code> · launch/teardown: <code>src/lib/ar/launch-ar.ts</code> · audio: <code>ar-audio.ts</code> · haptics: <code>ar-haptics.ts</code> · narration: <code>ar-narrator.ts</code></li><li>Button: <code>src/lib/components/EnterArButton.svelte</code></li><li>Full architecture: <strong>TA.md §immersive</strong> · spec: <strong>RFC-021</strong> / <strong>PRD-019</strong></li></ul>`,35)])])}const u=t(s,[["render",r]]);export{g as __pageData,u as default};
