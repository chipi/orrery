import{_ as o,o as s,c as t,a2 as a}from"./chunks/framework.Cdp8-jB6.js";const g=JSON.parse('{"title":"Handover — PWA upgrade-path diagnosis + icon rebrand (2026-07-01)","description":"","frontmatter":{},"headers":[],"relativePath":"wip/pwa-upgrade-path-handover.md","filePath":"wip/pwa-upgrade-path-handover.md"}'),i={name:"wip/pwa-upgrade-path-handover.md"};function n(r,e,d,c,l,h){return s(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="handover-—-pwa-upgrade-path-diagnosis-icon-rebrand-2026-07-01" tabindex="-1">Handover — PWA upgrade-path diagnosis + icon rebrand (2026-07-01) <a class="header-anchor" href="#handover-—-pwa-upgrade-path-diagnosis-icon-rebrand-2026-07-01" aria-label="Permalink to &quot;Handover — PWA upgrade-path diagnosis + icon rebrand (2026-07-01)&quot;">​</a></h1><p><strong>Status:</strong> mid-diagnosis. Icon + version work DONE (uncommitted). PWA &quot;fix&quot; attempts REVERTED to baseline in the working tree. Harness built but its first run hung before producing a verdict. Nothing committed, nothing pushed.</p><h2 id="the-three-things-marko-asked-for" tabindex="-1">The three things Marko asked for <a class="header-anchor" href="#the-three-things-marko-asked-for" aria-label="Permalink to &quot;The three things Marko asked for&quot;">​</a></h2><ol><li><strong>Icon rebrand (DONE, uncommitted).</strong> The PWA installed icon + browser-tab favicon were the <strong>NASA meatball</strong> (<code>logos/nasa.svg</code>). Replaced with a new Orrery mark: an <strong>eclipse-lit &quot;O&quot;</strong> (hand-authored vector). Marko approved direction &quot;n4&quot;, said <em>&quot;good for 1st version, we might fine-tune later&quot;</em>.</li><li><strong>Version.</strong> Footer already renders <strong><code>v0.7</code></strong> (it strips <code>-wip</code> + patch from <code>__APP_VERSION__</code>). Marko wanted the internal string clean too → bumped <code>package.json</code> <code>0.7.0-wip</code> → <strong><code>0.7.0</code></strong>.</li><li><strong>PWA stuck-on-old-version (OPEN — this is the real work).</strong> Users are stranded on <strong>0.6.3</strong>; it won&#39;t roll forward to 0.7.</li></ol><h2 id="critical-—-do-not-trust-the-old-diagnosis" tabindex="-1">CRITICAL — do not trust the old diagnosis <a class="header-anchor" href="#critical-—-do-not-trust-the-old-diagnosis" aria-label="Permalink to &quot;CRITICAL — do not trust the old diagnosis&quot;">​</a></h2><p>Four PWA &quot;fix&quot; commits shipped earlier (they&#39;re on <code>main</code> + deployed) assumed the cause was a <strong>1.7 GB image precache failing SW install on iOS</strong>. <strong>Marko is not convinced, and neither am I now:</strong></p><ul><li><strong>Fresh PWA install works</strong> on his desktop AND phone and gets 0.7. So install / precache is NOT broken for new clients.</li><li>The problem is narrower: <strong>existing 0.6.3 clients don&#39;t upgrade.</strong> That&#39;s an <strong>upgrade-path / SW-handoff</strong> problem, not (necessarily) a cache problem.</li></ul><p><strong>Rule for next agent: CONFIRM the mechanism with the harness BEFORE changing any SW code.</strong> Do not re-apply the precache/update fixes on a hunch.</p><h2 id="what-i-reverted-working-tree-only-—-not-committed-not-pushed" tabindex="-1">What I reverted (working tree only — NOT committed, NOT pushed) <a class="header-anchor" href="#what-i-reverted-working-tree-only-—-not-committed-not-pushed" aria-label="Permalink to &quot;What I reverted (working tree only — NOT committed, NOT pushed)&quot;">​</a></h2><p>Restored these to baseline <code>6dc4f2d57</code> (the commit right before the 4 PWA &quot;fix&quot; commits), so we diagnose from a known pre-fix state:</p><ul><li><code>vite.config.ts</code> → baseline (autoUpdate, <strong>image-heavy</strong> <code>globPatterns</code><code>**/*.{js,css,html,svg,png,jpg,webp,woff2,ico}</code>, skipWaiting+clientsClaim)</li><li><code>src/routes/+layout.svelte</code> → baseline SW registration (<code>navigator.serviceWorker.register(&#39;/sw.js&#39;, { scope: &#39;/&#39; })</code>, hardcoded)</li><li><code>package.json</code> → baseline build chain (no <code>check-precache-budget</code>)</li><li>Deleted (working tree): <code>scripts/check-precache-budget.mjs</code>, <code>tests/e2e/pwa-update.spec.ts</code></li></ul><p>Then re-applied ON TOP of baseline: <strong>version 0.7.0</strong> + <strong>favicon → favicon.svg</strong>.</p><h3 id="the-4-reverted-fix-commits-still-in-git-history-deployed" tabindex="-1">The 4 reverted &quot;fix&quot; commits (still in git history, deployed) <a class="header-anchor" href="#the-4-reverted-fix-commits-still-in-git-history-deployed" aria-label="Permalink to &quot;The 4 reverted &quot;fix&quot; commits (still in git history, deployed)&quot;">​</a></h3><ul><li><code>78a0ad9b9</code> register SW under BASE path + controllerchange auto-reload + visibilitychange update check</li><li><code>9a0c1c262</code> shell-only precache — <strong>ineffective in production</strong> (see findings)</li><li><code>c6b0dc195</code> guardrails (precache-budget script, sw telemetry, e2e spec)</li><li><code>8e7c9ad7a</code> drop flaky SW-active e2e assertion</li></ul><p><code>git status</code> right now:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span> M package.json                       (version 0.7.0)</span></span>
<span class="line"><span> M src/routes/+layout.svelte          (baseline SW reg + favicon.svg link)</span></span>
<span class="line"><span> M static/manifest.webmanifest        (icons → orrery set)</span></span>
<span class="line"><span> M vite.config.ts                     (baseline PWA config)</span></span>
<span class="line"><span> D scripts/check-precache-budget.mjs</span></span>
<span class="line"><span> D tests/e2e/pwa-update.spec.ts</span></span>
<span class="line"><span>?? static/favicon.svg</span></span>
<span class="line"><span>?? static/icons/                      (orrery-192/512/maskable-512 .png)</span></span>
<span class="line"><span>?? treehouse.toml                     (pre-existing, ignore)</span></span></code></pre></div><h2 id="icon-assets-created-in-working-tree" tabindex="-1">Icon assets (created, in working tree) <a class="header-anchor" href="#icon-assets-created-in-working-tree" aria-label="Permalink to &quot;Icon assets (created, in working tree)&quot;">​</a></h2><ul><li><code>static/favicon.svg</code> — hand-authored eclipse-O (dark tile, bold white ring with a light gradient limb = the &quot;eclipse&quot;, a few faint stars). Scales cleanly to 16px.</li><li><code>static/icons/orrery-192.png</code>, <code>orrery-512.png</code>, <code>orrery-maskable-512.png</code></li><li><code>manifest.webmanifest</code> icons array points at these; <code>&lt;link rel=&quot;icon&quot;&gt;</code> → <code>{base}/favicon.svg</code>. NASA logo removed from both.</li><li>Generation history: Higgsfield. Rejected: single-bead-on-ring reads as a <strong>piercing</strong> (Marko&#39;s call). Winner &quot;n4&quot; = eclipse-O. Source drafts in <code>/tmp/orrery-icons*/</code>. <code>logos/nasa.svg</code> still exists on disk (only de-referenced).</li></ul><h2 id="key-findings-evidence-backed" tabindex="-1">Key findings (evidence-backed) <a class="header-anchor" href="#key-findings-evidence-backed" aria-label="Permalink to &quot;Key findings (evidence-backed)&quot;">​</a></h2><ul><li><strong>Live deployed <code>sw.js</code> still precaches 1,816 JSON + 282 images</strong> despite the &quot;shell-only&quot; globPatterns. Root cause: <code>@vite-pwa/sveltekit</code>&#39;s <code>buildGlobPatterns</code> (<code>node_modules/@vite-pwa/sveltekit/dist/index.mjs:120</code>) <strong>force-adds</strong> <code>client/**/*.{js,css,ico,png,svg,webp,webmanifest}</code> + <code>prerendered/**/*.{html,json}</code> unless your patterns are <code>client/</code>- / <code>prerendered/</code>-prefixed. The fix that <em>would</em> work (if precache is ever confirmed relevant): use <code>client/**/*.{js,css,woff2,svg,ico}</code> + <code>prerendered/**/*.html</code>. <strong>But probably not the root cause</strong> (fresh install works).</li><li><strong>v0.6.3 has NO base-path support</strong> (no <code>VITE_BASE</code>) — always root-served, SW at <code>/sw.js</code>. It used the standard <code>useRegisterSW</code> (virtual:pwa-register). So stuck 0.6.3 clients were served from <strong>root (the VPS)</strong>, not <code>/orrery/</code>. The <code>/orrery/</code> GH-Pages base + SW-registration rework all came AFTER 0.6.3.</li><li>The <code>useRegisterSW</code> → manual <code>/sw.js</code> switch <strong>predates</strong> the 4 fix commits (baseline already has manual registration). Going &quot;all the way back&quot; to the <code>useRegisterSW</code> flow is a bigger revert than what I did — ask Marko if wanted.</li></ul><h2 id="the-harness-built-in-tmp-pwa-harness-—-not-in-repo-yet" tabindex="-1">The harness (built, in /tmp/pwa-harness/ — NOT in repo yet) <a class="header-anchor" href="#the-harness-built-in-tmp-pwa-harness-—-not-in-repo-yet" aria-label="Permalink to &quot;The harness (built, in /tmp/pwa-harness/ — NOT in repo yet)&quot;">​</a></h2><p>Proves the real upgrade path: serve OLD build, install its SW in headless Chromium, flip a symlink to the NEW build (= a deploy at the same origin), reload-loop, report UPGRADED vs STUCK with per-reload SW-state trace.</p><ul><li><code>/tmp/pwa-harness/harness.mjs</code> — the script (Node http server + Playwright)</li><li><code>/tmp/pwa-harness/v063/</code> — <code>git worktree</code> at tag <code>v0.6.3</code>, built → <code>build/</code> (real 0.6.3 SW, precaches 1,386 images). This is the <strong>OLD</strong>.</li><li><code>/tmp/pwa-harness/head/</code> — <code>git worktree</code> at HEAD, built → <code>build/</code> (the currently-deployed SW, precaches 282 images). This is the <strong>NEW</strong>.</li><li><code>node_modules</code> symlinked into <code>/tmp/pwa-harness/</code> and <code>.../head/</code> (skip npm ci).</li></ul><p>Re-run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/pwa-harness</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OLD_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tmp/pwa-harness/v063/build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NEW_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tmp/pwa-harness/head/build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PORT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">4321</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> harness.mjs</span></span></code></pre></div><p><strong>First run HUNG</strong> after printing only <code>serving swappable root at ...</code> — never reached a verdict. Suspect: <code>page.goto(&#39;/&#39;, {waitUntil:&#39;load&#39;})</code> never firing (some 0.6.3 resource never &quot;loads&quot;), or <code>waitController</code> stalling. <strong>Next agent: debug this</strong> — try <code>waitUntil:&#39;domcontentloaded&#39;</code> + a <code>page.goto</code> timeout, run <code>headless:false</code> to watch, and/or add step logging before/after goto. The server + Chromium launch themselves work (it got past those).</p><p>Also consider a <strong><code>/orrery/</code> base-path variant</strong> — the real GH-Pages topology, which the root-only harness doesn&#39;t reproduce.</p><h2 id="decisions-locked-for-when-we-reach-the-fix" tabindex="-1">Decisions locked (for when we reach the fix) <a class="header-anchor" href="#decisions-locked-for-when-we-reach-the-fix" aria-label="Permalink to &quot;Decisions locked (for when we reach the fix)&quot;">​</a></h2><ul><li><strong>Update UX</strong> = <em>notify + auto-apply shortly</em> (toast &quot;Updating to v0.7 · auto-reloads in 5s&quot;), per Marko&#39;s pick.</li><li><strong>Version</strong> → <code>0.7.0</code>. <strong>Icon</strong> → eclipse-O (approved, may fine-tune).</li></ul><h2 id="next-steps-in-order" tabindex="-1">Next steps (in order) <a class="header-anchor" href="#next-steps-in-order" aria-label="Permalink to &quot;Next steps (in order)&quot;">​</a></h2><ol><li><strong>Fix + run the harness → get the verdict.</strong> Does <code>0.6.3 → HEAD</code> upgrade or stick on desktop Chromium? <ul><li>If it <strong>UPGRADES</strong>: desktop mechanism is fine → the stuck bug is iOS-specific or an origin/topology issue (VPS root vs GH-Pages <code>/orrery/</code>, or clients on a different origin than where they now check). Investigate that. Confirms Marko&#39;s doubt.</li><li>If it <strong>STICKS</strong>: read the SW-state trace to see where (new SW <code>waiting</code> forever? install <code>redundant</code>? controller URL wrong?), then design the fix.</li></ul></li><li>Only then apply the confirmed fix (task #5/C, and #4/B <em>only if</em> implicated).</li><li>Consider promoting the harness to a <strong>CI deploy gate</strong> (task #8) — decide the &quot;old&quot; baseline source (live gh-pages vs last tag vs HEAD~1). Move harness into <code>scripts/</code> + a tracking GH issue + short design note.</li><li><code>npm run preflight</code> before anything pushable. <strong>NO push without Marko&#39;s explicit approval.</strong> (Note: reverting to baseline re-introduces image precache <ul><li>drops the guardrail test — that&#39;s intended for diagnosis, not for shipping.)</li></ul></li></ol><h2 id="session-gotchas" tabindex="-1">Session gotchas <a class="header-anchor" href="#session-gotchas" aria-label="Permalink to &quot;Session gotchas&quot;">​</a></h2><ul><li><strong>Native <code>Edit</code> tool is broken this session</strong> (lean-ctx shadowing — Read doesn&#39;t register). Use <code>node</code>/<code>python</code>/<code>sed</code> via Bash for edits.</li><li>Backgrounding with <code>( ... ) &amp;</code> inside a <code>run_in_background</code> Bash double-forks; the launcher returns exit 0 while the real work continues — check the log file.</li><li>Don&#39;t kill Marko&#39;s dev server (:5273/:5373). Harness used :4321.</li><li>Marko&#39;s cadence: commit only when he says; <strong>never push without explicit &quot;go&quot;</strong>; image changes need his per-image approval (n4 already approved).</li></ul><h2 id="task-list-taskcreate-ids" tabindex="-1">Task list (TaskCreate IDs) <a class="header-anchor" href="#task-list-taskcreate-ids" aria-label="Permalink to &quot;Task list (TaskCreate IDs)&quot;">​</a></h2><p>1 done · 2 done · 3 done · 4 pending (blocked on harness) · 5 pending (blocked on harness) · 6 pending · <strong>7 in_progress (harness — debug the hang)</strong> · 8 pending · 9 pending.</p>`,35)])])}const u=o(i,[["render",n]]);export{g as __pageData,u as default};
