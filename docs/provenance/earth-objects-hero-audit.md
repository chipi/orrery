# earth-objects hero audit — 2026-06-12

Generated by `scripts/audit-heroes.ts` (#5 Phase 1). Rubric locked in chat 2026-06-12; see commit footer for the principles.

## Summary

- ✓ clean: **4**
- ⇄ swap proposals: **0**
- ⊘ needs sourcing: **9**
- ✋ accepted overrides: **0**

## ⇄ Swap proposals

_None._

## ⊘ Needs sourcing

Hero is flagged by the rubric and **no clean alternate exists in the gallery**. Either source a better image (agency-first fetcher pattern) or add an explicit entry to `static/data/earth-objects-hero-overrides.json` if the current pick is genuinely the canonical option (Soviet-era / crashed-mission cohort).

| id | reasons |
|---|---|
| iss | subject names a different entity than iss |
| tiangong | vision-rejected: score-below-threshold |
| gps | vision-rejected: score-below-threshold; subject names a different entity than gps |
| galileo | subject: matches \bon (?:museum )?display|exhibition|placards?\b |
| glonass | subject: matches mock[-\s]?up|mockup|scale\s+(?:\S+\s+){0,2}model|scale_model; subject names a different entity than glonass |
| beidou | subject: matches mock[-\s]?up|mockup|scale\s+(?:\S+\s+){0,2}model|scale_model; score: 5 < threshold 6; subject names a different entity than beidou |
| geo | vision-rejected: score-below-threshold |
| lro | subject names a different entity than lro |
| jwst | subject names a different entity than jwst |

## ✋ Accepted overrides

_None._ When you decide a hero is the canonical pick despite the rubric flagging it, add it to `static/data/earth-objects-hero-overrides.json` with a short reason. Overrides win over rubric output silently — the audit lists them here so the override list stays visible + reviewable.

## ✓ Clean

4 entries currently pass the rubric. Not enumerated here — they're the silent majority. Run with `--list-clean` if you want them.
