# v0.8 (mobile branch) — merge + deploy readiness

*Assessment as of 2026-07-10. Branch `mobile` @ `1c8b01b07d`, 52 ahead / 1 behind `origin/main`. Scope: what remains before merging `mobile` → `main` and deploying to the web servers. The mobile **app** (iOS/Android/TV store submission) is a separate track — see §4.*

## 0. What's on the branch

52 commits over 0.7.3, four bodies of work:
- **Capacitor mobile wrapper** (ADR-078/079, #189–#221) — stream-heavy bundle, `assetUrl()` origin spine, iOS safe-area shim, WebGL context-loss recovery, deep-links/share/haptics, splash, Android-TV launchability.
- **WebP image delivery** (RFC-030 / ADR-080, #383) — WebP-only display ladder + `srcset`, git-LFS `masters/` store, `image-ladder.json`; 1.9 GB → 897 MB.
- **RFC-031 a11y + TV** — roving focus engine, command palette, canvas object indexes, accessibility statement (WCAG 2.1 AA), TV 10-foot layer + two-panel landscape landing + content-page widen.
- **Deep-review + rebase-reconciliation fixes** — a11y/mobile hardening, image-pipeline reconciliation (30 main-added images → masters + WebP tiers + provenance), the `rebuild-gallery-manifests` webp fix, docs consolidation.

Local `npm run preflight` is green. Pushed to `origin/mobile`.

## 1. Before merge to `main` — BLOCKING

- [ ] **Full CI green on the branch** (`ci.yml`: typecheck → lint → test → validate-data → build). Preflight mirrors it locally (green), but confirm the actual run — no CI run has been observed on `mobile` yet.
- [ ] **`docker-e2e.yml` green** — the full Playwright suite (desktop-chromium + mobile-chromium + landscape). **Preflight ≠ e2e; this is the big untested gate.** Notes:
  - `a11y-keyboard.spec.ts` is now guarded desktop-only (`test.skip(project !== desktop-chromium)`) — was CI-red on mobile projects before that fix.
  - **TV layout changes are all behind the TV detector** (`hover:none` + `pointer:coarse` + `min-width:1100` + `max-resolution:1.5dppx`) → desktop/mobile viewports don't match → **no visual-baseline drift expected**. Still: the landing DOM gained `.route-cards` + `formatDisplayVersion` — sanity-check the desktop/mobile landing visual snapshots.
- [ ] **`mobile-e2e.yml` green** — Capacitor streaming e2e (`tests/e2e/mobile/streaming.spec.ts`).
- [ ] **Rebase onto latest `origin/main`** immediately before merge (1 commit behind now; per operator rule #2 — every push/merge).
- [ ] **CHANGELOG** — populate `## [Unreleased]` with the v0.8 feature list (mobile wrapper, WebP delivery, a11y/TV). `release.yml` auto-publishes from the matching `## [X.Y.Z]` section, so this must be real before tagging.

## 2. Before deploy — BLOCKING

- [ ] **Version bump** `0.8.0-wip` → `0.8.0` (valid semver) for the release tag. (Footer label logic already renders `0.8-wip`; a real release drops `-wip`.)
- [ ] **Deploy order fixes the CDN skew — do it in this order:**
  1. Merge + **deploy web (`main`) first** (`deploy-prod.yml`) so the CDN (`chipi.github.io/orrery`) serves the **WebP** tree.
  2. *Then* ship/point the mobile app. The app streams display images from `STREAM_ORIGIN` (= that CDN); until main serves WebP, a released app requesting `NN.webp` 404s. This is the exact skew that surfaced on-device during development.
- [ ] **git-LFS masters** are `fetchexclude`d (`.lfsconfig`) — the web build uses the committed **derived** tree, so `deploy-prod` needs no LFS pull. Confirm the deploy job doesn't try to smudge `masters/`.
- [ ] Tag + GitHub Release + deploy are **three separate actions** (see CLAUDE.md) — `git push --tags`, then `gh release create`, then confirm/trigger `deploy-prod`.

## 3. Deferred — NOT blockers (documented)

- **RFC-031 S4** (free-fly camera keyboard/D-pad control) — deferred; content is fully reachable via the DOM object indexes. Documented in `docs/accessibility.md` + RFC-031.
- **ADR-080 Slice 4** ("auto-derive `validate-data` invariant") — TA.md's ADR-080 row still says *pending*, but the ladder-contract test + gallery-count webp gate now exist. **Doc drift to reconcile** (mark Slice 4 done, or scope what's still open).

## 4. Mobile app store track — SEPARATE from web merge/deploy

Not blockers for the web merge, but the app's own path:
- **iOS** — Apple developer account approved; App Store submission (signing, TestFlight, review) is its own process.
- **Android + Google TV** — Play Console listing; leanback banner + TV listing already scaffolded.
- The app ships on its own cadence once the web CDN serves WebP (§2).

## 5. Cleanup

- [ ] Delete the `mobile-prerebase-backup` ref once the merge lands (rollback safety no longer needed).
- [ ] Stop the local dev server on `:5388`.

---

**Bottom line:** code + docs are ready and preflight-green. The gating unknowns are the **CI + docker-e2e + mobile-e2e runs** (never exercised on this branch — preflight doesn't cover e2e) and the **deploy ordering** (web-with-WebP before the app). Everything else is a bump/changelog/rebase checklist.
