// Registration moved to Swift: Capacitor 6+ discovers plugins via the
// `CAPBridgedPlugin` conformance in ArBridgePlugin.swift (identifier / jsName /
// pluginMethods). The old ObjC `CAP_PLUGIN` macro is ignored on Cap 6+, so this
// file is intentionally empty — kept only because it's a member of the target.

#import <Foundation/Foundation.h>
#import <Capacitor/Capacitor.h>
