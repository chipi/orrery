// WebXR AR backend (#205 / RFC-021 §3) — Android web + Android wrapped.
// Stub: implemented in #205. Keeps the #204 factory (getArBackend) type-safe.

import type { ArBackend } from '../ar';

export function createWebXrBackend(): ArBackend {
  throw new Error('WebXR AR backend not yet implemented (#205)');
}
