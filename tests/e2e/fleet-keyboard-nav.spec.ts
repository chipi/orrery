import { gridKeyboardNavSuite } from './_helpers/grid-keyboard';

// 2-D roving keyboard navigation for the /fleet card grid. Shared with
// /missions via the grid-keyboard helper (both entity-card-grid layouts).
gridKeyboardNavSuite({ route: '/fleet' });
