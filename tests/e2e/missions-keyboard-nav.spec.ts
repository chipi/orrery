import { gridKeyboardNavSuite } from './_helpers/grid-keyboard';

// 2-D roving keyboard navigation for the /missions card grid. Shared with
// /fleet via the grid-keyboard helper (both entity-card-grid layouts).
gridKeyboardNavSuite({ route: '/missions' });
